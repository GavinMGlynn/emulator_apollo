# `008778-03` walked whole — coverage record

*Domain Series 3000/4000 Technical Reference*, Aug 1987. **209 PDF pages.**

## Why this file exists

`CLAUDE.md`: *a document that turns out to contain one unimplemented thing is a
document that must be read WHOLE — page by page, line by line.* This one
triggered it on 2026-08-19: Table 2-3 was found to name IRQ1 as "2681 SIO
**Port 1**" and to list **no on-board line for Port 2**, where `ap_sio_irq` ORs
both DUARTs into IRQ1 (`FINDINGS.md` C237). That is one fact this core did not
have, so the whole document is now being derived.

**A document consulted about one question was never consulted about the rest.**
`019411-A00` was grepped, declared silent, then read as page images and yielded
three registers, a whole address map, a fourth memory bank and a bank-layout
correction — from ten pages.

## How to read this record

Every page range gets a row, including the ones that yield nothing: a section
recorded as *no emulator content* is a finished section, and that is what makes
a **walked** document distinguishable from a **sampled** one later. Pages are
read as **images**, never as a text layer — OCR mangles precisely the register
and timing tables that matter, and `pdftotext` interleaved this document's
two-column tables when it was tried.

Status values: `derived` (everything in it is in code or in a named gap),
`confirms` (agrees with code already, nothing to change), `none` (no emulator
content), `GAP` (something is missing from code and is named).

## Resume here

**Next unread page: 71** (chapter 5, §5.4/§5.4.1.1 — the pages either side of
it are done; 71 was skipped over when 72's `IO_CS16.L` finding was chased).
Then 74 onward to the end of chapter 5.

**Owed**: page **71** only.

Chapters **1 (16–25)**, **2 (26–47)**, **3 (48–54)** and **4 (56–64)** are
walked end to end. Chapter 5 is read at 65–70, 72 and 73. **56 pages of
209.**

**Open gaps this chapter has produced**, for whoever picks it up: the OMTI's
`E4` controller-diagnostic command and its reject-until-passed gate, and the
five power-on default drive parameters in §5.4.5 — none modelled.

## Structure

| Chapter | Subject | PDF pages |
| --- | --- | --- |
| front | title, contents, illustrations, tables | 1–15 |
| 1 | Theory of Operation | 16–25 **(walked)** |
| 2 | AT-Compatible Bus | 26–47 **(walked)** |
| 3 | CPU/Motherboard | 48–54 **(walked)** |
| 4 | Power Supply | 56–64 **(walked)** |
| 5 | Winchester/Floppy Disk Controller | ? |
| 6 | Winchester Disk Drive | ? |
| 7 | Floppy Disk Drive | ? |
| 8 | Cartridge Tape Controller | ? |
| 9 | Cartridge Tape Drive | ? |
| 10 | Graphics Controllers | ? |
| 11 | Monitors | ? |
| 12 | Domain Low-Profile Keyboard | ? |
| 13 | Mouse | ? |
| 14 | 802.3 Network Controller | ? |
| 16 | PC Coprocessor | ? |
| A | Bus timing diagrams | ? |

## Two oracle defects the walk has found so far

The walk is checked against `ext/mame` where the oracle has an opinion, and it
has been wrong twice:

1. **`DN3500_RAM_END 0x3ffffff` for a 32 MB build.** `1000000`–`3FFFFFF` is
   48 MB. The same file gives `DN5500_RAM_END 0x2ffffff` for the same 32 MB,
   four lines apart, so the oracle contradicts itself and §1.5.2's four-point
   progression says which one is the slip. This core had imported the wrong one.
2. **`DN5500_MEM_PRESENT_BYTE 0xAA // 8-8-0-0`.** `019411-A00` §4.2.1.18's
   published table gives `AA` for **four 4-MB boards** and **`F0`** for
   `8 8 - -`. Our implementation, built from that table rather than from the
   oracle, produces `F0` — checked by running it. Nothing was changed here; it
   is recorded because it is the second time the published table has
   out-accurated the oracle.

## Coverage

| Pages | Section | Status | What it yielded |
| --- | --- | --- | --- |
| 33 | Table 2-3, Interrupt Request Priorities | **GAP** | IRQ1 is "2681 SIO **Port 1**"; the starred rows enumerate the on-board CPU lines (timer, SIO port 1, cascade, calendar, diagnostics) and **none is SIO Port 2**. `ap_sio_irq` ORs both DUARTs into IRQ1. The two "SPE Board Serial Line" rows (IRQ4, IRQ9) are an AT-bus expansion board, not the on-board second DUART. Tension: Domain/OS *arms* `sio2`'s IMR, which it would not do for a part it believed could not interrupt. `FINDINGS.md` C237 states the discriminating experiment |

| 16 | §1.1 System-Level Operation, §1.2 System Buses | `confirms` | Dual bus; the bus interface holds the DMA and interrupt controllers. **Series 4000 Address Translation Map**: noncontiguous DMA and the "512-KB window through which external AT-compatible bus masters can access CPU main memory" — already cited in `ap_atmap.h` |
| 17 | §1.3 Virtual Bus | `confirms` | "In the Series 3000, the virtual address appears to *wrap* at 26 bits, the five high-order (27:31) bits are simply ignored. The Series 4000 makes use of all virtual address bits." Already the `address_mask` of both maps in `ap_board.c`, quoted there |
| 17 | §1.3.1 **Virtual Cache** | **GAP** | "implemented in the Series 4000. It is an **8-KB, direct-mapped** cache that contains **2048 4-byte** instruction and/or data entries. The virtual cache uses a **write-through with write-allocate** design that causes the cache to be updated (along with main memory) for every memory write placed in the cache." **Nothing in `src/core` models a board-level virtual cache** — `grep -rn "virtual cache\|virtual_cache"` is empty. This is *not* the 68030's on-chip cache: §1.3 has the MC68020 generating the address and the cache sitting on the **virtual bus** beside a separate PMMU. **Open question the walk must settle in chapter 3**: whether a DS3500, whose 68030 has on-chip caches, carries this external cache at all, or whether it is a DS4000-era (68020 + 68851) structure |
| 17 | §1.3.2 **Write Buffer** | **GAP** | "a Series 4000 device ... that resides on the virtual bus **between the microprocessor and the PMMU**. This buffer allows the CPU to concurrently update the cache and execute cache read cycles **without waiting for write cycles to complete**." Nothing models it. It is a timing structure and this core's whole claim is emergent timing, so it is not cosmetic |
| 17 | §1.4 System Interrupts | `confirms` (one note) | IRQ0–IRQ15 into the controller. **Note for later**: "All of the devices shown on the AT-compatible bus ... **except for the floppy drive** ... use interrupts", against Table 2-3's "IRQ6 Floppy Drive or User Device" and `AP_DISK_FLOPPY_IRQ 6`. The two are reconcilable — the line is *assigned* and this drive does not *use* it — but it is flagged until a page settles it |

| 18 | Figure 1-1, DS3000 block diagram | `confirms` | **12-MHz MC68020**, MMU, FPU, boot PROM; main memory **2–8 MB**; physical bus 24 address / 32 data; 18-bit physical addresses to the memory control and mux; 8-MHz AT bus with 24 address, 16 data, **7 DMA channels**, 16 interrupt lines. `dn3000` in the model table is 68020 @ 12 MHz with 8 MB — agrees. **One** serial-port block, labelled `KBD` and `SIO1` only, which is why Table 2-3's single "2681 SIO Port 1" row fits a DS3000 exactly. **No virtual cache and no write buffer appear on this diagram** |
| 19 | Figure 1-2, DS4000 block diagram | **GAP (a whole model)** | **25-MHz MC68020**, **PMMU MC68851**, FPU **MC68881**, **VIRTUAL CACHE** on the logical bus and **WRITE BUFFER** between it and the PMMU; main memory **4–32 MB**; serial ports **`KBD`, `SIO1/2/3`**. **This machine is absent from the model table.** `--list-models` has `dn3000` (68020 @ 12 MHz = the DS3000) and `dn4500` (68030 @ 33 MHz = the Series *4500*); the DS4000 is a 25-MHz 68020 with a 68851 and a 68881 and is neither. `DS4000_MAP` in `ap_board.c` is named after it while actually serving the DS3500 |
| 18–19 | where §1.3.1/§1.3.2 belong | *resolves the open question above* | The virtual cache and write buffer are drawn on the **DS4000** figure and **not** on the DS3000's, and both sit on a **68020** logical bus beside a separate 68851. So they are DS4000-era structures, not something a 68030 DS3500 is shown to carry. Whether a DS3500 has them is still not settled by these figures; chapter 3 is where to look |
| 19 | FPU part number | **note** | The DS4000's FPU is drawn as **MC68881**, not the MC68882 the model table gives every Series 3500/4500 row. Consistent — different machine — but recorded so a later reader does not "correct" one to the other |

| 20 | §1.5.1 CPU/Motherboard, the element list | `confirms` + **note** | MC68020 CPU and **MC68881** FPU, both 25 MHz on the DS4000; DMA controller; interrupt controller; boot PROM; system timer; clock and calendar with battery backup. **"One full-duplex, programmable port for serial I/O. Three ... in the DS4000"**, plus **"One full-duplex, programmable serial port for the keyboard"** — so DS3000 = 2 channels = one 2681, DS4000 = 4 channels = **two**. **"Four PCB connectors for the memory modules"** — an independent Series 3000/4000 confirmation of the four-slot layout that `AP_CALENDAR_CONFIG_MEM_BOARDS_FITTED`, `AP_BOARDREG_MEM_PRESENT_SLOTS` and `AP_SIO_RAM_BANKS` all already carry, previously derived only from the DS5500 addendum and the strap table. **Note**: the text names the DS4000's PMMU as the MC68851 and leaves the *DS3000's* MMU part unnamed, where the model table asserts `68851 external` for `dn3000` |
| 21 | §1.5.1 FPU | `confirms` | "The FPU is **not an active bus master because it never accesses memory directly**. The MC68020 CPU fetches all operands for the FPU" |
| 21 | §1.5.1 DMA Controller | `confirms` | "**the floppy disk is the only DMA device**"; seven channels; "Channels 0 through 3 support 8-bit transfers ... **Channel 4 controls the cascade** ... Channels 5 through 7 support 16-bit transfers". The cascade on channel 4 is already what `ap_dmapage.c` records from `002398-04` p. 12-25 |
| 21 | §1.5.1 Interrupt Controller | **CONTRADICTION** | "All of the devices on the AT-compatible bus, **with the exception of the floppy disk**, use interrupts to request service by the CPU" — and §1.5.1's DMA paragraph makes the floppy the *only* DMA device. This core **does** raise `AP_DISK_FLOPPY_IRQ` (6) from the FDC's result phase, justified in `ap_board.c` by analogy with the fixed disk's IRQ14 rather than by measurement. Table 2-3 assigns the *line* ("IRQ6 Floppy Drive or User Device"), which is consistent with a line existing and this system's floppy not using it. **Not removed here**: it must be tested against a boot before being taken out, and "don't chase the boot" cuts both ways. Named as an item |
| 21 | §1.5.1 Boot PROM | `confirms` | "stores a read-only copy of the **Mnemonic Debugger (MD)** program, and can perform diagnostics" |
| 21 | §1.5.1 Timers and the System Clock | `confirms` | "In the DS3000, the system timers provide **4-, 8-, and 16-microsecond** clock signals" — exactly `AP_TIMER1_HZ` 250 kHz, `AP_TIMER2_HZ` 125 kHz and the third at 62.5 kHz already in `ap_timer.h`, there cited to §3.8. "In the DS4000, system clocking is derived from a **25-MHz oscillator**" |
| 21 | §1.5.1 Serial Lines | `confirms` (**the definitive mapping**) | "In the DS3000 ... **two** asynchronous serial lines, **SIO0 and SIO1**; in the DS4000, **four** ... **SIO0, SIO1, SIO2, and SIO3**. **SIO0 is the dedicated serial line for the keyboard.** SIO1, 2, and 3 are RS-232, general-purpose lines." So SIO0 = unit 0 channel A, which is exactly `KBD_UNIT 0` / `KBD_CHANNEL 0` in `ap_board.c`, and C230's "sio2 channel A" is SIO2. This is the mapping C237's question turns on: the DS4000's SIO2/SIO3 are a **second on-board 2681**, and Table 2-3 gives it no line |
| 21 | §1.5.1 External Connectors | `confirms` | Eight bus connectors; "The system bus has **four 70-pin connectors for the memory modules**"; 9-pin DIN for the keyboard, 25-pin D-shell for the general-purpose lines, and on the DS4000 a 3-port cable to reach all three |

| 22 | §1.5.1 System Status LEDs | **note** | "The **nine** LEDs (five on the front panel of the cabinet, and four inside on the CPU board) indicate the progress of the boot PROM through its initialization, and report status codes." This core models posted codes as the control register's upper byte; chapter 3 carries the LED description and is where to check the split |
| 22 | §1.5.2 Memory Module | **GAP FOUND AND FIXED** | "DS4000 memory boards contain **either 4 or 8 MB**, providing up to 32 MB" — an independent confirmation of the 4/8 MB board sizes in `ap_sio_ram_bank_layout`'s Series 4000 rows, with 16 MB boards appearing only on the DS5500. And the extent, one module at a time: "memory ranges from physical address **$1000000 to $17FFFFF** with one 8-MB module; **$1FFFFFF** for two; **$27FFFFF** with three; and an upper address limit of **$2FFFFFF** with four". `AP_BOARD_RAM_LIMIT` was `0x3FFFFFF` — **48 MB**, not 32 — copied from the oracle's `DN3500_RAM_END` without checking the arithmetic. **Corrected to `0x2FFFFFF`.** The DS3000 figures agree with the existing map: first module from `100000`, "a maximum of **$8FFFFF** with four modules", which is `DS3000_MAP.ram_limit` exactly |
| 22 | §1.5.3 Display Controllers | *out of scope, recorded* | 4-plane colour: 60 Hz noninterlaced, 1024×800, **512 KB in four 128-KB planes**, with a data path for bit-block transfers. DN4000 colour: **eight** planes, each 1024×1024, displayed 1024×800, 256 simultaneous colours from 16.7 million, **dual-port 1-MB image memory using 64K×4 DRAMs**, BLTs via gate arrays. This core models the **mono** controller; the colour controllers are their own item and these are their published figures |

| 26 | §2.1 Overview | `none` | Standard AT bus framing: multiple bus mastership, multiple interrupt levels, backward compatible to the PC/XT, DS3000 and DS4000 buses take 8- and 16-bit devices. One XT slot, the rest AT |
| 27 | §2.1 capability list | **sharpens C237** | "**11 Interrupt levels**" — and Table 2-3's *unstarred* rows number exactly **eleven** (IRQ3, 4, 5, 6, 7, 9, 10, 11, 12, 14, 15), the starred five (IRQ0, 1, 2, 8, 13) being "Used on-board CPU. Not available on the Bus." So the on-board set is **complete and closed at five**, and the second on-board 2681 has no line of its own anywhere in this document. That is the strongest statement `008778-03` makes on C237's question. Also: 64-KB I/O address space **hex 100 to hex 0FFFF**; 24-bit memory addresses (16 MB); seven DMA channels; I/O wait-state generation; arbitration; refresh |
| 27 | §2.1 I/O space vs our window | **note** | The bus's I/O space is ISA `100`–`FFFF`, and `ETHERNET.md`'s measured mapping is `physical = 0x040000 + (ISA << 7)`. Table 2-8's window `040000`–`05FFFF` is 128 KB, which at 128 bytes per ISA address covers ISA `000`–`3FF` only. So the board decodes the **low kilobyte** of the bus's I/O space, not all of it. Consistent — Table 2-8 is the authority for what this board decodes — but recorded so the difference is not mistaken later for a missing window |
| 27 | §2.2 Reference Documents | `none` | IBM PC/AT Technical Reference #1502494; Intel APX 286 Hardware Reference #210760-001; Intel Microsystem Components Handbook #230843-002. None held; none needed for anything this core models |
| 27 | §2.3.1 Connector Pin Listing (prose) | `none` | Eight 62-pin top-row and six 36-pin bottom-row sockets; P4/14 is the XT connector. Older DS3000 systems have six AT and two XT slots, at P4/14 and P5/15. Mechanical |

| 28–29 | §2.3.1 Figure 2-1 and connector geometry | `none` | Slot positions. Mechanical |
| 30 | **Table 2-1**, 62-pin signal list (P4–P11) | `confirms` | `SD0`–`SD7`, `SA0`–`SA19`, `IOR.L`/`IOW.L`, `SMEMR.L`/`SMEMW.L`, `AEN`, `BALE`, `CLK`, `OSC`, `REFRESH.L`, `OWS.L`, `IO_CH_CK.L`, `IO_CH_RDY`, `RESET_DRV`, `TC`, and interrupts **IRQ3, 4, 5, 6, 7, 9** with `DRQ1`–`DRQ3` / `DACK1`–`DACK3`. `TC` is the terminal count the 3c505 model already reports as `HSR`'s `DONE` |
| 31 | **Table 2-2**, 36-pin signal list (P15–P21) | **settles the count** | `SBHE.L`, `LA17`–`LA23`, `MEMR.L`/`MEMW.L`, `SD8`–`SD15`, `MEM_CS16.L`, `IO_CS16.L`, `MASTER.L`, `DRQ0`/`DACK0` and `DRQ5`–`DRQ7` / `DACK5`–`DACK7`, and interrupts **IRQ10, 11, 12, 14, 15**. Across both connectors the bus carries **IRQ3, 4, 5, 6, 7, 9, 10, 11, 12, 14, 15 — eleven exactly**, matching §2.1's "11 Interrupt levels" and Table 2-3's eleven unstarred rows. **IRQ0, 1, 2, 8 and 13 appear on neither connector.** Third independent confirmation that the on-board set is closed at five and that SIO Port 1 is one of them |
| 31 | §2.3.2 signal conventions | `confirms` | TTL; active (One) is >2.4 V high and inactive (Zero) <0.5 V low; a `.L` suffix is the logical negation of that structure. `I` is an input to the system board, `O` an output from it. `SA0`–`SA19` gate on the system bus when `BALE` is high and latch on its falling edge; with `LA17`–`LA23` they reach 16 MB. **`SA0` through `SA15` with `IOR.L`/`IOW.L` define I/O-space transfers** — sixteen address bits, which is the 64-KB I/O space §2.1 states |

| 32 | §2.3.2 `LA17`–`LA23` | `confirms` | Unlatched, valid while `BALE` is high, not latched during microprocessor cycles; with `SA0`–`SA19` they reach 16 MB; decoded by the adapter to generate `MEM_CS16.L` |
| 32 | §2.3.2 `CLK` | `confirms` | "the **6-MHz (Series 3000)** or **8-MHz (Series 4000)** system clock ... a cycle time of **167 nanoseconds** at 6 MHz or **125 nanoseconds** at 8 MHz ... 50% duty cycle". `ap_atbus.h` already divides its two appendices' figures by **166 ns and 125 ns**; this is the source of those numbers, stated as bus clocks rather than inferred |
| 32 | §2.3.2 `BALE` | **resolves the page-27 note** | "BALE may not occur on all 8-bit I/O cycles (**which use only address lines SA0 through SA9 for device decoding**)". `SA0`–`SA9` is ten bits — ISA `000`–`3FF` — which is **exactly** the ISA range Table 2-8's `040000`–`05FFFF` window covers at 128 bytes per address. So that window is not a partial decode of the 64-KB I/O space; it is the whole of the **8-bit** I/O decode space. The page-27 note is closed |
| 32 | §2.3.2 `IO_CH_CK.L` | **GAP** | "I/O Channel Check provides the system board with parity (error) information about memory or devices on the AT-compatible bus. When this signal is active, it indicates an **uncorrectable system error**. In the DS3000, this signal asynchronously sets a flip-flop that causes a **Non-Maskable-Interrupt (NMI)** to occur. This provides an **on-board register bit, which can be read and reset** by the system software responsible for handling the device error." **Nothing models any of it** — no `IO_CH_CK`, no NMI path from the AT bus, no such register bit. What would close it: the mechanism is fully stated, but *which* bit of which register is not, and C10's probe settled only which bits are storage, not what they mean. Nothing this core models currently raises a channel check, so the path is unexercised — which is why it went unnoticed, not a reason to leave it |
| 32 | §2.3.2 `SD0`–`SD15`, `RESET_DRV`, `IO_CH_RDY` | `confirms` + **note** | 8-bit devices use `SD0`–`SD7`, and 16-bit transfers to them are converted to two 8-bit transfers. `IO_CH_RDY` low lengthens a cycle "by an integral number of clock cycles (167 nanoseconds)" and **"should be held low for no more than 2.5 microseconds"** — a documented ceiling on wait states that this core's AT bus model does not enforce or record |

| 33 | §2.3.2 `IRQ0`–`IRQ15` prose, **Table 2-3** | **GAP** (recorded above) | Also on this page: "An interrupt request is generated when an IRQ line is **raised from low to high**. The line **must be held high until the microprocessor acknowledges** the interrupt request during the Interrupt Service routine" — edge-triggered on the rising edge and held. And "In the DS3000, [IRQ13] is connected to **Output Port Bit 7 of the 2681 SIO chip** and is used by diagnostics to verify the integrity of the interrupt controllers", which is exactly `AP_SIO_DIAGNOSTIC_IRQ 13` and the `OPCR`/`OP7` path already in `ap_sio.h` |
| 34 | §2.3.2 `SMEMW.L`/`MEMW.L` | `confirms` | `SMEMW.L` active only for the lower 1 MB; `MEMW.L` on all memory writes; a bus master must have addresses valid for one system clock period before driving `MEMW.L` |
| 34 | §2.3.2 `DRQ0`–`DRQ7`, **Table 2-4** | `confirms` + **corrects our own record** | Priorities DRQ0 highest to DRQ7 lowest; DRQ0–3 8-bit on controller 1, DRQ5–7 16-bit on controller 2; **DRQ4 cascades channels 0–3 and is not available on the bus**. Assignments: DRQ1 **Tape**, DRQ2 **Floppy**, DRQ7 **Reserved for Winchester** — all three already cited correctly in `ap_disk.h` and `ap_dmapage.c`. **And the table prints "802.3 Network Controller-AT #2" on *both* DRQ3 and DRQ6.** `ap_board.h` attributed that duplication to OCR damage; the page image shows it is an **erratum in the printed manual**. Corrected there, because the two readings differ in what a better scan would buy: nothing |
| 34 | §2.3.2 `DACK`, `AEN`, `REFRESH.L` | `confirms` | `AEN` unlatches the microprocessor from the bus so DMA can take place and is forced low when a bus master assumes control; `REFRESH.L` marks a refresh cycle driven by a bus master |

| 35 | §2.3.2 `TC` | `confirms` | "provides a pulse when the terminal count for any DMA channel is reached" |
| 35 | §2.3.2 `SBHE.L` and its encoding table | **GAP (small)** | A complete four-row encoding for 16-bit devices: `SBHE.L`/`SA0` = 0/0 word transfer, 0/1 byte on `SD8`–`SD15`, 1/0 byte on `SD0`–`SD7`, 1/1 **reserved**. "For 8-bit transfers, `SA0` is the least-significant address bit that defines whether an odd or even byte is being transferred on `SD0`–`SD7`. `SBHE.L` has no meaning for 8-bit devices and may be either high or low." This core's AT bus is a decoded window and models no `SBHE.L`; the byte-lane selection it expresses is done by address instead. Recorded as a published table with no model rather than left unstated |
| 35 | §2.3.2 `MASTER.L` | **GAP** | The bus-master protocol, with three timings this core does not have: after `MASTER.L` goes low the adapter "must wait **one system clock period** before driving the address and data lines, and **two clock periods** before issuing a Read or Write command", and "if this signal is held low for **more than 15 microseconds, the system memory may be lost because no refresh is performed** during that time". `ap_master.h` models the four-state handshake and cites §2.4.7's ordering; it has none of these three, and the 15 µs one is a **documented failure mode** rather than a mere constraint |
| 35 | §2.3.2 `MEM_CS16.L`, `IO_CS16.L` | `none` (recorded) | `MEM_CS16.L` from a decode of `LA17`–`LA23`, "requires all 16-bit memory devices to occupy at least **128 KB** of address space" and must not decode the `SA` lines; `IO_CS16.L` from a decode of `SA0`–`SA15`. Both open-collector, 20 mA |
| 35 | §2.3.2 `OSC` | `confirms` | "a high-speed clock with a **70-nanosecond period (14.31818 MHz)** ... not synchronous with the system clock ... 50% duty cycle". The standard ISA oscillator; nothing in this core derives from it |

| 36 | §2.4.1 16-Bit vs. 8-Bit Data Transfers | **bears on a named approximation** | "These two signals affect the timing of the bus cycle and generate a **1 wait-state cycle for 16-bit devices and a 4 wait-state cycle for 8-bit devices** if no `IO_CH_RDY` response is given by the device. This is typically the **fastest cycle time available** to the device". Also: word and longword transfers "must be **word-aligned** (started on an even byte address)"; 8-bit devices are reached over `SD0`–`SD7` only, and a 16-bit transfer to one becomes **two 8-bit transfers** |
| 36 | §2.4.2 Wait States | **bears on a named approximation** | "`IO_CH_RDY` is used by devices that need to produce more wait states than the **nominal 1 for 16-bit designs or 4 for 8-bit designs**." `OWS.L` reduces 16-bit from 1 to 0, and 8-bit "from 4 to a lower number". **`ap_atbus.h` records an open approximation** — "Neither table gives an I/O *cycle* time ... Cost to close: a published AT I/O cycle time, or a measurement" — and this is that quantity in another form: a **published wait-state count**. What is still needed is the base cycle the waits are added to, which the Appendix A/B diagrams carry and which this walk reaches later. So the approximation is closable **inside this document**, which was not known when it was written |
| 36 | §2.4.2 `OWS.L` vs `IO_CH_RDY` | `none` (recorded) | `OWS.L`'s setup and hold "must be met or the processor will malfunction"; `IO_CH_RDY` "is passed through a synchronizer", so missing its timing costs wait-state count rather than metastability. Most devices drive neither |

| 37 | §2.4.3 Read Cycles, §2.4.4 Write Cycles | `confirms` | The eight- and seven-step signal sequences. Step 3 restates the defaults a **third** time: "If no response is given ... a **1 wait-state cycle** will automatically occur on 16-bit transfers and a **4 wait-state cycle** ... on 8-bit transfers." Also "for memory cycles only, **LA address lines are not used during I/O cycles**". This core decodes a window rather than modelling bus signals, so the step sequences are recorded rather than implemented — deliberately, and now written down |
| 38 | §2.4.5 Direct Memory Access Cycles | `confirms` | The DMA handshake as `ap_dma` already models it |
| 39 | §2.4.6 **Refresh Cycles** | **GAP** | "The system board provides refresh cycles to the bus at **regular intervals (approximately 15 microseconds)** to refresh all of the system's dynamic memory on the system board and for any memory included in I/O adapters. **A state machine, driven by a timebase, provides this capability, inserting refresh cycles on the AT-compatible bus.**" Protocol: `BALE`, `AEN` and `REFRESH.L` asserted together; the address on `SA0`–`SA7`; `MEMR.L`/`SMEMR.L` at each `CLK`; `IO_CH_RDY` can stretch it; two `CLK` cycles after it rises the cycle ends. **This core models the refresh *source* and not the refresh *cycles*.** `ap_sio.h` has §3.9's 15 µs square wave on the 2681's `OP3` and calls it "what makes the memory refresh a real thing"; nothing inserts a bus cycle or consumes bus time for it. For a core whose claim is emergent contention, a cycle stolen every 15 µs is not cosmetic. **And it explains `MASTER.L`'s limit**: page 2-10's "more than 15 microseconds ... the system memory may be lost" is *this* interval — the same number, which is why holding the bus past it loses refresh |
| 39 | §2.4.7 Bus Mastership Arbitration | `confirms` | The DRQ → cascade-mode channel → DACK → `MASTER.L` sequence `ap_master.h` models, and "ownership implies responsibility for bus timing, concern for refresh of system memory, the disabling of all other DMA activity". The Series 4000's Master Request Register and "the address translation map ... restricts external Masters to transfers of **512 KB**" are both already quoted in `ap_boardreg.h` and `ap_atmap.h` |
| 39 | §2.4.8 Interrupts | **fourth confirmation, and exact** | "Interrupts are implemented using **two cascaded Intel 8259A** PICs. Of the 16 possible interrupts, **one is used for cascading, four are used internally by the system processor, and 11 are available on the AT-compatible bus**." One plus four plus eleven is sixteen exactly, and the four internal are IRQ0 timer, **IRQ1 SIO Port 1**, IRQ8 calendar, IRQ13 diagnostics. So the on-board set is not merely unlisted for a second SIO — it is **counted**, and there is no room in it. Also: "A device requests an interrupt by raising its Interrupt Request line from **low to high and keeping it held high until acknowledged**", which is the edge-and-hold behaviour `hash_i8259`'s `pins` field exists for |

| 40–41 | §2.5–§2.9 bus timing, voltage levels, interface circuitry, loading, termination | `none` | Electrical: drive levels, LS loading limits, termination networks. §2.5 defers the timing itself to Appendix A, which this walk reaches |
| 42 | §2.10 **Table 2-6**, DS3000 16-MB allocation | **GAP FOUND AND FIXED** | Row by row against `DS3000_PLACEMENT`: boot PROM `000000`-`007FFF` ✓, status and control ✓, `008200`-`0083FF` **NOT USED** and correctly unmapped ✓, interval timer, calendar, DMA #1/#2, **DMA page register `009200`** ✓, latch-page ✓, interrupt #1/#2 ✓, AT I/O and memory windows ✓, main memory `100000`-`8FFFFF` ✓. **Two rows disagreed.** (1) The SIO row is `008400`-`0087FF` — **one kilobyte, one part** — where we mapped `2 × AP_SIO_RANGE`, putting a *second* DUART at `008500` on a machine §1.5.1 gives one, and leaving `008600`-`0087FF` unmapped. The oracle agrees independently: `dn3000_map` sends the whole `008400`-`0087ff` to `m_sio` and the DN3000 configuration does `config.device_remove(APOLLO_SIO2_TAG)`. **Fixed** as four placements folding onto `AP_SIO1_ADDR`, which is what `canonical` is for. (2) `NETWORK ID PROM` spans `009600`-`03FFFF`, nearly 250 KB, where we map 256 bytes — aliasing, recorded and not acted on. **Not modelled and correctly so**: `C00000`-`CFFFFF` PC COPROCESSOR and `D00000`-`DFFFFF` its alternate, a board this core does not have |

| 43 | §2.10 **Table 2-7**, allocation within AT bus I/O space | `confirms` **(five addresses at once)** | The definitive ISA-to-physical map, and **every device address this core has matches its physical column exactly**: Winchester `04D000` (ISA `1A0`), tape `050000` (`218`), 802.3 `058000` (`300`), mono graphics `05D800` (`3B0`), floppy `05F800` (`3F0`). `ETHERNET.md`'s measured `physical = 0x040000 + (ISA << 7)` reproduces five of the six exactly — `2F8`→`057C00`, `300`→`058000`, `3B0`, `3D0`, `3F0` all land on the nose. **One row is the table's own inconsistency**: tape is named ISA `218` but given physical `050000`, and `218 << 7` is `050C00`; `050000` is ISA `200`. Our constant follows the *physical* column, which is what the board decodes. Several rows also print an end address that is not `ISA_end << 7` (`04D007` for `1A7`, `05F807` for `3F7`) while others are exact (`2FF`→`057F80`, `310`→`058800`), so the ends are unreliable and the starts are not. Also named here and not modelled: **SPE Board Serial Line 1 (SIO2)** at `3F8` and **Line 2 (SIO3)** at `2F8`, plus the SPE parallel line at `378` — the expansion board Table 2-3 gives IRQ4 and IRQ9 to, and the reason those rows are *not* the on-board second 2681 |

| 44 | §2.10 **Table 2-8**, DS4000 64-MB allocation | **CONFIRMS THE RAM FIX, AND FINDS 16 KB** | The table this core is built on, walked row by row. Confirmed: boot PROM `000000`-`00FFFF`, CPU status, control, **cache control `010200`**, task alias `010300`, **SIO1 `010400` and SIO2 `010500`** (two rows here where Table 2-6 gives the DS3000 one), interval timer, calendar, DMA #1/#2, interrupt #1/#2, network ID PROM, latch-page, master request, selective clear, the AT windows, the graphics spaces, and the network boot PROM at `0D0000`-`0DFFFF` "in place of alternate mono graphics" with its alternate at `0E0000`-`0EFFFF` — which is the ring option ROM this core already places. **`ADDRESS TRANSLATION MAP 017000-0177FF`** — 2 KB, confirming `AP_ATMAP_LIMIT` exactly, and confirming that the DS5500's 4 KB in `019411-A00` Table 2-5 is a **real model difference** rather than a transcription slip. **Two rows are not modelled at all: `012000`-`013FFF` CACHE RAM and `014000`-`015FFF` CACHE CONDITION CODE RAM — 8 KB each, 16 KB together.** That is §1.3.1's 8-KB virtual cache **made addressable**: the board exposes the cache's data RAM *and* its condition-code RAM to the processor, so this is not an internal structure a model may omit — firmware can read and write it, and a diagnostic almost certainly does. **And main memory is `1000000`-`2FFFFFF`, two 16-MB blocks, with `3000000`-`3FFFFFF` explicitly `NOT USED`** — which independently confirms the `3FFFFFF`→`2FFFFFF` correction made earlier in this walk from §1.5.2, and names the exact 16 MB that was being reported as "main memory" |

| 45 | §2.10 **Table 2-9**, DS4000 AT bus I/O space | **closes C237's last ambiguity** | Physically identical to Table 2-7 — same ranges, same devices, so **our five device addresses are confirmed for both families at once**. Two entries differ, and they are the whole point: the SPE board's serial lines are **SIO3 and SIO2** on the DS3000 and **SIO4 and SIO5** on the DS4000. The expansion board's lines are numbered *after* the on-board ones, so a DS3000's on-board SIO0–SIO1 are followed by SPE SIO2–SIO3, and a DS4000's on-board SIO0–SIO3 are followed by SPE SIO4–SIO5. **Therefore the DS4000's SIO2 and SIO3 are the on-board second 2681**, and Table 2-3's "SPE Board Serial Line 1/2" rows on IRQ4 and IRQ9 are a *different board*. C237 is now as sharp as this document can make it: the DS4000 carries an on-board second 2681 driving SIO2/SIO3, it is absent from an interrupt table that counts its on-board lines and closes them at four, and the two SPE rows that look like they might cover it demonstrably do not |

| 46 | §2.11 **Table 2-10**, DS3000 power allocation | `none` (electrical) **+ two facts** | Amps per rail. Two non-electrical facts fall out. (1) **"Apollo Token Ring (two-board set)"** — the DS3000's ring is *two boards*. (2) The system unit is "CPU, **DMMU**, and 2-MB Memory", so the DS3000's MMU is named **DMMU** where the DS4000's is PMMU — and §1.5.1 pointedly attaches "(MC68851)" only to the DS4000's. Also the Winchester capacities: **72, 155 and 348 MB** |
| 47 | §2.11 **Table 2-11**, DS4000 power allocation | `confirms` **(three at once)** | (1) The system unit is "CPU, **PMMU**, and 8-MB Memory", against the DS3000's DMMU — the two machines' MMUs are named differently by this manual, twice. (2) **"Apollo Token Ring Network Controller-AT"**, a *single* AT card, against the DS3000's two-board set — which independently confirms `RING.md` 136d's single-board/two-board split and that the Series 4000's is the **single-board** controller this core models. (3) **"4-MB Memory Board" and "8-MB Memory Board"** as separate line items, a third confirmation of the DS4000's board sizes after §1.5.2 and the strap table. Also names the disk controller part outright: **"Winchester/Floppy Disk Controller (OMTI 8621)"**, which is `ap_omti` and the `3000_OMTI_8621_102640-B` ROM |
| 47 | §2.12 Safety Requirements | `none` | UL ratings, slot isolation, power limits. Mechanical and regulatory |
| — | **`dn3000`'s MMU, a question this chapter raises** | **open** | The model table gives `dn3000` `.mmu = AP_MMU_M68851`, "68851 external". This manual calls the DS3000's MMU a **DMMU** in Table 2-10 and reserves "(MC68851)" for the DS4000's PMMU in §1.5.1 — twice, consistently. That is not proof the DS3000's part is *not* a 68851, and the oracle registers a Series 3000 as an `MC68020PMMU`; but it is two statements that the two machines' MMUs are not the same thing. Chapter 3 is where to settle it |

| 48 | §3.1 Description, Figure 3-1 DS3000 top view | `confirms` **(cross-document)** | Board is 12×14 inches carrying "the CPU, memory, SIO, timers, memory management, DMA, and calendar logic". The figure labels the **Memory Board Connectors as P22, P23, P24, P25** — which is exactly `019411-A00` §4.2.1.18's "Slot 0 is location **P25** ... Slot 3 is location **P22**", written four years later for the DS5500. **The DS5500 inherited the Series 3000/4000 board geometry**, so the slot ordering implemented from the addendum this morning is confirmed by the older manual. Also labels the **Test Code LEDs** on the board — §1.5.1's four of nine |
| 49 | Figure 3-2, DS4000 top view | `none` | Same layout, mechanical |
| 50 | §3.2 System Bus | `confirms` **(two behaviours already cited to this very section)** | The logical bus: `LA8`–`LA31`, `PDATA0`–`PDATA31`, `LAS-`/`LDS-`/`R/W-`/`DSACK0+1-`/`SIZ0+1`, arbitration `LBR-`/`LBG-`/`LBGACK-`, `HALT-`, `BERR-`, `IPL0`–`IPL2`, `FC0`–`FC2`. **"Contents of Interrupt Vector Byte"**: bits 7–3 are the programmed `T7`–`T3` and bits 2–0 the IRQ number — which is `ap_i8259.c`'s `vector_base = value & 0xF8` and `vector_base \| level`, exactly. And **"The parity error interrupt is a non-maskable interrupt to the CPU. It generates a Level 7 interrupt ... from the Level 7 autovector location in the CPU exception table (0 x 07c)"** — already quoted verbatim at the top of `ap_parity.h` and implemented. So §3.2 had been read before; this walk confirms it rather than finding it |
| 50 | §3.2 CPU space decode | **third DMMU/PMMU statement** | "The least-significant address bits **LA13 through LA19** on the logical bus are used to decode **CPU space** accesses. The devices selected from this decode are the **DMMU in the DS3000, PMMU in the DS4000**, the floating-point coprocessor, and the interrupt vector generation circuitry." That is the third consistent naming, now in the authoritative decode description rather than a parts list. The model table's `dn3000 .mmu = AP_MMU_M68851` rests on the oracle registering a Series 3000 as an `MC68020PMMU`; **this manual never says the DS3000 has a 68851**, and names its MMU differently from the DS4000's every time it mentions both. Our DN3000 oracle diff passes 29 of 29 CPU fields, so either the firmware does not exercise a difference or there is none to exercise — which is exactly what a discriminating experiment would have to separate |

| 51 | §3.2 (cont.) the level-7 NMI's **two sources** | **advances the chapter-2 `IO_CH_CK` gap** | "There are **two sources** of this interrupt condition: the **RAM parity circuit** on the CPU/motherboard, and the devices that generate the **`IO_CH_CHK.L`** signal on the I/O bus. The interrupt handler **checks the status register** to detect which one of these conditions exists. **Writing to the status register clears the interrupt status.**" So page 2-7's channel-check NMI is not a separate mechanism — it is the *same* level-7 autovector interrupt `ap_parity.h` already implements, and its status bit lives in the **CPU status register** beside the parity bits. The write-to-acknowledge is already modelled (`ap_boardreg`'s "a write carries nothing and *acknowledges*"). What remains unknown is only **which bit**, and it is now known to be in `010000`/`008000` rather than anywhere in the machine |
| 51 | §3.2 (cont.) physical bus | `none` (recorded) | `PA0`–`PA23`, `PDATA0`–`PDATA31`, `PAS-`, `PDS-`, `PBR-`, `PBG-`, `PBGACK-`. And the interrupt vector offset "contains the level on the controller IRQ inputs in the three least-significant bits", restating the vector-byte table |
| 51 | §3.3 Memory System | **GAP (timing)** + four confirmations | **`120 ns RAS access time and 60 ns CAS access time`** on *both* families — DS3000 with 256K×1 DRAMs, DS4000 with 1M×1. **Not modelled**: nothing in this core carries a DRAM access time, on a core whose claim is emergent timing. Confirmed: **"36 RAM chips. Thirty-two of these are used for data. The other four chips are used for the parity circuitry"** — four parity bits, which is exactly `AP_BOARDREG_STATUS_PARITY_MASK 0x00F0`'s four bits and the four lanes of `AP_BOARDREG_CONTROL_PARITY_LANE_MASK`; **"four F280 parity checker/generators ... can be forced bad by inputting to the F280 and writing into the parity RAM in diagnostic mode"**, which is the control register's force-bad-parity function; **"The refresh period is generated by the timer in the SIO DUART (2681) at a period of 15 microseconds"**, already quoted in `ap_sio.h`; and **"There is also a 32K x 8 EPROM on the CPU/Motherboard"**, which is the DS3000's `000000`-`007FFF` boot PROM exactly. Also, not modelled and recorded: the DRAM refresh interval is **4 ms**, over **256 row addresses** on a DS3000 and **1000** on a DS4000 |

| 52 | §3.4 AT-Compatible Bus Interface | **CLOSES A NAMED APPROXIMATION** | "The **normal AT bus cycle takes 500 nanoseconds for 16-bit transfers**. It takes **1 microsecond for 8-bit transfers to 8-bit devices**. It takes **2 microseconds for 16-bit transfers to 8-bit devices**. These are the **minimum cycle times** for devices on the AT bus." `ap_atbus.h` had recorded an open approximation and named its price — "a published AT I/O cycle time, or a measurement" — and this is the first. Cross-checks exactly against §2.4.2's wait-state counts on a two-clock base: 3 bus clocks and 6, which at 166.67 ns are 500 ns and 1000 ns. **Implemented**; the reference re-baselines and the boot's clocks rise 5.9%. Also restates `IO_CH_RDY`'s 167/125 ns extension granularity and its 2.5 µs ceiling |
| 52 | §3.5 Direct Memory Access | **names the missing document** + `confirms` | "For programming information, see the ***Domain Series 3000/Series 4000 Hardware Architecture Handbook* (007861)**" — the full title and number of the document `FINDINGS.md` C235 records as missing and bitsavers does not hold. **DMA channel usage**: Controller 1 "CH0 unused, **CH1 SDLC option**, **CH2 floppy**, CH3 unused"; Controller 2 "**CH4 cascade**, CH5/6/7 unused". That is *exactly* `002398-04` p. 12-23's list — SDLC, floppy, cascade, the rest available — which `RING.md` cites for "the ring is not among them, so there is no host channel to model", now confirmed from a second manual. **Timing**: "The fastest implicitly addressed transfer ... takes **1.66 microseconds in the DS3000 and 1.25 microseconds in the DS4000**" — ten bus clocks on both, and not modelled. Also: the **74LS612 Memory Mapper** supplies the upper 8 address bits, which is the DMA page register; "the CPU supports **implicit transfers only**", memory-to-memory being diagnostic-mode only; controller 1 must transfer within 64-KB blocks and controller 2 within 128-KB blocks and never on odd-word boundaries |
| 52 | §3.6 Calendar | `confirms` | "The calendar chip ... is the **MC146818**. This device combines three features: a time-of-day clock with an alarm and a 100-year calendar, a programmable periodic interrupt and square-wave generator, and **50 bytes of low-power static RAM**. The RAM is used to store configuration information." Fifty bytes is `AP_CALENDAR_BATTERY_BYTES` exactly, and the configuration use is what `ap_calendar_build_config` fills |

| 53 | §3.7 Diagnostic LED Indicators | `confirms` **(and the arithmetic closes)** | "The DS3000 has **nine LED indicators** for diagnostics that can be **set or reset by writing to the upper byte of the control register**. **Four** of these LEDs (in one block) are on the CPU/Motherboard ... The other **five** are on the front panel (the green **PWR** LED is **not used** for diagnostic error reporting)." Nine minus the non-diagnostic PWR is **eight — exactly one byte**, which is why the control register's upper byte drives them, and `ap_boardreg_post_code(regs, (uint8_t)(value >> 8))` is right. Figure 3-3 splits that byte across two physical blocks: four on the board, most-significant at top, and four on the front panel labelled **A B C D** |
| 53 | §3.8 Interval Time Clocks (MC6840) | `confirms` **(verbatim)** | "Three 16-bit timing elements ... within an LSI Timer device." Timer 1 **250 kHz (4 µs)**, Timer 2 **125 kHz (8 µs)**, Timer 3 **62.5 kHz (16 µs)** and "may be **prescaled** to make the effective input signal have a **128-microsecond period**". `ap_timer.h` carries all four figures and cites §3.8 for them; this is the section |
| 53 | §3.9 Serial Interface | `confirms` **(four facts, one verbatim)** | "two full-duplex serial ports in the DS3000; four in the DS4000"; "All ports are implemented using the **Signetics 2681** ... on-chip baud rate generator, which operates **up to 38.4K baud**"; "**SIO_0 is used for the keyboard** ... The control registers for the keyboard are located at **Channel 0 or Channel A**", which is `KBD_UNIT 0` / `KBD_CHANNEL 0` exactly; and "The counter/timer on the SIO chip is used for the **refresh count** ... in the **timer mode** to produce a square wave output on **output OP3**. The period of the output is **15 microseconds**" — the passage `ap_sio.h` already quotes as §3.9 |

| 54 | §3.9 (cont.), Figures 3-4 and 3-5, P2 SIO connector | **GAP, and it touches a live blocker** | The DS3000's single-port and DS4000's three-port pinouts, and then: "The interface to **SIO line 1 in the DS3000 and SIO1, 2, and 3 in the DS4000** is via RS-232 drivers and receivers. The protocol supports the following signals: **Serial Transmit Data, Serial Receive Data, Data Terminal Ready, Data Carrier Detect, Request To Send, Clear To Send**." The pinouts confirm it per line — `SIO1_RTS`/`SIO1_CTS`/`SIO1_DCD`/`SIO1_DTR` on pins 4/5/8/20, and the same four for SIO2 and SIO3. **This core models none of the four modem-control signals.** `ap_sio.h` already anticipated it without acting: "`IP0`-`IP6` of the **first** DUART are **not** serial handshake lines at all. They are strapped to a **RAM configuration byte**" — which implies that on the **second** DUART they are handshake lines, and nothing drives them. **The oracle cannot settle the wiring**: `apollo_sio::device_reset` copies the RAM configuration byte into `ip0`–`ip6` of *every* instance including `m_sio2`, which is a shared-class simplification rather than a board fact, and it models no DCD or CTS anywhere. **Bearing on `siologin` (C222/C234), flagged as an untested hypothesis and not a conclusion**: the symptom is that Domain/OS *arms* the line on `sio2` channel A (`imr A2`) and then services nothing. A driver that requires **carrier** before opening a line would behave exactly so, and this core's `sio2` input port is driven by nothing. **The CTS reading of that hypothesis is now REFUTED**, by reading the code rather than by a run — which is what flagging it as untested was for. `[MC68681]`'s CTS and RTS are **already fully modelled**: `MR2[4]` gates transmission on `IP0`/`IP1`, `MR1[7]` and `MR2[5]` drive receiver and transmitter auto-RTS on `OP0`/`OP1`, and all four are used in `ap_mc68681.c`. And the polarity is permissive: "**Asserted is low**, so the pin reading *set* is the one that holds transmission off" — an undriven input port reads all zeros, which is **clear to send**. So an unmodelled `sio2` input port cannot be holding the transmitter off. **What remains** is narrower and still real: **DCD and DTR** have no model at all and no pin assignment on this page, and a driver that reads the **input port register** directly to test carrier — rather than relying on the part's CTS gate — would see zeros. **And that form is now refuted too, by measurement and at no cost** — the read count was already in every boot report. On the SR10.4 reference boot, `sio2` shows **seven registers written and not one read**: regs 0, 1, 2, 6, 8, 9 and 10 (both channels' mode, clock-select and command registers), with **no `reg 13` line at all**. Register 13 read is the MC68681's **input port**, and `sio1` reads it twice — the RAM strap — so the instrument works and the second part simply is not consulted. A driver testing carrier must *read* the input port; nothing reads it. **Qualification**: this is the default configuration, where `sio2 armed imr 00`, not C230's line-2 volume where the mask is armed — so the measurement kills the hypothesis for the boot measured and leaves the siologin-configured case formally untested. What it does establish is the shape: the OS **configures both of `sio2`'s channels and then reads nothing back**. The modem-signal gap stays real and documented; it is not the blocker |

| 55 | blank | `none` | Chapter-end blank leaf. **Chapter 3 is complete at pages 48–54**, all nine sections: Description, System Bus, Memory System, AT-Compatible Bus Interface, Direct Memory Access, Calendar, Diagnostic LED Indicators, Interval Time Clocks, Serial Interface |

| 56–57 | §4.1, §4.1.1 Output Voltages, Table 4-1 | `none` (electrical) | Rails, tolerances, currents, connector pins, wire colours for the DS3000 |
| 60, 63 | Figure 4-2 Hold Up Time, §4.1.3 connectors, §4.1.4 protection | `none` **+ one chain recorded** | The only behavioural thread in this chapter, and it ends outside the register interface. **POWER GOOD** "asserted (high) ... indicates proper power supply operation" and is "a **logical AND of the dc output sense signals and the ac sense signal**". On a failure the **AC FAIL** signal "forces POWER GOOD low at least **500 µsec** before any output voltage falls below regulation limits" on the Series 3000, and **2 msec** on the Series 4000, after a **10 ms ride-through** from the mains failing. No register is named for any of it, and §2.3.2 puts the consequence on `RESET_DRV` — "used to reset or initialize the system logic at power-up or **during a low line-voltage outage**" — which this core models as a reset. Recorded so the chain is on file if a power-fail interrupt ever turns up in `007861` |
| 58, 59, 61, 62, 64 | Table 4-2, §4.1.2 Table 4-3, Figures 4-3/4-4, §4.3 | `none` (electrical and mechanical) **— owed reads now paid** | Read as page images, closing the debt recorded above. Table 4-2 is the DS4000's rails and pin assignments; Table 4-3 is input voltage, frequency, overvoltage protection, output power (210 W on the DS3000, 288 W on the DS4000), leakage, ripple and load currents, and restates the hold-up figures; Figures 4-3 and 4-4 are dc connector routing — and both label the memory board connectors **P22–P25** again, a second confirmation of the slot geometry `019411-A00` §4.2.1.18 gives the DS5500; §4.3 is physical dimensions and mounting. Nothing in any of them reaches a register |

| 65–66 | §5.1–§5.3 introduction, board specifications | `none` | Dimensions, environmental limits, power |
| 67 | Figures 5-2, 5-3, the two controllers | `confirms` | **OMTI 8610** — DS3000 only, with **LUN0 and LUN1** data cables for two Winchesters, and "the 348-MB drive will only operate with **Revision C or later**". **OMTI 8621** — DS3000 or DS4000, and "**the only** Winchester/floppy disk controller supplied with the DS4000". This core models the 8621, which Table 2-11 named and which our `3000_OMTI_8621_102640-B` ROM is |
| 68–70 | §5.3.4, §5.3.5 interface signals, Tables 5-2/5-3 | `none` | Cable level: **ST506/412** with MFM read and write data, J3 for LUN0 and J4 for LUN1, and the 34-pin floppy control connector |
| 72 | §5.4.1.2, §5.4.1.3, §5.4.2 | **GAP FOUND AND FIXED** + `confirms` | The hardware: "five VLSI chips from OMTI ... **5060** 4-channel DMA controller, **5050** disk sequencer, **5080** disk interface, **5090** host interface, **5070** ENDEC ... together with a **Z8681** microprocessor and local memory, control one or two ST506/412 hard disks". And the floppy: "**FDC765** floppy control chip and the **SMC 9229** data separator. All the hardware for the floppy control is **independent of the Z8681** and is operated **entirely by the host CPU**. The floppy disk operates using a **DMA channel**" — which is the split `ap_omti` models and why §1.5.1 makes the floppy the only DMA device. **§5.4.2 answered a stated unknown**: "The **Winchester disk uses the 16-bit (word) data transfer format**; the floppy disk uses the 8-bit (byte)". `ap_board_access_time` charged every card the 8-bit cycle because "nothing on this board is *known* to" assert `IO_CS16.L`. **Fixed**; reference re-baselines and the boot's clocks fall 11% |
| 73 | §5.4.3 Table 5-5 jumpers, §5.4.4, §5.4.5 | **GAP** | Jumper tables per drive — mechanical. **§5.4.4 Controller Initialization**: at power-on the controller lights the red LED and runs its diagnostics, "the same diagnostics performed when software issues a **controller diagnostic command (hex E4)**"; on failure the LED stays on and "**it is not possible to issue a disk-related command to the controller board without having it rejected**", though diagnostic commands still work and the **sense bytes** say which failed. **§5.4.5 Winchester Disk Drive Initialization** gives the **power-on defaults**, "the hex values refer to the parameter list of the Initialize command": **heads 4** (`03`), max cylinder **306** (`01`/`31`), **reduced write current 153** (`80`), **write precompensation cylinder 153** (`80`). **None of it is modelled** — no `E4` diagnostic command, no reject-until-diagnostics-pass gate, and no reset defaults for the drive parameters. A driver that relies on the defaults before issuing Initialize would see whatever this core happens to hold |

*(rows are added as pages are read; the walk is in progress)*
