# Apollo Token Ring — findings

The ring has **no runnable reference implementation**: MAME's Apollo driver
carries Domain networking over an emulated 3c505 802.3 card instead. So every
fact here cites a primary source — a manual section, a patent, or an address in a
dumped firmware ROM. Nothing in this file is established by reasoning about our
own code.

Status legend: `confirmed` · `open` · `provisional` · `deliberate divergence`

## Sources

| Key | Document |
| --- | --- |
| `[MAC]` | *Apollo Token Ring Media Access Control Layer and Physical Layer Protocols*, order 010005-00 rev 00, Oct 1987 (29 pp) |
| `[S3K]` | *Domain Series 3000/4000 Technical Reference*, 008778-03, Aug 1987 |
| `[EH]` | *Domain Engineering Handbook*, 002398-01/-03/-04 (Apr 83 / Feb 85 / Jan 87) — **carries a full RING REGISTERS section** for the DN3xx and DN5xx boards, finding 55 |
| `[ARCH]` | *Apollo Domain Architecture*, Feb 1981 (and the Jan 1981 preliminary) |
| `[PAT575]` | US patent 4,716,575, *Adaptively synchronized ring network for a computer system* |
| `[AEGIS]` | *AEGIS Internals and Data Structures*, Jan 1986 |
| `[PLAN]` | *Planning Domain Networks and Internets*, 009916-A00, Aug 1988 |
| `[ROM3500]` | `roms/firmware/3500_RING_10666_6.bin` — 8 KB, rev 3.6 |
| `[ROM3000]` | `roms/firmware/3000_RING_1818-4882_9-4-90.bin` — 8 KB, rev 4.0 |
| `[ROM4500]` | `roms/firmware/4500_RING_10666_8.bin` — 8 KB |
| `[ROM5500]` | `roms/firmware/5500_RING_1818-4882_R9_12-10-90.bin` — 8 KB, **byte-identical to `[ROM3000]`** (finding 54), so it is the same source and not a second one |

`[MAC]` structure, for citation: ch. 1 overview and protocol layers; ch. 2 MAC —
2.2.1.1 tokens free and claimed, 2.2.1.2 frame start and separator characters,
2.2.1.3 null separators, 2.2.2.1 frame start sequence, 2.2.2.2 packet header
sequence, 2.2.2.3 packet data sequence, 2.2.2.4 frame check sequence, 2.2.2.5
end-of-frame sequence; ch. 3 physical — 3.2 data stream, 3.3.1 phase-lock loops,
3.3.2 elastic-store buffer, 3.4 signal characteristics, 3.5 passive network
bypass; app. A coaxial driver, receiver and cable.

## Established

| # | Finding | Source | Status |
| --- | --- | --- | --- |
| 1 | Proprietary 12 Mbit/s ring over 75 Ω RG-6U coax, introduced 1981; not IEEE 802.5 and not interoperable with it | `[MAC]`, `[ARCH]` | confirmed |
| 2 | Controller occupies AT-bus I/O `0x220`–`0x23F`, appearing at Apollo physical `0x051000`; a second controller sits at AT `0x320`–`0x33F` / `0x059000` | `[S3K]` Table 2-9 | confirmed |
| 3 | Controller comprises a modem, input filtering and amplification, serial→parallel receive logic, parallel→serial transmit logic, a **dual-ported RAM buffer**, control logic, and relays that remove the node from the ring when powered off or offline | `[S3K]` §1.5.4 | confirmed |
| 4 | Older DN3000s use a two-board controller set; newer DN3000s and the DN4000 use a single board of the same function | `[S3K]` §1.5.4 | confirmed |
| 5 | Two board generations are in hand: Apollo part `10666` (DN3500, DN4500) and HP part `1818-4882` (DN3000, DN5500). Both ROMs identify as `Apollo Token Ring Network Controller-AT` | `[ROM3500]`, `[ROM4500]`, `[ROM3000]`, `[ROM5500]` | confirmed |
| 5a | `[ROM3000]` and `[ROM5500]` are **byte-identical** (md5 `6b8a2134474932c81acb3093c61619f4`), so the four dumps are three distinct images: `[ROM3500]`, `[ROM4500]`, and the one shared 1818-4882 image. The two dumps carry different dates in their filenames but the same content, so one image served the whole 1818-4882 generation. Disassemble it once, not twice | `[ROM3000]`, `[ROM5500]` | confirmed |
| 6 | Ring ROMs contain 68000 code and a self-test whose diagnostics are `ring: init error`, `ring: transmit error`, `ring: receive error` — so the firmware doubles as our first controller test | `[ROM3500]`, `[ROM3000]` string tables | confirmed |
| 6a | That 68000 code is executed by the *host* CPU rather than an on-board processor. **Inference, not yet confirmed:** `[S3K]` §1.5.4 enumerates the board's functional units and lists no microprocessor, and the ROM is 68000 rather than 8-bit code | `[S3K]` §1.5.4 + `[ROM3500]` | open — settle it in the disassembly |
| 7 | Both ROMs share an 8-byte prologue `33 5E 91 B6 00 00 A0 B6`, then ASCII `R` at +0x08 and a revision string at +0x14 (` 3.6` for `[ROM3500]`, ` 4.0` for `[ROM3000]`) — a common header format across both board generations | `[ROM3500]`, `[ROM3000]` | confirmed |
| 7a | Every option ROM in hand carries exactly **two non-zero bytes at offset `length`** — just past the checksummed image — followed by nothing but zero fill to the end of the 8 KB device: `$354F` `[ROM3500]`, `$BCF9` `[ROM4500]`, `$0057` `[ROM3000]`/`[ROM5500]`, `$56A0` on the 3C505 Ethernet ROM. Present across both board generations *and* across controller types, so it is part of the option-ROM format, not ring-specific. Purpose unknown — open question H | `[ROM3500]`, `[ROM4500]`, `[ROM3000]`, `[ROM5500]`, `3000_3C505_010728-00` | confirmed (existence), open (meaning) |
| 8 | Node ID PROM is a separate device at `0x011200`, distinct from the ring controller; 32 bytes (`3500_NI_1C874`). Domain/OS may instead take the node ID from the logical volume label of the first logical volume | `[S3K]` Table 2-8, MAME driver notes | confirmed |
| 9 | A single ring scaled to well over a hundred nodes without the degradation contemporary Ethernet suffered — the reason Apollo kept it proprietary | `[PLAN]` | confirmed |
| 10 | 12 Mbit/s is the **data** rate, not the symbol rate. The PHY is bi-phase encoded: "In the time it takes to transmit one bit (this is a bit cell, or 83.33 nsec), two windows exist: the clock window and the data window" — a clock window that must always carry a transition, and a data window whose transition (or absence) is the bit value. Each node encodes and decodes with "the 24-MHz clock generated by its transmit \[receive] phase-lock loop" | `[MAC]` §3.2 p.3-3 | confirmed |
| 10a | So the ring is **two** clock domains: a 12 MHz data clock and a 24 MHz line clock, and both must divide the time base exactly. This is what raised `AP_TIME_BASE_HZ` from 3.3 GHz to 6.6 GHz — 3.3 GHz divides 24 MHz only as 137.5 | `[MAC]` §3.2 p.3-3 | confirmed |
| 10b | 24 MHz is the transmit PLL's *nominal centre* frequency, not a fixed one: phase offset is ≤0.5 bit-times at 24 MHz −3 kHz and ≥1.5 bit-times at 24 MHz +3 kHz, and a node's elastic-store buffer re-initialises at 24 MHz after under/overflow. The emulated ring runs the nominal figure; per-node frequency deviation is a deliberate non-model until item D gives it a measured envelope | `[MAC]` §3.3.1–3.3.2 pp.3-4–3-5 | confirmed |

| 11 | The controller is addressed through a base pointer the firmware loads with `lea.l $59000.l, a2` (`[ROM3500]` `000CAA`, commented RING1 = AT I/O `0x320`–`0x33F`) and `lea.l $5a000.l, a2` (`000CBE`). That confirms finding 2's placement from the firmware's own side rather than from the manual's | `[ROM3500]` `000CAA`, `000CBE` | confirmed |
| 12 | **Four banks at `+000`, `+400`, `+800`, `+C00`, each with slots at `+0`, `+2`, `+4`, `+6`.** Fifteen distinct offsets are touched on the DN3500 and DN4500 ROMs and only the first bank's four on the DN3000's, so the bank structure is the *later* board's. Extracted mechanically from the listings, every offset with the ROM address of its first access | `[ROM3500]`, `[ROM4500]`, `[ROM3000]` disassembly | confirmed |
| 13 | `+400` carries a **status word whose bit 15 is polled**: `move.w $400(a2), d0` then `and.w #$8000, d0` then a branch, before the firmware clears `(a2)`, `+402`, `+404` and `+400` in that order. So `+400` is read for readiness and the group is then reset together | `[ROM3500]` `0000AE`–`0000C2` | confirmed |
| 14 | `+806` and `+C06` are **byte** registers written with the sequence `$30`, `$70`, `$B0` (`+806`) and `$30`, `$70` (`+C06`), immediately after that reset. Two banks driven with the same pattern one after the other suggests two identical sub-devices — consistent with finding 3's separate transmit and receive logic, though the assignment of which is which is *not* evidenced | `[ROM3500]` `0000C6`–`0000DE` | confirmed |
| 15 | `+000` is read with `movea.l (a2), a0` (`0008BC`, `0008FE`), so the first slot is read as a **long** and used as an address. `+800`, `+802`, `+804`, `+C00` and `+C02` appear only as `lea.l` operands — the firmware takes their addresses rather than their contents. Both point at those regions being **buffer** rather than register, which is question B's subject | `[ROM3500]` `0008BC`, `0008FE`, `000D62`–`000DA8` | confirmed |

| 16 | **The boot PROM's option-ROM validator, recovered.** `3500_BOOT_12191_7` matches a candidate in three variants at `01032`, `0104E` and `0106A`: all three require `magic0 = $335E91B6` at `+0`, then one requires `magic1 = $0000A0B6` at `+4` *and* a caller-supplied class in `d0` against `+8`, one requires `magic1` and a word match against `+1A`, and one requires `magic1 = $C000A0B7` alone. So `rom_id` at `+8` is matched against what the *caller* is looking for — `'R   '` when the firmware wants a ring board — which is why the same scan serves the ring and the 3C505 | `3500_BOOT_12191_7` `01032`, `0104E`, `0106A` | confirmed |
| 17 | **The checksum routine at `01080`, and what it does not read.** It skips the sum entirely when `hdr_ver` at `+18` is greater than 1, or when the fudge longword at `+10` is `$FFFFFFFF`; otherwise it takes `length` from `+0C`, shifts it right by two, and sums that many long words from the image base with `add.l (a0)+,d0`. The last long read is therefore at `length - 4`. The ring ROMs carry `hdr_ver = 1` and a real fudge longword, so they *are* checksummed | `3500_BOOT_12191_7` `01080`–`010AC` | confirmed |
| 17a | And the scan's shape was then seen from the running machine, which is the same code from the other side: a headless boot records the AT bus empty-slot addresses `00080000`-`00080003`, `00081000`-`00081003`, `00082000`-`00082003`, `00083000`-`00083003` — four bytes at each of four 4 KB slots, which is `magic0` being read and failing to match with no card fitted | measured, `--boot-limit 350000000` on the DN3500 | confirmed |
| 18 | **The four out-of-band characters, nine bits each.** Every one is a leading `0`, six `1`s, then two type bits, most-significant-bit first: `0 111111 00` separator, `0 111111 01` frame start, `0 111111 10` free token, `0 111111 11` claimed token — `0x0FC`, `0x0FD`, `0x0FE`, `0x0FF` as nine-bit values. They are *not* byte values; the leading zero occupies a bit time on the wire | `[MAC]` Figures 2-2 (p. 2-4), 2-3 and 2-4 (p. 2-5) | confirmed |
| 18a | **Read from the page images, not the text layer.** `pdftotext` renders those three figures as fragments like `MSB L.1_o--L_--'_ _ ..L~_U_S_T_BLiE_O_N_E......F~_-,--_---,-_1---11----,1 LSB` — a `1`, a `0`, and no way to tell which cell either belongs to. Rendered at 200 dpi with `pdftoppm` and read as images instead. `CLAUDE.md`'s rule about page images exists for exactly this | `[MAC]` pp. 2-4, 2-5 | confirmed |
| 19 | **Bit stuffing is what makes them recognisable.** "a transmitting node inserts a Zero ... after every five successive Ones", and the receiver extracts each zero following five ones — so six successive ones cannot occur in data, and their presence "tells a receiver: *the bit-stuffing protocol has been intentionally violated by the transmitter*". The two bits after the six ones then select the character | `[MAC]` §2.2.1 p. 2-4 | confirmed |
| 20 | Null separators: the **long** one is "a minimum of 8 bytes of Zeros" and precedes the packet; the **short** one is "a byte of Zeros" and occurs within the frame start sequence and within the frame check sequence. A minimum, modelled as a minimum | `[MAC]` §2.2.1.3 p. 2-5 | confirmed |
| 21 | **Packet header layout**: destination address `+0` (2 words), type `+4` (1 word), a zero byte `+6` and the early acknowledge byte `+7`, source address `+8` (2 words), header data from `+C`. 12 to 1024 bytes, always even — and 12 + 1012 = 1024, so the two figures close exactly. The controller "will always transmit the first 12 bytes ... even in a broken network", which is what beaconing rests on | `[MAC]` Figure 2-5 and §2.2.2.2 p. 2-6 | confirmed |
| 22 | **Type field** is a 16-bit word whose named bits are 7:1 — broadcast `<7>`, hardware diagnostics `<6>`, thank-you (reply) `<5>`, please (request) `<4>`, paging `<3>`, user/IPC `<2>`, software diagnostics `<1>`. Bits 15:8 **and bit 0** are reserved, so it is a seven-bit field and not an eight-bit one. Broadcast set means receivers ignore the destination field, whose bits are then "free for beaconing" | `[MAC]` Figure 2-6 p. 2-7 | confirmed |
| 23 | **Early acknowledge** (byte): bit 7 must be zero, 6:5 reserved, 4 must be zero, 3 intend-to-copy, 2 reserved, 1 odd parity, 0 must be zero. **Late acknowledge** (byte, in the end-of-frame sequence): 7 must be zero, 6 copied, 5 wait-ack, 4 must be zero, 3 intend-to-copy, 2 error, 1 odd parity, 0 must be zero. Both are inserted by the transmitter and modified by *other* nodes as the frame passes | `[MAC]` Figures 2-7 p. 2-8, 2-8 p. 2-9 | confirmed |
| 24 | **The CRC is not Ethernet's.** `[MAC]` gives the generator as a product, g(X) = (X^21 + 1)(X^11 + X^2 + 1), which expands to X^32 + X^23 + X^21 + X^11 + X^2 + 1 — register form `0x00A00805`, against Ethernet's `0x04C11DB7`. Initialised to zero, transmitted most-significant-bit first, no reflection or final inversion mentioned. A ring built on the familiar polynomial would emit frames no Apollo node accepts and no self-consistent test would catch it | `[MAC]` §2.2.2.4 p. 2-8 | confirmed |
| 25 | **The early acknowledge byte is CRC'd as zeros**: "ring hardware treats this field as a string of Zeros in its CRC calculation", which is what lets a receiver rewrite it in flight without the frame check going stale. The receiver's CRC covers the packet header and data sequences and the separators, ignoring bit-stuffing bits; the *late* acknowledge field is outside it entirely, being neither sequence | `[MAC]` §2.2.2.2 p. 2-8, §2.2.2.4 p. 2-8 | confirmed |
| 25a | Open detail inside 25: §2.2.2.4 says the CRC covers "the separators" without saying **how** a nine-bit out-of-band symbol is fed to a bit-serial CRC. This core feeds all nine bits. No capture exists to settle it, so the choice is marked `PROVISIONAL` in `ap_ring_frame.h` and will stay so until the ring firmware's own CRC routine is disassembled | `[MAC]` §2.2.2.4 p. 2-8 | provisional |
| 26 | Packet data: 0 to 4096 bytes, always even, "typically ... 1024 bytes". The controller may abort after any even byte on error or when the early acknowledge shows nobody is copying, and an abort sets the error bit in the late acknowledge field | `[MAC]` §2.2.2.3 p. 2-8 | confirmed |
| 27 | **Bi-phase, precisely.** Each 83.33 ns bit cell holds a clock window and a data window. "In each clock window, a transition ... must always be present or a bi-phase error will occur **and the corresponding data will be interpreted as having a bit value of Zero**"; in the data window a transition is a One and its absence a Zero. So the encoding is differential -- absolute level carries nothing -- and a corrupted cell yields a *bit*, not a gap, which keeps byte framing intact across a single glitch | `[MAC]` §3.2 p. 3-3 | confirmed |
| 28 | **Network stability is the whole purpose of the elastic store.** "the total delay around the network must be exactly an integral -- rather than a fractional -- number of bit-times", and each node's buffer contributes the fractional part that makes the sum whole. Nominally a **1-bit** delay when a node's transmit and receive PLLs are in phase; the range is `0.5 <= ESB <= 1.5` bits; underflow at "0.5 bit-times or less" and overflow at "1.5 bit-times or more", after which "the network is forced to re-initialize at a new operating frequency (24 MHz)". The bounds are therefore **inclusive failures** | `[MAC]` §3.3, §3.3.2 pp. 3-4 | confirmed |
| 29 | **PLL relationship.** Each node has one receive and one transmit PLL. Receive loops "track over a greater range of frequencies and adjust faster", so a node's receive PLL is always synchronised to the *preceding* node's transmit PLL. The transmit PLL tracks the receive PLL "with a damped response, so most phase jitter never propagates to the next node", and "the transmit phase-lock loop's phase gain is always less than one", which is what makes stability around the ring provable rather than hoped for | `[MAC]` §3.3.1 p. 3-4 | confirmed |
| 30 | **Passive bypass does two things at once**: relays join the node's input coax to its output coax *and* join its transmit output to its receive input. The second half is what lets a bypassed node run loopback self-tests -- so the ring firmware's own self-test, which is this controller's first real test, requires modelling both | `[MAC]` §3.5 p. 3-5 | confirmed |
| 31 | Analogue signal characteristics, recorded but **not modelled**: driver cutoff 18 MHz, receiver sensitivity -20 dBm, maximum 1 km between nodes, bypass insertion loss <= 1 dB | `[MAC]` §3.4 p. 3-5 | confirmed |
| 31a | **An inconsistency in `[MAC]` itself, not a transcription error.** §3.4 gives transmitted power as "18 dBm into 75 ohms (typically, 2.5 V peak-to-peak)". 2.5 V peak-to-peak into 75 ohms is about 10 dBm as a sine or 13 dBm as a square wave -- not 18. Verified against the page image at 220 dpi, so the manual really does say it. Recorded so a later reader who does the same arithmetic does not have to wonder whether we mis-copied it. Nothing depends on either figure | `[MAC]` §3.4 p. 3-5, page image | confirmed (as an inconsistency) |
| 32 | **A ring must be longer than its own token.** A token is nine bits and a transceiving station contributes one bit of delay (`[MAC]` §3.2 with §3.3.2's nominal), so a ring whose total delay is under nine bit-times has its token's head return before its tail has left, and the symbol overwrites itself. Real rings are nowhere near that bound because §3.3 counts "cable plant" as a static delay element and 1 km between nodes is about 60 bit-times at 12 Mbit/s. The medium now models per-hop cable delay, so a small ring can be given a realistic circumference: three stations with four bit-times of cable each is fifteen, comfortably over a nine-bit token | derived from `[MAC]` §3.2, §3.3, §3.3.2 | confirmed |
| 33 | **An idle ring still carries clock.** §3.2 requires a transition in every clock window, so a live ring transmits transitions even when it carries no data — a run of encoded Zeros, which alternates the line level. A line of all-zero cells has no transitions at all: that is a *dead* line, and a receiver decoding it reports a bi-phase error on every bit, correctly. The distinction matters for anything that models a cable, which has to start out holding idle rather than nothing | `[MAC]` §3.2 p. 3-3 | confirmed |
| 34 | **The transmit sequence, all eight steps.** A node acquires the ring by modifying a passing free token; "it breaks ring recirculation and begins to transmit its packet. Concurrently, it begins to discard received data (including its own packet ...). The process of discarding received data is called **stripping**"; it pads with Zeros just after the modified token; the MAC layer wraps the packet in a frame; the node "sends out a new free token to follow the frame"; it strips until it receives its own frame back or times out; "When a node stops stripping, recirculation resumes around the ring" | `[MAC]` §2.1 pp. 2-1, 2-2 | confirmed |
| 35 | **The one recovery figure with a number: the stripping timeout.** "until it finishes receiving its own frame, or until a **10.9 msec (2^14 byte)** timeout occurs. This timeout prevents a node from stripping bits forever (for example, if its frame has gotten lost on the ring)." The two forms cross-check: 2^14 bytes is 131,072 bits, and at 12 Mbit/s that is 10.923 ms. **The exponent does not survive a text extraction** — `pdftotext` renders it "214 byte", a plausible and entirely wrong number — so this came from the page image | `[MAC]` §2.1 step 7 p. 2-2 | confirmed |
| 36 | The bit stuffing is explicitly "a Synchronous Data Link Control- (SDLC-) type of bit-stuffing algorithm", and "all data must be transmitted most-significant-bit first (in a byte) and most-significant-byte first (in a multi-byte field)" — which confirms from the manual's own words the big-endian ordering `ap_ring_frame.*` had taken from the figures | `[MAC]` §2.2 p. 2-2 | confirmed |
| 37 | **Removing a node mid-run can lose the token, and that is not a defect.** §3.5's relays keep the *signal path* intact across a departing node, but a token that happens to be inside that node or the cable it drives goes with it. Measured here: after bypassing a node the far side stops seeing tokens entirely, and a token put on afterwards circulates normally. This is precisely the loss §2.2.1.1's forced claimed token recovers from | measured, `ring_station_suite` | confirmed |
| 38 | **A "unit" spans both AT windows.** `$CA0` maps a unit number to *two* base pointers: unit 0 gets `a1 = $51000` (ISA `0x220`) **and** `a2 = $59000` (ISA `0x320`); unit 1 gets `$52000`/`$5A000` (ISA `0x240`/`0x340`); anything else returns zero and the caller reports `ring: init error`. So finding 2's "a second controller sits at AT `0x320`" is how `[S3K]` lists the ranges, and the firmware drives the pair together as one unit. Whether that is one board with two decodes or two boards driven as a pair is *not* settled by this code | `[ROM3500]` `000CA0`-`000CCE` | confirmed |
| 39 | **`(a2)+000` is an 8-bit ID register.** `$CD0` clears the host's instruction cache (`movec cacr,d0` / `bset #3` / `movec d0,cacr`), reads a **byte** from `(a2)`, then burns three `and.w #$ffff,d0` as a delay. Init accepts only `$36` or `$37` — ASCII `'6'` and `'7'` — and otherwise returns the string `ring: init error` | `[ROM3500]` `000CD0`-`000CEA`, `00009C`-`0000AA` | confirmed |
| 40 | **`+400` bit 15 is a presence gate.** Init reads `$400(a2)`, masks `$8000`, and if it is **clear** returns success having touched nothing else — so a slot with no board is not an error. With it set the firmware clears `(a2)`, `+402`, `+404`, `+400` in that order and later writes `$800` (bit 11) to `+400` | `[ROM3500]` `0000AE`-`0000C2`, `00010A` | confirmed |
| 41a | **What the two 8254s actually are: packet counters, not interval timers.** `002398-04` p. 12-32 (DN3000 ring section) names the registers outright -- `RCV_HDR_CNT [+800]`, `RCV_PKT_CNT [+802]`, `RCV_MAX_CNT [+804]`, `XMIT_HDR_CNT [+C00]`, `XMIT_PKT_CNT [+C02]`, `BAD_PKT_CNT [+C04]` -- and says "are implemented with Intel 8254 Chips". So the counters are clocked by **ring traffic**, a header or a packet crossing the interface, and not by an oscillator. **This closes the question of their input frequency, which was recorded as a blocker**: there is no crystal to find, and a rate invented for one would have been wrong in kind rather than in value. The handbook also gives the access protocol -- `$3000`/`$7000`/`$8000` into the control register before loading, `$C200`/`$C400`/`$C800`/`$CE00` before reading, then successive word accesses with the byte in the *high* half of each word | `002398-04` p. 12-32 | confirmed |
| 41 | **`+800`-`+806` and `+C00`-`+C06` are two Intel 8254 programmable interval timers**, not the dual-ported RAM. This is confirmed rather than inferred, on four independent points: the control words written are `$30`, `$70`, `$B0` — counter 0/1/2, LSB-then-MSB, mode 0; counters are then loaded through a helper that writes LSB then MSB, matching that RW field; `$E4` is written to `+806`, which is the **read-back status** command for counter 1 and **exists only on the 8254, not the 8253**; and the status word read back from `+802` is tested with `btst #14`, which is the read-back byte's `NULL_COUNT` bit. So `+800`/`+802`/`+804` are counters 0/1/2 and `+806` is the control register, twice over | `[ROM3500]` `0000C6`-`0000E4`, `000D50`-`000DE2` | confirmed |
| 41a | Which **corrects finding 15**: it read `+800`, `+802`, `+804`, `+C00`, `+C02` appearing "only as `lea.l` operands" as pointing at "buffer rather than register". They are timer data ports, and the `lea` is there because the LSB-then-MSB access helper takes a pointer. Question B is therefore *not* about this region | `[ROM3500]` `000D84`-`000DC2` | confirmed |
| 42 | **The ring ROM never touches AT memory space.** Every absolute long address in `[ROM3500]` is one of exactly four: `$51000`, `$52000`, `$59000`, `$5A000` — all AT *I/O* windows. The firmware initialises and self-tests the board entirely through its registers, so the dual-ported RAM buffer is not reachable from this ROM and question B **cannot be answered from it**. That is a bounded negative result, not a gap in the reading: the buffer is the operating system driver's concern, and `[S3K]`'s AT memory-space table plus a Domain/OS driver are where it will come from | `[ROM3500]`, exhaustive scan of absolute long operands | confirmed |
| 43 | **`[S3K]` Table 2-6, the DS3000 physical address space**, read from the page image because the OCR turns it into `OAOOOO` and `OCOOOO`. AT-compatible bus **I/O** space is `040000`-`05FFFF`, which is exactly what this core models. AT-compatible bus **memory** space is `080000`-`09FFFF`, with `0A0000`-`0FFFFF` being *graphics* — plus further AT memory windows at `900000`-`91FFFF`, `920000`-`BFFFFF` and `FE0000`-`FFFFFF`. So a ring board's dual-ported RAM must appear in one of those, and `080000`-`09FFFF` is where an option card's buffer would go | `[S3K]` Table 2-6 p. 2-17, page image | confirmed |
| 43a | Two independent confirmations fall out of the same table: `MAIN MEMORY (FIRST MB)` at `100000` matches the model table's DN3000 `ram_base` of `0x100000`, and the AT I/O window matches `AP_BOARD_ATBUS_IO_BASE`/`_END` exactly. Both had been taken from other sources and now have a second witness | `[S3K]` Table 2-6 p. 2-17 | confirmed |
| 43b | **A discrepancy to resolve before the DN3500 board's map is trusted.** This core models AT bus memory as `080000`-`FFFFFF` for the DN3500, citing MAME's `dn3500_map`. Table 2-6 shows the DS3000 splitting that range: only `080000`-`09FFFF` is AT memory and `0A0000`-`0FFFFF` is graphics. Whether the DN3500 genuinely differs or MAME simplifies is **not settled** — Table 2-6 is the DS3000's, and the Series 3500 has no equivalent table on disk. Nothing measured so far depends on it: the 8.4 M empty-slot reads this session are in *I/O* space, which both agree on | `[S3K]` Table 2-6 vs `ap_board.h` | open |
| 44 | **The self-test is `ENTRY_05`, and it names both windows.** `0002E0`-`0002E2` sets `a3 = a1` (`$51000`) and `a4 = a2` (`$59000`) from `$CA0`'s pair, so every `(a4)` offset below is the *same* bank findings 12-15 catalogued and every `(a3)` offset is the `a1` window open question A listed as unexplained. The routine is a chain of subtests, each loading an error code `$E00000nn` into `d0`, calling a helper, and branching to the failure exit at `$8D2` on a non-zero compare | `[ROM3500]` `0002D4`-`0008D2` | confirmed |
| 45 | **`+400` is a status word with four more polled bits, each with its own helper and its own timeout.** `$9D2` polls bit 13 (`$2000`) with a **1000**-iteration limit (`#$3E8`); `$9FA` polls bit 2 (`$0004`) and `$A28` polls bit 1 (`$0002`), both with **65535**; `$B70` reads bit 11 (`$0800`) after a transmit. Each helper takes `d4` as the *expected polarity* -- `tst.w d4` selects whether `d2` is set to the bit or left zero -- and returns the comparison, so the caller's `bne $8D2` is "the bit did not reach the state this subtest wanted". `$A56`, `$A62` and `$A6E` are the same shape for `+402`, `+404` and `+400` read as whole words under a `d4` mask. So bit 15 (finding 40) is one of at least five live bits, and the polling structure -- which bit, which timeout, which polarity -- is now explicit | `[ROM3500]` `0009D2`-`000A78`, `000B70`-`000BAA` | confirmed |
| 46 | **`+406` is an auto-incrementing data port into the board's RAM, and `+006` is the pointer it advances from.** `$BE0`/`$C0A` write `d3+1` successive words to `+406` and `$C18`/`$C4E` read `d3+1` back and compare, with `move.w #$0,$6(a4)` before *each* pass -- so the port advances on access and `+006` resets it. The self-test runs this with `d3 = $7FFF`, four patterns deep (a walking byte pair, `$1111` rotated, and each inverted), which is **32768 words = 64 KB** | `[ROM3500]` `00033C`-`000440`, `000BE0`-`000C62` | confirmed |
| 46a | **The read port is pipelined, and a model that misses it fails by one word.** Both readers do a **dummy `move.w (a1),d1` before the loop** and discard it, then read `d3+1` more. That is the signature of a read-ahead port: the first access latches and returns stale data, and each subsequent one returns the word the previous access fetched. A model that returned the addressed word immediately would be one word out for the whole 64 KB and fail every pattern | `[ROM3500]` `000C1C`, `000C52` | confirmed |
| 47 | **Which corrects finding 42, in the way finding 41a corrected 15.** 42's *scan* stands -- every absolute long operand really is one of the four AT I/O windows -- but its conclusion, that "the dual-ported RAM buffer is not reachable from this ROM", is wrong. The buffer is not reachable as *memory*; it is reachable through the `+406` port, and the firmware tests all 64 KB of it. The error was assuming a buffer must be memory-mapped because `[S3K]` §1.5.4 calls it dual-ported. **Open question B is therefore answerable from this firmware after all**, and its size is answered: 64 KB | `[ROM3500]`, findings 42 and 46 | confirmed |
| 48 | **`+402` and `+404` are byte-wide command registers.** `+402` takes `#$1` (written by the transmit helper `$B70` immediately after loading the header, so `$1` is "go"), `#$2` and `#$6`; `+404` takes `#$8` and `#$0`, and is also written as a **word** with `#$800` at `000252` and read back as a byte at `00029A`. Both are also read as words by `$A56`/`$A62` under a `d4` mask, so each carries status as well as command. The individual bit meanings are **not** settled -- what is settled is the width, the direction, and the values the firmware uses | `[ROM3500]` `000B90`, `0004A0`, `000544`, `00048E`, `000252`-`00029A` | confirmed (shape), open (bits) |
| 49 | **The transmit path, `$B70`, matches `[MAC]`'s packet header field for field.** It writes `+006 = 0`, then `d3` high word and low word to `+406`, then `d6`, then `d4`, then `+002 = 0`, then `+402 = 1`, delays, and reads `+400` bit 11. Against finding 21's layout -- destination address two words, then the type word -- `d3` is a 32-bit **destination address** written most-significant-word first (finding 36's ordering, from the manual's own words), `d6` is the **type** field, and `d4` follows it. The call sites give `d6 = $4040` throughout and `d4 = $E` or `$A`. Bit 6 of `$4040` is finding 22's *hardware diagnostics* type bit; bit 14 is in the range finding 22 calls reserved, so the match is **partial and the reading of `d6` as the type field is inference**, not established | `[ROM3500]` `000B70`-`000BAA`, `000462`, `000518` | confirmed (the sequence), provisional (the field mapping) |
| 50 | **The self-test is a loopback test, which is why finding 30 had to model both halves of the bypass relay.** `$BAC` sets `+006 = $10`, reads four words back from `+406`, reassembles a long, and compares it against the same `d3` and `d6` the transmit at `$B70` sent. So the firmware transmits a frame and expects to receive its own -- exactly the self-test §3.5's relay arrangement exists to permit. This is the concrete shape of the plan's verification: what "the ring ROM's own self-test passes" requires is a transmit path that feeds the receive path when the node is bypassed | `[ROM3500]` `000BAC`-`000BDE` | confirmed |
| 50a | **The `a1` window is written, never read.** `$976` clears `$4(a3)`, `$400(a3)`, `$402(a3)`, `$406(a3)`; `$944` clears `$2(a3)`; `$A1E` and `$A4C` clear `$2(a3)` and `$4(a3)`. Across the whole ROM there is **no read** of any `(a3)` offset. So the second window is write-only to this firmware, and its registers cannot be characterised further from it -- a bounded negative result of the same kind as finding 42's, and this one holds | `[ROM3500]`, exhaustive scan of `(a3)` operands | confirmed |

| 51 | **The whole map confirmed across board generations, and the DN3500's bank split with it.** `[ROM3000]` -- the 1818-4882 board, byte-identical to `[ROM5500]` per 5a -- addresses the controller through a **table of eight base pointers** at `a1` rather than by fixed offsets from two windows, and the eight blocks correspond one for one to the DN3500's two windows of four banks. `$14(a1)` is the status block: `move.w (a2),d0` / `and.w #$8000,d0` is finding 40's presence gate verbatim, followed by `clr.w $2(a2)`, `clr.w $4(a2)`, `clr.w (a2)` and later `move.w #$800,(a2)` -- the same clear order and the same `$800` write. `$10(a1)` is the ID block, cleared as a word. `$18(a1)` and `$1C(a1)` are the two 8254s, taking `$30`/`$70`/`$B0` at `+6`. `(a1)` and `$4(a1)` are the second window's two blocks, cleared at `+2`/`+4`/`+6` and `+0`/`+2`. So `+400`-`+406` really is one register block and `+000`-`+006` another, and the identification does not rest on a single ROM | `[ROM3000]` `0000E8`-`00016C` vs `[ROM3500]` `00009C`-`00010A` | confirmed |
| 51a | **The four polled status bits are common to both generations.** `[ROM3000]` masks `$2000`, `$0004`, `$0002` and `$0800` against the status block in the same poll-with-timeout shape as finding 45's helpers. Identical masks on a different board from a different vendor part number, so the status word's layout is the *family's* and not one ROM's quirk -- which is what makes a model built from `[ROM3500]` worth applying to `[ROM3000]` | `[ROM3000]` `000C2A`, `000C56`, `000C88`, `000E10` | confirmed |
| 51b | One generational **difference**, recorded because it is the only one found: `[ROM3000]` writes `$30`, `$70` **and `$B0`** to *both* timers' control ports, where `[ROM3500]` writes `$B0` only to the first and leaves the second's counter 2 unprogrammed. Nothing here depends on it | `[ROM3000]` `000120`-`00013E` vs `[ROM3500]` `0000C6`-`0000DE` | confirmed |

| 51c | **`[ROM4500]` read too, and it closes that route.** The third distinct image differs from `[ROM3500]` in 1,413 bytes inside the self-test, so it is a real revision and not a reprint -- and it polls the **same four status masks**, tests the **same `$7FFF`** buffer extent, and uses `+406`/`+006` the same way. All three images therefore agree on the map, and none of them explains what `+400`'s bits *mean*: the self-test returns its expected value, actual value and register address to the caller as data, and the ROM's whole string table is five messages (`ring: init error`, `transmit`, `receive`, `invalid controller no.`, `operation aborted`) with no per-test text. Domain/OS's ring driver is the only route left for the bit meanings | `[ROM4500]` `0009AE`-`000B74`, `000312`; string scan of `[ROM3500]` | confirmed |
| 51d | **A tooling trap, recorded because it nearly invalidated 44-51.** The listings in `tools/ring-rom/` were generated before the `MOVEC` decode fix and were three months stale; an undecoded opcode desynchronises everything after it. Regenerated and diffed: the only differences are at `000110` and `000CD0` -- the two `MOVEC` sites -- each re-syncing within four instructions, plus the trailer at `000EAA`. None of the regions findings 44-51 rest on is affected. **Regenerate before reading, and diff rather than assume** | `tools/ring-rom/disasm.py` vs the on-disk listings | confirmed |
| 52 | **The elastic-store buffer is two flip-flops, and the hardware ceiling is two bits.** Claim 3(a): each NIU "is adapted to store a **maximum of two bits** between receipt and retransmission thereof". Claims 6(a1)-(a2) and 7 make the FIFO literally two bistable flip-flops "for holding a single bit", with first and second switch means "alternately coupling" input and output on alternate clock periods. So `[MAC]` §3.3.2's nominal 1-bit delay is the buffer half full, and its `0.5 <= d <= 1.5` legal range sits inside a two-bit capacity. **Independent corroboration of findings 28 and 29 from a different source and a different decade**, and it does not change the model -- `AP_RING_ESB_MIN/NOMINAL/MAX_CENTIBITS` are already 50/100/150 | `[PAT575]` claims 3, 6, 7 | confirmed |
| 52a | **What an overflow actually does, at the node.** "To aid in acquisition of the equilibrium condition, each ESB is monitored for an overflow condition. Upon detection of that condition the **receive and transmit counters are reset to a predetermined value**." `[MAC]` §3.3.2 describes the same event from the network's side -- "the network is forced to re-initialize at a new operating frequency (24 MHz)" -- so the two sources are the same event at two levels, not a contradiction. This is the mechanism half of open question D | `[PAT575]` col. 6 | confirmed |
| 52b | **The steady-state error is linear in frequency by construction**, which is why finding 29 measured it that way: "the loop filter introduces a tracking error that is a linear function of the VCO frequency, and because the VCO frequency tracks the RECEIVE CLOCK, the average number of bits in the ESB is a linear function of the RECEIVE CLOCK frequency". The closed loop is also specified to have gain "for all inputs less than unity in the steady state" | `[PAT575]` col. 6 | confirmed |
| 52c | **Two loop-filter types, contemplated in one network.** Claim 4: a filter with **no poles at the origin**, giving less-than-unity steady-state correction. Claim 5: another NIU's filter with **a pole at the origin**, "providing a phase-locked loop of at least second order" -- FIG. 9's, which "constrains ESB to store a fixed average number of data bits". The description says the two types "may be used in conjunction ... in one form of the invention". **Not** evidence that the Master runs one and the rest the other: the patent does not say so, and nothing here should assume it | `[PAT575]` claims 4, 5 and col. 6 | confirmed |
| 52d | And what `[PAT575]` does **not** give, having been read for it: no numeric PLL acquisition or lock time, no capture range, no damping factor or loop natural frequency, and nothing about the token-loss timeout of open question E. The patent is a physical-layer synchronisation document and carries no host-side register description at all, so it cannot address `+400`'s bits either | `[PAT575]`, whole | confirmed |
| 53 | **The Domain/OS route is open, and it was a string search away.** Finding 51c concluded "Domain/OS's ring driver is the only route left" and left it there. The installed disk carries the driver's **link map** with every entry point named: `RING8_$INT` and `RING8_$INT_DEFERRED`, `RING_$SENDP`, `RING_$RCV0`/`RCV1`, `RING_$START`, `RING_$STOP`, `RING_$KICK_DRIVER`, `RING_$SET_TMASK`, `RING_$GET_STATS`, `RING_$IOCTL`, `RING_$SVC_{OPEN,CLOSE,READ,WRITE,IOCTL}`, `RING_$PROC2_CLEANUP`, plus `PKT_ROUTABLE`, `ROUTE_ROUTABLE_PKT` and `HDR_CHKSUM`. `RING_PROC` ends at load address `FFCCF370`, with `ETHER_PROC` immediately after it | `media/dn3500-sr10.4-installed.awd` @ `0x00383108`-`0x00383355` | confirmed |
| 53a | **The interrupt is named**, which the plan recorded as having no source at all: `RING8_$INT` with a deferred half, `RING8_$INT_DEFERRED`, in the same shape as `ETHERNET_$INTERRUPT`/`ETHERNET_$INT_DEFERRED` beside it. That is a driver structure fact, not yet a vector number or a mask | same map | confirmed |
| 53b | **`RING_$POLL_STICKY_BPHERR` names a hardware status bit.** `BPHERR` is a **bi-phase error** and the driver *polls* it as **sticky** -- a latched condition, cleared by being read or by an explicit clear rather than tracking the line. This is the first name attached to any of `+400`'s five polled bits (finding 45) from a source outside the ROM, and a latched error bit is exactly the shape of one of them. **Which bit it is remains unestablished** -- this names a condition, not a position | same map | confirmed |
| 53c | **The condition taxonomy, all nineteen, from `domain_ring.pas`.** In the statistics vector's own order: `NACKs`, `WACKs`, `Xmit overrun`, `Xmit ACK par`, `Xmit bus err`, `Xmit timeouts`, `Xmit modem err`, `Xmit pkt error`, `Token inserted`, `Rcv DMA EOR`, `Rcv CRC error`, `Rcv timeouts`, `Rcv bus error`, `Rcv xmtr error`, `Rcv modem err`, `Rcv pkt error`, `Rcv overrun`, `Rcv ack parity`, `Rcv hdr chksum`. Note `Rcv DMA EOR` -- end-of-record -- which is the first evidence of the DMA path's shape the plan says nothing names | `…awd` @ `0x07110FA4`, `//us/us/mgrs/net/src/domain_ring.pas` | confirmed |
| 53d | **The ring's interrupt vector number, measured: `163` (`A3`).** The kernel's `VBR` is `3C400800` and its map puts `RING_VEC`/`RING_VEC1` at `3C400A8C`, so the slot is `(0xA8C-0x800)/4 = 163`; `RING_8025_VEC2` and `RING_8025_VEC1` at `3C400AAC`/`3C400AB0` are `171` and `172` the same way. This is the "vector number" the plan says the driver's names lacked, and it is arithmetic on two measured addresses rather than an inference | `m_vbr` from a state dump + `kernel_symbols` | confirmed |
| 53e | **And the slot holds the *generic* handler on a machine with no ring card**, which is the control that makes 53d trustworthy. Vector 163 reads `3C44E05C` = `IO_$COND_SWITCH_BACK+18`, next to `IO_$FLIH_TAB`, and so do its unclaimed neighbours; the vectors that *are* claimed resolve to real devices -- `160` `TIME_$DI_RTE+12`, `161` `SIO2681_$INT1_RTE`, `168` `SIO2681_$INT2_RTE` -- and match the boot report's own counts (8,428 timer, 42 DUART). So `RING8_$INT` (`3C4AEB6A`) is installed into 163 by the driver only when the card answers, and **the install path is what to read next**: it is where the mask and the enable sequence will be | oracle state dump, `or-w14` | confirmed |
| 53f | **The driver's code is readable offline, with no ring card and no boot.** `RING8_$INT` (`3C4AEB6A`) and `RING8_$INT_DEFERRED` (`3C4AE6DA`) are both *resident* in an ordinary oracle state dump -- Domain/OS loads the ring driver whether or not a controller answers -- so `oracle_walk.py` translates and reads them like any other memory. This retires "the driver's code" as a blocked source: it costs a `grep`, not an instrumented run on hardware nobody has | `or-w14` dump, `3C4AEB6A` -> `010D9B6A` | confirmed |
| 53g | **`RING8_$INT`'s prologue polls a device register at `+1400` and reads `+1404`.** Disassembled from 53f's bytes: `LEA $3C4D9000,A5` establishes a driver global block; `MOVEA.L $20(A0),A3` takes a pointer *out* of that block; then `MOVE.W $1400(A3),D1` / `BTST #1,D1` polls one bit of a word, and on the other arm `MOVE.W $1404(A3),$296(A4)` copies a second word into a structure field. A `BTST #3,$35(A2)` on the block itself gates both. **What is established** is the pair of offsets and that bit 1 of `+1400` is polled by the interrupt handler; **what is not** is that `$20(A0)` holds the controller's base -- that is the shape of the code rather than a measurement, and a second reader (the ring ROMs, which write these registers) is what would settle it | disassembly of `010D9B6A`-`010D9BC9` | provisional |
| 53h | **The cross-read does *not* confirm 53g's offsets, and that is the result.** The firmware drives `+400` (status), `+402`/`+404` (command) and `+406` (data) from a window base of `$59000` for unit 0 (findings 38, 40, 45, 48). The driver polls `+1400` and `+1404`. Those do not correspond under any simple reading: the two AT windows are `$8000` apart, not `$1000`, and taking `+1400` as an offset into window 0 lands on ISA `0x248`, inside *unit 1's* range rather than on a register either source names. So either `$20(A0)` is not a window base, or the driver maps the controller with a geometry not yet established. **Recorded as inconclusive rather than reconciled** -- a match manufactured here would be a wrong register map wearing the clothes of two agreeing sources. The step that settles it is the driver's *init* path, where `$20(A0)` is written; that is more offline reading of the same resident image | findings 38/40/45/48 against 53g | open |
| 53i | **WITHDRAWN: 53g's `+1400`/`+1404` are not controller registers.** The init path settles it without a boot. `3C4D9000` is `RING_$CCB` -- the driver's control block, with `RING_$CTL` at `+10`, which is exactly the ISR's `LEA $10(A0),A2`. Reading the block out of the dump shows `+1C` and `+20` both holding `3C4D9018`, and `+28`/`+2C` both holding `3C4D9024`: **two empty list heads pointing at themselves**. So `MOVEA.L $20(A0),A3` loads a *queue pointer*, and `$1400(A3)` is an offset into whatever structure a queued element belongs to -- not a device window. 53h recorded the cross-read as inconclusive and declined to reconcile it; this is why it could not be reconciled, and the caution was right | `RING_$CCB` at `01104000`, `or-w14` | confirmed |
| 53j | **What that costs, stated.** The driver is still the best source for *names* -- 53, 53b, 53c -- and for the interrupt vector, which 53d measured from a table rather than from code. It is a poor source for **register semantics**, because everything it touches is one indirection away from the hardware and the indirections are empty on every machine this project can dump: no ring card, no queued elements, self-referential heads. The register-level answer therefore stays where findings 38-50 already put it -- the four ring ROMs, which address the controller directly | 53i | confirmed |
| 54 | **There are three ring firmwares on disk, not four**, and the plan's "four independent readers" was one too many. `3000_RING_1818-4882_9-4-90.bin` and `5500_RING_1818-4882_R9_12-10-90.bin` are **byte-identical** by SHA-256 -- the same firmware shipped for both models, which their shared part number `1818-4882` says in the file name and nobody had checked. `[ROM3500]` (`10666` rev 6) and `[ROM4500]` (`10666` rev 8) are distinct from it and from each other: 53.5% and 53.8% of bytes differ from the `1818-4882` image, and 40.9% differ between the two `10666` revisions. So a cross-read has three sources, and the two sharing a part number are the closest pair | SHA-256 of `roms/firmware/*RING*.bin` | confirmed |
| 55 | **The ring controller's registers are documented after all, in a manual on disk since the start.** `[EH]` chapter 7 (DN3xx, pp. 7-28 to 7-30) and chapter 10 (DN5xx) both carry a **RING REGISTERS** section giving the whole page at `9800`: `+00` transmit command/status, `+02` receive command/status, `+04` TMASK, `+06` diagnostic command/status, `+08` ring ID. Every bit is named. Transmit command: `4000` interrupt enable, `2000` transmit enable "(start the transmit)", `1000` force transmit, with the notes "To start a transmit normally, use 6000", "To force transmit, use 7000", and "Writing anything to this register clears the transmit interrupt". Transmit status: `8000` interrupt pending, `4000` interrupt enabled, `2000` busy, `1000` disconnected, `0800` bi-phase error, `0400` elastic store buffer, `0200` no return, `0100` CRC error, `0080` ack parity error, `0040` external, `0020` protocol, `0010` icopy, `0008` ack byte errbit, `0004` copy, `0002` wack, `0001` underrun -- **with "a successful transmit will have a transmit status of 0014" and "a WACK will have 0012"**. Receive status and diagnostic status likewise, the latter including `2000` connected to the network, `1000` sticky bi-phase and a `01FF` bad-packet counter. Read from the **page images** at PDF pages 167-168, which confirm the text layer exactly | `[EH]` 002398-04 pp. 7-28..7-30, page images | confirmed |
| 55a | **It corroborates the driver, independently.** Finding 53c's nineteen condition names, taken from `domain_ring.pas` on the volume, are these bits: `WACKs`, `Xmit overrun`, `Xmit bus err`, `Rcv CRC error`, `Rcv overrun`, `Rcv ack parity`, and the rest. And 53b's `RING_$POLL_STICKY_BPHERR` is diagnostic status `1000 sticky bi-phase`, "error seen since bit was cleared" -- which is exactly the *sticky* reading 53b inferred from the name alone. Two sources that have never met agreeing on a vocabulary is what makes it safe to use | 53b, 53c against 55 | confirmed |
| 55b | **Whether this maps onto the DN3500's `+400` is NOT settled, and the tempting match fails arithmetic.** `[EH]` documents the DN3xx/DN5xx board at physical `9800` with 16-bit registers; the DN3500's is an AT-bus board whose firmware uses `+400` (status word), `+402`/`+404` (**byte**-wide commands) and `+406` (auto-incrementing data port) -- a different layout. The seductive step is that finding 48's `+402 = $6` looks like the note's "use 6000". **It does not follow**: on a big-endian part a byte written to `+402` is the *high* byte of the word there, so `$6` gives `0600`, and `6000` would need `$60`. What *is* suggestive and cheap to test is the other end -- 45's polled bits 2 and 1 are `0004 copy` and `0002 wack`, and `0014`/`0012` are the documented success and WACK codes, so a transmit helper polling exactly those two is polling exactly what the note says to read. Recorded as promising and unproven; the arithmetic above is why 48's bits stay open | 45, 48 against 55 | open |
| 56 | **The ring ROM's failure codes are a 40-entry subtest numbering, and two of them are a trap.** Every `MOVE.L #$E00000xx,D0` in `[ROM3500]` -- forty sites -- loads a code from the sequence `01 02 03 · 11-16 · 21-26 · 31 32 · 41 42 · 51 52 · 61-64 · 71-77 · 81-88`. That is group-and-subtest numbering, and it is what the self-test reports a failure as, which makes it the map for reading the "ring firmware's own self-test" that `CLAUDE.md` names as the controller's first real test. **The trap**: the codes loaded immediately before the transmit polls are `E0000012` and `E0000014`, and `[EH]` gives `0012` as a WACK and `0014` as a successful transmit (finding 55). They are not the same numbers twice -- the sequence above shows `12` and `14` are simply the second and fourth subtests of group 1, and `22`/`24` appear at the next pair of sites. Their adjacency to polls of bits 1 and 2, which are what `0012` and `0014` are *made of*, is coincidence of the most persuasive kind | `[ROM3500]`, all 40 sites | confirmed |
| 56a | ~~The polled bits are expected *set*~~ -- **CORRECTED by 56b, one iteration later.** The claim that `d4 = 1` means "wait for the bit to become 1" was read off the call sites without reading the helper's body, and it is wrong | superseded | retracted |
| 56b | **The polling helpers are stability tests, not waits, and `tools/ring-rom/` already had the listings.** `disasm.py` and `r3000.lst`/`r3500.lst`/`r4500.lst` have been on disk since 9 August; hand-decoding bytes for 56a was redoing work that existed. The three helpers share one shape: loop reading `+400`, mask the bit, **exit early when it reads CLEAR**, otherwise `dbra` to a bound; then `d2 = d4 ? bit : 0` and `cmp` against the last value read. So `d4 = 1` means *the bit must still be set when the bounded wait ends* -- it must **remain** set -- and `d4 = 0` means it must go clear. `$9D2` is bit 13 over 1000 iterations; `$9FA` is bit 2 over 65535 and clears `$2(a3)` before comparing; `$A28` is bit 1 over 65535 and clears `$4(a3)`. Those two side-effect writes are new: `a3` is the **first** window (`$51000`, finding 44), so each helper acknowledges something there after polling status in the second | `tools/ring-rom/r3500.lst` `09D2`-`0A54` | confirmed |
| 57 | **The two `10666` revisions corroborate each other exactly, and the `1818-4882` firmware is not a third reader of these registers.** `r3500` and `r4500` each poll `+400` under precisely four masks -- `$2000`, `$800`, `$4`, `$2` -- and use `+400`/`+402`/`+404`/`+406`; the two images differ in 41% of their bytes, so that is genuine independent agreement on bits 13, 11, 2 and 1. **`r3000` never references `+400` at all**: its register offsets are `(a2)`, `$2(a2)`, `$4(a2)`, `$6(a2)`, `$10(a1)`, `$14(a1)`. So finding 54's three distinct firmwares are **two** readers of the AT board's register block, and the third addresses a different one | `tools/ring-rom/r3000.lst`, `r3500.lst`, `r4500.lst` | confirmed |
| 57a | **And `r3000` is *not* established as the `[EH]` layout, though the offsets invite it.** Small offsets `+00`/`+02`/`+04`/`+06` and `+10`-`+16` are exactly finding 55's documented DN3xx page, which would make `r3000` a Rosetta stone between the documented registers and the AT board's. The **values refuse it**: `r3000` writes `#$800` to `(a2)` and `#$600`/`#$610`/`#$10` to `$6(a2)`, while `[EH]`'s transmit command is built from `4000`, `2000` and `1000` and its note says a normal transmit is `6000`. Offsets agreeing while values disagree is not an identification, and this is the fourth numeric near-match this file has had to refuse. What would settle it is which base `a2` holds in that firmware -- unread | `r3000.lst` against finding 55 | open |
| 58 | **`r3000` names eight window bases `0x400` apart, and that reframes what `+400` is.** Its absolute immediates are `$51000 $51400 $51800 $51C00 $52000 $52400 $52800 $52C00` -- eight of them, evenly spaced -- where `r3500` has **no** `#$5xxxx` immediate at all, because finding 38's `$CA0` computes its bases. Read against 38, which puts unit 0 at `$51000` and unit 1 at `$52000`, each unit is **four `0x400` windows** and the `1818-4882` firmware addresses them as eight separate bases. So the `10666` firmware's `$400(a4)` is plausibly the **second window of a unit** rather than an offset within one register block, which is a different thing to model: four windows with registers near their bases, not one block with registers 1 KB in | `r3000.lst` immediates against finding 38 | confirmed (the bases), open (the reading) |
| 58a | **And it settles 57a in the negative.** `a2` in `r3000` is loaded from `$14(a1)`, `$18(a1)`, `$10(a1)` or `(a1)` -- a pointer taken *out of a structure*, never set to a window constant. So its small offsets `$2(a2)`, `$6(a2)` are structure fields, not the `[EH]` register page, and the identification 57a declined is not merely unproven but **refuted**. This is the same shape as 53i, where the Domain/OS driver's `$20(A0)` turned out to be a queue pointer: on this controller, a small offset from a register loaded out of memory has twice now looked like a register map and twice not been one | `r3000.lst` `0000FA`, `000118`, `0002C2` | confirmed |
| 53d | And the **prose definitions** for six of them, which give ring semantics rather than register bits: a transmit timeout "counts the number of packets sent by this node that **did not return**" and "can indicate a break anywhere in the network"; a transmit bus error is a faulty transfer "between the controller and the node's memory"; a transmit acknowledge parity error is a packet that "returned with a corrupted hardware protocol field"; a transmit overrun is memory-to-ring contention that "can occur when the node is simultaneously trying to transmit packets and perform other internal data transfers such as disk I/O"; a **WACK** is a packet returned "with marks showing that the intended receiver saw the packet but could not receive it"; a **NACK** is one returned "without the mark that acknowledges receipt" | `…awd` @ `0x07110A06`-`0x07110F24` | confirmed |
| 53e | **What the search cost, and one false positive worth recording.** Scanning for the four polled masks as `ANDI.W` immediates gave exactly one cluster in 348 MB -- and it is **data**, a walking-bit table (`0240 4000`, `0240 2000`, `0240 1000`) among glyph patterns. The signature was wrong in principle as well as in fact: the ROM uses `and.w #mask` to test several bits at once, while a driver testing one bit uses `btst #n`, which contains no mask word at all. **Search a driver by its symbols, not by the ROM's instruction shapes** | `…awd` @ `0x04C425C8` | confirmed |
| 53f | **The driver's files are named, and the module is small.** Directory entries list `ring8a.drvr`, `ring8b.drvr`, `ring8a.dex`, `ring8b.dex` and `itr8.drvr` together. The link map sizes `RING_PROC` at **`0x3370`** bytes loaded at `FFCCC000`, so the whole thing is 13 KB -- and the symbol addresses give offsets *within* it: `RING_$SENDP` at `+0xABC`, `RING8_$INT` at `+0x758`, `RING8_$INT_DEFERRED` at `+0x2CC`, `RING_$POLL_STICKY_BPHERR` at `+0x19A4`, `RING_$START` at `+0x1EDA`, `RING_$KICK_DRIVER` at `+0x1F60`, `RING_$SET_TMASK` at `+0x1FAE`, `RING_$GET_STATS` at `+0x2026`, `RING_$IOCTL` at `+0x221E`. So once the 13 KB is in hand, every routine is at a known offset | `…awd` @ `0x09D754E4`, `0x0038306C` | confirmed |
| 53g | A second machine's map is in the same image at `0x00C5BC7D` with `RING_PROC` at `size = 340C`, and a third at `0x12047ADA` with `size = 3480` -- three revisions of the same driver. Also present: a **`RING8025`** family (`RING8025_UCB`, `RING8025_PIO_IOMAP`, `RING_8025`), which is 802.5 token ring and **not** this board; do not conflate them when searching | `…awd` @ `0x0037C13F` | confirmed |
| 53h | **What remains, and it is a filesystem problem rather than a research one.** The map's addresses are load addresses and the directory entries are AEGIS directory records, so extracting `ring8a.drvr` needs the on-disk filesystem walked -- there is no route from either to a byte offset. That is the single remaining step between here and `+400`'s bit meanings | — | open |


## Open

| # | Question | How it will be answered |
| --- | --- | --- |
| A | The controller's register map within `0x220`–`0x23F` | **Largely recovered — findings 11-15, 38-41a and 44-50a.** Settled: the unit-to-window mapping, `+000` as an ASCII ID register, `+400` bit 15 as a presence gate, and `+800`/`+C00` as two 8254 timers with their counter and control ports. Remaining: the meaning of `+402`, `+404`, `+406`, of `+400`'s other bits (bit 11 is written, purpose unknown), and of the `a1` window at `$51000` whose `+2`/`+4`/`+6`/`+400`/`+402` are cleared at init |
| B | Dual-ported RAM buffer: size, host window, and descriptor/queue layout | **Size and access path answered — findings 46, 46a and 47.** The buffer is **not memory-mapped**: it is reached through the auto-incrementing `+406` data port with `+006` as its pointer, and the self-test walks **64 KB** of it in four patterns. The read port is pipelined by one word. This overturns finding 42's conclusion, which assumed a dual-ported buffer had to appear in AT memory space and closed the question on not finding one there; 42's scan was right and its inference was not. What remains is the **descriptor and queue layout** — the self-test uses the buffer as flat memory and never as a structure, so the ROM cannot show one. Domain/OS's ring driver is still the route for that, and only that |
| C | Exact token and frame character encodings on the wire | `[MAC]` §2.2.1–2.2.2, transcribed with each format citing its subsection |
| D | Ring latency contributed per node by the elastic-store buffer, and PLL acquisition behaviour | **Partly answered — findings 28, 29 and 52-52c; `[PAT575]` now read and exhausted (52d).** The buffer's hardware ceiling is two bits and an overflow resets the receive and transmit counters to a predetermined value. What remains is a *numeric* acquisition time, which `[PAT575]` does not give and which no held source does The steady-state delay is now evidenced: nominally 1 bit per node, bounded by 0.5 and 1.5, with the offset linear in frequency deviation across ±3 kHz. What remains is *acquisition*: how long a PLL takes to lock, and what the ring does during the re-initialisation that an ESB under/overflow forces. `[MAC]` gives neither; `[PAT575]` is the remaining source |
| E | Token-loss detection, ring reconfiguration and node insertion/removal timing | **Partly answered — findings 34, 35 and 37.** The transmit sequence, stripping, and the 10.9 ms (2^14 byte) stripping timeout are evidenced, and node removal losing an in-flight token is measured. What remains is the **token-loss timeout itself**: §2.2.1.1 says a node forces a claimed token "after a specified timeout" and never specifies it. This core uses the stripping timeout as a `PROVISIONAL` stand-in, marked in `ap_ring_station.h`. **`[PAT575]` has now been read and does not contain it** (finding 52d) -- it is a physical-layer synchronisation patent. No held source names the timeout |
| G | How Domain/OS's single-level store maps onto ring packets — page faults across nodes | `[AEGIS]`, `[ARCH]` |

**H is resolved, and the answer is the negative one it was framed to accept.**
It asked what the 2-byte trailer at offset `length` means. The route named was to
disassemble the boot PROM's option-ROM scan -- "if that scan never touches offset
`length`, the field is not consumed by the machine and the question closes as
*not read by firmware*, which is a sufficient answer for emulation". It never
touches it. `length` is read by **exactly one instruction in the whole 64 KB
PROM** -- `move.l $C(a1),d1` at `01098`, searched for exhaustively as any
`(d16,A1)` operand with displacement `$000C` -- and it is used as a *count*: the
sum runs over `[0, length)` and stops one long word short of the trailer. The
three other `(d16,A0)` reads at that displacement are a vector-table setup and a
list walk, neither in the option-ROM path, which uses `A1` as the image base
throughout.

So the trailer is outside the image, outside the checksum, and outside anything
the firmware reads. It stays unexplained and it is **not a blocker**: an emulated
option ROM need not reproduce a field no code consumes. If a future disassembly
of the *ring* firmware turns out to read it, this reopens -- but the machine that
loads these ROMs does not.

**F is resolved** — see findings 10, 10a and 10b. It asked whether 12 Mbit/s was
the bit rate or the symbol rate, because a non-12 MHz line clock would force
`AP_TIME_BASE_HZ` to be recomputed. It did: the PHY is bi-phase with a 24 MHz
line clock, and the base is now 6.6 GHz. Resolving it before implementing the
medium was the point — the answer changed the unit of account, and changing that
after goldens exist would have invalidated every one of them.

Item D is now the remaining physical-layer unknown, and 10b is the part of it
that the medium will have to face first.

## Divergences from the oracle

None yet: there is no oracle for the ring to diverge from. When the 3c505 802.3
path is implemented, that one *does* have a runnable reference and gets its own
row in `tools/mame-oracle/FINDINGS.md`.
