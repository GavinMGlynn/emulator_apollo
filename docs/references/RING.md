# Apollo Token Ring — findings

The ring has **no runnable reference implementation**: MAME's Apollo driver
carries Domain networking over an emulated 3c505 802.3 card instead. So every
fact here cites a primary source — a manual section, a patent, or an address in a
dumped firmware ROM. Nothing in this file is established by reasoning about our
own code.

Status legend: `confirmed` · `open` · `provisional` · `deliberate divergence`

## Sources

| Key | Document |
| --- | --- |
| `[MAC]` | *Apollo Token Ring Media Access Control Layer and Physical Layer Protocols*, order 010005-00 rev 00, Oct 1987 (29 pp) |
| `[S3K]` | *Domain Series 3000/4000 Technical Reference*, 008778-03, Aug 1987 |
| `[EH]` | *Domain Engineering Handbook*, 002398-01/-03/-04 (Apr 83 / Feb 85 / Jan 87) — **carries a full RING REGISTERS section** for the DN3xx and DN5xx boards, finding 55 |
| `[ARCH]` | *Apollo Domain Architecture*, Feb 1981 (and the Jan 1981 preliminary) |
| `[PAT575]` | US patent 4,716,575, *Adaptively synchronized ring network for a computer system* |
| `[AEGIS]` | *AEGIS Internals and Data Structures*, Jan 1986 |
| `[PLAN]` | *Planning Domain Networks and Internets*, 009916-A00, Aug 1988 |
| `[ROM3500]` | `roms/firmware/3500_RING_10666_6.bin` — 8 KB, rev 3.6 |
| `[ROM3000]` | `roms/firmware/3000_RING_1818-4882_9-4-90.bin` — 8 KB, rev 4.0 |
| `[ROM4500]` | `roms/firmware/4500_RING_10666_8.bin` — 8 KB |
| `[ROM5500]` | `roms/firmware/5500_RING_1818-4882_R9_12-10-90.bin` — 8 KB, **byte-identical to `[ROM3000]`** (finding 54), so it is the same source and not a second one |

`[MAC]` structure, for citation: ch. 1 overview and protocol layers; ch. 2 MAC —
2.2.1.1 tokens free and claimed, 2.2.1.2 frame start and separator characters,
2.2.1.3 null separators, 2.2.2.1 frame start sequence, 2.2.2.2 packet header
sequence, 2.2.2.3 packet data sequence, 2.2.2.4 frame check sequence, 2.2.2.5
end-of-frame sequence; ch. 3 physical — 3.2 data stream, 3.3.1 phase-lock loops,
3.3.2 elastic-store buffer, 3.4 signal characteristics, 3.5 passive network
bypass; app. A coaxial driver, receiver and cable.

## Established

| # | Finding | Source | Status |
| --- | --- | --- | --- |
| 1 | Proprietary 12 Mbit/s ring over 75 Ω RG-6U coax, introduced 1981; not IEEE 802.5 and not interoperable with it | `[MAC]`, `[ARCH]` | confirmed |
| 2 | Controller occupies AT-bus I/O `0x220`–`0x23F`, appearing at Apollo physical `0x051000`; a second controller sits at AT `0x320`–`0x33F` / `0x059000` | `[S3K]` Table 2-9 | confirmed |
| 3 | Controller comprises a modem, input filtering and amplification, serial→parallel receive logic, parallel→serial transmit logic, a **dual-ported RAM buffer**, control logic, and relays that remove the node from the ring when powered off or offline | `[S3K]` §1.5.4 | confirmed |
| 4 | Older DN3000s use a two-board controller set; newer DN3000s and the DN4000 use a single board of the same function | `[S3K]` §1.5.4 | confirmed |
| 5 | Two board generations are in hand: Apollo part `10666` (DN3500, DN4500) and HP part `1818-4882` (DN3000, DN5500). Both ROMs identify as `Apollo Token Ring Network Controller-AT` | `[ROM3500]`, `[ROM4500]`, `[ROM3000]`, `[ROM5500]` | confirmed |
| 5a | `[ROM3000]` and `[ROM5500]` are **byte-identical** (md5 `6b8a2134474932c81acb3093c61619f4`), so the four dumps are three distinct images: `[ROM3500]`, `[ROM4500]`, and the one shared 1818-4882 image. The two dumps carry different dates in their filenames but the same content, so one image served the whole 1818-4882 generation. Disassemble it once, not twice | `[ROM3000]`, `[ROM5500]` | confirmed |
| 6 | Ring ROMs contain 68000 code and a self-test whose diagnostics are `ring: init error`, `ring: transmit error`, `ring: receive error` — so the firmware doubles as our first controller test | `[ROM3500]`, `[ROM3000]` string tables | confirmed |
| 6a | That 68000 code is executed by the *host* CPU rather than an on-board processor. **Inference, not yet confirmed:** `[S3K]` §1.5.4 enumerates the board's functional units and lists no microprocessor, and the ROM is 68000 rather than 8-bit code | `[S3K]` §1.5.4 + `[ROM3500]` | open — settle it in the disassembly |
| 7 | Both ROMs share an 8-byte prologue `33 5E 91 B6 00 00 A0 B6`, then ASCII `R` at +0x08 and a revision string at +0x14 (` 3.6` for `[ROM3500]`, ` 4.0` for `[ROM3000]`) — a common header format across both board generations | `[ROM3500]`, `[ROM3000]` | confirmed |
| 7a | Every option ROM in hand carries exactly **two non-zero bytes at offset `length`** — just past the checksummed image — followed by nothing but zero fill to the end of the 8 KB device: `$354F` `[ROM3500]`, `$BCF9` `[ROM4500]`, `$0057` `[ROM3000]`/`[ROM5500]`, `$56A0` on the 3C505 Ethernet ROM. Present across both board generations *and* across controller types, so it is part of the option-ROM format, not ring-specific. Purpose unknown — open question H | `[ROM3500]`, `[ROM4500]`, `[ROM3000]`, `[ROM5500]`, `3000_3C505_010728-00` | confirmed (existence), open (meaning) |
| 8 | Node ID PROM is a separate device at `0x011200`, distinct from the ring controller; 32 bytes (`3500_NI_1C874`). Domain/OS may instead take the node ID from the logical volume label of the first logical volume | `[S3K]` Table 2-8, MAME driver notes | confirmed |
| 9 | A single ring scaled to well over a hundred nodes without the degradation contemporary Ethernet suffered — the reason Apollo kept it proprietary | `[PLAN]` | confirmed |
| 10 | 12 Mbit/s is the **data** rate, not the symbol rate. The PHY is bi-phase encoded: "In the time it takes to transmit one bit (this is a bit cell, or 83.33 nsec), two windows exist: the clock window and the data window" — a clock window that must always carry a transition, and a data window whose transition (or absence) is the bit value. Each node encodes and decodes with "the 24-MHz clock generated by its transmit \[receive] phase-lock loop" | `[MAC]` §3.2 p.3-3 | confirmed |
| 10a | So the ring is **two** clock domains: a 12 MHz data clock and a 24 MHz line clock, and both must divide the time base exactly. This is what raised `AP_TIME_BASE_HZ` from 3.3 GHz to 6.6 GHz — 3.3 GHz divides 24 MHz only as 137.5 | `[MAC]` §3.2 p.3-3 | confirmed |
| 10b | 24 MHz is the transmit PLL's *nominal centre* frequency, not a fixed one: phase offset is ≤0.5 bit-times at 24 MHz −3 kHz and ≥1.5 bit-times at 24 MHz +3 kHz, and a node's elastic-store buffer re-initialises at 24 MHz after under/overflow. The emulated ring runs the nominal figure; per-node frequency deviation is a deliberate non-model until item D gives it a measured envelope | `[MAC]` §3.3.1–3.3.2 pp.3-4–3-5 | confirmed |

| 11 | The controller is addressed through a base pointer the firmware loads with `lea.l $59000.l, a2` (`[ROM3500]` `000CAA`, commented RING1 = AT I/O `0x320`–`0x33F`) and `lea.l $5a000.l, a2` (`000CBE`). That confirms finding 2's placement from the firmware's own side rather than from the manual's | `[ROM3500]` `000CAA`, `000CBE` | confirmed |
| 12 | **Four banks at `+000`, `+400`, `+800`, `+C00`, each with slots at `+0`, `+2`, `+4`, `+6`.** Fifteen distinct offsets are touched on the DN3500 and DN4500 ROMs and only the first bank's four on the DN3000's, so the bank structure is the *later* board's. Extracted mechanically from the listings, every offset with the ROM address of its first access | `[ROM3500]`, `[ROM4500]`, `[ROM3000]` disassembly | confirmed |
| 13 | `+400` carries a **status word whose bit 15 is polled**: `move.w $400(a2), d0` then `and.w #$8000, d0` then a branch, before the firmware clears `(a2)`, `+402`, `+404` and `+400` in that order. So `+400` is read for readiness and the group is then reset together | `[ROM3500]` `0000AE`–`0000C2` | confirmed |
| 14 | `+806` and `+C06` are **byte** registers written with the sequence `$30`, `$70`, `$B0` (`+806`) and `$30`, `$70` (`+C06`), immediately after that reset. Two banks driven with the same pattern one after the other suggests two identical sub-devices — consistent with finding 3's separate transmit and receive logic, though the assignment of which is which is *not* evidenced | `[ROM3500]` `0000C6`–`0000DE` | confirmed |
| 15 | `+000` is read with `movea.l (a2), a0` (`0008BC`, `0008FE`), so the first slot is read as a **long** and used as an address. `+800`, `+802`, `+804`, `+C00` and `+C02` appear only as `lea.l` operands — the firmware takes their addresses rather than their contents. Both point at those regions being **buffer** rather than register, which is question B's subject | `[ROM3500]` `0008BC`, `0008FE`, `000D62`–`000DA8` | confirmed |

| 16 | **The boot PROM's option-ROM validator, recovered.** `3500_BOOT_12191_7` matches a candidate in three variants at `01032`, `0104E` and `0106A`: all three require `magic0 = $335E91B6` at `+0`, then one requires `magic1 = $0000A0B6` at `+4` *and* a caller-supplied class in `d0` against `+8`, one requires `magic1` and a word match against `+1A`, and one requires `magic1 = $C000A0B7` alone. So `rom_id` at `+8` is matched against what the *caller* is looking for — `'R   '` when the firmware wants a ring board — which is why the same scan serves the ring and the 3C505 | `3500_BOOT_12191_7` `01032`, `0104E`, `0106A` | confirmed |
| 17 | **The checksum routine at `01080`, and what it does not read.** It skips the sum entirely when `hdr_ver` at `+18` is greater than 1, or when the fudge longword at `+10` is `$FFFFFFFF`; otherwise it takes `length` from `+0C`, shifts it right by two, and sums that many long words from the image base with `add.l (a0)+,d0`. The last long read is therefore at `length - 4`. The ring ROMs carry `hdr_ver = 1` and a real fudge longword, so they *are* checksummed | `3500_BOOT_12191_7` `01080`–`010AC` | confirmed |
| 17a | And the scan's shape was then seen from the running machine, which is the same code from the other side: a headless boot records the AT bus empty-slot addresses `00080000`-`00080003`, `00081000`-`00081003`, `00082000`-`00082003`, `00083000`-`00083003` — four bytes at each of four 4 KB slots, which is `magic0` being read and failing to match with no card fitted | measured, `--boot-limit 350000000` on the DN3500 | confirmed |
| 18 | **The four out-of-band characters, nine bits each.** Every one is a leading `0`, six `1`s, then two type bits, most-significant-bit first: `0 111111 00` separator, `0 111111 01` frame start, `0 111111 10` free token, `0 111111 11` claimed token — `0x0FC`, `0x0FD`, `0x0FE`, `0x0FF` as nine-bit values. They are *not* byte values; the leading zero occupies a bit time on the wire | `[MAC]` Figures 2-2 (p. 2-4), 2-3 and 2-4 (p. 2-5) | confirmed |
| 18a | **Read from the page images, not the text layer.** `pdftotext` renders those three figures as fragments like `MSB L.1_o--L_--'_ _ ..L~_U_S_T_BLiE_O_N_E......F~_-,--_---,-_1---11----,1 LSB` — a `1`, a `0`, and no way to tell which cell either belongs to. Rendered at 200 dpi with `pdftoppm` and read as images instead. `CLAUDE.md`'s rule about page images exists for exactly this | `[MAC]` pp. 2-4, 2-5 | confirmed |
| 19 | **Bit stuffing is what makes them recognisable.** "a transmitting node inserts a Zero ... after every five successive Ones", and the receiver extracts each zero following five ones — so six successive ones cannot occur in data, and their presence "tells a receiver: *the bit-stuffing protocol has been intentionally violated by the transmitter*". The two bits after the six ones then select the character | `[MAC]` §2.2.1 p. 2-4 | confirmed |
| 20 | Null separators: the **long** one is "a minimum of 8 bytes of Zeros" and precedes the packet; the **short** one is "a byte of Zeros" and occurs within the frame start sequence and within the frame check sequence. A minimum, modelled as a minimum | `[MAC]` §2.2.1.3 p. 2-5 | confirmed |
| 21 | **Packet header layout**: destination address `+0` (2 words), type `+4` (1 word), a zero byte `+6` and the early acknowledge byte `+7`, source address `+8` (2 words), header data from `+C`. 12 to 1024 bytes, always even — and 12 + 1012 = 1024, so the two figures close exactly. The controller "will always transmit the first 12 bytes ... even in a broken network", which is what beaconing rests on | `[MAC]` Figure 2-5 and §2.2.2.2 p. 2-6 | confirmed |
| 22 | **Type field** is a 16-bit word whose named bits are 7:1 — broadcast `<7>`, hardware diagnostics `<6>`, thank-you (reply) `<5>`, please (request) `<4>`, paging `<3>`, user/IPC `<2>`, software diagnostics `<1>`. Bits 15:8 **and bit 0** are reserved, so it is a seven-bit field and not an eight-bit one. Broadcast set means receivers ignore the destination field, whose bits are then "free for beaconing" | `[MAC]` Figure 2-6 p. 2-7 | confirmed |
| 23 | **Early acknowledge** (byte): bit 7 must be zero, 6:5 reserved, 4 must be zero, 3 intend-to-copy, 2 reserved, 1 odd parity, 0 must be zero. **Late acknowledge** (byte, in the end-of-frame sequence): 7 must be zero, 6 copied, 5 wait-ack, 4 must be zero, 3 intend-to-copy, 2 error, 1 odd parity, 0 must be zero. Both are inserted by the transmitter and modified by *other* nodes as the frame passes | `[MAC]` Figures 2-7 p. 2-8, 2-8 p. 2-9 | confirmed |
| 24 | **The CRC is not Ethernet's.** `[MAC]` gives the generator as a product, g(X) = (X^21 + 1)(X^11 + X^2 + 1), which expands to X^32 + X^23 + X^21 + X^11 + X^2 + 1 — register form `0x00A00805`, against Ethernet's `0x04C11DB7`. Initialised to zero, transmitted most-significant-bit first, no reflection or final inversion mentioned. A ring built on the familiar polynomial would emit frames no Apollo node accepts and no self-consistent test would catch it | `[MAC]` §2.2.2.4 p. 2-8 | confirmed |
| 25 | **The early acknowledge byte is CRC'd as zeros**: "ring hardware treats this field as a string of Zeros in its CRC calculation", which is what lets a receiver rewrite it in flight without the frame check going stale. The receiver's CRC covers the packet header and data sequences and the separators, ignoring bit-stuffing bits; the *late* acknowledge field is outside it entirely, being neither sequence | `[MAC]` §2.2.2.2 p. 2-8, §2.2.2.4 p. 2-8 | confirmed |
| 25a | Open detail inside 25: §2.2.2.4 says the CRC covers "the separators" without saying **how** a nine-bit out-of-band symbol is fed to a bit-serial CRC. This core feeds all nine bits. No capture exists to settle it, so the choice is marked `PROVISIONAL` in `ap_ring_frame.h` and will stay so until the ring firmware's own CRC routine is disassembled | `[MAC]` §2.2.2.4 p. 2-8 | provisional |
| 26 | Packet data: 0 to 4096 bytes, always even, "typically ... 1024 bytes". The controller may abort after any even byte on error or when the early acknowledge shows nobody is copying, and an abort sets the error bit in the late acknowledge field | `[MAC]` §2.2.2.3 p. 2-8 | confirmed |
| 27 | **Bi-phase, precisely.** Each 83.33 ns bit cell holds a clock window and a data window. "In each clock window, a transition ... must always be present or a bi-phase error will occur **and the corresponding data will be interpreted as having a bit value of Zero**"; in the data window a transition is a One and its absence a Zero. So the encoding is differential -- absolute level carries nothing -- and a corrupted cell yields a *bit*, not a gap, which keeps byte framing intact across a single glitch | `[MAC]` §3.2 p. 3-3 | confirmed |
| 28 | **Network stability is the whole purpose of the elastic store.** "the total delay around the network must be exactly an integral -- rather than a fractional -- number of bit-times", and each node's buffer contributes the fractional part that makes the sum whole. Nominally a **1-bit** delay when a node's transmit and receive PLLs are in phase; the range is `0.5 <= ESB <= 1.5` bits; underflow at "0.5 bit-times or less" and overflow at "1.5 bit-times or more", after which "the network is forced to re-initialize at a new operating frequency (24 MHz)". The bounds are therefore **inclusive failures** | `[MAC]` §3.3, §3.3.2 pp. 3-4 | confirmed |
| 29 | **PLL relationship.** Each node has one receive and one transmit PLL. Receive loops "track over a greater range of frequencies and adjust faster", so a node's receive PLL is always synchronised to the *preceding* node's transmit PLL. The transmit PLL tracks the receive PLL "with a damped response, so most phase jitter never propagates to the next node", and "the transmit phase-lock loop's phase gain is always less than one", which is what makes stability around the ring provable rather than hoped for | `[MAC]` §3.3.1 p. 3-4 | confirmed |
| 30 | **Passive bypass does two things at once**: relays join the node's input coax to its output coax *and* join its transmit output to its receive input. The second half is what lets a bypassed node run loopback self-tests -- so the ring firmware's own self-test, which is this controller's first real test, requires modelling both | `[MAC]` §3.5 p. 3-5 | confirmed |
| 31 | Analogue signal characteristics, recorded but **not modelled**: driver cutoff 18 MHz, receiver sensitivity -20 dBm, maximum 1 km between nodes, bypass insertion loss <= 1 dB | `[MAC]` §3.4 p. 3-5 | confirmed |
| 31a | **An inconsistency in `[MAC]` itself, not a transcription error.** §3.4 gives transmitted power as "18 dBm into 75 ohms (typically, 2.5 V peak-to-peak)". 2.5 V peak-to-peak into 75 ohms is about 10 dBm as a sine or 13 dBm as a square wave -- not 18. Verified against the page image at 220 dpi, so the manual really does say it. Recorded so a later reader who does the same arithmetic does not have to wonder whether we mis-copied it. Nothing depends on either figure | `[MAC]` §3.4 p. 3-5, page image | confirmed (as an inconsistency) |
| 32 | **A ring must be longer than its own token.** A token is nine bits and a transceiving station contributes one bit of delay (`[MAC]` §3.2 with §3.3.2's nominal), so a ring whose total delay is under nine bit-times has its token's head return before its tail has left, and the symbol overwrites itself. Real rings are nowhere near that bound because §3.3 counts "cable plant" as a static delay element and 1 km between nodes is about 60 bit-times at 12 Mbit/s. The medium now models per-hop cable delay, so a small ring can be given a realistic circumference: three stations with four bit-times of cable each is fifteen, comfortably over a nine-bit token | derived from `[MAC]` §3.2, §3.3, §3.3.2 | confirmed |
| 33 | **An idle ring still carries clock.** §3.2 requires a transition in every clock window, so a live ring transmits transitions even when it carries no data — a run of encoded Zeros, which alternates the line level. A line of all-zero cells has no transitions at all: that is a *dead* line, and a receiver decoding it reports a bi-phase error on every bit, correctly. The distinction matters for anything that models a cable, which has to start out holding idle rather than nothing | `[MAC]` §3.2 p. 3-3 | confirmed |
| 34 | **The transmit sequence, all eight steps.** A node acquires the ring by modifying a passing free token; "it breaks ring recirculation and begins to transmit its packet. Concurrently, it begins to discard received data (including its own packet ...). The process of discarding received data is called **stripping**"; it pads with Zeros just after the modified token; the MAC layer wraps the packet in a frame; the node "sends out a new free token to follow the frame"; it strips until it receives its own frame back or times out; "When a node stops stripping, recirculation resumes around the ring" | `[MAC]` §2.1 pp. 2-1, 2-2 | confirmed |
| 35 | **The one recovery figure with a number: the stripping timeout.** "until it finishes receiving its own frame, or until a **10.9 msec (2^14 byte)** timeout occurs. This timeout prevents a node from stripping bits forever (for example, if its frame has gotten lost on the ring)." The two forms cross-check: 2^14 bytes is 131,072 bits, and at 12 Mbit/s that is 10.923 ms. **The exponent does not survive a text extraction** — `pdftotext` renders it "214 byte", a plausible and entirely wrong number — so this came from the page image | `[MAC]` §2.1 step 7 p. 2-2 | confirmed |
| 36 | The bit stuffing is explicitly "a Synchronous Data Link Control- (SDLC-) type of bit-stuffing algorithm", and "all data must be transmitted most-significant-bit first (in a byte) and most-significant-byte first (in a multi-byte field)" — which confirms from the manual's own words the big-endian ordering `ap_ring_frame.*` had taken from the figures | `[MAC]` §2.2 p. 2-2 | confirmed |
| 37 | **Removing a node mid-run can lose the token, and that is not a defect.** §3.5's relays keep the *signal path* intact across a departing node, but a token that happens to be inside that node or the cable it drives goes with it. Measured here: after bypassing a node the far side stops seeing tokens entirely, and a token put on afterwards circulates normally. This is precisely the loss §2.2.1.1's forced claimed token recovers from | measured, `ring_station_suite` | confirmed |
| 38 | **A "unit" spans both AT windows.** `$CA0` maps a unit number to *two* base pointers: unit 0 gets `a1 = $51000` (ISA `0x220`) **and** `a2 = $59000` (ISA `0x320`); unit 1 gets `$52000`/`$5A000` (ISA `0x240`/`0x340`); anything else returns zero and the caller reports `ring: init error`. So finding 2's "a second controller sits at AT `0x320`" is how `[S3K]` lists the ranges, and the firmware drives the pair together as one unit. Whether that is one board with two decodes or two boards driven as a pair is *not* settled by this code | `[ROM3500]` `000CA0`-`000CCE` | confirmed |
| 39 | **`(a2)+000` is an 8-bit ID register.** `$CD0` clears the host's instruction cache (`movec cacr,d0` / `bset #3` / `movec d0,cacr`), reads a **byte** from `(a2)`, then burns three `and.w #$ffff,d0` as a delay. Init accepts only `$36` or `$37` — ASCII `'6'` and `'7'` — and otherwise returns the string `ring: init error` | `[ROM3500]` `000CD0`-`000CEA`, `00009C`-`0000AA` | confirmed |
| 40 | **`+400` bit 15 is a presence gate.** Init reads `$400(a2)`, masks `$8000`, and if it is **clear** returns success having touched nothing else — so a slot with no board is not an error. With it set the firmware clears `(a2)`, `+402`, `+404`, `+400` in that order and later writes `$800` (bit 11) to `+400` | `[ROM3500]` `0000AE`-`0000C2`, `00010A` | confirmed |
| 41a | **What the two 8254s actually are: packet counters, not interval timers.** `002398-04` p. 12-32 (DN3000 ring section) names the registers outright -- `RCV_HDR_CNT [+800]`, `RCV_PKT_CNT [+802]`, `RCV_MAX_CNT [+804]`, `XMIT_HDR_CNT [+C00]`, `XMIT_PKT_CNT [+C02]`, `BAD_PKT_CNT [+C04]` -- and says "are implemented with Intel 8254 Chips". So the counters are clocked by **ring traffic**, a header or a packet crossing the interface, and not by an oscillator. **This closes the question of their input frequency, which was recorded as a blocker**: there is no crystal to find, and a rate invented for one would have been wrong in kind rather than in value. The handbook also gives the access protocol -- `$3000`/`$7000`/`$8000` into the control register before loading, `$C200`/`$C400`/`$C800`/`$CE00` before reading, then successive word accesses with the byte in the *high* half of each word | `002398-04` p. 12-32 | confirmed |
| 41 | **`+800`-`+806` and `+C00`-`+C06` are two Intel 8254 programmable interval timers**, not the dual-ported RAM. This is confirmed rather than inferred, on four independent points: the control words written are `$30`, `$70`, `$B0` — counter 0/1/2, LSB-then-MSB, mode 0; counters are then loaded through a helper that writes LSB then MSB, matching that RW field; `$E4` is written to `+806`, which is the **read-back status** command for counter 1 and **exists only on the 8254, not the 8253**; and the status word read back from `+802` is tested with `btst #14`, which is the read-back byte's `NULL_COUNT` bit. So `+800`/`+802`/`+804` are counters 0/1/2 and `+806` is the control register, twice over | `[ROM3500]` `0000C6`-`0000E4`, `000D50`-`000DE2` | confirmed |
| 41a | Which **corrects finding 15**: it read `+800`, `+802`, `+804`, `+C00`, `+C02` appearing "only as `lea.l` operands" as pointing at "buffer rather than register". They are timer data ports, and the `lea` is there because the LSB-then-MSB access helper takes a pointer. Question B is therefore *not* about this region | `[ROM3500]` `000D84`-`000DC2` | confirmed |
| 42 | **The ring ROM never touches AT memory space.** Every absolute long address in `[ROM3500]` is one of exactly four: `$51000`, `$52000`, `$59000`, `$5A000` — all AT *I/O* windows. The firmware initialises and self-tests the board entirely through its registers, so the dual-ported RAM buffer is not reachable from this ROM and question B **cannot be answered from it**. That is a bounded negative result, not a gap in the reading: the buffer is the operating system driver's concern, and `[S3K]`'s AT memory-space table plus a Domain/OS driver are where it will come from | `[ROM3500]`, exhaustive scan of absolute long operands | confirmed |
| 43 | **`[S3K]` Table 2-6, the DS3000 physical address space**, read from the page image because the OCR turns it into `OAOOOO` and `OCOOOO`. AT-compatible bus **I/O** space is `040000`-`05FFFF`, which is exactly what this core models. AT-compatible bus **memory** space is `080000`-`09FFFF`, with `0A0000`-`0FFFFF` being *graphics* — plus further AT memory windows at `900000`-`91FFFF`, `920000`-`BFFFFF` and `FE0000`-`FFFFFF`. So a ring board's dual-ported RAM must appear in one of those, and `080000`-`09FFFF` is where an option card's buffer would go | `[S3K]` Table 2-6 p. 2-17, page image | confirmed |
| 43a | Two independent confirmations fall out of the same table: `MAIN MEMORY (FIRST MB)` at `100000` matches the model table's DN3000 `ram_base` of `0x100000`, and the AT I/O window matches `AP_BOARD_ATBUS_IO_BASE`/`_END` exactly. Both had been taken from other sources and now have a second witness | `[S3K]` Table 2-6 p. 2-17 | confirmed |
| 43b | **A discrepancy to resolve before the DN3500 board's map is trusted.** This core models AT bus memory as `080000`-`FFFFFF` for the DN3500, citing MAME's `dn3500_map`. Table 2-6 shows the DS3000 splitting that range: only `080000`-`09FFFF` is AT memory and `0A0000`-`0FFFFF` is graphics. Whether the DN3500 genuinely differs or MAME simplifies is **not settled** — Table 2-6 is the DS3000's, and the Series 3500 has no equivalent table on disk. Nothing measured so far depends on it: the 8.4 M empty-slot reads this session are in *I/O* space, which both agree on | `[S3K]` Table 2-6 vs `ap_board.h` | open |
| 44 | **The self-test is `ENTRY_05`, and it names both windows.** `0002E0`-`0002E2` sets `a3 = a1` (`$51000`) and `a4 = a2` (`$59000`) from `$CA0`'s pair, so every `(a4)` offset below is the *same* bank findings 12-15 catalogued and every `(a3)` offset is the `a1` window open question A listed as unexplained. The routine is a chain of subtests, each loading an error code `$E00000nn` into `d0`, calling a helper, and branching to the failure exit at `$8D2` on a non-zero compare | `[ROM3500]` `0002D4`-`0008D2` | confirmed |
| 45 | **`+400` is a status word with four more polled bits, each with its own helper and its own timeout.** `$9D2` polls bit 13 (`$2000`) with a **1000**-iteration limit (`#$3E8`); `$9FA` polls bit 2 (`$0004`) and `$A28` polls bit 1 (`$0002`), both with **65535**; `$B70` reads bit 11 (`$0800`) after a transmit. Each helper takes `d4` as the *expected polarity* -- `tst.w d4` selects whether `d2` is set to the bit or left zero -- and returns the comparison, so the caller's `bne $8D2` is "the bit did not reach the state this subtest wanted". `$A56`, `$A62` and `$A6E` are the same shape for `+402`, `+404` and `+400` read as whole words under a `d4` mask. So bit 15 (finding 40) is one of at least five live bits, and the polling structure -- which bit, which timeout, which polarity -- is now explicit | `[ROM3500]` `0009D2`-`000A78`, `000B70`-`000BAA` | confirmed |
| 46 | **`+406` is an auto-incrementing data port into the board's RAM, and `+006` is the pointer it advances from.** `$BE0`/`$C0A` write `d3+1` successive words to `+406` and `$C18`/`$C4E` read `d3+1` back and compare, with `move.w #$0,$6(a4)` before *each* pass -- so the port advances on access and `+006` resets it. The self-test runs this with `d3 = $7FFF`, four patterns deep (a walking byte pair, `$1111` rotated, and each inverted), which is **32768 words = 64 KB** | `[ROM3500]` `00033C`-`000440`, `000BE0`-`000C62` | confirmed |
| 46a | **The read port is pipelined, and a model that misses it fails by one word.** Both readers do a **dummy `move.w (a1),d1` before the loop** and discard it, then read `d3+1` more. That is the signature of a read-ahead port: the first access latches and returns stale data, and each subsequent one returns the word the previous access fetched. A model that returned the addressed word immediately would be one word out for the whole 64 KB and fail every pattern | `[ROM3500]` `000C1C`, `000C52` | confirmed |
| 47 | **Which corrects finding 42, in the way finding 41a corrected 15.** 42's *scan* stands -- every absolute long operand really is one of the four AT I/O windows -- but its conclusion, that "the dual-ported RAM buffer is not reachable from this ROM", is wrong. The buffer is not reachable as *memory*; it is reachable through the `+406` port, and the firmware tests all 64 KB of it. The error was assuming a buffer must be memory-mapped because `[S3K]` §1.5.4 calls it dual-ported. **Open question B is therefore answerable from this firmware after all**, and its size is answered: 64 KB | `[ROM3500]`, findings 42 and 46 | confirmed |
| 48 | **`+402` and `+404` are byte-wide command registers.** `+402` takes `#$1` (written by the transmit helper `$B70` immediately after loading the header, so `$1` is "go"), `#$2` and `#$6`; `+404` takes `#$8` and `#$0`, and is also written as a **word** with `#$800` at `000252` and read back as a byte at `00029A`. Both are also read as words by `$A56`/`$A62` under a `d4` mask, so each carries status as well as command. The individual bit meanings are **not** settled -- what is settled is the width, the direction, and the values the firmware uses | `[ROM3500]` `000B90`, `0004A0`, `000544`, `00048E`, `000252`-`00029A` | confirmed (shape), open (bits) |
| 49 | **The transmit path, `$B70`, matches `[MAC]`'s packet header field for field.** It writes `+006 = 0`, then `d3` high word and low word to `+406`, then `d6`, then `d4`, then `+002 = 0`, then `+402 = 1`, delays, and reads `+400` bit 11. Against finding 21's layout -- destination address two words, then the type word -- `d3` is a 32-bit **destination address** written most-significant-word first (finding 36's ordering, from the manual's own words), `d6` is the **type** field, and `d4` follows it. The call sites give `d6 = $4040` throughout and `d4 = $E` or `$A`. Bit 6 of `$4040` is finding 22's *hardware diagnostics* type bit; bit 14 is in the range finding 22 calls reserved, so the match is **partial and the reading of `d6` as the type field is inference**, not established | `[ROM3500]` `000B70`-`000BAA`, `000462`, `000518` | confirmed (the sequence), provisional (the field mapping) |
| 50 | **The self-test is a loopback test, which is why finding 30 had to model both halves of the bypass relay.** `$BAC` sets `+006 = $10`, reads four words back from `+406`, reassembles a long, and compares it against the same `d3` and `d6` the transmit at `$B70` sent. So the firmware transmits a frame and expects to receive its own -- exactly the self-test §3.5's relay arrangement exists to permit. This is the concrete shape of the plan's verification: what "the ring ROM's own self-test passes" requires is a transmit path that feeds the receive path when the node is bypassed | `[ROM3500]` `000BAC`-`000BDE` | confirmed |
| 50a | **The `a1` window is written, never read.** `$976` clears `$4(a3)`, `$400(a3)`, `$402(a3)`, `$406(a3)`; `$944` clears `$2(a3)`; `$A1E` and `$A4C` clear `$2(a3)` and `$4(a3)`. Across the whole ROM there is **no read** of any `(a3)` offset. So the second window is write-only to this firmware, and its registers cannot be characterised further from it -- a bounded negative result of the same kind as finding 42's, and this one holds | `[ROM3500]`, exhaustive scan of `(a3)` operands | confirmed |

| 51 | **The whole map confirmed across board generations, and the DN3500's bank split with it.** `[ROM3000]` -- the 1818-4882 board, byte-identical to `[ROM5500]` per 5a -- addresses the controller through a **table of eight base pointers** at `a1` rather than by fixed offsets from two windows, and the eight blocks correspond one for one to the DN3500's two windows of four banks. `$14(a1)` is the status block: `move.w (a2),d0` / `and.w #$8000,d0` is finding 40's presence gate verbatim, followed by `clr.w $2(a2)`, `clr.w $4(a2)`, `clr.w (a2)` and later `move.w #$800,(a2)` -- the same clear order and the same `$800` write. `$10(a1)` is the ID block, cleared as a word. `$18(a1)` and `$1C(a1)` are the two 8254s, taking `$30`/`$70`/`$B0` at `+6`. `(a1)` and `$4(a1)` are the second window's two blocks, cleared at `+2`/`+4`/`+6` and `+0`/`+2`. So `+400`-`+406` really is one register block and `+000`-`+006` another, and the identification does not rest on a single ROM | `[ROM3000]` `0000E8`-`00016C` vs `[ROM3500]` `00009C`-`00010A` | confirmed |
| 51a | **The four polled status bits are common to both generations.** `[ROM3000]` masks `$2000`, `$0004`, `$0002` and `$0800` against the status block in the same poll-with-timeout shape as finding 45's helpers. Identical masks on a different board from a different vendor part number, so the status word's layout is the *family's* and not one ROM's quirk -- which is what makes a model built from `[ROM3500]` worth applying to `[ROM3000]` | `[ROM3000]` `000C2A`, `000C56`, `000C88`, `000E10` | confirmed |
| 51b | One generational **difference**, recorded because it is the only one found: `[ROM3000]` writes `$30`, `$70` **and `$B0`** to *both* timers' control ports, where `[ROM3500]` writes `$B0` only to the first and leaves the second's counter 2 unprogrammed. Nothing here depends on it | `[ROM3000]` `000120`-`00013E` vs `[ROM3500]` `0000C6`-`0000DE` | confirmed |

| 51c | **`[ROM4500]` read too, and it closes that route.** The third distinct image differs from `[ROM3500]` in 1,413 bytes inside the self-test, so it is a real revision and not a reprint -- and it polls the **same four status masks**, tests the **same `$7FFF`** buffer extent, and uses `+406`/`+006` the same way. All three images therefore agree on the map, and none of them explains what `+400`'s bits *mean*: the self-test returns its expected value, actual value and register address to the caller as data, and the ROM's whole string table is five messages (`ring: init error`, `transmit`, `receive`, `invalid controller no.`, `operation aborted`) with no per-test text. Domain/OS's ring driver is the only route left for the bit meanings | `[ROM4500]` `0009AE`-`000B74`, `000312`; string scan of `[ROM3500]` | confirmed |
| 51d | **A tooling trap, recorded because it nearly invalidated 44-51.** The listings in `tools/ring-rom/` were generated before the `MOVEC` decode fix and were three months stale; an undecoded opcode desynchronises everything after it. Regenerated and diffed: the only differences are at `000110` and `000CD0` -- the two `MOVEC` sites -- each re-syncing within four instructions, plus the trailer at `000EAA`. None of the regions findings 44-51 rest on is affected. **Regenerate before reading, and diff rather than assume** | `tools/ring-rom/disasm.py` vs the on-disk listings | confirmed |
| 52 | **The elastic-store buffer is two flip-flops, and the hardware ceiling is two bits.** Claim 3(a): each NIU "is adapted to store a **maximum of two bits** between receipt and retransmission thereof". Claims 6(a1)-(a2) and 7 make the FIFO literally two bistable flip-flops "for holding a single bit", with first and second switch means "alternately coupling" input and output on alternate clock periods. So `[MAC]` §3.3.2's nominal 1-bit delay is the buffer half full, and its `0.5 <= d <= 1.5` legal range sits inside a two-bit capacity. **Independent corroboration of findings 28 and 29 from a different source and a different decade**, and it does not change the model -- `AP_RING_ESB_MIN/NOMINAL/MAX_CENTIBITS` are already 50/100/150 | `[PAT575]` claims 3, 6, 7 | confirmed |
| 52a | **What an overflow actually does, at the node.** "To aid in acquisition of the equilibrium condition, each ESB is monitored for an overflow condition. Upon detection of that condition the **receive and transmit counters are reset to a predetermined value**." `[MAC]` §3.3.2 describes the same event from the network's side -- "the network is forced to re-initialize at a new operating frequency (24 MHz)" -- so the two sources are the same event at two levels, not a contradiction. This is the mechanism half of open question D | `[PAT575]` col. 6 | confirmed |
| 52b | **The steady-state error is linear in frequency by construction**, which is why finding 29 measured it that way: "the loop filter introduces a tracking error that is a linear function of the VCO frequency, and because the VCO frequency tracks the RECEIVE CLOCK, the average number of bits in the ESB is a linear function of the RECEIVE CLOCK frequency". The closed loop is also specified to have gain "for all inputs less than unity in the steady state" | `[PAT575]` col. 6 | confirmed |
| 52c | **Two loop-filter types, contemplated in one network.** Claim 4: a filter with **no poles at the origin**, giving less-than-unity steady-state correction. Claim 5: another NIU's filter with **a pole at the origin**, "providing a phase-locked loop of at least second order" -- FIG. 9's, which "constrains ESB to store a fixed average number of data bits". The description says the two types "may be used in conjunction ... in one form of the invention". **Not** evidence that the Master runs one and the rest the other: the patent does not say so, and nothing here should assume it | `[PAT575]` claims 4, 5 and col. 6 | confirmed |
| 52d | And what `[PAT575]` does **not** give, having been read for it: no numeric PLL acquisition or lock time, no capture range, no damping factor or loop natural frequency, and nothing about the token-loss timeout of open question E. The patent is a physical-layer synchronisation document and carries no host-side register description at all, so it cannot address `+400`'s bits either | `[PAT575]`, whole | confirmed |
| 53 | **The Domain/OS route is open, and it was a string search away.** Finding 51c concluded "Domain/OS's ring driver is the only route left" and left it there. The installed disk carries the driver's **link map** with every entry point named: `RING8_$INT` and `RING8_$INT_DEFERRED`, `RING_$SENDP`, `RING_$RCV0`/`RCV1`, `RING_$START`, `RING_$STOP`, `RING_$KICK_DRIVER`, `RING_$SET_TMASK`, `RING_$GET_STATS`, `RING_$IOCTL`, `RING_$SVC_{OPEN,CLOSE,READ,WRITE,IOCTL}`, `RING_$PROC2_CLEANUP`, plus `PKT_ROUTABLE`, `ROUTE_ROUTABLE_PKT` and `HDR_CHKSUM`. `RING_PROC` ends at load address `FFCCF370`, with `ETHER_PROC` immediately after it | `media/dn3500-sr10.4-installed.awd` @ `0x00383108`-`0x00383355` | confirmed |
| 53a | **The interrupt is named**, which the plan recorded as having no source at all: `RING8_$INT` with a deferred half, `RING8_$INT_DEFERRED`, in the same shape as `ETHERNET_$INTERRUPT`/`ETHERNET_$INT_DEFERRED` beside it. That is a driver structure fact, not yet a vector number or a mask | same map | confirmed |
| 53b | **`RING_$POLL_STICKY_BPHERR` names a hardware status bit.** `BPHERR` is a **bi-phase error** and the driver *polls* it as **sticky** -- a latched condition, cleared by being read or by an explicit clear rather than tracking the line. This is the first name attached to any of `+400`'s five polled bits (finding 45) from a source outside the ROM, and a latched error bit is exactly the shape of one of them. **Which bit it is remains unestablished** -- this names a condition, not a position | same map | confirmed |
| 53c | **The condition taxonomy, all nineteen, from `domain_ring.pas`.** In the statistics vector's own order: `NACKs`, `WACKs`, `Xmit overrun`, `Xmit ACK par`, `Xmit bus err`, `Xmit timeouts`, `Xmit modem err`, `Xmit pkt error`, `Token inserted`, `Rcv DMA EOR`, `Rcv CRC error`, `Rcv timeouts`, `Rcv bus error`, `Rcv xmtr error`, `Rcv modem err`, `Rcv pkt error`, `Rcv overrun`, `Rcv ack parity`, `Rcv hdr chksum`. Note `Rcv DMA EOR` -- end-of-record -- which is the first evidence of the DMA path's shape the plan says nothing names | `…awd` @ `0x07110FA4`, `//us/us/mgrs/net/src/domain_ring.pas` | confirmed |
| 53d | **The ring's interrupt vector number, measured: `163` (`A3`).** The kernel's `VBR` is `3C400800` and its map puts `RING_VEC`/`RING_VEC1` at `3C400A8C`, so the slot is `(0xA8C-0x800)/4 = 163`; `RING_8025_VEC2` and `RING_8025_VEC1` at `3C400AAC`/`3C400AB0` are `171` and `172` the same way. This is the "vector number" the plan says the driver's names lacked, and it is arithmetic on two measured addresses rather than an inference | `m_vbr` from a state dump + `kernel_symbols` | confirmed |
| 53e | **And the slot holds the *generic* handler on a machine with no ring card**, which is the control that makes 53d trustworthy. Vector 163 reads `3C44E05C` = `IO_$COND_SWITCH_BACK+18`, next to `IO_$FLIH_TAB`, and so do its unclaimed neighbours; the vectors that *are* claimed resolve to real devices -- `160` `TIME_$DI_RTE+12`, `161` `SIO2681_$INT1_RTE`, `168` `SIO2681_$INT2_RTE` -- and match the boot report's own counts (8,428 timer, 42 DUART). So `RING8_$INT` (`3C4AEB6A`) is installed into 163 by the driver only when the card answers, and **the install path is what to read next**: it is where the mask and the enable sequence will be | oracle state dump, `or-w14` | confirmed |
| 53f | **The driver's code is readable offline, with no ring card and no boot.** `RING8_$INT` (`3C4AEB6A`) and `RING8_$INT_DEFERRED` (`3C4AE6DA`) are both *resident* in an ordinary oracle state dump -- Domain/OS loads the ring driver whether or not a controller answers -- so `oracle_walk.py` translates and reads them like any other memory. This retires "the driver's code" as a blocked source: it costs a `grep`, not an instrumented run on hardware nobody has | `or-w14` dump, `3C4AEB6A` -> `010D9B6A` | confirmed |
| 53g | **`RING8_$INT`'s prologue polls a device register at `+1400` and reads `+1404`.** Disassembled from 53f's bytes: `LEA $3C4D9000,A5` establishes a driver global block; `MOVEA.L $20(A0),A3` takes a pointer *out* of that block; then `MOVE.W $1400(A3),D1` / `BTST #1,D1` polls one bit of a word, and on the other arm `MOVE.W $1404(A3),$296(A4)` copies a second word into a structure field. A `BTST #3,$35(A2)` on the block itself gates both. **What is established** is the pair of offsets and that bit 1 of `+1400` is polled by the interrupt handler; **what is not** is that `$20(A0)` holds the controller's base -- that is the shape of the code rather than a measurement, and a second reader (the ring ROMs, which write these registers) is what would settle it | disassembly of `010D9B6A`-`010D9BC9` | provisional |
| 53h | **The cross-read does *not* confirm 53g's offsets, and that is the result.** The firmware drives `+400` (status), `+402`/`+404` (command) and `+406` (data) from a window base of `$59000` for unit 0 (findings 38, 40, 45, 48). The driver polls `+1400` and `+1404`. Those do not correspond under any simple reading: the two AT windows are `$8000` apart, not `$1000`, and taking `+1400` as an offset into window 0 lands on ISA `0x248`, inside *unit 1's* range rather than on a register either source names. So either `$20(A0)` is not a window base, or the driver maps the controller with a geometry not yet established. **Recorded as inconclusive rather than reconciled** -- a match manufactured here would be a wrong register map wearing the clothes of two agreeing sources. The step that settles it is the driver's *init* path, where `$20(A0)` is written; that is more offline reading of the same resident image | findings 38/40/45/48 against 53g | open |
| 53i | **WITHDRAWN: 53g's `+1400`/`+1404` are not controller registers.** The init path settles it without a boot. `3C4D9000` is `RING_$CCB` -- the driver's control block, with `RING_$CTL` at `+10`, which is exactly the ISR's `LEA $10(A0),A2`. Reading the block out of the dump shows `+1C` and `+20` both holding `3C4D9018`, and `+28`/`+2C` both holding `3C4D9024`: **two empty list heads pointing at themselves**. So `MOVEA.L $20(A0),A3` loads a *queue pointer*, and `$1400(A3)` is an offset into whatever structure a queued element belongs to -- not a device window. 53h recorded the cross-read as inconclusive and declined to reconcile it; this is why it could not be reconciled, and the caution was right | `RING_$CCB` at `01104000`, `or-w14` | confirmed |
| 53j | **What that costs, stated.** The driver is still the best source for *names* -- 53, 53b, 53c -- and for the interrupt vector, which 53d measured from a table rather than from code. It is a poor source for **register semantics**, because everything it touches is one indirection away from the hardware and the indirections are empty on every machine this project can dump: no ring card, no queued elements, self-referential heads. The register-level answer therefore stays where findings 38-50 already put it -- the four ring ROMs, which address the controller directly | 53i | confirmed |
| 54 | **There are three ring firmwares on disk, not four**, and the plan's "four independent readers" was one too many. `3000_RING_1818-4882_9-4-90.bin` and `5500_RING_1818-4882_R9_12-10-90.bin` are **byte-identical** by SHA-256 -- the same firmware shipped for both models, which their shared part number `1818-4882` says in the file name and nobody had checked. `[ROM3500]` (`10666` rev 6) and `[ROM4500]` (`10666` rev 8) are distinct from it and from each other: 53.5% and 53.8% of bytes differ from the `1818-4882` image, and 40.9% differ between the two `10666` revisions. So a cross-read has three sources, and the two sharing a part number are the closest pair | SHA-256 of `roms/firmware/*RING*.bin` | confirmed |
| 55 | **The ring controller's registers are documented after all, in a manual on disk since the start.** `[EH]` chapter 7 (DN3xx, pp. 7-28 to 7-30) and chapter 10 (DN5xx) both carry a **RING REGISTERS** section giving the whole page at `9800`: `+00` transmit command/status, `+02` receive command/status, `+04` TMASK, `+06` diagnostic command/status, `+08` ring ID. Every bit is named. Transmit command: `4000` interrupt enable, `2000` transmit enable "(start the transmit)", `1000` force transmit, with the notes "To start a transmit normally, use 6000", "To force transmit, use 7000", and "Writing anything to this register clears the transmit interrupt". Transmit status: `8000` interrupt pending, `4000` interrupt enabled, `2000` busy, `1000` disconnected, `0800` bi-phase error, `0400` elastic store buffer, `0200` no return, `0100` CRC error, `0080` ack parity error, `0040` external, `0020` protocol, `0010` icopy, `0008` ack byte errbit, `0004` copy, `0002` wack, `0001` underrun -- **with "a successful transmit will have a transmit status of 0014" and "a WACK will have 0012"**. Receive status and diagnostic status likewise, the latter including `2000` connected to the network, `1000` sticky bi-phase and a `01FF` bad-packet counter. Read from the **page images** at PDF pages 167-168, which confirm the text layer exactly | `[EH]` 002398-04 pp. 7-28..7-30, page images | confirmed |
| 55a | **It corroborates the driver, independently.** Finding 53c's nineteen condition names, taken from `domain_ring.pas` on the volume, are these bits: `WACKs`, `Xmit overrun`, `Xmit bus err`, `Rcv CRC error`, `Rcv overrun`, `Rcv ack parity`, and the rest. And 53b's `RING_$POLL_STICKY_BPHERR` is diagnostic status `1000 sticky bi-phase`, "error seen since bit was cleared" -- which is exactly the *sticky* reading 53b inferred from the name alone. Two sources that have never met agreeing on a vocabulary is what makes it safe to use | 53b, 53c against 55 | confirmed |
| 55b | **Whether this maps onto the DN3500's `+400` is NOT settled, and the tempting match fails arithmetic.** `[EH]` documents the DN3xx/DN5xx board at physical `9800` with 16-bit registers; the DN3500's is an AT-bus board whose firmware uses `+400` (status word), `+402`/`+404` (**byte**-wide commands) and `+406` (auto-incrementing data port) -- a different layout. The seductive step is that finding 48's `+402 = $6` looks like the note's "use 6000". **It does not follow**: on a big-endian part a byte written to `+402` is the *high* byte of the word there, so `$6` gives `0600`, and `6000` would need `$60`. What *is* suggestive and cheap to test is the other end -- 45's polled bits 2 and 1 are `0004 copy` and `0002 wack`, and `0014`/`0012` are the documented success and WACK codes, so a transmit helper polling exactly those two is polling exactly what the note says to read. Recorded as promising and unproven; the arithmetic above is why 48's bits stay open | 45, 48 against 55 | open |
| 56 | **The ring ROM's failure codes are a 40-entry subtest numbering, and two of them are a trap.** Every `MOVE.L #$E00000xx,D0` in `[ROM3500]` -- forty sites -- loads a code from the sequence `01 02 03 · 11-16 · 21-26 · 31 32 · 41 42 · 51 52 · 61-64 · 71-77 · 81-88`. That is group-and-subtest numbering, and it is what the self-test reports a failure as, which makes it the map for reading the "ring firmware's own self-test" that `CLAUDE.md` names as the controller's first real test. **The trap**: the codes loaded immediately before the transmit polls are `E0000012` and `E0000014`, and `[EH]` gives `0012` as a WACK and `0014` as a successful transmit (finding 55). They are not the same numbers twice -- the sequence above shows `12` and `14` are simply the second and fourth subtests of group 1, and `22`/`24` appear at the next pair of sites. Their adjacency to polls of bits 1 and 2, which are what `0012` and `0014` are *made of*, is coincidence of the most persuasive kind | `[ROM3500]`, all 40 sites | confirmed |
| 56a | ~~The polled bits are expected *set*~~ -- **CORRECTED by 56b, one iteration later.** The claim that `d4 = 1` means "wait for the bit to become 1" was read off the call sites without reading the helper's body, and it is wrong | superseded | retracted |
| 56b | **The polling helpers are stability tests, not waits, and `tools/ring-rom/` already had the listings.** `disasm.py` and `r3000.lst`/`r3500.lst`/`r4500.lst` have been on disk since 9 August; hand-decoding bytes for 56a was redoing work that existed. The three helpers share one shape: loop reading `+400`, mask the bit, **exit early when it reads CLEAR**, otherwise `dbra` to a bound; then `d2 = d4 ? bit : 0` and `cmp` against the last value read. So `d4 = 1` means *the bit must still be set when the bounded wait ends* -- it must **remain** set -- and `d4 = 0` means it must go clear. `$9D2` is bit 13 over 1000 iterations; `$9FA` is bit 2 over 65535 and clears `$2(a3)` before comparing; `$A28` is bit 1 over 65535 and clears `$4(a3)`. Those two side-effect writes are new: `a3` is the **first** window (`$51000`, finding 44), so each helper acknowledges something there after polling status in the second | `tools/ring-rom/r3500.lst` `09D2`-`0A54` | confirmed |
| 57 | **The two `10666` revisions corroborate each other exactly, and the `1818-4882` firmware is not a third reader of these registers.** `r3500` and `r4500` each poll `+400` under precisely four masks -- `$2000`, `$800`, `$4`, `$2` -- and use `+400`/`+402`/`+404`/`+406`; the two images differ in 41% of their bytes, so that is genuine independent agreement on bits 13, 11, 2 and 1. **`r3000` never references `+400` at all**: its register offsets are `(a2)`, `$2(a2)`, `$4(a2)`, `$6(a2)`, `$10(a1)`, `$14(a1)`. So finding 54's three distinct firmwares are **two** readers of the AT board's register block, and the third addresses a different one | `tools/ring-rom/r3000.lst`, `r3500.lst`, `r4500.lst` | confirmed |
| 57a | **And `r3000` is *not* established as the `[EH]` layout, though the offsets invite it.** Small offsets `+00`/`+02`/`+04`/`+06` and `+10`-`+16` are exactly finding 55's documented DN3xx page, which would make `r3000` a Rosetta stone between the documented registers and the AT board's. The **values refuse it**: `r3000` writes `#$800` to `(a2)` and `#$600`/`#$610`/`#$10` to `$6(a2)`, while `[EH]`'s transmit command is built from `4000`, `2000` and `1000` and its note says a normal transmit is `6000`. Offsets agreeing while values disagree is not an identification, and this is the fourth numeric near-match this file has had to refuse. What would settle it is which base `a2` holds in that firmware -- unread | `r3000.lst` against finding 55 | open |
| 58 | **Corroboration, not discovery: the four-bank structure was already finding 12's, and `ap_ring_ctl.h` already models it.** `BANK_ID` `+000`, `BANK_STATUS` `+400`, `BANK_TIMER_A` `+800`, `BANK_TIMER_B` `+C00` under a `0xC00` mask, in a `0x1000` window, have been in the header all along -- so "`+400` is a bank, not an offset into one block" is not news, and reading it as one was my own gap. What *is* new is two independent confirmations of it. **From the firmware:** `r3000`'s absolute immediates are `$51000 $51400 $51800 $51C00 $52000 $52400 $52800 $52C00` -- eight bases at exactly the bank spacing, two controllers' worth -- where `r3500` has no `#$5xxxx` immediate at all because `$CA0` computes its bases. **From the manual:** `[S3K]` Table 2-9's AT `0x220`-`0x23F` is 32 ISA ports, which through `0x040000 + (ISA << 7)` is `$51000`-`$51FFF` -- `0x1000`, exactly four `0x400` banks. The arithmetic works here, where four earlier numeric matches did not | `r3000.lst`; `[S3K]` Table 2-9; `ap_ring_ctl.h` | confirmed |
| 58a | **And it settles 57a in the negative.** `a2` in `r3000` is loaded from `$14(a1)`, `$18(a1)`, `$10(a1)` or `(a1)` -- a pointer taken *out of a structure*, never set to a window constant. So its small offsets `$2(a2)`, `$6(a2)` are structure fields, not the `[EH]` register page, and the identification 57a declined is not merely unproven but **refuted**. This is the same shape as 53i, where the Domain/OS driver's `$20(A0)` turned out to be a queue pointer: on this controller, a small offset from a register loaded out of memory has twice now looked like a register map and twice not been one | `r3000.lst` `0000FA`, `000118`, `0002C2` | confirmed |
| 59 | **Why the boot PROM does not call the ring ROM, from the PROM's own code.** It carries **two** option-ROM matchers, both taken by address as callbacks to the scan at `$F7E`. `$104E` accepts magic `335E91B6` / `0000A0B6` **and** requires `field_1a` to equal `d0`; `$106A` accepts `335E91B6` / `C000A0B7` with no such check. A ring ROM is the first kind -- its `field_1a` is `0002` -- and the scan that runs early uses the **second**, so it reads our magic1 at PC `001072`, sees `0000A0B6` where it wants `C000A0B7`, and rejects. Measured: `+1A` is never read, which is the tell, since only `$104E` reads it | `3500_BOOT_12191_7.bin` `00104E`-`00107E`, `--boot-watch-read` | confirmed |
| 59a | **And `d0` comes from the calendar: register `2B` is the option-ROM class selector.** `001784` loads the config table, checks the valid pattern and `tst.b $1D(a0)`, and on success takes `d0` from register `2B` and `d4` from `2C` before `LEA $104E(PC),A0` / `BSR.W $F7E`. So the byte whose *only* established property was "must be non-zero or the warning prints" is the class this machine looks for, and `ap_calendar.h`'s "what the two then select is not decoded here" is now decoded. **But seeding `2B = 2` does not make it happen**: `001784` is not reached at all in 60 M instructions of a PROM-only run, so what remains open is not the selector but *when* the accepting scan runs -- later in the boot, or on a path needing a disk or display | `00178A`-`0017A6`; `--calendar-ram` with `2B = 2`, `--boot-stop-pc 1784` | confirmed |
| 59b | **Calendar register `2B` selects *which card's* ROM runs, and every option ROM on disk agrees.** All three images carry magic1 `0000A0B6` -- the `$104E` class, the one gated on `field_1a == d0` -- and their `field_1a` values are the selector: ring `3500_RING_10666_6` and `3000_RING_1818-4882` are both **`0002`**, and `3000_3C505_010728-00` is **`0006`**, with ROM classes `'R   '` and `'E   '` matching. ~~**None** of them is the `C000A0B7` kind, so the scan that runs early is hunting a card class this project does not hold~~ -- **that half is REFUTED by finding 70; see it before relying on this row.** The rest stands: the three `0000A0B6` images and their `field_1a` selectors are as stated, our ring ROM's rejection at `001072` was never about the ring ROM, and it still predicts the oracle -- `ETHERNET.md` finding 10a caught MAME's 3c505 option ROM probing from PC `0008xxxx`, which under this reading means that machine's `2B` selects `6` | headers of all three ROMs in `roms/firmware/` | partly retracted |
| 70 | **REFUTES 59b's absence claim: the `C000A0B7` ROM was on disk the whole time, and it is the DN4500's Matrox graphics image.** A sweep of `magic1` across **every** ROM in `roms/firmware/` -- not just the option ROMs 59b looked at -- gives exactly one `c000a0b7`: `4500_Matrox_013748_04.bin`. It is a valid Apollo option ROM by `tools/ring-rom/disasm.py` (checksum VALID, four entry points, `hdr_ver` 1), and per finding 16 the `$106A` matcher that accepts this class requires **magic1 alone**, with no `field_1a`/`d0` check -- which is why it needs no calendar selector | `xxd -s 4 -l 4` over `roms/firmware/*.bin` | confirmed |
| 70a | **And the early accepting scan does take it: measured.** Booted with `--option-rom roms/firmware/4500_Matrox_013748_04.bin` (a new flag: an option ROM with *no card behind it*, which `--ring-rom` and `--3c505-rom` cannot express), the run ends with `final PC 000805A8` -- **inside the image**, at `080000 + $5A8`. So the scan this project has watched reject three ROMs accepts this one and executes it. `RING.md` 59a left "when the accepting scan runs" as the open half; it runs early, in an ordinary boot, and needs no configuration | `--option-rom`, `--boot-limit 40000000` | confirmed |
| 70b | **The error 59b made is the one this file keeps making: an absence claimed from a partial sweep.** It examined "all three images" -- the two ring ROMs and the 3c505's -- and generalised to "this project does not hold". Seventeen ROMs are in that directory. The check that settles it is four bytes per file and was never run. Recorded because `CLAUDE.md`'s rule about walking *every* field of *every* register is the same rule one level up, and because a wrong absence is what sends the next reader looking for a card that is already here | 59b against 70 | confirmed |
| 60 | **`+400` reads `F806` on an idle board -- the firmware's own subtest 01 says so, and it is the first hard fact about these bits.** `entry_05` loads `d2 = d4 = $F806` and code `E0000001`, calls `$A6E` -- `lea $400(a4),a1 / move.w (a1),d1 / and.w d4,d1 / cmp.w d1,d2` -- and reports failure otherwise. So `(+400) & $F806 == $F806`: **bits 15, 14, 13, 12, 11, 2 and 1 all read set** on a healthy board that has just been reset. Five documentary and cross-reading attempts (53h, 53i, 55b, 57, and the option-ROM route) failed to settle any of these bits; the ROM asserts seven of them in one instruction. It is not fitting the model to the test -- the assertion is what a working board reads, and later subtests constrain the same register independently | `[ROM3500]` `00031A`-`00032C`, `$A6E`; measured by `--ring-selftest` | confirmed |
| 60a | **And the loop advances: fixing the idle value moves the firmware to subtest 02.** With `+400` reset to `F806` the self-test runs **262,241 instructions** instead of 75 and 65,542 ring writes instead of 2, then reports `SUBTEST 02 failed: value 00000001 mask 00000000 at 00059406`. `+406` is finding 46's auto-incrementing data port, so subtest 02 exercises the board's RAM through it. This is the measurable loop the item wanted: a numbered subtest, an address, and a next question | `--ring-selftest` after the reset change | confirmed |
| 61 | **A defect of ours, found by the firmware: the data port was accessed twice per word.** The board delivers a `move.w` as two byte accesses, and `+406` advances its pointer on *every* access. The read path called `read16` for each half -- two advances per word -- and the write path was worse: a read-modify-write per byte meant **four** advances for one `move.w`, so the firmware's fill wrote every fourth slot and its read-back compared the wrong ones. `ap_ring_ctl.h` already documented this exact hazard for the two 8254s ("the odd half of a word access must *not* reach the part") and the data port, which has the same side effect, was not guarded. Now the even half performs the access and latches, and the odd half is served from the latch; the write side holds the high byte and commits on the low. **Subtest 02 passes and the firmware reaches subtest 03**, running 6.2 M instructions and 917 K ring accesses where it managed 262 K and 65 K before | `--ring-selftest`, `[ROM3500]` `$BE0`/`$C18` | confirmed |
| 61a | **And a unit test alone would not have found it.** `ring_ctl_suite`'s reproduction of the firmware's fill-and-compare passes *against the broken model*, because it drives `ap_ring_ctl_write16`/`read16` directly and never crosses the byte bus the board uses. The defect lives in the byte-splitting layer between them. It took running the firmware through the real board path to see it, which is the argument for the self-test harness in one sentence | `tests/ring_ctl_suite.c` against the pre-fix model | confirmed |
| 62 | **`+000` is a word whose low lane reads bit 3 clear, and subtest 03 is the only thing that ever asked.** Finding 39 reads the ID as a *byte*, so the odd half never mattered and this core answered with the pull-ups, `FF`. Subtest 03 reads the same address as a **word** -- `move.w (a4),d1 / andi.w #$8,d1 / cmp.w d1,d2` with `d2 = 0` -- so `36FF` fails and a healthy board reads bit 3 clear there. The rest of the lane is unconstrained by anything measured; zero is modelled because it asserts nothing the firmware did not, where `F7` would claim six pull-ups nobody has seen | `[ROM3500]` `000442`-`000452`; `--ring-selftest` | confirmed |
| 62a | **Three subtests in one fix: the firmware now reaches subtest 13.** With the ID lane corrected it runs **6,562,174 instructions** and passes 03, 11 and 12 before stopping at `SUBTEST 13 failed: value 000000F0 mask 00000000 at 00059402` -- `+402`, finding 48's byte-wide command register, which the firmware expects to read back `F0`. Four subtests are now satisfied by this model where the whole register block was open questions a day ago | `--ring-selftest` | confirmed |
| 63 | **`+402`'s low lane is status, and subtest 13 says what it reads.** Finding 48 established that the command registers "carry status as well as command" without saying what the status half holds. The firmware writes `#$2` to `+402` -- the command byte, the high lane on a big-endian part -- and then requires `(+402) & $F0 == $F0`, which a register that merely stored `0200` cannot give. So **bits 7-4 of the low lane read set** on a healthy board. Only those four are asserted, so only those are answered: the same restraint as the ID lane in 62 | `[ROM3500]` `0004A0`-`0004CC`, `$A56` | confirmed |
| 63a | **Two tests said "storage and nothing more" and were half right.** `ring_ctl_suite` asserted `+402` round-trips `BEEF` and `1111` whole, which passed for as long as nothing drove the register. They now assert the *command lane* round-trips and the status lane answers -- a sharper claim than the one they replaced, and one the firmware can check. Subtests 13 and 14 pass, and the self-test reaches **subtest 15**: `(+404) & $F8 == $E0`, the same shape one register along | `tests/ring_ctl_suite.c`; `--ring-selftest` | confirmed |
| 64 | **`+404` carries status in its low lane too, and the two command registers share one shape.** Subtest 15 requires `(+404) & $F8 == $E0` after the firmware has written only the command lane -- so bits 7-5 read set and bits 4-3 clear, against `+402`'s bits 7-4 set. Two registers, the same arrangement: a command byte in the high lane, a fixed status pattern in the low. `ring_ctl_suite`'s third "storage and nothing more" assertion is corrected with it | `[ROM3500]` `0004E0`-`0004F2`, `$A62` | confirmed |
| 64a | **And subtest 16 asks a new kind of question: bit 11 must *clear*.** It requires `(+400) & $FF08 == $F000`, and this core answers `8100`. Two things follow. The bits 15-12 the mask keeps are status that survives whatever the firmware has written -- our model stores the host's write into the whole word, so `move.b #$1,$400(a4)` at `00045C` leaves `01` in the high lane and the read comes back `81xx` instead. And **bit 11, which subtest 01 required *set* at reset, must read clear here** -- after finding 40's `$800` has been written to `+400`. A latched condition cleared by writing a one to it is the obvious reading and is not yet evidence; what is evidence is that the same bit is required both ways at two points, so `+400` is not storage | `[ROM3500]` `0004F6`-`000502`; `--ring-selftest` | open |
| 65 | **CLOSED, open question A for bit 11: writing anything to `+400` clears it, and two independent sources say so.** Write-one-to-clear was tried first and is **refuted** -- it leaves the bit set after `move.b #$1,$400(a4)`, and subtest 11, which had been passing, fails. The rule that fits every data point is that a *write of any value* clears it: subtest 01 requires it **set** at reset, subtest 11 requires it **clear** after the `#$1`, and subtest 16 requires it clear under a `$FF08` mask. And `[EH]`'s ring section (finding 55) states it in words for the DN3xx board's transmit command -- **"Writing anything to this register clears the transmit interrupt"** -- so a documented rule for one generation and the AT board's own self-test requiring the same thing agree, which is corroboration rather than a fit to three numbers. Bits 15-12 survive the write; the presence gate is one of them, which is why finding 40's `clr.w +400` does not make the board vanish | `[ROM3500]` subtests 01/11/16; `[EH]` p. 7-28 note 4 | confirmed |
| 65a | **Eleven subtests pass, and `ring_ctl_suite` had encoded the unknown as storage.** Its bit-11 assertion said the board "keeps what the host wrote" *because* the bit's purpose was open -- the comment says so -- and it is now inverted, naming both sources so it cannot drift with the model. The self-test reaches **subtest 22**: `(+400) & $4 == 0`, bit 2, which subtest 01 required *set* at reset. The same shape as bit 11, one bit along | `--ring-selftest`; `tests/ring_ctl_suite.c` | confirmed |
| 66 | **`PROVISIONAL`: a `$6` command to `+402` completes an operation and clears `+400`'s bits 13 and 2.** Subtest 22 writes `#$6` and polls both with `d4 = 0`, so finding 56b's polarity requires them *clear*; subtest 16 requires bit 13 **set** after subtest 12 wrote `#$2` to the same register, so the value separates them and not the act of writing. Modelled as "a command carrying `$4` completes immediately", which is the least that satisfies both. **The approximation is named**: real hardware would set them busy and clear them when the operation *finished*, and this model has no duration. `[EH]`'s DN3xx vocabulary calls bit 13 *busy* and bit 2 *copy*, which fits a completion -- but that board's command encoding is plainly not this one's (`$6` against `6000`), so the names are not carried across | `[ROM3500]` `000544`-`00055A`; subtests 12, 16, 22 | provisional |
| 66a | **And subtest 23 shows `+402`'s command lane is not storage either.** It requires `(+402) & $FFF0 == $00B0` after the `#$6`, where this core answers `06F0`: the command lane must read **0** once the command has been taken, and the status lane **`B0`**, not the `F0` subtest 13 established for an idle register. So the low lane's bit 6 clears with the operation. Twelve subtests pass; this is the next | `[ROM3500]` `00055E`-`00056C`, `$A56` | open |
| 67 | **`+402`'s command lane self-clears, and its status drops bit 6 with the operation.** Subtest 23 requires `(+402) & $FFF0 == $00B0` after a `$6` command: the lane reads **zero** once the command has been taken, not the value written, and the status half goes `F0` -> `B0`. Both halves of one register recording one event. Subtest 24 then requires `+400`'s bit **1** clear as well, so a completing command clears bits 13, 2 and 1 together. Thirteen subtests pass | `[ROM3500]` `00055E`-`000580`; `--ring-selftest` | confirmed |
| 67a | **RESOLVED by reading the two helpers, which is what the stop was for.** Subtest 25 requires `(+404) & $FFF8 == $00A0` -- bit 6 clear -- where subtest 15 required `E0`, bit 6 set, and both follow a `move.b #$8,$404(a4)`. Reading `$976` (it writes zero to `+404`) and `$944` (it loads the two 8254s from `d3`/`d4` and clears `+002` in both windows) shows the lead-in to both is **identical**. The only difference is the command written to `+402` next: `#$2` before 15, `#$6` before 25. So the completing `$6` clears `+404`'s command lane and status bit 6 as well -- **one completion event across three registers**, not three register rules. Forced by the evidence rather than fitted: nothing was invented to satisfy 25, and 15 still passes | `[ROM3500]` `$944`, `$976`, subtests 15/25 | confirmed |
| 68 | **Fourteen subtests pass: all of group 1 and five of group 2.** The self-test now reaches `SUBTEST 26`, the last of its group, requiring `+400` to read `B00E` under a `$D000` mask where this core answers `D000` -- bit 14 clear, bit 13 set, and bits 3-1 set, which is a different arrangement from anything established so far and the next question. The register block that was entirely open when this loop began now has: `+400`'s idle word, its write-clears-bit-11 rule (corroborated by `[EH]`), and its completion behaviour for bits 13/2/1; `+000`'s low lane; and `+402`/`+404` as command-over-status registers whose command lanes self-clear | `--ring-selftest` | confirmed |
| 69 | **Subtest 26 cannot be satisfied by a model without duration, and that is finding 66's stated cost arriving.** It requires `(+400) & $FF8E == $B00E`: bit 15 set, bit 14 **clear**, bit 13 set, bits 11-8 and 7 clear, and bits **3, 2 and 1 set**. Subtests 22 and 24 required bits 2 and 1 **clear**, at `000552`-`000580`, and there is **no write to any ring register between them and subtest 26** at `00059A` -- only the polls themselves. So those bits are not a state a write sets and leaves: they go clear while an operation is in flight and are set again once it is done, which is what "wait for the bit to clear" (finding 56b's `d4 = 0` polarity) is *for*. A register model that clears them on the completing command, as finding 66 provisionally does, can satisfy 22 and 24 or 26 but not both. **Closing it needs the transmit path with real duration** -- precisely the cost 66 named when the approximation was made, now due | `[ROM3500]` subtests 22, 24, 26; `$A6E` | open |
| 68a | **CORRECTION to 68: subtest 26's mask is `$FF8E`, not `$D000`, and `D000` is what *this core answered*.** From the ROM: `$59A` loads `d2 = $B00E` (expected), `$59E` loads `d4 = $FF8E` (mask), and the helper `$A6E` does `move.w (a1),d1` / `and.w d4,d1` / `cmp.w d1,d2` -- so `d1` is the register read **already masked**. 68 read our own harness's output, which labelled `d1` "mask", and wrote the actual reading down as the mask. Finding 69 had it right from the ROM; 68 did not, and the two have disagreed in this file ever since | `[ROM3500]` `$59A`-`$5A8`, `$A6E` | confirmed |
| 68b | **The harness was mislabelling its own output, and that is the defect worth recording.** `loc_08D2`'s three out-parameters are `d2`, `d1`, `a1` -- **expected**, **masked reading**, address -- and `--ring-selftest` printed the first two as "value" and "mask". A harness that mislabels its output is worse than one printing nothing, because the wrong reading gets written down, which is exactly what 68 did. Now printed as `expected ... read ... (masked)`, with the **exclusive-or** beside it, since which bits differ is the question every one of these failures has actually been asking and it was being done by hand each time | `main.c` `run_ring_selftest` | confirmed |
| 68c | **So subtest 26's requirement, machine-printed rather than hand-decoded**: `expected 0000B00E, read 0000D000 (masked)`, `differing bit(s): 0000600E`. Four bits, in two directions -- bit **14** must go **clear** where this core has it set, and bits **13**, **3**, **2** and **1** must be **set** where this core has them clear. Bit 14 is the new one: subtest 01 requires it *set* at reset (finding 60's `F806`), so it changes state too, which is a fifth bit for the state machine finding 69 says duration has to drive | `--ring-selftest` | confirmed |

| 70 | **A constant duration was built, measured, and abandoned -- and the failure is the useful part.** With the harness stepping the machine (69b), the controller was given an in-flight state, a countdown in base units and a completion driven from the board's advance, with the duration **derived rather than invented**: `[MAC]` §2.2.2.2's "will always transmit the first 12 bytes" at §3.2's 12 Mbit/s is 96 bits, **8 microseconds**. The firmware rejected it -- `SUBTEST 22 failed: expected 00000000, read 00000004` -- because the operation *finished before subtest 22 ever polled*, so the bit it needs to see clear had already come back | `--ring-selftest` with the timed model | confirmed |
| 70a | **Which makes 69a's design required rather than preferable, and that is what this turn bought.** The fix for 70 is a longer constant, and there is no source for one: the only way to choose it is to try values until subtest 22 and subtest 26 both pass, which is a parameter search against our own test and is what `CLAUDE.md` forbids in as many words -- "Never by trial-and-error parameter search on our own code". A duration with an independent source would be admissible and none exists. So the completion has to come from `ap_ring_station`, where the time is the **medium's** and is derived from the bit clock the ring already models. The timed scaffolding is reverted rather than left dormant, because a mechanism that cannot be driven correctly is a trap for the next reader | `CLAUDE.md`; 70 | confirmed |
| 70b | And the attempt bounds the answer from below, which no reading had: the operation must still be in flight when subtest **22** polls and must have finished by subtest **26**, with only subtests 23, 24 and 25 -- all polls with early exits -- in between. So the duration is longer than those three take and shorter than the firmware's patience, which is a constraint on the station-driven model rather than a number | 70 | confirmed |

| 71 | **The duration's source is named by the firmware itself: it loads the two 8254 counters immediately before issuing the command.** `$538`-`$544` is one sequence -- `move.w #$1FF,d3`, `move.w #$3FF,d4`, `bsr $944`, then `move.b #$6,$402(a4)`. And `$944` is the counter loader: `#$30` to `+C06` then `d3` low-then-high into `+C00`, `#$70` to `+C06` then `d4` into `+C02`. So **511 and 1023 are loaded into counters 0 and 1 and the command is issued against them** | `[ROM3500]` `$538`-`$544`, `$944` | confirmed |
| 71a | **Which joins two findings that had never been connected.** 41a established from `002398-04` p. 12-32 that these 8254s are **packet counters clocked by ring traffic** -- `RCV_HDR_CNT`, `RCV_PKT_CNT`, `XMIT_HDR_CNT`, `XMIT_PKT_CNT` -- and closed "what clocks them" as having no crystal to find. 70a concluded the completion must come from the medium. 71 shows the firmware routing one into the other: traffic clocks the counters, the counters were just loaded with 511 and 1023, and the operation the command starts is what the polled bits report on | 41a + 70a + 71 | confirmed |
| 71b | ~~The completion is a **count to be reached** rather than a duration~~ -- **REFUTED by 71c, before anything was built on it.** The counters being loaded immediately before the command is real; reading that as *gating* the command was the inference, and it was wrong | superseded | retracted |
| 71c | **The counters are read back four instructions after subtest 26, so they measure the operation rather than gate it.** `$5B0` sets `d4 = 1` and `$5B2`-`$5D4` is a loop: subtest **31** calls `$A7A` -- the timer **read-back** helper, which selects a counter by `d4` and issues `$E2`/`$E4` read-back commands at `+806` -- then subtest **32** at `$AF8`, then `d4++` while `d4 <= 5`. Five counters walked and checked. So the load at `$538` puts them in a known state *before* the operation so that what they count during it can be verified after: they are the instrument, not the trigger | `[ROM3500]` `$5B0`-`$5D4`, `$A7A` | confirmed |
| 71d | **Which leaves 70a's conclusion standing and adds a quantitative specification to it.** The completion still has to come from the medium, and now there is a check on what the operation must *produce*: subtests 31 and 32 verify the counts five times over, so a station-driven model is not free to transmit any amount of traffic -- the firmware measures it. That is a stronger constraint than a duration and it is the firmware's own, which is what the next attempt should be built against. **The wire 71b called the item is still unmade** -- nothing clocks the 8254s from ring traffic -- but it is needed so 31 and 32 can pass, not so 26 can | 71c, 41a | open |

| 72 | **What the `$6` command is, from four sites in two ROM revisions: a *counted* operation whose two counts are its arguments.** Every one of the four `$6` writes in `[ROM3500]` is preceded by the identical five-step preamble -- `bsr $976` (clears `+404` and the `a3` window), `move.b #$8,$404`, a `d3`, a `d4`, `bsr $944` (loads the two 8254s with them), then `move.b #$6,$402`. `[ROM4500]` has the same four sites with the same shape at its own addresses, and both ROMs write `$2` exactly twice, `$6` exactly four times and `$1` once. **The only thing that varies across the four is the counter pair**: `$1FF`/`$3FF` at `$544`, `$68A` and `$714`, and `$5`/`$5` at `$7F4` | `[ROM3500]` `$52E`-`$544`, `$674`-`$68A`, `$6FE`-`$714`, `$7DE`-`$7F4`; `[ROM4500]` | confirmed |
| 72a | **Which is more than "unknown" and less than "transmit".** Finding 66 left the command's meaning open and finding 49 established that **`$1`** is the transmit go, written by the transmit helper `$B70` after loading a header -- so `$6` is not that. What 72 adds is that it is *parameterised*, and by quantities `002398-04` p. 12-32 says count **ring traffic** (41a). An operation given two traffic counts, run, and then read back through those same counters (71c) is a **measured transfer of a stated size**, whatever it is called | 49, 41a, 71c, 72 | confirmed |
| 72b | **And it gives the station-driven model a shape to be built to, without a constant anywhere.** The operation's extent is the firmware's own `$1FF`/`$3FF` -- or `$5`/`$5` at the fourth site -- so a model whose completion follows the counts reproduces all four sites from the ROM's numbers rather than from one chosen here, and the `$5`/`$5` site is an independent check on it: a model tuned to the large counts and wrong about the small one would show there. This is what 70a asked for and 71b guessed at wrongly: the counters are the operation's **arguments and its instrument**, not its gate | 70a, 71c, 72 | open |

| 73 | **Bit 14's rule is confirmed by the machine: the command clears it and it does not come back.** 68c read it off the differing-bits line; acting on it moves that line from `0000600E` to **`0000200E`**, so bit 14 now reads as subtest 26 wants while subtests 01 and 16 -- which need it *set* -- still pass. Four wrong bits where there were five, and the one closed was closed by a rule with two firmware sites behind it rather than by a fitted value | `--ring-selftest` | confirmed |
| 73a | **And the extent is now bracketed from both sides, which is the useful negative.** Two *derived* durations have been tried and both refused. Finding 70's 8 us -- `[MAC]`'s 12-byte minimum transmission at 12 Mbit/s -- finished **before subtest 22 polled**, failing it. The firmware's own larger count at `[MAC]` §3.2's 83.33 ns bit cell -- 1023 cells, **85 us** -- had **not finished by subtest 26**, leaving bits 13, 3, 2 and 1 clear. So the true extent lies strictly between 8 and 85 microseconds | two `--ring-selftest` runs | confirmed |
| 73b | **Which is exactly why the next value must not be chosen here.** Picking a number inside that bracket, or taking the *smaller* counter because the larger failed, is a parameter search against our own test -- and the bracket makes it an easy and invisible one, which is what makes it worth naming. Completion therefore stays finding 66's immediate `PROVISIONAL` until `ap_ring_station` drives it, and the bracket is recorded as the check that a station-driven model will have to land inside | 70a, 73a | open |

| 74 | **CORRECTION to 69, and it dissolves the timing question: there ARE writes between subtests 22 and 26.** 69's premise was "no write to any ring register between them -- only the polls themselves", and the polls are not passive. `$9FA` polls `+400` bit **2** and ends `move.w #$0,$2(a3)`; `$A28` polls bit **1** and ends `move.w #$0,$4(a3)`; `$9D2`, which polls bit 13, writes nothing. **Finding 56b had already recorded those two writes** -- "each helper acknowledges something there after polling status in the second" -- and 69 was written without them. Two helpers, two addresses, and each acknowledges the bit it just waited on | `[ROM3500]` `$A1E`, `$A4C`; 56b | confirmed |
| 74a | **Modelled and confirmed by a prediction made before the run.** A write to the first window's `+2` restores `+400`'s bit 2, and one to `+4` restores bit 1 -- the bit each helper polled, at its own address, with **no duration anywhere**. The prediction was that the differing-bits line would go from `0000200E` to `00002008`, closing bits 2 and 1 and leaving 13 and 3; it did, exactly, with subtests 22 and 24 still passing. So bits 2 and 1 come back **when their condition is acknowledged**, not when a clock runs out | `--ring-selftest` | confirmed |
| 74b | **Which retires the 8-85 microsecond bracket rather than narrowing it.** 73a bracketed a duration on the assumption that time restores the bits; 74 shows an acknowledge does. Two derived durations were refused because both were answers to the wrong question, and picking a third from inside the bracket would have produced a model that passed subtest 26 for a reason the hardware does not have. The bracket stands as a fact about those two runs and no longer as a specification | 73a, 74a | superseded |
| 74c | **What is left is two bits and one genuine ambiguity.** Bits **13** and **3** are still wrong at subtest 26 (`differing bit(s): 00002008`). Bit 13 is polled by `$9D2`, which acknowledges **nothing**, and bit 3 is polled by no helper at all -- so both must be restored by one of the two writes above, and the self-test's own sequence cannot say which: subtests 22, 24 and 26 run in the same order at three of the four `$6` sites, and `$A56`/`$A62`/`$A6E` write nothing. Assigning them to `+2` or `+4` to make 26 pass would be the parameter search in another guise. The fourth site, whose counts are `$5`/`$5`, is the place to look for a sequence that differs | `[ROM3500]` `$9D2`, `$A56`-`$A6E` | open |

| 75 | **Subtest 26 passes, and 31 with it -- the acknowledge model is the whole answer.** Bits 13 and 3 return on the same first-window acknowledge as bit 2, gated on a `$6` command being outstanding. The gate is not decoration: setting bit 3 on *every* `+2` write fails **subtest 16**, which runs after a `$2` command and requires bit 3 clear -- `expected 0000F000, read 0000F008`, measured, not reasoned. An acknowledge with nothing to acknowledge must do nothing | `--ring-selftest` | confirmed |
| 75a | **Which of the two acknowledges carries bits 13 and 3 is unobservable, and that is why choosing is not fitting.** Bit 13's helper `$9D2` writes nothing and bit 3 has no helper, so both must ride one of the two. Nothing reads `+400` between them at any of the four `$6` sites -- subtests 23 and 85 read `+402`, 25 reads `+404`, and `$A56`/`$A62` write nothing -- so `+2` and `+4` are **behaviourally identical** choices for this firmware, including at `$7F4`, which never polls bit 13 and still requires it set at subtest 86. A fit changes behaviour to match a test; this cannot. The earlier acknowledge is taken and the equivalence recorded, so a second firmware could settle it | `[ROM3500]` all four `$6` sites | confirmed |
| 75b | **Six more subtests: 22, 23, 24, 25, 26 and 31 now pass**, where fourteen passed before. The self-test stops at **SUBTEST 32**: `expected 0000FC03, read 0000FFFF` at **`00059800`** -- the first 8254, `BANK_TIMER_A`. So the very next thing the firmware asks for is the counters having *counted*, which is the traffic wire findings 41a and 71d named as missing and which nothing has yet built. The item's own test has walked straight to it | `--ring-selftest` | confirmed |
| 75c | And a harness limit surfaced on the way: `--ring-selftest` hardcodes `entry_05` at `+2D4`, which is `[ROM3500]`'s offset, so `[ROM4500]` stops at its first instruction with a fault. Each image's entry table gives its own offset and the harness should read it -- recorded rather than fixed here, since it is a second ROM's *harness* and not this item | `--ring-selftest` on `[ROM4500]` | open |

| 76 | **Subtest 32 is the traffic check, and it states the traffic exactly.** `$976` -- called before every `$6` command -- loads `d3 = $FF` twice into each of `+800`, `+802`, `+804` and `+C04`, so **every counter starts at `$FFFF`**. Subtest 32's helper `$AF8`/`$B60` then latches a counter through `+806` and reads it back low-then-high, comparing against a constant per counter: **`$FC03` for `+800`**, **`$FC00` for `+802`**, **`$FC00` for `+804`**. Counting down from `$FFFF`, that is **1020, 1023 and 1023 counts** | `[ROM3500]` `$976`, `$AF8`-`$B6E` | confirmed |
| 76a | **So finding 71d's prediction is met in full: 31 and 32 are the quantitative check on what the operation must produce.** 41a names these three `RCV_HDR_CNT`, `RCV_PKT_CNT` and `RCV_MAX_CNT`, and the firmware requires the first to count **three fewer** than the other two -- 1020 against 1023 -- which is a structural fact about the operation and not a round number anyone would choose. Our model reads `FFFF` on all three: nothing counts, because nothing clocks them. That is the wire 41a left unmade, now with a specification precise enough to verify rather than approximate | 41a, 71d, 76 | confirmed |
| 76b | **And it is a specification a fitted model cannot satisfy by accident.** Three counters, two distinct expected values, a fixed difference of three between them, and the same check run five times over by subtests 31/32's `d4 = 1..5` loop. A traffic model that produced "about a thousand" events would fail all three arms; one that produced 1023 uniformly would fail the first. What clocks each counter, and why the header count trails by three, is the open question -- but it is now a question with an answer that can be checked | 76 | open |

| 77 | **`002398-04` p. 12-32 re-read for the clock source, and it does not give one -- named here so nobody reads it again for this.** The page confirms 41a exactly: the six counters, their offsets, "are implemented with Intel 8254 Chips", and the full access protocol (`$3000`/`$7000`/`$8000` before loading, `$C200`/`$C400`/`$C800`/`$CE00` before reading, successive word accesses with the byte in the high half). What it never states is **what drives the CLK pin of any of them**. So the documentary route ends here, and `CLAUDE.md`'s rule applies: what was read is named, and what it failed to settle is the clock | `002398-04` p. 12-32 | confirmed |
| 77a | **What the firmware's own numbers suggest, marked suggestive because this file has been wrong this way before.** `$944` loads timer **B** -- `XMIT_HDR_CNT` `+C00` and `XMIT_PKT_CNT` `+C02` -- with `$1FF` and `$3FF`, and `$976` starts timer **A**'s three receive counters at `$FFFF`. Subtest 32 then requires `RCV_PKT_CNT` to have counted **1023**, which is exactly the `$3FF` loaded into the transmit packet counter. A loopback that sends what the transmit counter allows and receives all of it would produce that. **But `RCV_HDR_CNT`'s 1020 does not match the `$1FF` = 511 in `XMIT_HDR_CNT`**, so the pattern does not close, and one matching pair out of two is precisely the numeric resemblance this file has refused four times | 72, 76 | open |
| 77b | **The measurement that would settle it cannot be taken yet, and that is the honest blocker.** With no medium attached the self-test controller generates no traffic at all, so nothing can clock the counters however they are wired -- the loopback of finding 50, which `[MAC]` §3.5's bypass relay exists to permit, has to exist first. So the order is: model the loopback, then see what the counters count, then compare against 1020/1023/1023. Reading more of the ROM will not produce a clock signal | 50, 76, 77 | open |

| 78 | **CLOSED 75c, and it bought a second independent witness.** `--ring-selftest` hardcoded `entry_05` at `+2D4`, which is `[ROM3500]`'s offset; `[ROM4500]` puts it at **`+2A2`** and so ran one instruction and faulted. The offset is in every Apollo option ROM's own header -- `n_entries` at `+48`, then six-byte records of a word id and a long offset -- which `tools/ring-rom/disasm.py` has parsed since it was written, and the harness now reads it. An image whose table does not name id 5 falls back and **says so in the report** rather than silently entering the wrong address | `main.c`; `[ROM4500]` header | confirmed |
| 78a | **And the two firmwares agree exactly, which is the corroboration this whole line of findings needed.** `[ROM4500]` differs from `[ROM3500]` in 1,413 bytes inside the self-test (finding 51c) and 41% of the image overall (57), and it now runs to the **same stopping point with the same numbers**: `SUBTEST 32 failed: expected 0000FC03, read 0000FFFF at 00059800`, differing bits `000003FC`. So every model change this session -- the acknowledge rule, the operation gate, bits 14, 13, 3, 2 and 1 -- is confirmed against a second independent revision rather than fitted to one | `--ring-selftest` on both images | confirmed |
| 78b | **What it does *not* settle, recorded because 75a hoped it would.** 75a said a second firmware could distinguish whether bits 13 and 3 ride the `+2` or the `+4` acknowledge. It does not: `[ROM4500]` stops at the same subtest, so it never reads `+400` between the two acknowledges either. The choice remains behaviourally unobservable, and now on two firmwares rather than one -- which strengthens the case that it is genuinely unobservable rather than merely untested | 75a, 78a | confirmed |

| 79 | **`002398-01` (Rev 1, Apr 83) carries a ring section the later revisions dropped, and it was on disk the whole time.** Every `[EH]` citation in this file has been to **Rev 4**; Rev 1 is the DN3xx/DN4xx era, when the ring was standard equipment, and its peripheral-I/O chapter documents the controller far more fully. The web adds nothing here -- a search of bitsavers' whole Apollo directory returns only documents this project already holds -- so this is the fourth time the answer was in an unread file rather than an unfound one | `002398-01` pp. 6-31..6-32 | confirmed |
| 79a | **The self-test's loopback is a DMA loop, not a wire loop -- which reshapes the next step.** Rev 1's **DIAGNOSTIC COMMAND** register is given bit by bit: `8000` **"dma test (loop xmit DMA to rcv DMA)"**, `4000` enable interrupt, `2000` connect to the network, `1000` disconnect, `0800`/`0400` delay off/on, `0200` snoop. So the board can loop its *transmit DMA back into its receive DMA* internally, with no medium involved at all. Finding 50 read the self-test as needing `[MAC]` §3.5's bypass relay and a ring; 77b then made "model the loopback" a medium-and-station job. **On this evidence it need not be**, and the counters can be clocked by an internal loop | `002398-01` p. 6-32 | confirmed |
| 79b | **And the acknowledge model of finding 74 is documented, on a different board.** Rev 1's DN4XX/DN600 Ring/Disk map lists **`+1A` "Network Trans Interrupt ACK"** and **`+1C` "Network Rec Interrupt ACK"** as write-only registers distinct from the command and status ones. 74 recovered exactly that shape from the AT board's firmware -- `$9FA` and `$A28` each writing an acknowledge to the *first* window after polling -- with no document in hand. Two boards a decade and an address space apart, one mechanism, and the model was built from the firmware before the page was found | `002398-01` p. 6-32 vs finding 74 | confirmed |
| 79c | **The bad-packet counter's own behaviour, which is the first counter semantics any source has given.** Rev 1: the count is "the number of times this node found an error in a pkt going by ... found the error bit in the ackbyte clear, and so was the first to set the error bit"; its interrupt fires "when the counter counts from 255 to 256 (i.e. first uses its highest order bit)"; it **sticks at 511**; and "writing anything to low byte of diagnostic command register clears interrupt and all sticky bits". That is a 9-bit saturating counter with a defined clock *event* -- the first time any document has said what advances one of these | `002398-01` p. 6-31 | confirmed |
| 79d | **What it still does not give**, so the next reader is not sent after it: Rev 1 documents the **DN3xx/DN4xx** boards at `9800`/`BC00`, not the AT board this core models, and finding 55b's arithmetic against the AT layout still refuses the mapping. So `8000 dma test` is not an address to write here -- it is evidence that an *internal* loopback exists on this family, which changes what to build rather than telling us where to poke | 55b, 79a | open |

| 80 | **The loopback's traffic is modelled and the counters count -- prediction made before the run, and met exactly.** 79a's DMA loop needs no medium, so the `$6` operation moves packets internally and the receive counters count what arrives. **How many is the firmware's own number**: the transmit packet counter `+C02`, loaded immediately before the command (finding 72), and the transfer runs until it is exhausted. The prediction was `1023/1023/1023` against subtest 32's `1020/1023/1023`, failing only the header arm; the run gives `expected 0000FC03, read 0000FC00`, **differing bits `00000003`**. Both ROM revisions agree | `--ring-selftest` on both images | confirmed |
| 80a | **So the remaining gap is exactly three counts on `RCV_HDR_CNT`, and nothing else.** `+802` and `+804` would read the `FC00` subtest 32 wants; only `+800` is short, and the loop stops there before reaching them. Three is not a rounding error or a scale factor -- it is a fixed, small difference between the header counter and the two beside it, on a transfer of 1023. The firmware has told us the shape of the answer and one number of it | 76, 80 | confirmed |
| 80b | **What is `PROVISIONAL` about it, stated precisely.** The transfer is **instantaneous** -- 1023 clocks delivered when the operation is acknowledged -- where real hardware spreads them over the ring's bit clock. Subtest 32 reads the counters afterwards and cannot tell the difference; anything watching them *during* a transfer could, and the DMA path (the next plan item) is exactly such a thing. Cost to close is the same as finding 66's: drive the clocking from the transfer rather than from its completion | 80 | provisional |
| 80c | **Why the header counter trails by three is open, and worth not guessing.** Candidates the evidence does not yet separate: headers cut short when the transmit counter expires; a gate held low for the first counts; or the header counter clocked by a different event from the packet counters. `002398-01` p. 6-31 gives *one* counter's semantics in that family -- `bad_pkt_cnt` counts packets this node was first to mark in error, and sticks at 511 -- which shows these counters do have per-counter event definitions, and that the page for these three has not been found | 79c, 80a | open |

| 81 | **Headers and data are *separate DMA streams*, which no finding here had established.** `002398-04` p. 10-24 gives the DN5xx's RING MAP / WORD COUNT registers: `9820` receive **header** address, `9824` receive **data** address, `9828`/`982C` the transmit pair, and at `9830`-`983C` a word count *and* an end count for each of the four. So a packet is moved as two independent transfers, header and data, in each direction -- and note `(***) bit 11 of transmit header count = 1 => data length = 0`, a header-only packet. This is a different mechanism from the 8254 packet counters and is recorded because it is the architecture behind them | `002398-04` p. 10-24 | confirmed |
| 81a | **But the three receive counters' event definitions are in no held source and not on the web, and that is now searched rather than assumed.** All three Engineering Handbook revisions were grepped for the counter section: only Rev 4 p. 12-32 has it, and it gives names, offsets, "implemented with Intel 8254 Chips" and the access protocol -- never what advances any of them. Rev 1 p. 6-31 defines exactly **one** counter in this family, `bad_pkt_cnt`, and that definition is per-counter and specific ("the number of times this node found an error in a pkt going by ... and so was the first to set the error bit"), which is evidence the other three have their own definitions somewhere unheld. A web search returns nothing Apollo-specific: bitsavers' whole Apollo directory is already on disk | all three `[EH]` revisions; web | confirmed |
| 81b | **So finding 80c's three counts stay open with the documentary route closed.** Naming what was read and what it failed to settle, per `CLAUDE.md`: the counter section (Rev 4), the ring register pages (Rev 1 and Rev 4), the DN5xx word-count registers (81), `[S3K]`, and the web. None defines what clocks `RCV_HDR_CNT`. What is left is the firmware, which states the *answer* -- 1020 against 1023 -- without the mechanism, and the two candidate readings 80c names. A three-count difference on a 1023-packet transfer is a small enough residual to leave standing rather than invent a rule for | 80c, 81a | open |

| 82 | **The ring controller has no interrupt path at all -- the same dangling shape the 3c505 had -- and wiring it is blocked by three sources that disagree.** There is no `ap_ring_ctl_irq`, no line constant and no board wiring, so a driver enabling the interrupt would wait forever. But which line it is cannot be settled from what is held: **`[S3K]` Table 2-3** gives `IRQ3` as "Network Board or User Device"; **`FINDINGS.md` C11** measured the master's `ICW3 = 08` and the slave's `03`, putting the **cascade** on IR3 on this machine; and **finding 53d** measured the ring's vector as **163**, which through `ap_intr.h`'s measured base `A0` is master **IR3** again. All three name IR3, and it cannot be both the cascade and the ring | `[S3K]` Table 2-3; `FINDINGS.md` C11; 53d | open |
| 82a | **The candidate readings, none of which is established.** (a) The manual's "IRQ3" is a *bus* line name and the PIC input it reaches differs -- `ap_intr.h` already resolves one Table 2-3 anomaly in favour of the measurement, so the table's numbering is known to be unreliable here. (b) Vector 163 is the cascade's own vector, which a cascaded master never supplies -- the slave's is used instead -- so 53d's `RING_VEC` slot would name a vector the hardware cannot deliver, which would make 53d's inference wrong rather than the table. (c) The ring board on a DN3500 sits on a *slave* line the way the 3c505 does on IRQ10, and neither source describes that machine. **Nothing here picks between them**, and a wrong line is worse than none: it delivers interrupts a real machine would not | 82 | open |
| 82b | **What would settle it, in order of cost.** The DN3500 boot PROM programs both 8259s and then, if it finds a ring board, must unmask *something* -- `writetrace.lua` over `011000`-`0111FF` with a ring ROM fitted would show which mask bit changes, exactly as C11 read the ICWs. That is a measurement this project already has the tool for, and it is cheaper than any further reading. Rev 1's ring section documents the *board's* interrupt bits -- transmit status `8000` interrupt pending / `4000` enabled, and the diagnostic register's `bad_pkt_cnt` overflow interrupt with "writing anything to low byte of diagnostic command register clears interrupt" (79c) -- so the device half is documented and only the *line* is missing | `002398-01` p. 6-31; C11's method | open |

| 82c | **82b's measurement was run in three configurations and does **not** isolate the line -- recorded so it is not run again.** Watching the slave controller's mask register `011101` across a full 350 M boot: with **no ring**, nine writes ending `FF`; with **`--ring`** (controller, no option ROM), the identical nine writes ending `FF` and the boot reaching its normal final PC; with **`--ring-rom`**, only four, because that boot never gets there (82d). In every case the slave ends **fully masked**, so nothing unmasks a ring line in any configuration and there is no bit to identify | `--boot-watch-write 11101`, three boots | confirmed |
| 82d | **And `--ring-rom` diverts the boot rather than extending it.** With the option ROM fitted the machine ends at `PC 000007A8` -- inside `FINDINGS.md` C109's console-selection poll at `00078E`-`0007AE` -- instead of the reference boot's `00002EE6`, and its hash is `A8EFAA26038B4E89` against `A354786119A3931D`. So it sits waiting for a console character and never reaches Domain/OS, which is why no driver installs. `--ring` alone leaves the boot on its normal path (hash `CAECA5C58CC265DC`, differing only as a fitted card must) | same three boots | confirmed |
| 82e | **Which relocates the blocker from documentation to the driver.** Finding 53e established that `RING8_$INT` is installed into vector 163 **only when the card answers**; these runs show it never answers well enough for that, since nothing is ever unmasked. So the line will be identified by making Domain/OS's ring driver *probe and accept* this controller -- at which point it unmasks the line itself and names it -- rather than by any further reading. That is a bigger step than a trace, and it is the honest one | 53e, 82c | open |

| 83 | **The configuration table's valid pattern is `$1234ABCD`, recovered from the PROM's own comparison.** `001784` does `lea.l $1090E,a0` then `cmpi.l #$1234abcd,$4(a0)`, and `$1090E` is the calendar at `010900` plus register `$0E` -- the stride-1 placement `ap_calendar.h` measured -- so the pattern sits at **registers `$12`-`$15`**, exactly where `002398-04` p. 12-3 puts "VALID PATTERN". Both 59a and `ETHERNET.md` 14c recorded this constant as unrecovered and named it as what stood between here and a configured machine. The path checks the **pattern only**; no checksum is consulted before `+1D` and `+1E` are read | `[ROM3500]` `$1784`-`$179E` | confirmed |
| 83a | **And setting it does *not* silence "Configuration information is not initialized", because that message is not the PROM's.** A boot with the pattern written, `--ring` fitted and DEV BIT ARRAY bit 4 set still prints it -- and the string **does not occur anywhere in `3500_BOOT_12191_7`**, searched exhaustively. It comes from the **SELF_TEST diagnostic loaded off the disk**, which the console shows arriving immediately before it, and that program validates the table its own way -- the checksum at `$0E`-`$11` being the obvious candidate the PROM path skips. So the pattern is right and the message has a different author | string scan of `[ROM3500]`; boot console | confirmed |
| 83b | **What that leaves, stated so the next attempt is aimed properly.** 83 closes the constant 59a wanted, and it governs the **PROM's** use of the table -- the option-ROM class in `+1D`/`+1E` (59a, 59b). It does **not** configure the machine as far as Domain/OS or the disk diagnostic is concerned, so 82e's route to the ring's interrupt line -- making the driver probe and accept the card -- is not unlocked by it. The checksum algorithm is the next thing, and it is in the SELF_TEST image on the volume rather than in any ROM | 82e, 83, 83a | open |

| 84 | **The AEGIS filesystem walk is *documented*, in a manual this project has held since the start and never cited for it.** `[AEGIS]` chapter 4 is the on-disk format: **4.3.4.1 VTOC Header**, 4.3.4.3 The VTOC Block, **4.3.4.4 VTOC Entries**, **4.4.1 Locating an Object in the VTOC**, with Figures 4-6 (VTOC header), 4-7 (VTOC map and blocks), 4-8 (VTOC entry), 4-12 (VTOC index) and 4-13 (the index for the disk entry directory). `53h` called extracting `ring8a.drvr` "a filesystem problem, not a research one" and the plan has carried it as the single step between here and `+400`'s bit meanings; it is a *documented* filesystem problem, which is a different size of task | `[AEGIS]` ch. 4 | confirmed |
| 84a | **The entry point, and it is one step from what `ap_volume` already parses.** §4.3.4.1: "The VTOC header resides in the **logical volume label**", with Figure 4-6's offsets "based from the start of the logical volume label" -- and `image/ap_volume.*` already reads the *physical* label and its UIDs. §4.4.1 then gives the index format outright: a **VTOCX** is "the logical disk address of the VTOC block that contains the object's VTOCE and the index within the block", laid out as **bits 31:4 the block's DADDR and bits 3:0 the index**. The header's pointer to the disk entry directory is itself a VTOCX, so `/` is reachable from the label in two decodes | `[AEGIS]` §4.3.4.1, §4.4.1, Fig. 4-12 | confirmed |
| 84b | **And the manual works its own example, which is the check any implementation needs.** "If its value is 734D0, then the VTOCE for `/` is the first entry in VTOC block 734D, physical disk block 734E (assuming that the logical volume begins at DADDR 1)" -- so the low nibble is the index, the rest is the block, and the physical address is the logical one plus the volume's start. A walker that reproduces that arithmetic on the installed volume is checkable before it has parsed anything | `[AEGIS]` §4.4.1, Fig. 4-13 | confirmed |
| 84c | **Read the figures as page images.** 4-6, 4-8 and 4-12 are field diagrams and the text layer already renders 4-12's caption as "Flpre 4-11. VTO C IDdex" and its example value inconsistently. `CLAUDE.md`'s rule exists for exactly this, and `RING.md` 18a records what ignoring it cost on `[MAC]`'s figures | `[AEGIS]` text layer | confirmed |
| 84d | **What it unblocks, which is why it is worth doing before more ring reading.** `ring8a.drvr` (53f: 13 KB, every routine at a known offset) for `+400`'s bit meanings; the SELF_TEST image for 83a's configuration checksum; and `ETHERNET.md`'s and `GRAPHICS.md`'s equivalents. Three items that have each stalled on "the answer is in a file on the volume" | 53f, 83a, 84 | open |

| 85 | **AUDIT, `[MAC]` chapter 2 line by line against the code: the frame layer and the station layer are never connected.** `ap_ring_mac` (stuffing, four characters), `ap_ring_frame` (header fields, CRC), `ap_ring_framer` (all five sequences), `ap_ring_phy`, `ap_ring_medium` and `ap_ring_station` each implement their own section and each has a suite. **But nothing outside `ap_ring_framer`'s own module and tests ever calls it** -- an exhaustive grep of `src/` for the assembly entry points finds no caller -- so **no frame is ever put on the medium**. The station forwards bits, claims tokens and strips; frames exist only in unit tests | exhaustive grep of `src/`; `ap_ring_station.c` | confirmed |
| 85a | **Three normative parts of §2.1's transmit sequence are therefore absent, and the plan claims otherwise.** Step 3's "breaks ring recirculation and **begins to transmit its packet**" -- the station sets `stripping` and drives Zeros, and never transmits. Step 6's "The transmitting node **sends out a new free token to follow the frame**" -- nothing anywhere emits a free token after transmitting, so a station that claims the ring holds it until the strip timeout. Step 7's "until it **finishes receiving its own frame**, or until a 10.9 msec timeout" -- only the timeout arm exists, so stripping always runs the full 131,072 bits. `COMPLETION_PLAN.md` states on a **ticked** item that "The transmit sequence of `[MAC]` §2.1 ... are implemented" | `[MAC]` §2.1 steps 3, 6, 7 vs `ap_ring_station.c` | confirmed |
| 85b | **§2.2.2.2's address matching does not exist.** "The hardware compares the contents of the 32-bit destination address field with the node address of the target ... a node receives a message if the destination address field matches its node address, or if the broadcast bit in the type field is set." `ap_ring_frame` defines `AP_RING_HDR_DESTINATION` and `AP_RING_TYPE_BROADCAST`, but `ap_ring_station_t` **has no node address at all** and nothing compares one. There is no receive filtering, so no station can tell whether a frame is for it | `[MAC]` §2.2.2.2 vs `ap_ring_station.h` | confirmed |
| 85c | **And the acknowledge fields are never modified in flight.** Findings 23 and 25 record that both fields are "inserted by the transmitter and **modified by *other* nodes as the frame passes**" -- intend-to-copy, copied, wack, and the error bit an abort sets (§2.2.2.2). `ap_ring_frame` can get and set them on a header buffer and `ap_ring_framer` writes and reads the late field, but no station touches either as a frame goes by, because no frame goes by | `[MAC]` §2.2.2.2, Figs 2-7/2-8 vs `src/core/ring` | confirmed |
| 85d | **What the audit does *not* find, stated so the result is not read as worse than it is.** Every module checked against its own section is **correct**: the stuffing threshold and the six-ones violation, all four out-of-band characters and their type bits, the three separator characters, the short null separator in both the frame start and frame check sequences, the 12-to-1024 even header rule, the packet header field offsets including the zero byte at `+6` and the early acknowledge at `+7`, and the CRC generator. The defect is **composition**, not content: correct parts, unconnected | `[MAC]` ch. 2 vs each module | confirmed |
| 85e | **Which explains why the transmit/receive handshake item has resisted.** That item needs a transmit path; there is none above the token layer, on either the station side or the controller side. `ap_ring_ctl`'s `+406` buffer port and the station's bit stream have never been joined either. So the item is not "finish the last few status bits" -- it is "build the transmit path", and the status-bit work of findings 66-80 sits on top of a ring that cannot carry a frame | 85, 85a | open |

| 86 | **The audit's three missing steps are now implemented, and a frame crosses the ring.** `ap_ring_station_queue_frame` assembles through `ap_ring_frame_emit` into a caller-lent buffer -- `src/core` allocates nothing -- and the station drives it one bit per bit time while stripping, which is §2.1 step 3's "begins to transmit its packet" *concurrently* with discarding, as the manual puts them in one sentence. Step 6's new free token is originated the instant the last frame bit goes out. Step 7's first arm now exists: the station watches for its **own frame start** coming back and counts its own frame's length, so stripping ends on the frame returning rather than always running the full 131,072-bit timeout | `ap_ring_station.*`; `ring_station_suite` | confirmed |
| 86a | **Two defects the new test caught, both of them the same shape as one already documented above them.** First, the own-frame watch was gated on transmission being *finished*, and a ring is a few bit times around while a frame is a thousand bits long -- so the frame start came back long before the gate opened and was missed every time. Second, and worse, the stripping branch **overwrote the free token step 6 emits**: a Zero written over a sourced bit turns the token back into padding, the ring is never released, and no other station can ever transmit. `ap_ring_station.c` already carried a comment explaining that exact hazard for the *claim* bit -- "stripping it back to a Zero turns the claimed token into a free one again" -- and the release path repeated it | `ring_station_suite` | confirmed |
| 86b | **What is still absent, so 85's audit is not read as closed.** §2.2.2.2's **destination matching** (85b) and the **acknowledge fields modified in flight** (85c) remain unimplemented: a station still has no node address and nothing sets intend-to-copy, copied, wack or the abort error bit as a frame passes. Those are the *receive* half; this is the transmit half. The ring probe golden `ring_probes.txt` is byte-unchanged, so none of it disturbs the measured timings | 85b, 85c | open |

| 87 | **85b closed: §2.2.2.2's receive decision now exists.** A station has an address, and destuffs the passing stream far enough to take it -- destination (2 words) and type (1 word), the first six header bytes, which is all the section needs and stops well short of parsing a frame it is only forwarding. Broadcast is checked **first**, because "if it is set, receivers ignore the destination address field", so the comparison is not reached rather than merely overridden | `[MAC]` §2.2.2.2; `ring_station_suite` | confirmed |
| 87a | **One trap in it worth recording.** The header does **not** begin after the frame start character: §2.2.2.1's frame start *sequence* is the character, a **null separator**, and a **separator character**, so a receiver that starts capturing at the frame start puts the null separator's eight zero bits into the top of the destination address. The capture therefore waits for the separator. Bit stuffing is undone inline rather than by `ap_ring_bitreader`, because a station sees one bit at a time and cannot rewind | `[MAC]` §2.2.2.1 | confirmed |
| 87b | **Tested with the bystander, which is the arm that matters.** A station reporting every frame as its own passes an addressee test and is useless, so the suite requires station 2 to have **seen** the frame and **not** accepted it, alongside the addressee accepting. A second test covers the broadcast arm with a destination matching *neither* station, which a model that only compared addresses would accept nowhere | `ring_station_suite` 13 -> 15 | confirmed |
| 87c | **What remains of the audit: 85c alone.** The acknowledge fields are still not modified in flight -- nothing sets intend-to-copy, copied, wait-ack, or the error bit as a frame passes. The station now knows *whether* it is addressed, which is the precondition Figures 2-7 and 2-8 attach every one of those bits to ("an **addressed** receiver sets this bit"), so the remaining work is rewriting bytes in the forwarded stream rather than deciding anything new. The ring probe golden is byte-unchanged throughout | 85c | open |

| 88 | **85c's early acknowledge is now modified in flight, and the chapter-2 audit's transmit and receive halves are both closed.** "A node's ring transmitter inserts an early acknowledge field; another node's receiver modifies it", and Figure 2-7 makes intend-to-copy the act of "an **addressed** receiver". The field is header byte **7** and 87's decision is taken at byte 6, so it is always known in time. The bit is altered on the bit time it is forwarded -- the same mechanism claiming a token uses, and the only one available to a station with a one-bit delay | `[MAC]` §2.2.2.2, Fig. 2-7 | confirmed |
| 88a | **Two things make it safe, and both are the manual's own.** No CRC recalculation is needed because "ring hardware treats this field as a string of Zeros in its CRC calculation" (§2.2.2.4, finding 25). And the **bit stuffing cannot be disturbed**: Figure 2-5 puts a byte of zeros at `+6` immediately before the field, and its legal bits are 3 and 1, so the longest run of ones it can carry is one -- nowhere near the five that would force a stuffed zero. Neither is an assumption; both are stated | `[MAC]` §2.2.2.2, §2.2.2.4, Fig. 2-5 | confirmed |
| 88b | **Odd parity is maintained, and the first version of the test got it wrong in a way worth keeping.** Setting intend-to-copy adds a One, so the parity bit must flip; the station does that. The test originally wrote its outgoing field with `set_early_ack(0)` -- which writes **verbatim**, so an all-zero field went out with **even** parity, illegal per Figure 2-7 -- and the assertion failed on the parity, not the bit. `ap_ring_ack_with_parity` exists for exactly that and the test now uses it. A helper unused by the code that needed it, twice over: `ap_ring_header_addresses` is §2.2.2.2's comparison and the station had never called it either | `ring_station_suite` | confirmed |
| 88c | **The test checks the field at the *sender*'s downstream neighbour, not at the modifier.** Station 2 forwards the frame after station 1 has rewritten it, so what station 2 captures at byte `+7` is what actually travelled. Asserting at the station that made the change would have passed against a purely local variable, which is the failure mode this audit exists to catch | `ring_station_suite` 15 -> 16 | confirmed |
| 88d | **What is left of 85c: the *late* acknowledge.** Figure 2-8's copied, wait-ack, intend-to-copy and error bits live in the **end-of-frame sequence**, past the frame check, so reaching them needs the forwarding path to track a frame to its end rather than six bytes into its header. The early field was the one every addressed receiver must set; the late one carries the reply the *sender* reads, and is what a WACK or an error report is made of | `[MAC]` §2.2.2.5, Fig. 2-8 | open |

| 69b | **The harness could not have supported a duration model, and that is fixed first.** `--ring-selftest` stepped `ap_m68030_step` -- the processor and nothing else, with no interrupt sampling, no bus tick and no device advanced -- so **no time passed at all** during the firmware's run. `FINDINGS.md` C109 caught and fixed exactly this on the boot path and the self-test harness kept the old shape, which was harmless only while the controller was pure registers. It stops being harmless the moment finding 69 asks for an operation that completes *after a while*: a while never passed. Now `ap_machine_run(&machine, 1u)`, the machine's own loop once, as C109 made it | `main.c` `run_ring_selftest` | confirmed |
| 69c | **Shown rather than claimed**: the self-test now reports `elapsed 122,231,950,714,368 base unit(s)` over 6,895,043 instructions, where the same run produced **zero** before. And it is a prerequisite and not a fix to the item -- the firmware still stops at **SUBTEST 26** with the same expected value `B00E` under mask `D000`, so all fourteen passing subtests are unchanged and finding 69's question is exactly where it was | `--ring-selftest` | confirmed |

| 69a | **Where that leaves the item.** Fourteen subtests pass and the fifteenth is a timing question rather than a register one, which is a different kind of work: `ap_ring_sched` and `ap_ring_station` already model the ring with duration, so the next step is to drive the controller's status bits from them instead of from the register write. That is the transmit/receive handshake this item is named for, and the self-test is now the test for it | -- | open |
| 53d | And the **prose definitions** for six of them, which give ring semantics rather than register bits: a transmit timeout "counts the number of packets sent by this node that **did not return**" and "can indicate a break anywhere in the network"; a transmit bus error is a faulty transfer "between the controller and the node's memory"; a transmit acknowledge parity error is a packet that "returned with a corrupted hardware protocol field"; a transmit overrun is memory-to-ring contention that "can occur when the node is simultaneously trying to transmit packets and perform other internal data transfers such as disk I/O"; a **WACK** is a packet returned "with marks showing that the intended receiver saw the packet but could not receive it"; a **NACK** is one returned "without the mark that acknowledges receipt" | `…awd` @ `0x07110A06`-`0x07110F24` | confirmed |
| 53e | **What the search cost, and one false positive worth recording.** Scanning for the four polled masks as `ANDI.W` immediates gave exactly one cluster in 348 MB -- and it is **data**, a walking-bit table (`0240 4000`, `0240 2000`, `0240 1000`) among glyph patterns. The signature was wrong in principle as well as in fact: the ROM uses `and.w #mask` to test several bits at once, while a driver testing one bit uses `btst #n`, which contains no mask word at all. **Search a driver by its symbols, not by the ROM's instruction shapes** | `…awd` @ `0x04C425C8` | confirmed |
| 53f | **The driver's files are named, and the module is small.** Directory entries list `ring8a.drvr`, `ring8b.drvr`, `ring8a.dex`, `ring8b.dex` and `itr8.drvr` together. The link map sizes `RING_PROC` at **`0x3370`** bytes loaded at `FFCCC000`, so the whole thing is 13 KB -- and the symbol addresses give offsets *within* it: `RING_$SENDP` at `+0xABC`, `RING8_$INT` at `+0x758`, `RING8_$INT_DEFERRED` at `+0x2CC`, `RING_$POLL_STICKY_BPHERR` at `+0x19A4`, `RING_$START` at `+0x1EDA`, `RING_$KICK_DRIVER` at `+0x1F60`, `RING_$SET_TMASK` at `+0x1FAE`, `RING_$GET_STATS` at `+0x2026`, `RING_$IOCTL` at `+0x221E`. So once the 13 KB is in hand, every routine is at a known offset | `…awd` @ `0x09D754E4`, `0x0038306C` | confirmed |
| 53g | A second machine's map is in the same image at `0x00C5BC7D` with `RING_PROC` at `size = 340C`, and a third at `0x12047ADA` with `size = 3480` -- three revisions of the same driver. Also present: a **`RING8025`** family (`RING8025_UCB`, `RING8025_PIO_IOMAP`, `RING_8025`), which is 802.5 token ring and **not** this board; do not conflate them when searching | `…awd` @ `0x0037C13F` | confirmed |
| 53h | **What remains, and it is a filesystem problem rather than a research one.** The map's addresses are load addresses and the directory entries are AEGIS directory records, so extracting `ring8a.drvr` needs the on-disk filesystem walked -- there is no route from either to a byte offset. That is the single remaining step between here and `+400`'s bit meanings | — | open |


## Open

| # | Question | How it will be answered |
| --- | --- | --- |
| A | The controller's register map within `0x220`–`0x23F` | **Largely recovered — findings 11-15, 38-41a and 44-50a.** Settled: the unit-to-window mapping, `+000` as an ASCII ID register, `+400` bit 15 as a presence gate, and `+800`/`+C00` as two 8254 timers with their counter and control ports. Remaining: the meaning of `+402`, `+404`, `+406`, of `+400`'s other bits (bit 11 is written, purpose unknown), and of the `a1` window at `$51000` whose `+2`/`+4`/`+6`/`+400`/`+402` are cleared at init |
| B | Dual-ported RAM buffer: size, host window, and descriptor/queue layout | **Size and access path answered — findings 46, 46a and 47.** The buffer is **not memory-mapped**: it is reached through the auto-incrementing `+406` data port with `+006` as its pointer, and the self-test walks **64 KB** of it in four patterns. The read port is pipelined by one word. This overturns finding 42's conclusion, which assumed a dual-ported buffer had to appear in AT memory space and closed the question on not finding one there; 42's scan was right and its inference was not. What remains is the **descriptor and queue layout** — the self-test uses the buffer as flat memory and never as a structure, so the ROM cannot show one. Domain/OS's ring driver is still the route for that, and only that |
| C | Exact token and frame character encodings on the wire | `[MAC]` §2.2.1–2.2.2, transcribed with each format citing its subsection |
| D | Ring latency contributed per node by the elastic-store buffer, and PLL acquisition behaviour | **Partly answered — findings 28, 29 and 52-52c; `[PAT575]` now read and exhausted (52d).** The buffer's hardware ceiling is two bits and an overflow resets the receive and transmit counters to a predetermined value. What remains is a *numeric* acquisition time, which `[PAT575]` does not give and which no held source does The steady-state delay is now evidenced: nominally 1 bit per node, bounded by 0.5 and 1.5, with the offset linear in frequency deviation across ±3 kHz. What remains is *acquisition*: how long a PLL takes to lock, and what the ring does during the re-initialisation that an ESB under/overflow forces. `[MAC]` gives neither; `[PAT575]` is the remaining source |
| E | Token-loss detection, ring reconfiguration and node insertion/removal timing | **Partly answered — findings 34, 35 and 37.** The transmit sequence, stripping, and the 10.9 ms (2^14 byte) stripping timeout are evidenced, and node removal losing an in-flight token is measured. What remains is the **token-loss timeout itself**: §2.2.1.1 says a node forces a claimed token "after a specified timeout" and never specifies it. This core uses the stripping timeout as a `PROVISIONAL` stand-in, marked in `ap_ring_station.h`. **`[PAT575]` has now been read and does not contain it** (finding 52d) -- it is a physical-layer synchronisation patent. No held source names the timeout |
| G | How Domain/OS's single-level store maps onto ring packets — page faults across nodes | `[AEGIS]`, `[ARCH]` |

**H is resolved, and the answer is the negative one it was framed to accept.**
It asked what the 2-byte trailer at offset `length` means. The route named was to
disassemble the boot PROM's option-ROM scan -- "if that scan never touches offset
`length`, the field is not consumed by the machine and the question closes as
*not read by firmware*, which is a sufficient answer for emulation". It never
touches it. `length` is read by **exactly one instruction in the whole 64 KB
PROM** -- `move.l $C(a1),d1` at `01098`, searched for exhaustively as any
`(d16,A1)` operand with displacement `$000C` -- and it is used as a *count*: the
sum runs over `[0, length)` and stops one long word short of the trailer. The
three other `(d16,A0)` reads at that displacement are a vector-table setup and a
list walk, neither in the option-ROM path, which uses `A1` as the image base
throughout.

So the trailer is outside the image, outside the checksum, and outside anything
the firmware reads. It stays unexplained and it is **not a blocker**: an emulated
option ROM need not reproduce a field no code consumes. If a future disassembly
of the *ring* firmware turns out to read it, this reopens -- but the machine that
loads these ROMs does not.

**F is resolved** — see findings 10, 10a and 10b. It asked whether 12 Mbit/s was
the bit rate or the symbol rate, because a non-12 MHz line clock would force
`AP_TIME_BASE_HZ` to be recomputed. It did: the PHY is bi-phase with a 24 MHz
line clock, and the base is now 6.6 GHz. Resolving it before implementing the
medium was the point — the answer changed the unit of account, and changing that
after goldens exist would have invalidated every one of them.

Item D is now the remaining physical-layer unknown, and 10b is the part of it
that the medium will have to face first.

## Divergences from the oracle

None yet: there is no oracle for the ring to diverge from. When the 3c505 802.3
path is implemented, that one *does* have a runnable reference and gets its own
row in `tools/mame-oracle/FINDINGS.md`.
