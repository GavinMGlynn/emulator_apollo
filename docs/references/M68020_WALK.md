# `[020]` — MC68020 User's Manual, coverage record

| Tag | File | Pages | Native | Text layer |
| --- | --- | --- | --- | --- |
| `[020]` | `motorola/MC68020_32-Bit_Microprocessor_Users_Manual_1984.pdf` | 452 | **600 ppi** jbig2 | yes, and OCR — read the images |

## STATUS: **started 2026-09-09.** §1 (10/10), §2 (24/24), §3 (10/10), §4 (6/6), §7 (4/4) done; **§6 in progress, 7 of 24**. **61 of 452.**

### Method note: proving a page blank without reading it

Two pages so far are section-end fillers. A blank page still has to be *opened* —
"a range cannot be characterised without opening it" is this project's most
expensive repeated lesson — but it does not have to be read as an image. Render
at 100 dpi and count dark pixels:

    pdftoppm -r 100 -png -f N -l N <pdf> out
    # p. 55: dark pixels: 1 of 631400

One speck in 631,400 is a blank page, established rather than assumed, for a
fraction of the cost of looking at it. p. 31 was read as an image before this was
worked out; p. 55 by the count.

Started because the deferral that kept it closed had a false premise. The plan
item read "Phase 2b and Phase 7 parts ... **Deferred until those processors are
built**" — and the 68020 *is* built into the model table already:
`src/core/model/ap_model.c` gives **three** rows `.cpu = AP_CPU_M68020` (DN3000,
DN4000/DS4000 and a third), and `ap_model.h` cites "MC68020 User's Manual §7.1.1
and §1" for cache sizes that are live in that table today. A part with rows in
the model table is not a future part.

## The citation audit, which is where this walk starts

The rule the batch item sets — "do the citation audit first, per document; it is
one grep" — and its answer here is the opposite of `[030]`'s. There, §7-§9 and
§11 held 88 of 90 citations and were *verification*, which halved the job.

Here: `src/` and `tests/` mention the 68020 **473 times**, and only **six** of
those cite this manual with a place — §1, §7.1.1, §9.2.1 and Appendix D. The
other 467 are prose about the part, written from `[030]`'s and `[PRM]`'s
descriptions of what the 68020 does differently. **So there is almost no
verification to lean on and the 452 pages are new reading.** Recorded because it
is the estimate the walk is planned against, not an aside.

## Section boundaries, by PDF page

| § | Title | PDF pages |
| --- | --- | --- |
| 1 | Introduction | 22-31 |
| 2 | Data Organization and Addressing Capabilities | 32-55 |
| 3 | Instruction Set Summary | 56-65 |
| 4 | Signal Description | 66-71 |
| 5 | Bus Operation | 72-123 |
| 6 | Processing States | 124-147 |
| 7 | On-Chip Cache Memory | 148-151 |
| 8 | Coprocessor Interface Description | 152-189 |
| 9 | Instruction Execution Timing | 190-217 |
| 10 | Electrical Specifications | 218-… |
| 11 | Ordering Information and Mechanical Data | … -452 |

*§10 and §11 are **not** to be characterised without opening them.* That is this
project's most repeated walk lesson: `[8259]` hid `TJLJH` in a range dismissed as
electrical, `[8237]` hid six numbered DESIGN CONSIDERATIONS on its last page, and
the `[8237]` record had to correct a row that described nine pages it had not
read.

## Coverage

| p. | § | Yield | What it contained |
| --- | --- | --- | --- |
| 128, 130, 140, 143-146 | **§6 PROCESSING STATES — in progress, 7 of 24.** 6.2.1 Exception Vectors (**Table 6-2**); 6.2.4 Exception Processing Sequence and 6.2.5 Multiple Exceptions; 6.4.1 Special Status Word (Figure 6-2); 6.5.7 Stack Frame Summary (**Figure 6-9**) | **A DIVERGENCE FOUND** — and a cross-printing of a register Domain/OS depends on | **Figure 6-9 gives format `1011`, the long bus fault frame, as *44 words*. This core has 46.** `ap_m68030_exception.h` reads `AP_M68030_FRAME_LONG_BUS_FAULT = 0xB, /* 46 words */`, which is the **68030's** size and is right for the part `ap_machine` builds — and wrong for the three model rows that declare `AP_CPU_M68020`. A 68020 taking a bus fault pushes 44 words and its `RTE` pops 44. That is the **third** register-level difference this walk has found between the declared part and the built one, after §7's four-bit CACR against the 68030's eleven and §1's five control registers against ten. Figure 6-9 also fixes the rest of the family's formats: `0000` short 4 words, `0001` throwaway 4, `0010` instruction exception 6, `1000` MC68010 bus fault 29, `1001` coprocessor mid-instruction 10, `1010` MC68020 short bus fault 16, with `0011`-`0111` and `1100`-`1111` reserved. **Figure 6-2's Special Status Word matches `ap_m68030_ssw.h` field for field** — `FC` 15, `FB` 14, `RC` 13, `RB` 12, `DF` 8, `RM` 7, `RW` 6, `SIZ` 5-4, `FC0-FC2` 2-0, at stack offset `$A` for both fault formats — which is a second printing of a register our header derives from `[030]` Figure 8-9 and calls out as what "Domain/OS's demand paging rests on". One wording difference: `[030]` calls bits 11-9 "for internal use only" where this draws them as literal `0`. **And one clause `[030]` does not give**: "the RB and/or RC bits will be set if the corresponding fault bit is set, **or if a prefetch for the stage is pending**, so that the default is to have the processor rerun the bus cycle(s)". Our encoder enforces only the first half — rerun set when fault set — so the pending-prefetch case is a documented condition this model does not produce. Named, not implemented: it needs prefetch-stage state the core does not carry, and the shape is already expressible (the header notes a rerun without a fault is how an address error is distinguished). **Table 6-2 gives a fourth divergence of the same shape**: vectors **48 through 63 are "(Unassigned, Reserved)" on the 68020**, where `ap_m68030_exception.h` defines `AP_M68030_VECTOR_MMU_CONFIGURATION = 56` — a 68030 MMU vector that this part does not have. And it **confirms** the space each vector is fetched from: "all exception vectors are located in supervisor **data** space, except the reset vector which is located in supervisor **program** space", which is `ap_m68030_step.c` reading offsets 0 and 4 with `AP_M68030_FC_SUPERVISOR_PROGRAM` against `SUPERVISOR_DATA` everywhere else. Vector 12 is unassigned, 13 coprocessor protocol violation, 14 format error, 15 uninitialized interrupt, 24 spurious, 25-31 the seven autovectors, 32-47 `TRAP #0-15`, and **64-255 "User Defined Vectors (192)"**. §6.2.4's four-step sequence **confirms** three things this core does: the `T1`/`T0` clear and `S` set on entry, the vector offset as "vector number multiplied by four ... added to the vector base register", and — the one worth naming — "**if the exception is an interrupt and the M bit is set, the M bit is cleared, and a second stack frame is created on the interrupt stack**", which `ap_m68030_step.c` implements as the throwaway frame quoting the same rule. §6.2.5 adds that loading the new context includes "**the first three instruction words** at the new program counter location". **Figures 6-7 and 6-8 together make the divergence structural, and confine it to the long frame.** *The short frame agrees completely*: Figure 6-7's Format `$A` is SR `$00`, PC `$02`, `1010`+vector `$06`, internal `$08`, SSW `$0A`, stage C `$0C`, stage B `$0E`, data cycle fault address `$10`, two internal registers, **data output buffer `$18`**, two more internal registers, ending `$20` — 16 words, and every offset this core models matches. *The long frame does not*, and the difference is sharper than a length. `ap_m68030_ssw.h` places `DATA_OUTPUT` at `$18` in the group it marks **common to both frames**, from `[030]` Table 8-6, with `STAGE_B_ADDRESS $24`, `DATA_INPUT $2C` and `VERSION $36` marked "long frame only". So **the 68030 keeps the data output buffer at `$18` in both frames; the 68020 moves it to `$28` in the long frame alone** — and puts the stage B address at `$20` where the 68030 has `$24`, and has **no version field** where the 68030 has one at `$36`. So "44 words against 46" understates it: the trailing half of the frame is laid out differently, and a handler written for one part reading the other's frame would take the data output buffer from the middle of an internal-register block. Figure 6-8's full map, for the record: SR `$00`, PC `$02`, `1011`+vector offset `$06`, internal register `$08`, SSW `$0A`, pipe stage C `$0C`, pipe stage B `$0E`, data cycle fault address `$10`, internal registers 6 words `$14`, stage B address `$20`, internal registers 2 words `$24`, data output buffer `$28`, data input buffer `$2C`, internal registers 20 words `$30`, ending `$56`. §6.5.6 also states when it is used: "whenever the processor detects a bus cycle fault and recognizes that it is **not on an instruction boundary**", with the PC being "the address of the instruction that was executing when the fault occurred (**which may not be the instruction that generated the faulted bus cycle**)". *Fidelity*: all six pages read as images. **§6's other 18 pages are not read** and this row does not describe them |
| 66-71 | **§4 SIGNAL DESCRIPTION**, 4.1 Function Codes through 4.11 Signal Summary; Figure 4-1, **Tables 4-1 and 4-2** | `confirms` **— including the footnote that separates one reserved code from the other two** | **Table 4-1's function codes match `ap_m68030_regs.h` value for value**, and so does its footnote: "**Address space 3 is reserved for user definition**, while 0 and 4 are reserved for future use by Motorola" is why that header has `AP_M68030_FC_USER_DEFINED 3u` distinct from `AP_M68030_FC_RESERVED_0` and `_RESERVED_4` rather than three interchangeable holes. **Table 4-2's signal summary establishes what the 68020 does *not* have**, which is the useful half for a core that models the 68030: no `STERM` (its only transfer acknowledge is `DSACK0`/`DSACK1`), no `CIIN`/`CIOUT`, no `CBREQ`/`CBACK` burst pair, no `MMUDIS` — the 68030's synchronous-termination, cache-inhibit, burst and MMU-disable pins are all absent, and `DBEN` (Data Buffer Enable) is a 68020 signal the 68030 drops. `RESET` and `HALT` are bidirectional and **open drain**. *Three behavioural statements worth having*: §4.5.1, the processor "may start an instruction fetch cycle and **then abort it if the instruction word is found in the cache**", driving address, size and function code but **not** address strobe — a speculative bus cycle a per-cycle model of this part would have to produce; §4.5.3, `RMC` "is asserted for the duration of the read-modify-write cycle", which is the third printing of the fact behind the open `AP_M68030_RMC_FIRST_READ` item; and §4.4, `SIZ0`/`SIZ1` "indicate the number of bytes of an operand **remaining** to be transferred during a given bus cycle", which is what makes dynamic bus sizing work. *Fidelity*: pp. 67 and 71 (Tables 4-1, 4-2) read as page images; pp. 66, 68-70 as text |
| 56-65 | **§3 INSTRUCTION SET SUMMARY**, 3.1 Data Movement through 3.10 Multiprocessor Operations; **Tables 3-1 to 3-10** | `confirms` **— and Table 3-2 checks the divide form that implementations get wrong** | **Table 3-2 gives the size signatures exactly**: `DIVS/DIVU` in three forms — `<ea>,Dn` 32/16 → 16:16, `<ea>,Dr:Dq` **64/32 → 32:32**, `<ea>,Dq` 32/32 → 32 — and `DIVSL/DIVUL` `<ea>,Dr:Dq` as **32/32 → 32:32**; `MULS/MULU` as 16x16 → 32, 32x32 → 32 and 32x32 → 64. **`ap_m68030_step.c` distinguishes the two 32/32 forms correctly and says why**: it writes the remainder *first*, so that a plain `DIVS.L <ea>,Dq` — the same encoding with `Dr` equal to `Dq` — lands the quotient on top of it, which is what "the remainder is discarded" means. Written the other way round it would leave the remainder in the quotient's register for every plain long divide in the machine. Also: `EXT` 8→16 and 16→32 with `EXTB` 8→32, and `CMP2 <ea>,Rn` at 8, 16 and 32 as "lower bound <= Rn <= upper bound". §3.10 states that **`TAS`, `CAS` and `CAS2` "execute indivisible read-modify-write bus cycles"** — the `RMC` line whose placement is the open `AP_M68030_RMC_FIRST_READ` sub-item, here stated for a third part. §3.6's bit-field operations are "fields of up to 32 bits" with all eight `BF` forms. *Fidelity, and the reason*: p. 58 (Table 3-2) read as a page image; pp. 56-57, 59-64 read as text. These ten pages are a **summary** of instruction semantics that `[PRM]` carries authoritatively and that walk has already derived 646/646, so their role here is corroboration rather than derivation — the one table read at image fidelity is the one carrying the 68020's *new* forms, where corroboration could still find a discrepancy. **p. 65 established blank by pixel count**, 0 of 631,400 |
| 32-55 | **§2 DATA ORGANIZATION AND ADDRESSING CAPABILITIES**, 2.1 Operand Size through 2.12 Queues; Figures 2-1 to 2-7, **Tables 2-1, 2-2 and 2-3** | `confirms` **— the shared operand decoder, field for field and row for row** | **Table 2-1's two extension-word formats match `ap_m68030_ea.c` bit position for bit position**: brief is `D/A` 15, Register 14-12, `W/L` 11, Scale 10-9 (00=1, 01=2, 10=4, 11=8), format bit 8 = 0, displacement 7-0; full is the same head with bit 8 = 1, then `BS` 7, `IS` 6, `BD SIZE` 5-4 with **`00` = Reserved**, bit 3 = 0, `I/IS` 2-0. **Table 2-2's IS-I/IS memory indirection encodings match `decode_indirect` row for row**, including both reserved cells — that function's own comments already quote `"0  100  Reserved"` and `"1  100-111  Reserved"`, so this is the source they were quoting, now read. Table 2-3's effective-address encoding summary matches the mode/register decode. **So the operand decoder is established correct for the 68020 as well as the 68030**, which is what three `AP_CPU_M68020` rows in the model table need and what was previously assumed. *Two counts of the same set*: §1's bullet says "**Five** Special Purpose Control Registers" and §2.2 says "the **six** control registers (SR, VBR, SFC, DFC, ...)" — the difference is whether SR is counted, and the set is the same either way. **Confirms** three behaviours this core implements: §2.2.3's "undefined bits [of SR] are read as zeros and ignored when written" and the CCR's "upper byte is read as all zeroes and ignored when written", which is `AP_M68030_CCR_MASK`; §2.2.2's address-register rule — no byte operands, the whole register written whatever the size, and "if the operation size is word, operands are **sign extended to 32 bits** before the operation is performed"; and §2.10's `M`/`S` selection among USP, MSP and ISP. **One behavioural rule worth naming**: §2.10, "data entry on the stack is restricted so that data is always put on the stack on a **word boundary** ... byte data is pushed on to or pulled from the system stack as the **high order byte of a word**; the low order byte is unused." *Cross-printing corroboration rather than a new fact*: §2.9's "the longest instruction for the MC68020 contains **ten extension words**" is the same bound `M68030_WALK.md` records from `[030]` §2.5 and notes as stated-but-unenforced; two printings, one invariant, still unasserted. **One question the document does not answer**: bit 3 of the full format is drawn as a literal `0` and neither manual says what a `1` there does; `ap_m68030_ea.c` ignores the bit. Named rather than guessed. *Fidelity*: pp. 50 and 51 (Tables 2-1, 2-2, 2-3) read as page images; pp. 32-37, 44, 49, 52-54 as text with their figures identified; pp. 38-43 and 45-48 are the per-mode descriptions, each a Generation / Assembler Syntax / Mode / Register block whose content is Tables 2-1 to 2-3 restated, read as text against the tables already read at image fidelity; **p. 55 established blank by pixel count** |
| 22-31 | **§1 INTRODUCTION**, 1.1 Data Types and Addressing Modes, 1.2 Instruction Set Overview, 1.3 Virtual Memory/Machine Concepts, 1.4 Pipelined Architecture; Figures 1-1 to 1-5, Tables 1-1 and 1-2 | `confirms` **— and pins two model-table facts to their source** | The opening bullets give **"Five Special Purpose Control Registers"** and **"18 Addressing Modes"**, and Figure 1-3's Supervisor Programming Model Supplement names the five: `ISP`, `MSP`, `SR`, `VBR`, `SFC`/`DFC`, `CACR`, `CAAR` — five *control* registers being VBR, SFC, DFC, CACR, CAAR, where the 68030 adds `CRP`, `SRP`, `TC`, `TT0`, `TT1` for its on-chip MMU. Figure 1-2's user model is D0-D7, A0-A6, A7 (USP), PC, CCR, and Figure 1-4's status register carries the **`M` master/interrupt bit** that `ap_m68030_regs.h` already names as "the 68020-and-later addition". **Table 1-1's eighteen addressing modes are the 68030's, mode for mode** — through memory indirect pre- and post-indexed and their PC-relative forms, with the same `Xn.SIZE*SCALE` note (SCALE 1, 2, 4 or 8) — so the shared operand decoder is right for both parts. **Table 1-2 confirms `ap_model.h`'s claim about `CALLM`/`RTM`**: both are in the 68020's set, and the table has no MMU instructions at all, the PMMU being external. Also stated and worth having: the bus is non-multiplexed 32/32 with **dynamic bus sizing** determining port size "on a cycle-by-cycle basis", "eliminating all data alignment restrictions"; and §1.3's virtual-machine model puts the governed operating system in **user** mode. *Fidelity note*: pp. 26 and 27 (Tables 1-1, 1-2) and pp. 22, 24 (Figures 1-2, 1-3) read as page images; pp. 23, 25, 28-30 as text with their figures identified; **p. 31 opened and genuinely blank**, a section-end filler, which is recorded because a blank page asserted rather than opened is the same error as a range called electrical |
| 148-151 | **§7 ON-CHIP CACHE MEMORY**, 7.1 Design and Operation, 7.1.1 Organization, 7.1.2 Cache Control, 7.1.2.1-7.1.2.5 CACR and its four bits, 7.2 Cache Address Register, 7.3 Cache Disable Input, 7.4 Cache Initialization | `confirms` **two model-table values — and finds a register this core would get wrong for three of its own models** | **Confirms** `ap_model.h` exactly: "a direct-mapped cache of **64 long word entries**" is its 256 bytes, and the NOTE "**Data accesses are not cached, regardless of their associated address space**" is its zero data cache. **Three facts the table does not carry**: the tag is the upper 24 address bits **plus `FC2`**, so one address in supervisor and user space occupies two entries; the index is `A2-A7` and `A1` selects the word within the entry; and "the processor always prefetches instructions externally with **long word, aligned bus cycles**, [so] both words of the entry will be updated, regardless of which word caused the miss". **And the register: the 68020's CACR is four bits** — `C` clear cache (3), `CE` clear entry (2), `F` freeze (1), `E` enable (0), with "the unused bits (including bits [31:8] which are not shown) ... always read as zeros". The 68030's has eleven. `ap_machine` builds an `ap_m68030_cpu_t` unconditionally, so a DN3000 or DN4000 in this core gets a **68030** control register — the same gap the model table's `.mmu` item names, now with a second register behind it. Also: `E` "is automatically cleared whenever the processor is reset"; `CE` uses CAAR's index bits 2-7, invalidates "regardless of whether or not it provides a hit", and is "independent of the state of the E or F bits, or the external Cache Disable (`CDIS`) pin"; `C` and `CE` both "always read as zero"; CAAR is 31-8 Cache Function Address, 7-2 Index, and "the MC68020 only uses this register for the clear entry (CE) function"; `CDIS` is synchronised, takes effect "on the first cache access after" recognition, and disables "independent of the enable bit"; and §7.4's reset clears every valid bit **and** the `E` and `F` bits |
