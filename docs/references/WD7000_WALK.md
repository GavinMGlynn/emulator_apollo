# `96-000494` Rev. X3 walk — coverage record

*WD7000-ASC Engineering Specification*, Western Digital, 96-000494 Rev. X3,
August 1988. **141 printed pages, 140 in the PDF.**
`docs/references/westernDigital/96-000494X3_WD7000-ASC_Engineering_Spec_Aug88.pdf`.

## Why this document is on the shelf at all

The DS5500's SCSI controller had no name and no address anywhere on the Apollo
shelf: `019411-A00`'s text layer has **zero** occurrences of "SCSI" and its
Figure 1-5 labels a block "Disk or SCSI/Disk Controller" with no part number;
`[GPIO]` Table 3-1 allocates no SCSI address; `019411-A00` Table 2-5 names none.

The machine's own software did. `tools/awd_read.py` extracted
`/install/ri.apollo.os.v.10.4/sau14/scsi14.drvr` off the DN3500 volume, and it
says **"Western Digital WD7000-ASC SCSI Host Adapter"** outright. Its reset
routine writes to `050002` and polls `050000` masked `F0` — ISA `200`, which is
where `[GPIO]` Table 3-1 puts the **Tape Controller** and where `AP_TAPE_ADDR`
already is. Detail in `docs/PROJECT_STATUS.md`, *The DS5500's SCSI controller is
a WD7000-ASC at ISA `200`*.

So this is the part's own manual for a controller this core does not model at
all, which makes it a document to be **derived in full** rather than queried.

## Method

- **Native resolution is 400 ppi**, `pdfimages -list` confirmed, **1-bit
  JBIG2**, with an Adobe Paper Capture OCR text layer of about 260,000
  characters. The OCR is readable for prose and **unusable for the tables**,
  which is most of what this document is: it turns `0` into `O`, `D0` into `DO`
  and `ICMB` into `lCMB`, and every register table here is made of exactly those
  characters. Pages carrying a table or a figure are read as **600-dpi images**.
- Printed page number and PDF page number differ by one: printed *p. n* is PDF
  page *n − 1* (the footer of PDF page 86 reads "87").
- The DS5500 is an **AT-bus** machine and the ASC is an AT card, so the host
  half of this document applies directly. The floppy controller on the same
  board does not: the DS5500's floppy is the OMTI's, at ISA `3F0`.

## READ WHOLE — 140 of 140 PDF pages. **Finished 2026-09-12. Nothing is owed.**

## Coverage

| PDF | Doc | Section | Yield | What it contained |
| --- | --- | --- | --- | --- |
| 1-7 | — | Cover, TOC | `none` | Contents, list of figures and tables |
| 8 | 9 | **§1 Purpose, §2 Reference Documents** | `context` | "an intelligent host bus adapter that interfaces a SCSI bus (multiple target or initiator) to a multi-user IBM AT system ... contains an onboard **Z80** CPU". Notation: an appended `-` means active low. Reference documents name the parts: **WD33C93 SBIC**, **WD37C65 floppy controller**, ANSI SCSI X3T9.2, the IBM AT Technical Reference, the Z80 manuals |
| 10 | 11 | **§3 General Description** | **the whole shape of the part** | AT to SCSI channel; 50-pin SCSI connector; two AT card edges. **Z80 LCPU and a 16-byte FIFO on the AT bus** sustaining 4.0 MB/s synchronous SCSI. **First-party DMA** carries command, data and status through an **outgoing and incoming mailbox** system. The **WD33C93 SBIC** does arbitration, selection, disconnection and reselection; usually initiator, can be target, and **selection as a target raises an unsolicited interrupt to the host if enabled**. An optional WD37C65 floppy controller and an optional BIOS ROM are on the same board — **neither is what the DS5500 uses**, whose floppy is the OMTI's at ISA `3F0` |
| 11 | 12 | **§3.1 ASC Features** | `figures` | 0-55 °C; 4.0 MB/s sync and **2 MB/s async**; first-party DMA; 16-byte FIFO; **any block size up to 16 MB**; even or odd DMA start address; programmable DMA bus on/off times; **up to 16 logical threads**; initiator or target; disconnect and reconnect; full SCSI command set; parity option; queued and linked commands; on-board diagnostics; IBM XT form factor; single +5 V |
| 12 | 13 | **§4 ASC Architecture, §4.1 LCPU** | `structure` | Nine blocks: LCPU, program ROM, scratchpad RAM, first-party DMA, host interface logic, SBIC, FDC, BIOS ROM, control support logic. A command written to the command port is read by the LCPU immediately, decoded, and either executed locally or queued for the SBIC; **the host is told within 70 µs whether the byte was accepted** |
| 13 | 14 | **Figure 4-1, WD7000-ASC Block Diagram** | **the data path, in one picture** | SBIC WD33C93(A) to a **16-byte FIFO** to **BDL1/BDL2** to **BDL3/BDL4** to SD7-SD0 and SD15-SD8 — two stages of bi-directional latch, which is what makes 2 bytes per 375 ns possible. FIFO control lines START, XENDEF, XLASTB, FIFEN, TOHOST, ARBFE, ODDADR out and FULL-, EMPTY-, LBYTE-, LWORD- back. First-party DMA block drives IOCHRDY, SBHE-, MEMR-, MEMW-, DRQ, DACK-, MASTER- and a **24-bit address counter A23-A0**. **16K × 8 ROM, Z80 LCPU, 2K × 8 SRAM**, control and support logic, host status, command port, IRQ vector to ICMB or diagnostic status, WD37C65, BIOS ROM 8K × 8 (optional) |
| 14 | 15 | **§4.1.1 LCPU Clock, §4.2 Program Memory, §4.3 Scratchpad RAM, §4.4 First Party DMA** | **the numbers a timing model needs** | **LCPU 4 MHz**, divided from the SBIC's 8 MHz; T cycle **250 ns**; machine cycles 4 to 23 T states. **8K × 8 PROM** with provision for 32K, the other 32K of the Z80's space reserved for data, split by A15. (Figure 4-1 says 16K × 8; the two disagree and neither is host-visible.) **2K × 8 SRAM**, enough for **16 logical threads**, and the SCAN mailbox command exceeds 16 without further host intervention. FPDMA: a word across the AT bus **every 375 ns**, word or byte mode, even or odd addresses, **24-bit counter, 16 MB reach**, bus on/off times host-programmable |
| 15-16 | 16-17 | **§4.4.1 DMA Transfer Detail** | **the DMA state machine, both directions** | SCSI→AT: LCPU loads the 24-bit counter, writes the FIFO Control Register (direction, out of reset, enable), **FCR bit 0 for an odd AT address, bit 1 to arbitrate only when the FIFO is full, bit 6 to drop bus mastership when the FIFO is empty**. SBIC REQ fills the FIFO; the SM writes BDL1 then BDL2, arbitrates when the Time Off Bus Counter (TOFBC) is at terminal count, asserts MASTER-, moves BDL1/2 into BDL3/4 and starts a memory write; masters until the Time On Bus Counter (TONBC) reaches terminal count. **One byte can be stranded** — an even byte count from an odd start, or an odd count from an even start — and the LCPU sees it in the FSR and sets **FCR bit 4** to flush it. AT→SCSI is the mirror image |
| 17 | 18 | **§4.5-§4.8 HIL, SBIC, FDC, BIOS ROM** | `structure` | The host interface is **16 data lines, a 24-bit address bus** and control, and holds the Command Register, Status Register, Interrupt Status Register, interrupt strobe, static reset and SCSI bus reset. The SBIC is **initiator when its status shows reselection and target when selection status is set**. BIOS ROM 8K × 8, address space configurable `C0XXX`-`DEXXX`, low 13 bits addressing the ROM |
| 18 | 19 | **§4.9 Control Support Logic, §4.9.1 Reset Logic, §4.9.2 I/O Address Space** | **three reset sources, and one asymmetry** | Reset comes from **power-up, the host, or the SCSI port**. **A SCSI-port reset resets the SBIC but not the LCPU**, deliberately, so the host command queue survives and queued commands restart without the host re-issuing them. After RESETDRV the host **must write a logic 1 to the onboard reset D-FF** before the ASC leaves reset and runs its diagnostics; a second D-FF resets the SCSI bus |
| 19-20 | 20-21 | **§5 Host Interface Protocol, §5.1 General Signal Flow** | **the command handshake** | Three levels: host ASC control, SCSI DMA access, interrupt generation. The host writes a control byte to the command port; **the strobe clears COMMAND PORT READY**, and further parameter bytes go in when it is set again, **about 70 µs apart**. The START I/O command byte carries a **mailbox number**, from which the ASC computes the mailbox address holding the command block pointer. **16 or 32 bytes of command block** are DMA'd into SRAM before decode. A **direction bit** the host sets on every read protects host memory from a runaway target. Commands to one LUN can be **linked** through the host CDB, with the interrupt programmable per command or per chain. In target mode each SCSI phase is a separate host command. **No more than 16 commands queued at once** |
| 21 | 22 | **§5.1.1 Power-Up Sequence** | **the exact byte sequence `scsi14.drvr` performs** | Minimum RESET pulse width **25.0 µs**; SCSI and ASC reset strobes may be asserted together, and if the response is wrong, **SCSI reset first, then ASC reset**. On-board diagnostics run for **about 2 seconds**. The status port then reads **4FH** because initialization has not begun, "the lower nibble should be masked off by all application drivers". The host reads the interrupt status **before** issuing any command, because no IRQ is raised for a power-up diagnostic failure; **01 means no diagnostic error**. Initialization is one command plus **9 parameter bytes**; a rejected byte may simply be resent; **5FH** from the status port means the channel is initialized. **This is the sequence the Apollo driver runs, and the four values it accepts — `40`, `50`, `4F`, `5F` — are exactly this page's and the next's** |
| 22 | 23 | **§5.2 Host Hardware Interface, Table 5-1, §5.2.1 Status Port** | **the host register set, and an address range the Apollo card is outside of** | Table 5-1 is Table A-1 again, identically. "Address lines **A9-A3** are jumper configurable so that the ASC can reside in **any address space from 300-3FF**" — which §7.4.1 states as A7-3 selectable with SA9-8 fixed high, and **neither range contains the Apollo card's ISA `200`**. Recorded, not reconciled: the Apollo board decodes outside what its own part's specification allows for, so it is a variant of this design rather than a stock WD7000-ASC. Status port: on reset the upper nibble clears and the lower nibble reads 1s (`0FH`) because **nothing drives the low nibble**, which is why every driver masks it — and why `scsi14.drvr` masks `F0`. **`40H` diagnostics complete, `50H` initialization complete, `70H` a rejected command byte** (p. 22 says `60H` for the same thing — the document contradicts itself, and the driver accepts neither) |
| 23 | 24 | **Table 5-2: ASC Status Byte Format, §5.2.1.1-2** | **the status byte, bit by bit** | D7 **Interrupt Image Flag**, 0 inactive / 1 active — the image of the hardwired interrupt, so a host sharing an IRQ can poll it; stays asserted until an ASC reset or until the host writes the port at address 1. D6 **Command Port Ready**, 0 busy / 1 ready; writing the command port resets it and the ASC sets it when ready for the next byte, **about 70 µs**, or 35 µs with a faster LCPU clock. D5 **Command Port Byte Rejected**, 0 accepted / 1 illegal command or parameter. D4 **ASC Initialized Flag**, 0 init required / 1 initialized. **D3-D0 reserved, set to 1, "mask off this bit"** |
| 24 | 25 | **§5.2.1.3-4, §5.2.2 Interrupt Status Byte** | **two rules a model must honour** | D5 also says an SCSI command entered the SBIC queue or a local command finished; **when the SBIC queue is full the command byte is always rejected**. On power-up, any command other than Initialization sets D5, and **any later initialization is rejected unless preceded by an ASC reset**. D4 is reset by host master reset (HMR or RESET DRV) or the ASC reset port and set by the LCPU when Initialization completes. The interrupt status register carries the **mailbox number** for the current interrupt, and doubles as the **diagnostic error code** because the two events are mutually exclusive |
| 25 | 26 | **Table 5-3: Interrupt Status Byte / Diagnostic Error Code** | **the code table** | `00` power-on, no diagnostics executed; `01` no diagnostic error; `02` RAM failed; `03` FIFO R/W failed; `04` SBIC register R/W failed; `05` initialization D-FF failed; `06` host IRQ D-FF failed; `07` ROM checksum error; `10NNNNNN` outgoing mailbox N free (only after "Interrupt on Free OGMB"); `11NNNNNN` incoming mailbox N needs service. Codes 00-07 valid once READY is asserted; **05-06 only at first power-up**. `scsi14.drvr` prints exactly these seven failures by name |
| 26 | 27 | **§5.2.3 Command Port, §5.2.4 Interrupt Acknowledge** | `behaviour` | The command port queues at any time, as against START MULTIPLE I/O's single scan; **queue full rejects the byte**. **A second command to a target/LUN whose previous command has not completed is terminated without being issued** — linking is the only way to stack commands on one LUN. The interrupt acknowledge strobe clears the hardware IRQ **and frees an ICMB**; the ASC will not raise the next interrupt until the previous one is acknowledged |
| 27 | 28 | **§5.2.5 Host Control Register, Table 5-4** | **the write side of the register `scsi14.drvr` drives** | D0 **ASC Reset**, D1 **SCSI Port Reset**, D2 **DMA Request Enable** (0 DRQ disabled), D3 **Interrupt Enable**, **D4-D7 reserved, not used**. "On power-up Reset or RESET DRV, all registers are cleared to all zeros", and a driver should mask the upper nibble. §5.2.5.1: the host writes **a 1 then a 0** so the reset pulse width is the host's to choose; **25 µs recommended**; after it the ASC must be re-initialized with the 10-byte sequence |
| 28 | 29 | **§5.2.5.2-4** | **the Apollo driver's exact bytes** | "**both the ASC reset and the SCSI hardware resets can be asserted simultaneously by writing a `03H` to this port followed by a `00H`**" — which is literally `scsi14.drvr`'s first two writes to `050002`. A SCSI reset raises no host interrupt unless the unsolicited mask is enabled, **except** when commands are in execution, when it always does, and the two cases carry different ICMB codes. D2 and D3 gate tri-state DRQ/IRQ drivers so several cards can share a channel, and **both must be enabled on the card before the corresponding system function**. The worked DMA sequence is `O 322 4` / `O D6 C2` / `O D4 2` — port base+2 with bit 2, then the system 8237 into cascade mode |
| 29-30 | 30-31 | **§5.3 Mail Boxes, Table 5-5** | **the whole host/controller handshake** | A mailbox is **4 bytes**: status byte then a **3-byte command-block pointer, MSB first**. `OGMB address = base + n*4`; `ICMB address = base + m*4 + 4*(number of OGMBs)` — all outgoing boxes first, then all incoming, **not in pairs**, and the counts need not match. **Up to 64 of each.** An OGMB status of zero means the ASC has taken the mail; non-zero means full. When every OGMB is full and commands are being rejected the host either polls or issues **Interrupt on Free OGMB**. An ICMB's first byte is the completion code and the next three the CDB address, `FFFFFF` when meaningless |
| 31 | 32 | **§5.3.2.1 ICMB Return Status, §5.4 Command Blocks** | `structure` | ICMB codes split at `7F`: **`00`-`7F` solicited**, in response to a host command; **`80`-`FF` unsolicited**, from an ASC-detected condition, maskable. Two block types: the **SCB**, always used when the ASC is initiator, and the **ICB**, a functional command for the ASC itself. **Multi-byte parameter fields are MSB first**, to match the SCSI CDB |
| 32-33 | 33-34 | **Table 5-6: SCSI Command Execution Block**, §5.4.1 | **the 32-byte SCB, field by field** | `00` S/I bit 7 + command opcode — **`00` means a SCSI command with the ASC as initiator**, `01`-`7F` reserved, `80`-`FF` ASC command codes. `01` Target ID in bits 7-5, two zero bits, **LUN** in bits 2-0. `02`-`13` the **12-byte SCSI CDB**, written straight to the SBIC. `14` **SCSI return status byte, unmodified by the ASC**. `15` **vendor unique error code** — not always a failure: `40H` means the target moved less data than byte 16-18 allowed. `16`-`18` **maximum data transfer length**, the runaway-target guard. `19`-`21` **SCSI data block pointer**, 3 bytes, 16 MB reach. `22`-`24` **next command link pointer**. `25` bit 7 **direction**, set when data is written to the host, so every SCSI read sets it and every write clears it. `26`-`31` reserved, to be zeroed |
| 34 | 35 | **Table 5-7: ICB Command Execution Block**, §5.4.2 | **the 16-byte ICB** | `00` S/I + opcode, `01`-`14` fourteen parameter bytes defined per command, `15` **ASC vendor unique error status** |
| 35 | 36 | **§5.5 Command Queuing, Fig. 5-1** | **four queues and their sizes** | **16 logical threads**; FIFO; **no more than one command per LUN** pending or in execution, never both. Four queues: **CPQ** 16 × 1-byte commands, **SBQ** 16 × 2-byte pointers, **CCQ** 16 × 2-byte pointers (completion order is random), **IRQ** 32 × 1-byte ICMB numbers. SCB storage is **16 × 32 bytes**, reached through CURSCB. Serviced round robin; the counters are CPQCNT, SBQCNT, SBQLFT and IRQCNT |
| 36 | 37 | **§5.5 continued** | **the arbitration rule between LCPU and DMA** | "If the bus is being used by the DMA channel, then command execution is suspended until freed by the DMA channel. By the same token, if the data bus is being used by the LCPU ... then DMA transfers cannot start until the LCPU has finished". **A new host command is not de-queued until the previous one has been added to the interrupt queue** with its ICMB and vue written |
| 37 | 38 | **§5.6 SCSI Protocol, §5.6.1 Messages, §5.7 Exception Handling** | **which messages exist** | ANSI SCSI, as the **WD33C93 data sheet** specifies. Most messages are handled by the LCPU transparently; ABORT and SCSI RESET are also reachable from the command port. Of the extended messages **only synchronous data transfer request** is supported; **EXTENDED IDENTIFY and MODIFY DATA POINTER are not**; the **link message set is**. Errors land in ICMB byte 0, SCB bytes 14/15, or ICB byte 15. **No standard timeout value is defined** — the host picks one per target |
| 38-39 | 39-40 | **§5.7.1 Error Reporting, §5.7.1.1-3** | **the error philosophy, and two numbers** | Every command with an OGMB gets a host interrupt. ICMB byte 0 is **`01` for a successful status-phase completion**, `02` when the command completed but something was anomalous. The SCSI status byte in SCB 14 is **never modified by the ASC**; anything but `00` forces ICMB `02`. **Parity error retries default to 2**, the ANSI recommendation, and are programmable. Unsolicited mask bits 4-0 produce interrupts `84`-`80`, and **a maskable interrupt is delivered anyway if a command is in execution** |
| 40 | 41 | **§5.7.3 Hardware Errors** | **the unrecoverable case** | An unexpected SBIC interrupt status terminates the command **with the target still connected** and no abort attempted, because the SBIC is lost; only a host reset of the ASC recovers. A SCSI reset during a command is reported through an ICMB with a valid CDB address; **with no command running and the mask enabled the address field is all `FF`s** |
| 41 | 42 | **§6 Commands** | `structure` | SCSI commands pass through; ASC commands are executed locally. **In INITIATOR mode the SCB carries the whole command; in TARGET mode the host drives the ASC through the phases one ICB at a time.** After power-up the host **must** issue Initialization even if a diagnostic error occurred, then enable the card's DMA and IRQ **before** the system's |
| 42 | 43 | **Table 6-1: ASC Command Port Opcodes**, §6.1.1-2 | **the command-port opcode map** | `00` No Operation — toggles status bit 6 only, a liveness check; `01` **Initialization, 10-byte sequence**; `02` Disable Unsolicited Interrupts; `03` Enable Unsolicited Interrupts; `04` Interrupt on Free OGMB; `05` **SCSI Soft Reset, 2-byte sequence**; `06` SCSI Hard Reset Acknowledge; `07`-`7F` reserved; **`10NNNNNN` Start Command in OGMB #N** (`80`-`BF`); **`11NNNNNN` Start Multiple I/O**, N a 6-bit scan signature (`C0`-`FF`) |
| 43-44 | 44-45 | **Table 6-2: Initialization Command Format**, §6.1.2-4 | **the ten bytes, and the DMA timing** | `00` opcode `01`; `01` **3-bit SCSI ASC ID**; `02` **Bus On Time**, `03` **Bus Off Time**, both **125 ns per count**; `04` reserved; `05`-`07` **starting address of the mail block, MSB first**; `08` number of OGMBs, `09` number of ICMBs, **max 64, and 0 or 1 both mean 1**. Changing any of it needs an ASC reset; at any other time the command is rejected. Bus-off zero means the DMA gives up the bus for only **125 ns**; bus-on should stay under **15 µs less all refresh overhead**; a word crosses every **375 ns**, so **3.0 µs moves the FIFO's 8 words**, and **arbitration overhead is 1-2 µs**. Byte 22 bit 0 of the parameter block is the same switch as opcodes `02`/`03` |
| 45-47 | 46-48 | **§6.1.5-9, Table 6-3: SCSI Soft Reset Format** | **the reset and start opcodes in full** | Soft reset is two bytes: `05`, then **Target ID in 7-5, a zero, `R.T.` in bit 3 — 0 an ABORT message, 1 a BUS DEVICE RESET message — and LUN in 2-0**; abort ends one command, reset ends every command to that target whatever the LUN, so the host may get more than one interrupt. Set parameter byte 22 bit 1 duplicates **Interrupt on Free OGMB**. **SCSI Hard Reset Acknowledge** notifies the host only when the SCSI queue has work, the completion queue is empty and every earlier interrupt is acknowledged; the ASC then sits in a **pseudo idle loop** accepting only reset/abort commands until a second `06` releases it. Start Multiple I/O scans the mailbox memory **16 bytes per poll from the base address**, so a command in a late OGMB costs more, and the scan itself raises an extra completion interrupt |
| 47 | 48 | **§6.2 ASC Commands (ICB)** | **the target-mode ICMB format** | Every ICB completion loads an ICMB and raises IRQ. For a command another initiator started, **ICMB byte 0 bit 7 is set**, values `80`-`87`; byte 0 is normally `81`, byte 1 the vue, byte 2 the packed SCSI ID + LUN. Target mode needs unsolicited interrupts enabled and the buffers opened ahead of time |
| 48 | 49 | **§6.2 continued** | `behaviour` | ICMB byte 3 is `FF` or reserved. Any exception halts a combination command at once and the host finishes it by hand with the component ICBs. **"Less data transferred" is a warning, not an error** — the scratchpad CDB says how many bytes actually moved |
| 49 | 50 | **Table 6-4: ICB Command Opcodes** | **the eighteen ASC commands** | `80` Open Inbound Data Buffer (init-to-init), `81` Receive Command from another Initiator, `82` Receive Data, `83` Receive Data with Status, `84` Send Data, `85` Send Data with Status, `86` Send Command Status, `87` Open Scratchpad CDB, `88` Read Initialization Bytes, `89` Read ASC SCSI ID, `8A` Set Execution Parameters, `8B` Read Execution Parameters, `8C` Read Firmware Revision Level, `8D` Execute Onboard Diagnostics, `8E` Prepare Sense Data, `8F` Prepare Inquiry Data, `90` Open Outbound Data Buffer, `91` Set Host Bus On/Off Times |
| 50-51 | 51-52 | **Table 6-5: Open Inbound Data Buffer (80)** | **the target-mode buffer** | `01` **LUN acceptance vector**, one bit per LUN — `81H` enables LUNs 7 and 0, `FFH` all; `02`-`04` buffer length MSB-first; `05`-`07` buffer start; `08` **bit 7 Open** (default closed) with the **Initiator ID** in the low bits; `15` vue. **Up to 8 buffers, one per SCSI ID**, the local host's slot a placeholder. The buffer **closes itself** once the SEND executes, and must be re-opened *before* the completion IRQ is acknowledged |
| 52 | 53 | **Table 6-6: Receive Command Block (81)** | `format` | Same shape with a **command** buffer: `01` SCSI Device ID + LUN, `02`-`04` length, `05`-`07` address, `15` vue. Issued only in response to being selected. **Command length 6 to 12 bytes**; 1 to 5 are accepted but are outside ANSI |
| 53-55 | 54-56 | **Tables 6-7 to 6-9: Receive Data (82), Receive Data with Status (83), Send Data (84)** | `formats` | All the same 16-byte shape — device ID + LUN, 3-byte length, 3-byte address, vue — differing only in opcode and in whether byte 14 carries a **SCSI status byte to be sent**. The ASC arbitrates, reselects, sends Identify, moves the data, then **Save Data Pointer + Disconnect**; with status it sends the completion byte and **Command Complete** instead. A nonrecoverable error means it disconnects **without** a Disconnect or Command Complete message, and the host learns of it only from the ICMB |
| 56-57 | 57-58 | **Tables 6-10, 6-11: Send Data with Status (85), Send Status (86)** | `formats` | Send Status carries only the device ID and byte 14's **completion status byte**; everything else is reserved |
| 58-59 | 59-60 | **Table 6-12: Open Scratchpad CDB (87)** | **the catch-all CDB buffer** | Bytes `01`-`04` are the **non-standard CDB lengths for SCSI groups 3, 4, 5 and 7**, each 6 to 12, which is how the ASC knows how many command-phase bytes to take for a group the standard does not define. `05`-`07` buffer address. The buffer is **fixed at 32 bytes, the same layout as an SCB**, and **only one can be open**. It receives any CDB the ASC cannot handle itself — not SEND/REQUEST SENSE/INQUIRY, or one of those three with no buffer prepared, or any of them with an exception. It is free again only when the host acknowledges the interrupt; **a second selection before that returns BUSY to the initiator** |
| 59-60 | 60-61 | **Tables 6-13, 6-14: Read Initialization Bytes (88), Read ASC Device Address (89)** | `formats` | `88` writes the stored initialization bytes back to host memory, **max length 9** (the opcode is not stored), zero writes nothing and over 9 is an error. `89` writes the ASC's **own SCSI ID into byte 01 bits 7-5** of the ICB |
| 61 | 62 | **Table 6-15: Set/Read Execution Parameters (8A/8B)** | **the parameter block's addressing** | `02`-`04` data transfer length, `05`-`07` buffer address, `08`-`09` **index relative to the parameter within the block**, so the host can change one parameter or a contiguous run without allocating the whole block. **Length 1 to 25 decimal (19 hex)** |
| 62 | 63 | **§6.2.11.1 Synchronous Rates (0-F)** | **the sync-rate table, and the default** | **Even bytes 0-E are the requested rate for targets 0-7**, upper nibble transfer period and lower nibble buffer offset, as the SBIC's register 11H defines; **odd bytes 1-F are the negotiated rate, written by the ASC and not to be modified**. Recommended value **45H**; **default `40H`, which is asynchronous**, for every device. Negotiation happens only in a selection phase, and is flagged by **bit 7 of the even byte** being set; clearing bit 7 forces re-negotiation |
| 63-65 | 64-66 | **§6.2.11.2-10 the rest of the parameter block** | **every remaining programmable parameter** | `10H` SBIC ID — reserved. `11H` **SBIC Control**: only bit 0 may be set, to enable **SCSI bus parity**; altering any other bit may make the ASC malfunction. `12H` **SCSI Timeout** = timeout(ms) × SBIC clock(MHz) ÷ **80**, rounded up; **default 25 (19H) = 250 ms**. `13H`, `14H` reserved. `15H` **Source ID**: bit 7 permits **target disconnection**, bit 6 permits **selection by another initiator**; **default 80H**. `16H` **User Flags**, default `00`: bit 2 returns the SBIC's residual count in **SCB bytes 16-18**, bit 1 is Interrupt-on-free-OGMB, **bit 0 is the master enable for unsolicited interrupts and must be set for target mode**. `17H` **Unsolicited Interrupt Mask**: bit 0 unexpected reselection, 1 unexpected selection, 2 abort message received, 3 bus device reset received, 4 bus reset with no commands, 5 unexpected request sense, 6-7 reserved; ICMB byte 0 comes back `80`-`87`. **Reset to zero on power-up.** An unexpected reselection is **always aborted** whatever the bit says; a SCSI hardware reset with commands outstanding **always** interrupts, vue `05H`, and with none outstanding gives `84H` only if the bit is set. `18H` **Parity Retries, default 2** |
| 66 | 67 | **§6.2.12 Read Execution Parameters (8B)** | `behaviour` | Read the block in the set command's own format, change what is wanted, and write it back, so defaults are not disturbed |
| 67 | 68 | **Table 6-16: Read Firmware Revision Level (8C)** | `format` | Byte 1 primary and byte 2 secondary revision, both written by the ASC |
| 68-71 | 69-72 | **Table 6-17 and §6.2.14.1-4: Execute Diagnostics (8D)** | **the diagnostics, and the power-up status sequence** | Type codes `00` short, `01` walking 1s, `02` DMA with a fixed pattern, `06` DMA with an incrementing pattern; in-house `90` execute from RAM, `A0` write ASC RAM, `C0` read it, each behind a keyword sequence in a source listing this project does not have. Bytes `08`-`09` ASC RAM address, `10` initial test pattern. **A power-up reset always runs the long walking-1s diagnostic and a warm reset the short one**, decided by a 2-byte key; **no interrupt is raised**, so the host polls for status `40` and may then read the interrupt status register, which **stays unchanged until the host's first command**. Short diagnostics complete in **under 250 ms** and run ROM checksum, RAM stack, SBIC registers, FIFO and RAM; walking 1s takes about **2 seconds**; **a failure leaves the green LED lit** and posts a code, success turns it off and posts `01`. Cold reset adds two tests the user cannot invoke: the **IRQ channel and the ASC-initialized D-FFs**. **`60` is posted for a rejected initialization byte** — stated here and on p. 22, against p. 23's `70H`, so `70H` is the outlier. After the 10 bytes the host waits for **`50`**. DMA diagnostics destroy the buffer they are given and it must be **no larger than 64 K** |
| 72-74 | 73-75 | **Tables 6-18, 6-19: Prepare Sense Data (8E), Prepare Inquiry Data (8F)** | `formats` | Sense: `01` **target LUN**, length, address, `08` bit 7 **Open** + **initiator ID**. It anticipates a REQUEST SENSE from that initiator; **if the next command is not one, the buffer is closed and the host is interrupted**. With no such command pending the ASC either interrupts (mask bit 5 set) or sends a **'no sense' block** by itself. **Up to 8, one per initiator ID.** Inquiry: `01` **LUN acceptance vector** and `08` **initiator ID acceptance vector**, so one command can answer for many LUN/ID combinations; **up to 8**, and **both vectors zero cancels them all** |
| 75-76 | 76-77 | **Table 6-20: Open Outbound Data Buffer (90)** | `format` | The initiator-to-initiator send path: the ASC accepts a **6-byte SCSI RECEIVE CDB** to any LUN in the vector and sends the host buffer. A CDB that is not RECEIVE goes to the scratchpad. **Up to 8 buffers**, and the host must re-open one **before** acknowledging the completion IRQ |
| 77 | 78 | **Table 6-21: Set DMA Bus On/Off Times (91)** | `format` | Bytes `01` and `02` override the initialization command's values **without re-initializing**, taking effect from the write of this command's ICMB. Issuing it mid-transfer splits a packet across the old and new values with no other effect |
| 78-83 | 79-84 | **§7 Interface Connectors, §7.1 and Tables 7-1, 7-2** | **the AT-bus pin list, and which lines this card does not use** | Four connectors: the 62-pin P1 and 36-pin P2 AT edges, a 50-pin SCSI header and a 34-pin floppy header. **At most 2 LSTTL loads per line.** Not used by the ASC: **BALE, SMEMR-/SMEMW-, IOCHRDY-, IOCHCHK-, 0WS, TC, CLK (6/8 MHz), OSC (14.31818 MHz)**; `REFRESH-`, about every 15 µs, **is** used — it is how the ASC surrenders the bus for host refresh, which is the same mechanism our core's refresh counter models on the other side. **MASTER- is asserted after a DRQ in cascade mode receives DACK-, and the card may hold the bus no more than 15 µs.** At least one clock before driving address and data, two before a memory strobe. **DRQ0-DRQ3 are 8-bit and are not used; DRQ5-DRQ7 are the jumper options**, so this card is a 16-bit DMA device. IRQ9-15 outrank IRQ3-7, and the line is **held high until acknowledged** |
| 84 | 85 | **Table 7-3: SCSI Interface Connector** | `pins` | 50-pin, single-ended, **cable no longer than 6.0 m**; all odd pins but 25 grounded; DB0-7 + DBP, TERMPWR on 26, and ATN-, BSY-, ACK-, RST-, MSG-, SEL-, C-/D, REQ-, I-/O on the evens. Initiator or target per the **WD33C93 data sheet** |
| 85 | 86 | **Table 7-4: Floppy Interface Connector** | **not this machine's floppy** | 34-pin, WD37C65. "The Floppy port is controlled **entirely by the host**. The ASC hosts the floppy interface hardware but does not control it in any fashion." The DS5500's floppy is the OMTI's at ISA `3F0`, so this half of the board is not in play here |
| 86 | 87 | **§7.4.1 ASC I/O Address Space (W3)** | **the address, and why `200` is possible** | "Address lines SA7-3 are selectable, SA9-8 are fixed at a high level ... The ASC is **currently selected to decode 320-323** (SA9-SA0), the LSB 3 bits are used to select onboard ASC registers." So the factory base is `320` and W3 moves it; the Apollo board is at `200`, which the driver's own constants give. **SA9-8 "fixed at a high level" is a claim about the stock board that the Apollo one contradicts** — recorded, not reconciled, and §7.4.1's own jumper table is on the page after |
| 87 | 88 | **Table 7-5: ASC I/O Address Space Jumpers (W3)** | **the whole jumper range — and the Apollo card is outside it** | Five jumpers SA7-SA3, **32 addresses, `300H` through `3F8H` on an 8-byte step**, default **`320H`** (starred). Every row has SA9 = SA8 = 1. **`200H` is not producible on this board**, and `scsi14.drvr` uses physical `050000`, which is ISA `200`. Recorded as measured, not reconciled — but it is the **second** time Apollo has done this: `[GPIO]` Table 3-1 puts the Winchester controller at ISA `1A0`, where a PC AT's is at `1F0`. Apollo re-addressing an ISA card outside its stock range is established practice on this machine, so the DS5500's board is a WD7000-ASC **design** with an Apollo address decoder rather than a stock card |
| 88 | 89 | **§7.4.2 BIOS ROM Address Space (W4)** | **not this machine's** | Eight 8 KB windows `C0000`-`DE000`, default `C8000`, and a jumper to disable the ROM entirely. The DS5500 boots from its own PROM, so this is inert here |
| 89-90 | 90-91 | **§7.4.3-8: DRQ/DACK, IRQ, terminator power and floppy jumpers** | **the channel and line the card can use** | W2 selects **DMA channel 5, 6 or 7, standard 6**. W1/W2 select **IRQ 3, 4, 5, 7, 9, 10, 11, 12, 14 or 15, standard 3** — and `008778-03` Table 2-3 gives Apollo's tape drive **IRQ5**, which is the line a SCSI card replacing it would want. W5 supplies terminator power to the SCSI bus, W7/W8/W6/W9 are floppy options |
| 91-93 | 92-94 | **§8.1-8.5.1 Specifications** | `figures` | 1.5 MB/s through the host adapter; **+5 V ± 5 %, 1.5 A typical, 2.0 A max**; 13.25 × 4.2 in, the IBM XT form factor; 0-55 °C operating; DC characteristics for the AT edge |
| 94-97 | 95-98 | **§8.5.2 AC Characteristics, Figures 8-1 to 8-4, Tables 8-2 to 8-5** | **every host-side timing number** | Programmed I/O / ROM read: t1 address setup **30 ns min**, t2 read active **250 ns min**, t3 address hold **50 ns**, t4 data access **220 ns max**, t5 data hold **10 ns min / 50 max**. I/O write: t6 **30 ns**, t7 **250 ns**, t8 **50 ns**, t9 data setup **150 ns**, t10 data valid **60 ns**, t11 data hold **50 ns**. DMA read: t12 DACK setup **125-200 ns**, t13 read active **250 ns**, t14 data valid setup **10 ns**, t15 data hold **0**. DMA write: t16 **125-200 ns**, t17 **250 ns**, t18 **20 ns max**, t19 hold **65 ns**. "The ASC DMA memory cycle is indicated by the **absence of any valid address lines**", AEN high, DACKX- starting the transfer |
| 98-99 | 99-100 | **Appendix A.1-A.2, Tables A-1, A-2** | **the host register set, twice over** | **Table A-1, the host register set**: `+0` read **ASC Status** D7-D0, write **Command Register** byte; `+1` read **Host Interrupt Status** byte, write **ASC Interrupt Acknowledge** (a strobe); `+2` read *reserved*, write **Host Control register** D3-D0; `+3` reserved both ways. This is what identifies `scsi14.drvr`'s reset routine, which writes `3, 0, 2, 0` to base+2 and polls base+0 masked `F0`. A-2 repeats Table 5-4 and adds the ordering rule: "**the ASC DMA port must be initialized first in order to drive the DRQ channel to the host**", and since the lines are tri-statable some device must drive them or the host's channels must be disabled while they float |
| 100-104 | 101-105 | **A.3 Host Command Protocol, Tables A-3 to A-7** | **the command tables restated** | A-3 the command-port opcodes, A-4 the initialization command with the DMA timing worked: **typical bus-on `40H` = 8.0 µs, practical maximum 12.0 µs; typical bus-off `0FH` = 1.875 µs**. A-5 the 32-byte SCB, A-6 the 16-byte ICB, A-7 the eighteen ICB opcodes. **A-4 labels the mail-block address bytes `05` LSB through `07` MSB, where Table 6-2 and Table A-8's own mailbox layout are MSB-first** — a contradiction inside one document, recorded rather than resolved |
| 105 | 106 | **A.3 continued, Table A-8: Mail Box Format** | **the mailbox arithmetic, and a number the body does not give** | `OGMB address = start + n*4`; `ICMB address = start + m*4 + 4*(total OGMBs)`; pointers MSB-first; up to 64 of each. And **"up to 32 IRQs can be queued internally to the ASC"**, which pins the IRQ queue the body only calls 32 deep in §5.5 |
| 106-107 | 107-108 | **Figures A-1, A-2: Execution and Completion of SCB/ICB** | **the whole loop in two pictures** | Startup: the host issues Single or Start Multiple I/O; the OGMB's pointer reaches the CDB; the CDB's data pointer reaches the buffer whose transfer length is the **maximum host allocation**. Completion: the ASC interrupts with an ICMB offset; the host processes it and **acknowledges only after preparing any response** |
| 108-110 | 109-111 | **A.4 Host Status Port (Table A-9), A.5 Interrupt Status Byte (Table A-10), A.6 Host Status Codes** | **the two status bytes, restated** | A-9 and A-10 repeat Tables 5-2 and 5-3 exactly. A.6: `00`-`7F` solicited, `80`+ unsolicited; the **SCSI hardware reset appears twice because it can arrive mid-command or idle**, and mid-command it is unmaskable; scan-mailbox and abort/reset each get their own ICMB code because either may leave commands unexecuted |
| 111 | 112 | **Table A-11: ICMB Return Status Values** | **the completion codes** | `00` reserved; `01` complete, no errors; `02` complete, error logged in SCB/ICB 14/15; `03` scan OGMB completed (1-3 = `FF`); `04` failed without SCSI status, see vue; `05` terminated, bus reset by an external device during an SCB; `06` SBIC or hardware failure, **requires a host reset** (1-3 = `FF`); `07` soft reset completed; `08`-`7F` reserved; `80` unexpected reselection, `81` unexpected selection, `82` abort message, `83` SCSI reset message, each with byte 1 = vue and byte 2 = ID+LUN; `84` hardware reset with no commands queued; `85`-`FF` reserved |
| 112-113 | 113-114 | **A.7 Vendor Unique Error Codes** | **all 46 of them, in three bands** | **Hardware (1-31)**: `00`-`07` as the diagnostic codes, plus **`18` DMA error during host system memory diagnostics**; `08`-`0F` unexpected SBIC response per phase (data out, data in, command out, status in, unspecified out, unspecified in, message out, message in), `10` unexpected SBIC interrupt status — **the target is left connected and only a host reset recovers**. **Illegal parameters (32-63)**: `20` command issued with the OGMB marked empty, `21` illegal parameter, `22` cannot execute now, `23` target data direction contrary to the one specified, `24`/`25` set/read index or count out of range, `26` a previous command is in progress for the same target+LUN. **SCSI protocol (64-88)**: `40` target sent less than the allocation length, `41` wants to send more, `42` wants to transfer with allocation zero, `44` tag aborted, `45` target received reset, `4D` selection/reselection timeout, `4E` no ID message or invalid source ID, `4F` no record of the SCB, `50`-`57` unrecoverable disconnect per phase, `FF` not applicable. **Target-mode only**: `80` hard reset during a solicited selection, `81` valid command bytes in the scratchpad, `82` no scratchpad opened, `83` illegal CDB length (<6 or >12), `84` no inbound buffer or wrong id/LUN, `85` no sense buffer for the LUN, `86` buffer opened but the next command was not REQUEST SENSE, `87` no inquiry buffer qualified by lav/iav, `88` no outbound buffer |
| 114-116 | 115-117 | **A.8 Set/Read Execution Parameter Data Block** | **every default, in decimal indices** | Bytes 0-15 the sync rates, **default `40` (asynchronous)**, recommended `45H`; 16 reserved; **17 (`0C`) SBIC control**, only bit 0 host-settable; **18 (`19`) timeout, about 250 ms**; 19, 20 reserved; **21 (`80`) source ID**, bit 7 enable reselection so the target can disconnect, bit 6 enable selection for multiple initiators; **22 (`00`) user flags**, bit 2 return bytes transferred in SCB 16-18 (**the maximum transfer count is then lost**), bit 1 interrupt on free OGMB, bit 0 the unsolicited master enable; **23 (`00`) unsolicited mask**, bits 0-4 → interrupts `80`-`84`, bit 5 unexpected REQUEST SENSE (ICMB0 `81`, vue `85`); **24 (`02`) parity retries**, not to be altered by a host driver; **25 (`00`) reserved for in-house DMA control**. The §6.2.11 indices are hex and these are decimal, and **they agree** |
| 116-117 | 117-118 | **A.9 I/O Address Space, Tables A-12, A-13** | **the ASC's internal map, not the host's** | **Table A-12, the 8085-side addresses**: `80` SBIC Address register R/W, `81` SBIC Task File registers R/W, `40` Time On Bus (**125 ns per count**) W, `41` Time Off Bus W, `42`/`43`/`44` DMA Address high/mid/low W, `45` FIFO Control (D7 Start FIFO Transfer, D6 End if Empty/Full, D5 spare, D4 Transfer Last Byte Left in FIFO, D3 FIFO Reset- written 0 then 1, D2 Set FIFO Direction 1 = ASC to AT, D1 Start Arbitration when Empty/Full, D0 Transfer On an Odd Address), `46` Read or Write FIFO Data Byte R/W, `47` spare. A-13, the rest: writes `20` host IRQ register D0, `21` host interrupt status byte, `22` set host command port ready (strobe), `23` set host status bits — **D5 command rejected, D4 ASC initialized**, `26` SBIC reset (write 1 then 0), **`27` diagnostic LED, 1 = test failed**; reads `20` host command register, `21` **ASC status: D7 FIFO Full-, D6 FIFO Empty-, D5 Last Byte Left-, D4 Last Word Left-, D3 host command port not ready, D2 SBIC INTRQ, D1 ASC channel initialized, D0 host IRQ**. **Not host-visible**: an emulation reproduces the behaviour these produce |
| 118-127 | 119-128 | **Appendix B, Figures B-1 to B-9** | **nine flowcharts, and the host sequence in one picture** | B-1 host initialization: jumpers, **control reg `03` then `00` with ≥ 25 µs between**, poll status masked to **`40`/`00`**, send the init bytes, **wait for `50`/`5F`**, set ASC DMA/IRQ control to **`04`/`0C`**, then enable the system's DMA channel and IRQ, then SCB/ICB and Single/Scan. B-2 ASC reset: LED on, 16-bit ROM checksum, RAM stack, SBIC/FIFO reset, read the RAM power-up key, walking 1s on a cold start, LED off, Z80 defaults, mailboxes and bus times, host ID, SBIC registers. B-3 the ASC idle loop: host command, host interrupt pending, new SCSI command, SCSI command done, host soft reset. B-4 host command interrupt: **a Z80 NMI**, valid/immediate/queue-full, port status `50`/`5F` on success and **`D0`/`F0`/`70` on rejection**. B-5 execution, B-6 initiator mode, B-7 completion, B-8 host IRQ service, B-9 soft reset |
| 127-139 | 128-140 | **Appendix C, Programming Notes, Listings 1-4** | **the host-side rules that bind our own 8237 model** | First-party DMA: the AT's 8237 does **arbitration only**; the channel must be put in **cascade mode** — mode register bits 7 and 6 set with the channel in bits 1-0, everything else zero — and the channel mask cleared. **DRQn is high-active and the ASC tri-states it whenever Host Control bit 2 is clear**, so the 8237 channel must be masked *before* enabling or disabling the card's DMA "or the system is likely to hang"; the same for IRQ and the 8259 OCW1. Listing 1 gives the order exactly: enable = set control bit 2, write mode `C2` (cascade, channel 2 of the second chip = **channel 6**), clear the mask; disable = set the mask first, then clear the control bit. Listing 2-4 are DOS-specific vector plumbing and an interrupt handler that reads the interrupt status, switches on the ICMB status byte, then writes the acknowledge port and an 8259 EOI |
| 140 | — | **Cover memo, Chandru Sippy, 8-12-88** | **the errata for this very revision** | "The WD7000-ASC Engineering Specification has been updated to include all of the target mode commands. **As of the present writing the following operations do not function**: 1. Target Synchronous mode of operation, 2. **Receive Command (81)**, 3. **Set Bus On/Off times (91)**, 4. **Open Outbound Data Buffer (90)**", with 1 and 2 expected around 9-2-88. So three of the eighteen ICB commands and target synchronous transfer were **unimplemented firmware** when this document was printed — which a model of a 1991 Apollo board should treat as a question about which firmware revision it carries, not as settled |

## What the walk yielded

**The part is fully specified and nothing of it is implemented.** This core
models no SCSI at all, so every row above is a gap; what the walk buys is that
the gap is now *bounded* — a WD7000-ASC model is four host registers, a
mailbox protocol, two command sets (10 command-port opcodes and 18 ICB
opcodes), a 32-byte SCB, a 46-code error vocabulary, and a first-party DMA
engine, all of them written down here with their defaults.

**Five things this walk found that a targeted read would not have.**

1. **The Apollo card decodes an address its own part cannot be jumpered to.**
   Table 7-5 lists all 32 W3 settings and they run `300H` to `3F8H`; the DS5500's
   ASC is at ISA `200`, physical `050000`, which `scsi14.drvr` uses and which is
   where `[GPIO]` Table 3-1 puts the **Tape Controller**. Apollo did the same
   thing to the Winchester controller — ISA `1A0` where a PC AT's is `1F0` — so
   this is a pattern on this machine and not an anomaly, and it is the mechanism
   behind `[RN104]` §3.3.6's ban on carrying a non-SCSI ctape drive with SCSI.
2. **The driver's reset routine is this document's §5.2.5.2 verbatim.** "Both
   the ASC reset and the SCSI hardware resets can be asserted simultaneously by
   writing a `03H` to this port followed by a `00H`" — `scsi14.drvr` writes
   exactly that to `050002`, waits, then polls `050000` masked `F0` for the four
   values Figures B-1 and §5.1.1 name (`40`, `50`, `4F`, `5F`). Two independent
   artefacts agreeing to the byte is what makes the identification safe.
3. **First-party DMA binds our own 8237.** Appendix C: the AT's 8237 does
   arbitration only; the channel goes into **cascade mode** and the mask is
   cleared, and because the ASC tri-states DRQn whenever Host Control bit 2 is
   clear, the 8237 channel must be masked *before* the card's DMA is enabled or
   disabled "or the system is likely to hang". `dma_suite` already models the
   8237; a SCSI model would drive it through exactly this path.
4. **The last page is errata for this revision.** A cover memo of 8-12-88 names
   four things that **do not function**: target synchronous mode, Receive
   Command (`81`), Set Bus On/Off Times (`91`) and Open Outbound Data Buffer
   (`90`). A model of a 1991 Apollo board has to decide which firmware revision
   it carries; without this page the question would never have been asked.
5. **The document contradicts itself once, not twice.** Table A-4 labels the
   mail-block address `05` LSB through `07` MSB where Table 6-2 and Table A-8's
   own mailbox layout are MSB-first; three against one, and a little-endian
   field in a structure whose every other pointer is MSB-first would be the
   only one, so this part takes MSB-first and says so.

   > **CORRECTED 2026-09-12, while implementing it.** This entry said the
   > document contradicts itself *twice*, the second being that a rejected
   > command byte is `60H` on pp. 22 and 70 and `70H` on p. 23. **It is not a
   > contradiction.** `60` is `READY | REJECTED` and `70` is
   > `READY | REJECTED | INITIALIZED`, and both pages that say `60` are
   > describing a rejected *initialization* byte, which by definition arrives
   > before that flag is set. The bit definitions produce both values with no
   > special case, which `wd7000_suite`'s
   > `test_sixty_and_seventy_are_the_same_rejection` asserts on the model.
   > *Original text, kept because a walk that reported a contradiction and a
   > later reading that dissolved it are two different claims and the record
   > should hold both:* "**The document contradicts itself twice, and both are
   > recorded.** A rejected command byte is `60H` on pp. 22 and 70 and `70H` on
   > p. 23; and Table A-4 labels the mail-block address `05` LSB through `07`
   > MSB where Table 6-2 and Table A-8's own mailbox layout are MSB-first."

**Where the part's own manual stops.** The SBIC's registers are not here —
"refer to the WD33C93 data sheet" is said three times, for the synchronous-rate
encoding, the SBIC control register and the timeout register — so a model that
implements the sync-rate negotiation faithfully needs that datasheet too, which
is **not** on this shelf. It is named as the next document to fetch rather than
as a blocker: the ASC's firmware does the negotiating, and the host sees only
the parameter block bytes this document does define.
