# `002398-04` walked whole — coverage record

*Domain Engineering Handbook*, Rev 4, January 1987. **330 PDF pages.**
Marked `APOLLO CONFIDENTIAL. INTERNAL USE ONLY.` on every page.

## Why this file exists

`CLAUDE.md`: *a document that turns out to contain one unimplemented thing is a
document that must be read WHOLE — page by page, line by line.* This one has
triggered it repeatedly and was never derived in full. It has been *consulted*
at least five times and yielded something every time:

- p. 12-2 gave the keyboard beeper its command sequence and its 300 ms auto-off,
  which `ap_kbd.h` records as having "stopped being a decline when the handbook
  turned up".
- p. 12-23 and p. 12-25 gave the DMA channel usage and the DMA page register's
  offset-to-channel map — "the handbook that prints the table `008778-03`
  Table 2-6 omits" (`ap_dmapage.c`).
- p. 12-28 gave the DN3000's interrupt request line assignments, which settled
  the ring's `IRQ2` against a three-way disagreement (`ap_board.h`).
- its `CRASH_STATUS` list gave *supervisor fault while resource lock(s) set*
  for `00120020`.

Five consultations, five facts. **A document consulted about one question was
never consulted about the rest** — the same sentence that opened the
`008778-03` walk, which is now finished at 209 of 209 pages and whose record is
`008778-03_WALK.md`.

## How to read this record

Every page range gets a row, including the ones that yield nothing: a section
recorded as *no emulator content* is a finished section, and that is what makes
a **walked** document distinguishable from a **sampled** one later. Pages are
read as **images**, never as a text layer.

Status values: `derived` (everything in it is in code or in a named gap),
`confirms` (agrees with code already, nothing to change), `none` (no emulator
content), `GAP` (something is missing from code and is named).

**Debts are recorded in the Resume block, not left implicit.** The `008778-03`
walk incurred two — both from reading a later page out of order to establish a
section's extent — and both were paid, *because they were written down*.

## Resume here

**Next unread page: 269.** Nothing is owed. The Low-Profile Model II charts want a transcription pass against `ap_kbd` rather than a skim; see the plan item. Chapter 4's out-of-order debt is **paid**: 69–73 are now contiguous. Owed: PDF 74–93. — chapter 1 is **read end to end** (PDF 12–34);
**chapter 2 is read end to end** (PDF 35–58); **chapter 3 is read end to end** (PDF 59–68); **chapters 4 and 5 are READ END TO END** (PDF 69–93 and 94–116); **chapters 4, 5 and 6 are READ END TO END** (PDF 69–93, 94–116, 117–138); **chapter 7 is READ END TO END, PDF 139–177**; **chapter 8 is READ END TO END, PDF 178–227**; **chapter 9 is READ END TO END, PDF 228–241**; chapter 10 started, PDF 242–268 read.

**CHAPTER 12 IS READ END TO END: PDF 279–313, all 35 pages.** Front matter read
(5–11). Chapters 1–11 and the appendices are unread and are *not* a debt — see
the reading order below; they are scheduled, not skipped.

**35 content pages of 330.**

**Owed: nothing. Chapter 12's three delegated sections are read**, out of order
and recorded as such. They are chapter 12's content held in other chapters, and
all three **confirm** this core:

| Delegated from | To | Read at | Result |
| --- | --- | --- | --- |
| p. 12-25 | chapter 7 | PDF 153–155 (7-15 – 7-17) | `confirms` the 68030 frames |
| p. 12-26 | chapter 7 | PDF 157–158 (7-19 – 7-20) | `confirms` the 68881 FPCR |
| p. 12-35 | chapter 9 → chapter 7 | PDF 241, 170 (9-14, 7-32) | `confirms` the DUART; the `dtr_b` bit is **defined nowhere** |

The rest of chapters 7 and 9 stays scheduled in its normal place; only the three
delegated sections were read early, and the rows below say which pages. The
timers are delegated too — p. 12-35's "same as all previous systems" — with no
chapter named, so there is nothing to follow.

**Chapter 1 owes nothing.** All twenty-three pages, PDF 12–34.

## The reading order, and why it is not front-to-back

`008778-03` was walked in page order because every chapter described the machine
this core models. This handbook does not: **chapters 7 through 11 are other
machine families** — DN300/320/330, DN400/420/600, DN460/660/DSP160, DN5xx,
DSP80/80A/90 — and only **chapter 12 is the DN3000**, which is the family
`src/core/model/` is built around.

So this walk takes **chapter 12 first**, then chapters 1–6 (which are
machine-independent: AEGIS structures, the file system, error codes, debugging,
peripheral I/O), then 7–11 and the appendices. Every page is still read; only
the order changes, and it changes so that the pages most likely to hold a
missing fact are reached first.

That decision is recorded here rather than left to look like an omission, and
the Resume block above is the authority on what has actually been covered.

## Structure

| Chapter | Subject | Handbook pages | PDF pages |
| --- | --- | --- | --- |
| front | title, conventions, contents | i–xii | 1–11 **(read)** |
| 1 | AEGIS — OS structures, ring packet format, trap codes | 1-1 – 1-23 | ? |
| 2 | File system — ACLs, BAT, directories, **volume label**, VTOC | 2-1 – 2-24 | ? |
| 3 | Programming information — address space, stack frame, status word | 3-1 – 3-10 | ? |
| 4 | Error codes — **boot PROM diagnostics per family**, MD, sysboot | 4-1 – 4-25 | ? |
| 5 | System debugging — DB, MD, **crash analysis**, system dumps | 5-1 – 5-17 | ? |
| 6 | Peripheral I/O — device addresses, **keyboard charts**, magtape | 6-1 – 6-20 | ? |
| 7 | DN300, DN320, DN330 | 7-1 – 7-32 | ? |
| 8 | DN400, DN420, DN600 | 8-1 – 8-50 | ? |
| 9 | DN460, DN660, DSP160 | 9-1 – 9-14 | ? |
| 10 | DN5xx | 10-1 – 10-26 | ? |
| 11 | DSP80, DSP80A, DSP90 | 11-1 – 11-9 | ? |
| **12** | **DN3000** — the modelled family | 12-1 – 12-35 | **279–313** |
| A | ASCII character set | A-1 | ? |
| B | Powers of two | B-1 | ? |
| — | Index | I-1 | ? |

**Both navigation aids are to be checked against the document**, which is the
last lesson `008778-03` taught: its front contents omitted chapter 15 and its
back index omitted chapter 16, each missing a chapter the other had. The table
above is from the contents (PDF 6–11); the index has not yet been read.

## What chapter 12 is expected to carry

Recorded before reading, so that "we already had that" cannot be decided after
the fact. Its contents list names, for the **DN3000**: address space by physical
address, beeper, calendar, **cartridge tape** with six named registers (data,
command, control, status, DMA go, DMA reset), configuration, **CPU control/
status register**, **disk (floppy/Winchester) controller**, display colour and
monochrome, fault frames and fault types, fault vectors, floating-point
registers, **parity error register**, **programmable interrupt controller**,
**ring**, **SIO**, and **timers**.

Every one of those has a module in `src/core`. This is a register-level chapter
for exactly the machine this core models, and it has never been read whole.

## Coverage

| Pages | Section | Status | What it yielded |
| --- | --- | --- | --- |
| 5 | Conventions | `none` (recorded) | The symbol table the rest of the document is written in: `+00` is "offset, in hexadecimal", `-` an unused bit, `<pa \| va>` a physical or virtual address, `set of 0..31` is 32 bits, `1..32` is "32 values; 5 bits". Worth having before any register table is read, since a misread convention is a misread table |
| 6–11 | Table of contents | `none` | Transcribed into the structure table above |
| 262, 278 | probes for chapter 12's start | `none` (recorded) | Read to map handbook pages to PDF pages, which the contents does not give. 262 is 10-21 and 278 is 11-9, so chapter 12 begins at **PDF 279**. Recorded rather than left implicit, per the rule that a page read out of order is either recorded or owed. 278 also carries the DSP80's **PROM ENTRY POINTS** table -- `getc`, `putc`, `init_dsk`, `read_dsk`, `reload_font`, `pollc`, `quiet_ret`, `write_disk`, `log_error`, `crash`, `led_update` at `$100`-`$12C` -- and chapter 12 has its own, which is where to compare against the DN3500 boot PROM |
| 279 | §12 **ADDRESS SPACE BY PHYSICAL ADDRESS** | **SETTLES A QUESTION `008778-03` LEFT OPEN THREE TIMES** + `confirms` **(the whole core board)** | Three columns -- physical, virtual, **I/O Bus** -- which is what `008778-03` Table 2-7 has and gets wrong in one row. **`50000 tape ... 200`.** The `008778-03` walk hit that arithmetic three times: Table 2-7 names ISA `218` beside physical `050000`, Table 8-1's prose repeats "Device Address 218", and Figure 15-5 shows the *SPE board's alternate* jumpering claiming `218`-`21F`. `218` maps to `050C00` and `200` maps to `050000`, so the physical column and the ISA column could not both be right. **The handbook says `200`**, agreeing with the physical column, with our measured `AP_TAPE_ADDR`, and with the oracle tap. Recorded in `board/ap_tape.h`; the constant does not change, but it no longer rests on preferring one column of a self-contradicting table. **Confirms, in one table**: `4D000 win`/`1A0`, `58000 ethernet brd1`/`300`, `5D800 Mono`/`3B0`, `5F800 flp`/`3F0`, and the core-board block `8000 mmu/cpu`, **`8400 sios` — one row, the DS3000's single 2681** — `8800 timers`, `8900 calendar`, `9000`/`9100` DMA, `9200` DMA page register, `9300` parity, `9400`/`9500` the two PICs, plus `20000 PMMU` and `22000 68881 FP` in CPU space and the DMMU's RP/TB/TB-flush/status at `0`/`4000000`/`8000000`/`C000000`. **Two things to follow up**: the table lists **eight ring pages** (`51000`-`51C00` at ISA `220`-`238` and `59000`-`59C00` at `320`-`338`), which is a layout `RING.md` should be checked against; and it puts **ethernet board 2 at ISA `308`** where `008778-03` Figure 14-4 jumpers the alternate card to `310`. Neither is resolved here |
| 280 | §12 address space (cont.), **BEEPER** | `confirms` **+ names the mapping's granularity** | The tail of the table -- `5FC00 sio2`, `A0000 dsp_mem color`, `FA0000 dsp_mem mono`, `100000-8FFFFF main mem`, `900000->FFFFFF` AT-compatible memory space -- and then the formula: "**Every 8 bytes** of the bus i/o space (0-3FF) is mapped to **one page** of DN3000 space. Formula ... `(AT_addr/8) * $400 + $40000`". That is arithmetically identical to `ETHERNET.md`'s measured `physical = 0x040000 + (ISA << 7)`, since `(a/8) * 0x400` is `a << 7` -- so the measurement and the handbook are the same rule. What the handbook adds is the **granularity**: the unit is an 8-byte ISA block occupying a 1-KB page, which is the shape `ap_tape.h`'s measured "four registers at stride 1 ... aliased through the 256-byte range" is a sample of. **BEEPER** is the passage `ap_kbd.h` already quotes -- `$FF $21 $81 $00` on, automatic off after 300 ms, `$FF $21 $82 $00` off -- re-read in place |

| 281 | §12 **CALENDAR** `[8900 | 3FFAD00]` | `confirms` **(the whole page, offset for offset)** | The MC146818's register file and the 50 bytes of battery-backed RAM behind it: `00`–`09` the time and alarm registers with their ranges, `0A` command, `0B` status, `0C`/`0D` unused, then the configuration table — `0E-11` CHECKSUM, `12-15` VALID PATTERN, `16-1D` MEM BOARD ARRAY, `1E-21` NODEID, `22-25` DEV BIT ARRAY, `26` RING TYPE, `27` DISP TYPE, `28` DISK TYPE, `29-3F` unused — and the DEV BIT ARRAY's eight bits: `0 = flp, 1 = ctape, 2 = win, 3 = fpu, 4 = ring, 5 = user device, 6 = ethernet, 7 = serial/parallel board`. **Every one of those is already in `board/ap_calendar.h`**, which quotes this same layout in its header and defines each offset and each device bit. Nothing to change. Recorded as a `confirms` rather than skipped, because that is the difference between *establishing* that a module is complete against its source and *assuming* it: this page is what the calendar's configuration table was derived from, and the walk is what proves the derivation was total rather than partial |

| 282 | §12 calendar **Command**/**Status** registers, **CARTRIDGE TAPE** `[50000 | 3FF9C00]`, **Tape Status Byte 0** | `confirms` **(both parts)** + **one naming trap** | The calendar's two control registers, bit 7 each: `0A` "Command Register" carries **UIP**, "1 => update in progress"; `0B` "Status Register" carries **SET**, "1 => abort current update, allow initialization". Both are in `device/ap_mc146818.h` as `AP_MC146818_A_UIP` and `AP_MC146818_B_SET` at `0x80`. **The trap is the naming**: the handbook calls `0A` *Command* and `0B` *Status*, where the part's own datasheet calls them Register A and Register B — and it is A that holds UIP and B that holds SET, so the bit positions agree and only the labels differ. A reader moving between the two documents should not read "Status Register" as the part's Register B. The page also cites `/os/ker/os_cal_asm.asm` as where these are defined, which is an AEGIS source path and not something this core can reach. Then **CARTRIDGE TAPE at `50000`** — the address settled two rows above — and its **Data Register (read)**: "Drive status information is returned as a series of **6 bytes** through the data register after a read status command is sent", which is `AP_QIC_STATUS_BYTES 6`. **Tape Status Byte 0** in full: bit 7 zero to mark the byte, then `noc` cartridge not in place, `usd` unselected drive, `wp` write protected, `eom` end of media, `ude` unrecoverable data error, `bnl` bad block not located, `fm` filemark detected. All eight are in `device/ap_qic.h` at the same positions -- `AP_QIC_EXS_BYTE_0` through `AP_QIC_EXS_FILE_MARK` -- and byte 1's bits are there too, so this page confirms a layout the module already carries whole |

| 283 | §12 **Tape Status Byte 1**, the **STATUS SUMMARY**, bytes 2–5, **Command Register (write)** | `confirms` **(and independently recovers two opcodes)** + **one table to check against** | Status byte 1's eight bits — `ill` illegal command, `nod` no data detected, `mgn` marginal block, `bom` beginning of media, `bpe` reserved for bus parity error, `erm` reserved for end of recorded media, `por` power on/reset — all in `device/ap_qic.h` at the same positions. Bytes **2–5** are named: high and low bytes of the **data error counter** and of the **underrun counter**, which is what `ap_qic`'s `data_errors` and `underruns` are, now confirmed as 16-bit counters split across two bytes each. **The Command Register decomposes the opcodes**: bits 7–5 are a command *type* and bits 4–0 the data — `000` Select, `001` Position, `010` Write Data, `011` Write File Mark, `100` Read Data, `101` Read File Mark, `110` Read Status, `111` reserved. Every opcode in `ap_qic.h` falls out of that: `40`/`60`/`80`/`A0`/`C0` are the five types with zero data, `01` is Select with `MMMMM = 00001`, and `21`/`22`/`24`/`26`/`27` are Position with `00MMM` = 1, 2, 4, 6, 7. **That independently recovers the two opcodes `[SC499]`'s summary table had struck through with a previous owner's pen** — `22` ERASE and `26` SELECT Q11 — which `ap_qic.h` had restored from §1.13.1's numbered descriptions and defended by the series they sit in. A second document, in a different form, generates the same series. **One table recorded and not yet acted on**: a fifteen-row **STATUS SUMMARY** giving which *pairs* of status bytes mean which condition — "no cartridge", "no drive", "read abort", "read error, no data and EOM", and so on. This core sets status bits individually, and the summary is the check on whether it can ever emit a pair the controller would not. Its "Status 0" column also prints patterns with bit 7 set, where byte 0's marker bit is zero, so the column's convention needs establishing before it is used |

| 284 | §12 tape **Control**, **Status**, **DMA Go** and **DMA Reset** registers | `confirms` **— and it is the polarity confirmation, from Apollo's side** | Four registers, and this page independently states the fact `device/ap_sc499.h` calls the thing "this suite used to get wrong". **Control (write only) `[50001]`**: bit 7 `rst` "Reset controller microprocessor", 6 `req` "Request to LSI chip", 5 `ien` "Enables interrupts", 4 `dni` "Enables DONE int, dni = 0 masks DONE int", 3–0 unused — `AP_SC499_CTL_RESET`/`REQUEST`/`IEN`/`DNIEN` at `80`/`40`/`20`/`10` exactly. **Status (read only) `[50001]`**: bit 7 `irq` **"0 =>"**, 6 `rdy` **"0 =>"**, 5 `exc` **"0 =>"**, 4 `don` **"1 =>"**, 3 `dir` **"1 =>"**, 2–0 unused — which is `AP_SC499_ST_*` at `80`/`40`/`20`/`10`/`08` with `ST_UNUSED 0x07`, and **the three active-low bits are exactly the three this core marks active low**. `[SC499]`'s own page prints that polarity in a column its text layer drops, and reading `40` as "ready asserted" was the misreading this module was corrected for; here is a second document, from the machine's vendor rather than the controller's, printing the same polarity as an explicit `0 =>`. **And the interrupt flag is stated as a disjunction with its gating**: "Interrupt request flag, 'or' of rdy and exc, and done **if dni is set**" — which is `ap_sc499_irq`'s list *and* its DONE-gated-on-DNIEN term, in one sentence. **DMA Go `[50002]`** and **DMA Reset `[50003]`**: "Any write to this register will cause DMAGO / RSTDMA to be active", which is why both are modelled as functions that ignore what is written. Cites `/os/ker/ct.pas` |

| 285 | §12 **CONFIGURATION**, the DN3000 block diagram | `confirms` **(structure, no registers)** | An ASCII block diagram of "the major components of the DN3000 cpu and memory system, AT-compatible bus, and peripherals". **CPU**, **MMU** and **FP** on one side; **SIO**, **PROM**, **TIMER**, **CALENDAR**, **DMA** and **RAM** hanging off the **DN3000 bus**; then a **Bus Interface** block to the **AT-compatible Bus**, carrying **Winch**, **Floppy**, **Graphics**, **Network** and **Ct Tape**. That is the dual-bus arrangement `008778-03` §1.1 and §1.2 describe and `board/ap_board.c` models — the DN3000 bus for the core-board devices, the AT bus behind an interface for the cards — drawn rather than stated. No register appears on it, so nothing to derive; recorded because a `confirms` row is what distinguishes a page that was read and agreed from one nobody opened |

| 286 | §12 **CPU (MMU) CONTROL/STATUS REGISTER** `[8000 | 03FFB400]` / `[8100 | 03FFB500]` | **THREE GAPS NAMED** + `confirms` **(the parity field and the acknowledge)** | The register `board/ap_boardreg.h` models, given as two bit maps. **Status (read)**: `mto` mem space timeout, `uto` i/o bus, `dto` i/o bus DMA timeout, `pdm` parity during DMA, `pio` IO parity error on an i/o bus reference, `cto` on-board CPU timeout "(ref to non-existent mem)", **`pe3`–`pe0` "1 => parity error (failing byte)"**, `ip` int pending, `fp` fp owner trap, `iot` i/o bus i/o cycle timeout, `nm` normal mode. The four `pe` bits are `AP_BOARDREG_STATUS_PARITY_MASK 0x00F0` exactly, and **"To clear bus timeouts or parity conditions from status register, write to the status register. This register is readonly"** is `ap_boardreg`'s write-acknowledges rule, stated. **Control (write)**: `ld7`–`ld0` in the upper byte, `pe3`–`pe0` "force parity err (bytes 3-0)" at 7–4 -- `AP_BOARDREG_CONTROL_PARITY_LANE_MASK` -- then `dg` enable diag mode, `fp` enable fp owner trap, `rsa` reset on-board devices, `nme` non-maskable interrupt enable at bits 3, 2, 1, 0. Bits 3 and 2 are `FORCE_BAD_PARITY` and `FPU_TRAP` already. **The three findings**: (1) the upper byte is documented **"1 => led off"** — **since settled**: the reference boot opens `FF EF DF FE EE DE ...` and reaches `00` once, which is a machine starting all-dark and lighting lamps as it goes, so the polarity is real and `ap_boardreg_post_code` is right to store the byte uninverted; the polarity is now recorded beside it so a **clear** bit reads as a **lit** lamp; (2) **bit 1 `rsa`, "reset on-board devices", has no constant at all**, and the page adds the exclusion "**Neither RSA nor the reset instruction reset the SIO lines**"; (3) bit 0 is `AP_BOARDREG_CONTROL_INTERRUPT_ENABLE` here but is **`nme`, non-maskable interrupt enable**, and the page states a gate this core does not implement -- "**Non-maskable interrupts must be enabled to receive parity errors**", which would make `ap_parity`'s level-7 path conditional on this bit. Also stated: the register "is write-only (**the BSET instruction may not be used** to turn bits on and off)" and both registers "respond to byte mode access". All three are named in `docs/COMPLETION_PLAN.md`; none is implemented here, because each needs the firmware checked against it rather than a constant added |

| 287 | §12 **DISK (FLOPPY/WINCHESTER) CONTROLLER** `[4D000 | 3FFA800]` — status, jumper and interrupt mask registers | `confirms` **(the status register bit for bit)** + **one transposition** | The Winchester half of `ap_omti`, at the `4D000`/ISA `1A0` page 279's table gives it. **Status register**: `int` interrupt request, `dma` dma request, `slc` controller selected, `ph` "1 => command phase, 0 => data phase", `dir` "1 => transfer to host", `avl` available for transfer — which is `[OMTI]` Table 4-2's `IREQ`/`DREQ`/`BSY`/`C/D`/`I/O`/`REQ` at `20`/`10`/`08`/`04`/`02`/`01`, all six in the same positions as `AP_OMTI_ST_*`. "Write anything to this register to RESET controller" and, for the jumper register, "write anything ... to generate controller select" are Table 4-1's two write-side functions, stated. **Jumper Setting Register `[4D002]`** names the configuration byte's low four bits `j1`-`j4`, "jumperN in place" — `[OMTI]`'s `W20`-`W23`, so this core's measured `FC` is jumpers 1 and 2 fitted. Recorded in `ap_omti.h`. **The transposition**: the **Interrupt Mask Register `[4D003]`** is drawn with `dma` at bit 1 and `int` at bit 0, where `[OMTI]` §4.2 gives "Bit 1 INTERRUPT ENABLE ... Bit 0 DMA ENABLE" and `omti8621.cpp` has `INTE 0x02`/`DMAE 0x01`. The part's own manual is followed; the disagreement is named in `ap_omti.h`. **Two drawing conventions to know before reading any register on these pages**: unused bits are printed `0` where `[OMTI]` says "Not Used (Set to 1)" — so `AP_OMTI_ST_FIXED` is unaffected — and the *section header's* address is repeated on the first register, which is why the status register is printed at `4D000` when p. 12-10 gives that address to the data port and Table 4-1 puts status at offset 1 |

| 288 | §12 **Command/Data/Status Register** `[4D000]` — the CDB, the control byte, the status phase byte | `confirms` **(a second source for the whole descriptor block)** + **one gap named** | "All commands are given by moving **6 bytes** ... The copy command is the only exception which requires **10**", and the three phases — which is `AP_OMTI_CDB_SHORT`/`_LONG` and `ap_omti_phase_t`. The byte layout is §5.1.1's: `MSB CYL` at byte 1 bit 7, head in 4-0, `Cyl.High` in byte 2's top two with the sector below, cylinder low in byte 3, interleave-or-block-count in 4, control in 5. It draws `LUN` across bits 6-5 where §5.1.1 has bit 6 "not used" and bit 5 the LUN; ours is bit 5. **The control byte's step table matches `[OMTI]` §5.2 value for value in seven of eight rows**, and the eighth is Apollo's own: `001` — "10 microseconds, buffered step" — is marked **N/A**, a rate none of this machine's drives takes, and `000` is named "unknown drive". Recorded in `ap_omti_cdb.h`. **Status phase byte**: bit 1 error and bit 5 drive number are §5.3's, and the page adds a legend for **bit 6**, "1 => winchester status, 0 => tape status", which §5.3 gives as one of four bits set to zero — not modelled, since the tape is an SC-499 at its own address and does not answer here. **The gap this page opened is §5.3's own**: bits 3-2 are an **Error Recovery Status** field this file had never named. Always `00` here because nothing in the model retries, which is now stated with the four values rather than left implicit — `COMPLETION_ERROR_RECOVERY_*` |

| 289 | §12 **Error and Operation Codes** | **GAP FOUND AND FIXED** + `confirms` **(twenty op codes and nineteen sense codes)** | A sense-code list and an op-code list, both agreeing with `[OMTI]` Appendix A and §5.1.2 almost everywhere. **The finding is `23 Volume Overflow`** — printed here beside the four codes this core emits, and there was no path in `ap_omti.c` that could produce it. Appendix A-5 defines it: "after the **commencement of a multiblock command**, the end of volume was reached", one line below `21 Illegal Disk Address`, "a command with a Sector Address beyond the capacity of the drive". **The two divide the same failure by when it happens, and every multiblock path here reported `21` for both** — telling a driver its descriptor block was wrong when the block had been accepted and sectors had already crossed the bus. Eight sites fixed: `feed`, the streaming write, READ VERIFY, `1E`, `1F`, COPY, READ LONG and WRITE LONG's placement; the address-refusal helper now takes the code, and two arms that ended with a bare sense byte now report **where** the transfer stopped. Two tests. Detail in `PROJECT_STATUS.md`. **Two op codes differ** and both are recorded rather than adopted: Assign Alternate Track `$10` (§5.1.2: `11`; `10` is CHECK TRACK FORMAT) and Read to Sector Buffer `$30` (§5.1.2: `1E`) — and the second is settled by measurement, since Domain/OS issues `1E` in this core's own boot. Apollo also lists two diagnostic codes the appendix stops short of, `32` and `33`, which is where a vendor firmware ROM would show. And the page gives **MD's disk error line** — `DISK operation ERROR: xx recordnum unit type`, "returned by `sense_status`" — now in `MD.md` |

| 290 | §12 **Floppy** `[5F800]` — Digital Output and Main Status registers | `confirms` **+ three careless annotations** | The floppy half at ISA `3F0`, with `3F2` and `3F4` where `[OMTI]` Table 4-3 puts them. **Digital Output**: `BEN`/`AEN` motor enables at 5 and 4, `ENB` "enable interrupts and DMA" at 3, `RST` at 2, `SLC` drive select at 0, bits 7-6 reserved — `AP_OMTI_DOR_*` exactly. **Main Status**: `RQM` "data register ready to send/receive", `DIO` direction, `EXM` "execution mode for non-DMA transfers", `CB` busy — ours, and the prose "Recalibrate, Specify, and Seek return no status information" confirms `ap_omti_fdc_result_bytes` answering zero for those three. **What not to take from it**: the reset bit is annotated "1 => Reset" where both OMTI manuals say the floppy is reset when the bit is **0** — `AP_OMTI_DOR_NOT_RESET` is right — and the annotation immediately above it reads "Drive motor B enable" for the bit named `AEN`, printing the same legend twice. The status register is drawn with **four** seek-mode bits `D3B`-`D0B`, the generic 765's, where `[OMTI]` Table 4-3 and the 8640's §5.1 both give bits 3-2 as reserved and two drives; ours is two. Three loose statements on one page, which is the calibration for p. 12-14 |

| 291 | §12 **Status Registers 0, 1 and 2** `[5F805]` | `confirms` **(ST1 and ST2 entirely; ST0 but for two bits)** | ST1's six live bits — `ECY` end of cylinder, `DE` data error, `OR` overrun, `ND` no data, `NW` write protected, `MA` missing address mark — and ST2's seven — `CM` deleted data-address mark, `DD`, `WC` wrong cylinder, `SH` scan equal hit, `SN` scan not satisfied, `BC` bad cylinder, `MD` — are `AP_OMTI_ST1_*` and `AP_OMTI_ST2_*` position for position, every one. ST0's interrupt code, seek end, equipment check and unit field likewise. **It differs in two bits**: `NR` not ready at 3 and `HD` head address at 2, which `[OMTI]` §6.4.1 gives as "Not Used - Always zero". Same shape as p. 12-14's ST3 and resolved the same way — the part's own manual, with the handbook's names recorded in `ap_omti.h` |

| 292 | §12 **Status Register 3**, **Additional Control Register** `[5F806]`, **Digital Input Register** `[5F807]` | **TWO GAPS FIXED** + **one open question named** | **The Additional Control Register is a documentary gain**: `[OMTI]` Table 4-3 lists it by name and never decomposes it, and this page is the only source on the shelf for its fields — `WP2`-`WP0` write precompensation at bits 2-0, `PN2` "interface pin 2 (density & speed control)" at 3, `PN4` and `PN6` above. Named in `ap_omti.h` and exposed; none is acted on, because precompensation and recording density are properties of a magnetic surface and an `.afd` image is decoded sector data. **Reading it against the code found a defect it would not otherwise have shown**: `3F6` and `3F7` were writing into **one byte**, so selecting a data rate cleared whatever precompensation had been programmed. Table 4-3 has them as two registers; they are two fields now, `3F7`'s rates transcribed from the 8640's §5.1 — `00` 500 Kbit/s, which is the rate `008778-03` §7.2 requires of this drive and so the value a reset already selects. Hashed, tested. **ST3 is the open question.** `[OMTI]` §6.4.4 and the 8640's §5.6.4 give three live bits (`WP`, `T0`, `HD`) and five constants; this page gives the generic 765's eight — `FT`, `WP`, `RDY`, `TR0`, `TS`, `HD`, `UN1`, `UN0`, each "from drive" — and `008778-03` §5.4.1.2 does name the part an "FDC765". **Our code was following neither**: it built the byte as `ALWAYS | unit`, the part's constant and the handbook's unit field at once, answering `03` for drive B — a value no document describes. Resolved to the part's own manual, with the handbook's table, the reason for the choice and the discriminating experiment recorded in `ap_omti.h` and `PROJECT_STATUS.md`. The page also names all seven bits `[OMTI]` calls reserved in the **Digital Input Register** — `WG`, `HD3`-`HD0`, `UN1`, `UN0` — recorded and not modelled, since three of them describe a surface this image format does not have |

| 293 | §12 **Diskette Control Register** `[5F807]` | `confirms` **— of a fix made one page too early** | Bits 7-2 unused, bits 1-0 `TRN RTE` transfer rate: `00` 500 KBPS, `01` 300, `10` 250, `11` N/A. Cited to `/os/ker/flp.pas`. That is the table `ap_omti.h` had just taken from the **8640** manual's §5.1 because `[OMTI]` does not decompose the register — and here it is for this machine, at this address, agreeing row for row. It also confirms, from Apollo's side, the thing p. 12-14 had made visible: `3F7` is a register in its own right and not part of `3F6`. Nothing to change; recorded because a fix that a later page independently confirms is worth more than one that merely still passes its test |

| 294 | §12 **DISPLAY (COLOR)** `[5E800]`, display memory, status and display-type registers | `confirms` **(the geometry and the ID)** + **one disagreement with the oracle** | "4 planes, each **1024x1024** (1024x800 visible)" and display memory at `A0000`, which is `ap_graphics`'s 4-plane geometry and its buffer width — taken from `008778-03` §1.5.3's "512 KB ... in four 128-KB planes" and now stated directly. The **Display-Type Register at `5E801` reads `08`**, which is `AP_SCREEN_COLOUR_4_PLANE = 8`: the screen-kind encoding came from the oracle and is now Apollo's. The status register's eight bits are the eight this core drives — but **two of the colour board's names are exchanged relative to the oracle's aliases**: bit 5 is `syn` "composite sync" where MAME calls it `DONE`, and bit 2 is `ad` "a-d conversion done" where MAME calls it `SYNC`. Bit 0 is `lok`, "okay to load LUT", where MAME calls it `V_FLAG` on a 4-plane board. `PROVISIONAL`: what this core *drives* is the oracle's arrangement, the boot PROM's display-present probe tests bit 2, and the handbook describes the 4-plane board where the 8-plane one is a later design. Named in `ap_graphics.h` and `PROJECT_STATUS.md` with the experiment that would settle it |

| 295 | §12 **Raster Op Register** `[5E802]`, **BLT Control Register** `[5EC00]` | `confirms` **(all sixteen operations and the mode field)** + **one field explained** | The raster op register is **16 bits on the DN3000**, four 4-bit fields `rp3`-`rp0`, one per plane — which is the baseline `008778-03` §10.3's "ROP Register specifiers increased to **32 bits**" is measured from, so the two documents describe the same register on two boards and this core models the wider one. All **sixteen** operations match `ap_graphics_rop_t` name for name, `$0` Zero through `$F` One. The BLT Control Register is `CR0`: bits 7-5 the mode, bits 4-0 the shift. **Six of the eight modes are listed and `101` and `110` are not** — the same two the oracle marks `???`, so two independent sources have the same hole, which is a stronger statement than "MAME does not know". **And the shift count is "Signed ... (positive is right-shift)"**, five bits, -16 to +15. That is what the shifter's rotate-when-≥16 branch has always implemented — copied from the oracle and described here as a mechanical rule — so the field now has a meaning rather than a recipe. `ap_graphics_cr0_shift_signed` added, tested |

| 296 | §12 **Misc Control Register** `[5EC02]`, **Plane Select Register** `[5EC04]` | `confirms` **(both registers, bit for bit)** | `CR1`'s eight bits, in Apollo's names: `adb` additive blt or normal memory access, `dvc` disable vert mode clock, `dhc` disable horiz clock, `ren` raster op enable, `rst` reset sync generator, `dpc` diagnostic pixel clock, `syg` enable sync generator, `ven` video enable — `AP_GRAPHICS_CR1_*` exactly, including the colour board's top two. It also **explains** what this core had observed: the three clock bits are "watched by hardware only when **syg = 0**", so with the sync generator off they are the clocks, which is why writing one steps a counter. `CR2` likewise: bits 7-6 the BLT source control with all four values printed — "all ones (used for vectors)", "replicate 4 lsb of data bus", "replicate lsb of shifter", "use source data unchanged (normal use)" — bits 5-4 the source plane, bits 3-0 the destination mask with "**0 => select plane N**" and "any combination". The active-low destination select and the four access modes were the oracle's; they are Apollo's now |

| 297 | §12 **Chip Init Register** `[5EC06]`, **Write-enable Register** `[5E800]`, the **LUT registers** | **A GAP CLOSED** + **a part named** + `confirms` | **`CR3A` is an 8255A's control register** — "Initializes the 8255A chip. Set just once per system reset", `$80` "set all ports to basic output mode". This core implements the part's bit set/reset command on it and ignores anything with bit 7 set, which was a mask that happened to work; it is now the part's own mode-set word, and port C is wired to `CR1`. **The initialization sequence is printed in full** — `Chip_reg $80`, `Misc_cntl $8B` "enable display, normal memory access", `Plane_sel $CE` "select plane 0", `Blt_cntl $E0` "normal memory access, no shift" — four registers with Apollo's own reading of each value, which `graphics_suite` now asserts as one test. Write-enable: sixteen bits, "**0 => writing enabled, 1 => writing disabled**", confirming the protect-and-OR model. **And the gap**: the 4-plane board's palette is three registers, Red `5EC01`, Green `5EC03`, Blue `5EC05`, each an entry in the top nibble and a four-bit level in the bottom, write-only, sixteen entries. `ap_scanout_palette` answered an even grey ramp flagged "not real" and named these registers as the thing it could not model. **Implemented**; detail in `PROJECT_STATUS.md` |

| 298 | §12 **Diagnostic Register** `[5EC07]` | **CLOSES A "no document exists" NOTE** | The A/D converter's own port on the colour board. **Write**: bits 7-3 zero, bit 2 one, bits 1-0 the channel — 00 Red, 01 Green, 10 Blue **Video Output**. **Read**: `00` 0.00 V, `01` 0.01, `80` 1.28, `FF` 2.55. `ap_graphics.h` carried "the search for a document is exhausted ... the only mention of it anywhere in `docs/references/` is `002398-04` p. 4-23, and it is an *error code* rather than a specification", and described the converter as reading "a monitor's identification and a brightness pot". Both wrong, and the page that says so was four pages from the ones being quoted, in the chapter the registers live in. The channel encoding is **the one `ap_graphics_adc` already implements** — arrived at from the oracle, now confirmed from Apollo — and the read format adds the scale nothing here had: the byte is hundredths of a volt, so the boot PROM's `[52, 70)` range check is 0.52 V to 0.70 V. The register was gated "8-plane only" and answered `FF`, so the one board with a port of its own was the one that could not be asked. **Implemented and tested** |

| 299 | §12 **DISPLAY (MONOCHROME)** `[5D800]`, status, display-type, raster op, BLT control | `confirms` **(all eight status bits and the ID)** | "Display memory `[FA0000]` **2048x1024** (1280x1024 visible)", which is `ap_graphics`'s monochrome geometry and the buffer width `008778-03` §1.5.3's 256 KB divides out to. The **Display-Type Register at `5D801` reads `09`** = `AP_SCREEN_MONO_19_INCH`. The status register's eight — `blk`, `vb`, `hs`, `rmc`, `alt`, `vs`, `hck`, `vd` — are this core's monochrome names in this core's positions, every one, which is what makes p. 12-16's two exchanged colour aliases a disagreement about the *colour* board specifically rather than about the register. The raster op register has one 4-bit field, "definitions for rp values same as color controller", and the BLT control register is "same as color". **Both are printed at the colour block's addresses** (`5E802`, `5EC00`) although this section's own block is `5D800`: the monochrome pages were written by copying the colour ones and only the first two addresses were changed |

| 300 | §12 monochrome **Misc Control** and **Plane Select**, chip init, write-enable | `confirms` **+ one digit corrected** | The monochrome `CR1`: bit 7 `inv` "Inverse Video Control (0 => 1 is black ...)" and bit 6 `b17` — "**BLT Mode 4 destination address bit 17** (syg = 1) / when syg=0, disable vert mode clock" — against this core's `AP_GRAPHICS_CR1_MONO_DADDR_16`. **Bit 17, not 16**, and the bit has two functions chosen by whether the sync generator runs; the oracle's constant name is kept and the digit recorded, since neither function is modelled. The monochrome plane select keeps only the BLT source control in bits 7-6 and marks 5-0 unused, which is what one plane leaves. Chip init and write-enable are "same as color". Cites `color4_$disp_regs in /us/ins/color4.ins.pas`. Again at the colour block's addresses |

| 301 | §12 **DMA CONTROL** `[9000]`/`[9100]`, channel usage, command and status registers | `confirms` **(all eight command bits)** + **one table that disagrees with `008778-03`** | "DMA control uses two Intel **8237** DMA Controller Chips. The first supports only 8 bit transfers (channels 0-3) and the second 16 bit transfers (channels 5-7). Channel 4 is used to cascade channels 0-3 to the cpu and so is not available for dma." The **Command Register**'s eight bits — `ack` DACK sense active high, `drq` DREQ sense active low, `slt` extended write, `pri` rotating priority, `tim` compressed timing, `ctl` controller disable, `hld` hold enable, `mmc` memory to memory — are `AP_I8237_CMD_*` position for position, the two polarity bits included. The **Status Register** is requests in 7-4 and terminal counts in 3-0, which is what `ap_i8237`'s `status` field holds. **The channel usage table is a kernel allocation table and disagrees with the hardware one**: `CH1 SDLC option` where `008778-03` Table 2-4 has DRQ1 **Tape** and Table 8-1 straps the SC-499 to "DMA Channel 1", and `CH7 avail` where Table 2-4 reserves DRQ7 for the Winchester. Reading that fits both: "avail" means unclaimed in the base configuration — and it follows that a machine cannot carry both the tape and the SDLC option, which nothing states outright. Recorded in `ap_dmapage.h`; nothing changes |

| 302 | §12 8237 **mask**, **mode** and **clear byte pointer** registers | `confirms` **+ one address slip** | Single Channel Mask at `900A` (channel in 1-0, `msk` at 2), All Channels Mask at `900F` (`c3m`-`c0m`, 1 disables), Mode at `900B` — mode in 7-6 with `00` demand, `01` single, `10` block, `11` cascade; `ads` address decrement at 5; `aut` auto-initialize at 4; transfer type in 3-2 with `11` illegal; channel in 1-0. Every one is `AP_I8237_MODE_*` and `AP_I8237_REG_*` unchanged. **The slip**: "Clear Byte Pointer `[900D]`. This register must be written to (any value) before loading address and count registers" — the 8237A's byte-pointer flip-flop is offset **`C`**, and `D` is **master clear**, so a driver following that line would reset the controller before every transfer. The other five addresses on the page are right, which is what makes it a slip and not a board difference |

| 303 | §12 **DMA Page Registers** `[9200]`, and a forward reference | `confirms` **(a page already derived)** + **schedules chapter 7** | The seven page registers in their non-order, the "high 8 physical address bits", and the 1024-byte note — all already quoted verbatim in `ap_dmapage.h`, which cites this page. Re-read in place rather than skipped, which is the difference between a page consulted and a page walked. **And the finding is the last two lines**: "FAULT FRAME AND FAULT TYPES — Refer to the FAULT FRAME section of **CHAPTER 7**, DN300,320". So chapter 12 delegates the DN3000's fault frames to a chapter the reading order treats as another machine family. Recorded in the Resume block: chapter 12 is not finished until chapter 7's fault-frame section is read |

| 304 | §12 **FAULT VECTORS** | `confirms` **(the vector table and eleven IRQ assignments)** + **one disagreement about the modelled card** | The whole 68000 exception table with the DN3000's own annotations — `1F 07C` "**Parity Error non-maskable level 7**", which is `ap_parity`'s autovectored level 7 and its vector; `18 060` spurious; `19-1E` "Auto vectors 1-6 (unused)"; the FPU's seven at `0C0`-`0D8`; and `40-9F 100` "**User Int Vectors - used for prom entry pts**", which is where this family's boot PROM keeps the entry-point table PDF 278 showed for the DSP80. Then vectors `A0`-`AF` at `280`-`2BC` against the sixteen IRQs: timers, sio "(2 ports)", **ring**, "PIC 2 to PIC 1 (unseen by software)", tape, floppy, calendar, the two ethernet boards, PC-board primary and alternate, "Diagnostic Interrupt", winchester. `IRQ2` for the ring and `IRQ3` for the cascade are `AP_BOARD_RING_IRQ` and the measured cascade, stated a second time; the ring's vector `A2` also confirms the `162` side of the tension `ap_board.h` records against finding 53d's `163`. **The disagreement**: "IRQ9 - Ethernet board 1, IRQ10 - Ethernet board 2", where `008778-03` §14.1 says the *first* card uses IRQ10 and the second IRQ9, and Figure 14-3 draws one card jumpered "Interrupt Level 10" and "Hex Address 300" **together**. `AP_BOARD_ETHERNET_IRQ` stays 10 — a figure of one card's straps outranks two summary tables, and the handbook's neighbours transpose a mask register and misaddress a DMA register — and the disagreement is named in `ap_board.h` rather than resolved away |

| 305 | §12 **PARITY ERROR REGISTER** `[9300]`, **PROGRAMMABLE INTERRUPT CONTROLLER** `[9400/9500]` | **GAP FOUND AND FIXED** + `confirms` | `008778-03` Table 2-8 gives the register at `9300` only a name — "Latch Page on Parity" — and this core modelled it as sixteen bits of storage on that basis. This page gives its contents: "**PARITY ERROR REGISTER (read only)**", bits 13-0 the **FAILING PPN**, "the upper two bits must be masked off", cited to `mmu_$parity_ppn`. `ap_board.c`'s `parity_error` already latched `address >> 10`, which the fourteen-bit field confirms as a page number on a 1024-byte page — but a **bus write stored here**, so a program could overwrite the one thing a parity handler exists to read. **Fixed**: read-only, with a test, and the width recorded as the DN3000's 24-bit space rather than enforced, since `019411-A00` gives the Series 4000's page number as bits `<25:10>`. The PIC section confirms the rest: two 8259As, master `9400` taking lines 0-7, slave `9500` taking 8-15 "cascaded through request line **3** of the master", `0A`-then-read for the request register, `0B` for in-service, `20` non-specific EOI, and the mask register with "1 => channel is masked" |

| 306 | §12 **PIC Initialization Sequence**, **Interrupt Request Line Assignments** | `confirms` **(every initialization word)** | "Interrupt vectors are programmed to start at **$280**", then the words themselves: master `ICW1 19` "level triggered, need ICW4", `ICW2 A0`, `ICW3 08` "have slave at IRQ3", `ICW4 01` "normal EOI, 8086 mode", `OCW1 FF`; slave `ICW2 A8`, `ICW3 03` "am slave with ID 3", the rest alike. `ap_board.h` already cited the boot PROM's own `ICW2 = A0`; here is the whole sequence with Apollo's reading of each word, and every one is a value `ap_i8259` decodes. The assignment table repeats p. 12-26's, and adds the consequence: "On slave interrupt, EOI slave first, then master. The priority of **IRQ8-15 is higher than IRQ4-7** since they use the second PIC which is slaved to the master at IRQ3" — which this core gets from modelling the cascade rather than from a priority table, and is the kind of statement a model has to *earn* rather than encode. One difference between the chapter's two tables: p. 12-26 names `IRQ13` "Diagnostic Interrupt" and this one leaves it blank |

| 307 | §12 **RING** `[51000-51C00 59000-59C00]` — the buffer, and the whole register map | `confirms` **(the read/write map and the pipelined port)** + **two version differences recorded** | The eight ring pages page 279 flagged, tabulated: ISA address, physical, AEGIS virtual, and **what each offset means read and written**, which are different registers — `220`-`226` read `Node_ID3`-`Node_ID0` and write `SOFT_RCV_REQ`, `XMIT_ACK`, `RCV_ACK`, `TIMO_ACK`; `228`-`22E` write `ERR_BITS_CLR`, `GPS_CLR`, `SOFT_XMIT_REQ`, `LERR_CLR`; `23E` reads `Node_ID_CHECKSUM`; `320` reads `BOARD_TYPE` and writes `BOARD_RESET`; then `MISC`/`XMIT`/`RCV` status and command, `RAM_ADDR`/`RAM_DATA`, and the six packet counters. **Every write-side name is `AP_RING_CTL_W1_*` already**, and the whole second-window layout is what `ap_ring_ctl` decodes — this page is where several of those names came from, re-read in place. **And it states the pipelining outright**: "After setting ram_addr, the **first word read should be discarded**", which this core derived from the firmware ("both readers discard a `move.w` before their loop") and which is now documented rather than inferred. **Two version differences, both consistent with the single-board/two-board split `008778-03` §2.11 shows**: the DN3000's first window reads the **node ID** where this core's reads a **PROM image**, and its buffer is "an **8K WORD** buffer ... room for 7 rcv msg buffers (each 1k bytes of header and 1k bytes of data) and 1 xmit msg buffer" — 16 KB, internally consistent, against the 64 KB this core's ring firmware walks in its own memory test. Recorded, not reconciled: two boards, and the modelled one is the board the firmware tests |

| 308 | §12 **MISC_STS**, **RCV_STS** `[59400/59404]` | `confirms` **(every bit of both)** | `nct`, `tmo`, `xby`, `rby`, `iov`, `rlk`, `esb`, `bpe`, `gps`, `xi`, `ri`, `tmi` — the twelve live bits of MISC_STAT, at the twelve positions `AP_RING_CTL_STATUS_*` gives them, with the active-low ones marked `0 =>` as this core has them. RCV_STAT's sixteen likewise, `pe` down to the three `rc2`-`rc0` counter-output bits whose meanings this file already quotes from this page. Cited by `ap_ring_ctl.h` before the walk reached it; read whole now, and nothing in it is new |

| 309 | §12 **XMIT_STS** `[59402]`, both layouts | `confirms` **(including the layout switch)** | One register with two meanings for bits 14-8, chosen by bit 15 `pe`: with it clear, `cpd`/`wak`/`icp`/`pke`/`de`/`abt`/`ife`; with it set, `tmo`/`syn`/`ern`/`frm`/`akp`/`abt`/`ife`. That is `AP_RING_CTL_XMIT_PE` "selects bits 14-8" and the two sets of constants beneath it. The transmit tags are confirmed as a *state* encoding rather than two flags — "`00` msg complete, `01` data being transmitted, `11` hdr being transmitted" — and `ife` is documented as meaningful "IFF abt = 1", which is the conditional this core already applies |

| 310 | §12 **MISC_CMD**, **RCV_CMD**, **XMIT_CMD**, the address registers | `confirms` **(and the firmware settles a bit position the page leaves ambiguous)** | `bpm`, `nct`, `td1`, `td2`, `lpb` in MISC_CMD; `rcv` alone in RCV_CMD; `fen`, `ten`, `ine` in XMIT_CMD — every one `AP_RING_CTL_*_CMD_*`. Three statements this core models and one it records: "Force Transmit flag is a **modifier** to Transmit Enable, not a separate command"; "Set `ten` to 0 to **abort** an enabled transmit"; "Set `rcv` to 0 to abort an enabled receive"; and "**Bad packet marking enable is only operational on the single board**", which is a fourth instance of the two-version split. The bit *positions* are read from the firmware rather than counted off the ASCII box — `RING_PROC` writes `$800` and `$900` to MISC_CMD, which is `nct` alone and `nct` with `lpb`, fixing the field at bits 11 and 8. The address registers' byte-swapped layout is the one `ap_ring_ctl.c` already cites from this page |

| 311 | §12 **COUNTER REGISTERS**, hardware packet types | `confirms` **(the counters are 8254s, and the control words prove it)** | "The packet counters ... are implemented with **Intel 8254** Chips", which is what `ap_ring_ctl` models at the second window's `+800` and `+C00` banks. The page prints the control words a driver writes, and every one decodes as an ordinary 8254 word: `$3000`, `$7000`, `$B000` to load counters 0, 1 and 2; `$C200`, `$C400`, `$C800` to latch one counter's count **and status**, or `$CE00` "to prepare to read all three" — which is the read-back command for all three counters. So the documented sequences exercise `ap_i8254`'s read-back path, and the "status in high byte of word, lsb, msb" ordering is that command's own reply format. **Also here, and for `RING.md` rather than for code**: the hardware packet types — `80` broadcast, `40` hw_diag, `20` thank you, `10` please, `8` paging, `4` user, `2` sw_diag |

| 312 | §12 **RING SOFTWARE STATUS INFO** | `none` **(AEGIS, not the controller)** + `confirms` **(the XMIT_STS bit names, from a third angle)** | `RING_$SEND_STAT_T`'s sixteen bits, what each is counted in, what `netstat` calls it, and — the useful half — **how each is derived from the DN3000's XMIT_STS**: "copied" is "NO errors AND `cpd` AND `icp`", "nacked" is "NOT `pe` AND NOT `icp`", "overrun" is "`abt` AND `ife`", "not connected" is `nct`, and so on. That is AEGIS's mapping and not the controller's, so there is nothing here to implement; what it does is name every XMIT_STS bit a third time, in a document written for whoever reads a running system's statistics. Four bits are marked "**Status not defined for DN3000 transmits**" — bus error, biphase error, esb error and one more — which is worth knowing before a DN3000's `netstat` output is trusted |

| 313 | §12 **SIO** `[8400]`, **TIMERS** `[8800]` — the chapter's last page | `confirms` **+ delegates two more sections** | "**Channel A is the keyboard, Channel B is the single sio line**", which is `ap_sio.h`'s "§3.9: `SIO_O` is used for the keyboard. Channel A of the first part" from a second document, and confirms the single user line one 2681 gives this machine. "The chip is also used to provide **timing for memory refresh**" — a use no register exposes and nothing here models, recorded because it explains why the part is programmed on a machine that is not using its second channel. Then the delegations: SIO is "same as first chip of **DN460 (CHAPTER 9)**, except for the definition of the output port register in which the **`dtr_b` bit has moved**" — so chapter 9 holds this machine's SIO register definitions and one named difference — and TIMERS is "same as all previous systems", with no chapter named at all |

### Chapter 12's delegated sections, read out of order

Recorded here rather than under chapters 7 and 9, because they are chapter 12's
content and the reason they were read early is that chapter 12 sends the reader
to them. The rest of both chapters is unread and stays scheduled.

| Pages | Section | Status | What it yielded |
| --- | --- | --- | --- |
| 153–155 | §7 **BUS/ADDRESS ERROR STACK FRAME FORMAT (DN330)**, **FRAME FORMAT/VECTOR OFFSET WORD**, **SPECIAL STATUS WORD**, **FAULT TYPES** — p. 12-25's delegation | `confirms` **(the frame set and both bus-fault layouts)** | The medium frame at `+00` SR, PC, `1010 0000 0000 1000`, internal register, special status word, instruction pipe stages C and B, data fault address, internal registers, data output buffer — sixteen words — and the long frame's continuation through stage B address, data input buffer and internal registers to `+5C`, forty-six words. The format table gives `0000` four-word, `0001` throwaway, `0010` six-word, `1001` coprocessor mid-instruction (10 words), `1010` medium bus error (16), `1011` long bus error (46), which is `ap_m68030_frame_format_t` code for code and word count for word count. The one entry this core does **not** have is `1000`, "29-word 68010 bus error", which the 68030 does not produce — correctly absent rather than missed. Also here: "Real fault address: `DF` => data fault address; `FB` => if medium then PC + 4, if long then stage b address", and the special status word's FC/size/read-write/RMW fields. `FAULT TYPES` groups the exceptions by whether the instruction is aborted, taken before the next instruction, or processed normally — the 68000 grouping, unchanged |
| 157–158 | §7 **FLOATING-POINT FORMAT**, **FLOATING-POINT REGISTERS (DN330)** — p. 12-26's delegation | `confirms` **(the whole FPCR)** | Single and double precision with the implied normalisation bit, exponent plus 127 and plus 1023 — IEEE, and what `ap_m68882_format` implements. The **Floating Point Control Register** is the 68881's: zero in 31-16, then `bsun`, `snan`, `operr`, `ovfl`, `unfl`, `dz`, `inex2`, `inex1` in 15-8, `prec` in 7-6 (00 extended, 01 single, 10 double, 11 "undefined, reserved"), `rnd` in 5-4 (00 nearest, 01 zero, 10 minus infinity, 11 plus infinity) and unused in 3-0. `AP_M68882_FPCR_IMPLEMENTED` is `0x0000FFF0` — exactly bits 15-4 — and `AP_M68882_ROUND_*` and `AP_M68882_PRECISION_*` carry the same four and three values in the same order. Every field of the register, confirmed against a document written for this machine family rather than against the part's manual alone |
| 241, 170 | §9 **SERIAL I/O INTERFACE** (9-14) and §7 **SERIAL I/O INTERFACE** (7-32) — p. 12-35's delegation, which delegates again | `confirms` **(the part, the register map and both baud tables)** + **one fact defined nowhere** | Chapter 9 sends the reader to chapter 7, so the chain is 12 → 9 → 7 and both were read. The part is a **Signetics SC2681**, which is `ap_mc68681`'s family, and 7-32 prints the whole register map — MR/SR-CSR/CR/RHR-THR for both channels, IPCR-ACR, ISR-IMR, IPR-OPCR, and the two Set/Reset Output Port Bits registers — at the offsets this core decodes, with the counter registers and the reserved one omitted from the summary rather than absent. **Both pages state the baud constraint**: "one SIO line can't have a baud rate from Group A while the other SIO line is set from Group B", Group A being 50 and 7200 and Group B 75, 150, 2000 and 19.2K. That is `ACR[7]` selecting one of two rate tables **for the whole chip**, which `ap_mc68681` already models — and the six rates Apollo names as group-exclusive are exactly entries where this core's two tables differ, at codes 0, 3, 7, 10 and 12. A precise confirmation of both tables from an Apollo document. **And the fact chapter 12 flagged as *different* is in none of the three chapters**: p. 12-35 says the DN3000's SIO is the DN460's "except for the definition of the output port register in which the `dtr_b` bit has moved", and no page in the chain decomposes the output port register. `008778-03` gets one bit of it — "In the DS3000, [IRQ13] is connected to **Output Port Bit 7** of the 2681 SIO chip", which `ap_sio` already implements — and says nothing about DTR beyond a connector pin. Recorded as a documentary dead end rather than an open question, because the chain is exhausted. Chapter 7's machines also carry a **separate MC6850** for the display keyboard at `8420`/`8422`, which the DN3000 does not — its keyboard is the DUART's channel A |

### Chapter 4 — error codes, READ END TO END (PDF 69–93, all 25 pages)

Begun out of order, chasing the question p. 3-10 handed over; the two skipped
pages were read next and the chapter is contiguous from its start.

| Pages | Section | Status | What it yielded |
| --- | --- | --- | --- |
| 69 | §4 **AEGIS ERROR CODES**, the status word and modules 01–03 | `confirms` **(p. 3-10's layout)** + **the code format** | The same four fields as p. 3-10, and the rule for the low half: "**0 = OK status, negative = warning, positive = error**". Then the codes themselves as `000M000E`, module by module: `01` the BAT manager — "disk is full", "disk needs salvaging" — `02` the VTOC manager, `03` the AST manager |
| 70 | §4 modules `03`–`06` — AST, **MST**, **PMAP**, **MMAP** managers | `none` **(the paging layer's failures)** | The rest of the AST manager, then seventeen MST codes — "reference to illegal address", "no asid is available", "guard fault", "wrong type - can't map system objects", "virtual memory resources exhausted" — seventeen for PMAP and nine for MMAP. These are the errors the structures chapter 1 laid out report when they fail, so the two chapters read together: `mst_t`, `pmape_t` and `mmape_t` there, their failure codes here. `guard fault` is p. 3-3's guard segment, named from the software side |
| 71 | §4 modules `06`–`08`, the **disk manager** | `none` **(reference for reading a failure)** | The MMU manager's ten codes, then forty for the disk manager: "disk controller time-out", "bad disk format", "invalid disk address", "disk block header error", "software detected checksum error", "floppy door has been opened", "bus error occurred during disk DMA transfer". These are the codes a Domain/OS disk failure reports, and this core's `ap_omti` sense codes are what produce several of them one layer down |
| 72 | §4 modules `09`–`0E` — eventcount, process, **terminal**, DBUF, time, and the naming server's first codes | **names module `0E` outright** + **two cross-references worth keeping** | The module heading is printed: "**OS / naming server**", which is what p. 4-5's company implied and this states — so `E0007`'s module is settled by the document twice over. **Module `0B`, the terminal manager, is the one that touches this core's work**: `data carrier detect (dcd) changed` and `clear to send (cts) changed` are the modem-control signals `008778-03` §3.9 lists and the completion plan records as having "no model at all" for DCD — here is the OS-level event they raise. And `speed incompatible with partner SIO line` is the **baud-group constraint** chapters 7 and 9 give as a property of the SC2681's shared rate selector, surfacing three chapters away as a user-visible error. Also `input buffer overrun - characters lost`, `character framing error`, `character parity error` — the three the DUART's status bits produce |

| 80 | §4 module `21` tail, `22` **magtape manager**, `23` ACL manager | `none` **(and the warning band again)** | The badspot manager ends with four codes about the **logical volume's badspot extension block** — "bad daddr for lv label badspot extension block", "too many extensions to lv badspot list", "badspot extension uid <> logical volume uid" — which is p. 2-19's `FFFFFFFE` overflow sentinel from the software side. Then the magtape manager, whose two warnings sit at `FFFE` and `FFFF`, and thirty-four errors including the memorable "unit will not fit thru 25\" hatch". The ACL manager's nineteen close the page |
| 81 | §4 modules `23`–`28`, ending at the **cartridge tape manager** | `none` **+ this core's tape status seen from the top** | The PEB manager — the floating-point board, with `fpu is hung`, the four FP exceptions and `attempted use of unimplemented opcode` — then colour display, VME, and **module `28`, the cartridge tape manager**, which is `ap_qic` and `ap_sc499` from the operating system's end. Its **six warnings occupy the negative band** `FFFA`-`FFFF`, and three of them are drive status bits this session worked on: **`tape power on/reset`** is QIC-02's `POR`, **`tape at load point`** and **`warning: tape not at load-point`** are `BOM`, and **`warning: tape unit is offline`** is the unselected-drive condition. So the exception bits corrected here — the `ST0` summary bit that was inverted, `WRITE_PROTECTED` that was never set, `ill` that could not latch — are the things these warnings are made of. The errors below them are the driver's own: `no tape controller on system`, `invalid buffer length`, `bad buffer alignment` |
| 82 | §4 module `28` complete, `29` msg, `2A` symbolic link, `2B` internet routing | **the page that sent a whole standard to be walked** | The cartridge tape manager's twenty-eight errors, and they are `QIC-02 Rev D` §5.3's exception summary decoded row for row: `(00280010)` "no cartridge in drive" ↔ row 1, `(00280011)` "drive does not exist" ↔ row 2, `(00280012)` "tape is write protected" ↔ row 3, `(00280013)` "end of tape" ↔ row 4, `(00280014)` "read/write abort" ↔ row 5, then the four read errors ↔ rows 6-10, `(0028001A)` "filemark detected" ↔ row 11, `(0028001B)` "illegal drive command" ↔ row 12, `(0028001C)` "marginal block detected" ↔ row 14. **Domain/OS decodes seven bits `ap_qic` could not set**, which is what made the standard a document to be obtained and read whole rather than quoted from — `docs/references/QIC-02_WALK.md`, five defects. The rest of the page is the message, symbolic-link and internet-routing managers |
| 83 | §4 modules `2B`–`2F`: internet routing, IIC, graphics processor, DMA, Ethernet | `none` | The internet interface controller's sixteen include `IIC: dma got multibus read timeout error` and `wrong board revision level`; the graphics processor manager's seventeen include `no GPU microcode loaded` and `illegal values for physical page use limits`; the **DMA manager has exactly five** — `illegal channel`, `illegal byte count`, `channel in use`, `channel not allocated for operation`, `operation did not finish` — which is the whole of what AEGIS says can go wrong with the 8237s `ap_dma` models |
| 84 | §4 module `2F` tail, `30` audit trail — the end of the AEGIS status codes | `none` | The Ethernet driver's last ten, then a single-entry module: `(00300001) could not create Event Server Process`. **The status-code list ends here**, at module `30` |
| 85 | §4 **BOOT ERRORS (PROM)** | **a third independent decode of QIC-02 §5.3** | `error: boot not found - The SYSBOOT read from records 2 thru B did not have a good boot header`, then `disk init error <SC> <RCD> <UNIT> <W/F/S/C>` with the W/F/S/C qualifier decoded as Winchester/Floppy/SMD/**Cartridge Tape**. Three status-code tables: Winchester's five, the floppy's twelve, and Disk/Tape's, whose `11`-`1F` are controller conditions (`controller diagnostic failed`, `dma overrun/underrun`, `dma not at end of range`, `disk still busy`) and `21`-`29` drive conditions (`no index pulse`, `no track 0 on restore`, `address mark not found`, `ECC error in sector ID field`). **`30`-`3A` are the tape's, and they are `QIC-02 Rev D` §5.3 again** — `31` filemark, `32`/`33` no data qualified, `34` no data, `35` filler block, `36` bad block, `37` read or write abort, `38` end of media, `39` drive not present, `3A` no cartridge. So the boot PROM, Domain/OS module `28` and the standard all decode the same fourteen rows. **One misprint**: `32` and `33` both read "no data and BOM"; §5.3 rows 9 and 10 make one of them EOM, and the page does not say which |
| 86–87 | §4 **BOOT PROM DIAGNOSTIC ERROR CODES (except 020 machines)** | **the code list a stuck boot is read with** | Sixteen "steady state" values `0`-`F`: `0` first instruction at "init", `3` parity cleared in first 2 pages, `4` sio's have been initialized, `7` display init got bus error, `C` just printed MD's banner msg, `F` mmu enabled. Then two-digit codes: `02` "booting: waiting for disk I/O to complete", `0C` "waiting for command input (service mode only)", diagnostics 1-11 (PROM checksum, PFT, PTT, IOMAP, their interaction, physical/virtual memory, timers/MULTIBUS/calendar, ring, VME), `A` DISK ERROR, `B` NETWORK ERROR, **`C` SYSBOOT ERRORS** (`C2` unable to read physical volume label, `C4` unable to read logical volume label, `C5` salvaging boot volume, `C8` SAUn not found in root directory, `CF` wrong machine type), `D` NETBOOT, `F` AEGIS CRASH — `F1` aegis crash, `FF` normal shutdown complete. Diagnostic 9's sub-codes name the tests this core's boot report already prints: `timer 1/2/3 failed`, `calendar hardware clock not incrementing seconds`, `DMA continuity test for channel 0`-`3 failed` |
| 88–90 | §4 boot PROM LED codes for DN330, DSP90, DN5xx-T, DN560/570/580 | `none` | Four families this core does not model, in Upper/Lower or Lower/Middle LED pairs. Kept in the record because the *format* is the same as the DN3000's on the next page and the shared entries (`0 0 Turn off LEDs`, `0 1 Checksum PROM`, `- F Software detected error`, `- Arrow Unexpected exception`) show the numbering is a house convention rather than per-model invention |
| 91–92 | §4 **BOOT PROM DIAGNOSTIC ERROR CODES (DN3000)**, MD error codes | **our family's LED table, and two confirmations** | Ext./Int. LED pairs naming every power-on test: `0 2` Refresh circuitry, `0 5` Keyboard SIO, `0 6` Parity circuitry, `0 A` **DMA page register**, `0 B`/`0 C` DMA controller 1 and 2, `0 D` Calendar and configuration, `1 0` Critical memory (Megabyte 1), then the display: `2 2` **8255A test**, `2 4`/`2 5` horizontal and vertical sync counters, `2 A` **LUT red, blue, green high level output**, `2 B` BLTs, `2 C` ROP logic, **`2 F` A/D converter error**. The last two confirm this session from a second direction — the 4-plane lookup table and the colour status register's A/D bit were both derived from p. 12-19 and the boot PROM, and here is the power-on test list naming each. `7 0`-`7 2` are disk existence, controller self-test and read block 0; `8 0`/`8 1` the ring's digital and analog loopback. Then MD's own single-letter codes: `A` address error, `B` bus error, `S` trap instruction or breakpoint, `U` unimp inst trap, `Z` divide by zero, each with the registers it prints |
| 93 | §4 the SR and FC field maps, SYSBOOT ERROR CODES in words | `none` **(confirms `ap_m68030`)** | The 68k status register drawn field by field (`T-S--III|---XNZVC`, interrupt mask "000 - enabled, 111 - disabled") and the fault code as `RNFFF` with the five function codes — user data `001`, user program `010`, supervisor data `101`, supervisor program `110`, interrupt acknowledge `111`. Both match this core. The SYSBOOT codes of p. 4-19 repeated as the sentences the PROM prints |

### Chapter 5 — system debugging, READ END TO END (PDF 94–116, all 23 pages)

Chapter 5 is an operator and software reference rather than a hardware one, so
its yield is **technique and confirmation**, not registers. That is worth saying
plainly: a walk that only reports register findings would record this chapter as
empty, and it is not.

| PDF pages | Section | Yield | Notes |
| --- | --- | --- | --- |
| 94 | §5 BOOT SHELL COMMANDS | **the shell this project drives, in full** | Twenty-eight commands with their arguments. `MTVOL`/`DMTVOL {W\|S\|F} <lvno> [<pathname>]` is the mount syntax whose silent failure cost a session; `STCODE <status-code>` prints the textual definition of a status code — chapter 4's table, from inside the machine; `LD [<pathname>] [-A [-D]] [-U]`, `TB` stack trace back, `SHUT`, `GO` "load as if in normal mode" |
| 95 | §5 DEBUG command extensions | `none` | `FPREGS`, `REGS`, `DB`, and the `-CDB`/`-DDD` options, "not advertised to the public" |
| 96–97 | §5 **DB (machine level debugger)** | **the crash-analysis toolkit** | Roughly ninety commands, of which the crash-analysis set is the relevant one: `dr` display registers at crash, `st` display status at crash, `ff <addr>` print fault frame, `clf` "cherchez la (fault) frame (starts w/ A7)", `fnf` find stack frame, `dp [<pid>]` display PCB, `caot` check AOT/AST link consistency, `le` list system error log, `dvt` print disk volume table. And four that print the structures this core models: **`pvl <addr>` print pv label**, **`lvl <addr>` print lv label**, `vb <addr>` print vtoc block, `ve <addr>` print vtoce |
| 98 | §5 Lights Program | `none` | Displays the ring transmit and receive status registers at the bottom of the screen. "Do not execute the lights program from a color node (unless it is a DN3000)! It will crash the node!" |
| 99–107 | §5 **MNEMONIC DEBUGGER (PROM)** | **MD's whole command set, marked by family** | Every command with a `*` for DN3000-only and `#` for DN3000-and-DNx60. `AR <ctl_reg>` accesses **TC, RP, DFC, SFC, CACR, CAAR** — the 68030 control registers `ap_m68030` carries. `DI <W\|F\|N> [<nn>]\|<S> <0-3> <1-10>` defines the boot device, "Defaults are: W, 0, 1". `TE` forces execution of the boot PROM diagnostics; `SK` "forces the MD to enter its keyboard polling loop **without requiring a REset**"; `P` turns off mapping, "MMU is assumed at FFB400"; **`XE`/`XD` enable and disable the X-On protocol "used for communication with dumb terminals"**. Then the command grammar in BNF, with `:I`/`:B`/`:W`/`:L` size specs and `:O`/`:D`/`:H`/`:A` base specs |
| 108 | §5 **CRASH ANALYSIS** | **the crash record's format, stated** | "Most fatal errors ... are reported by the crash_system routine, which prints a status code (see Chapter 4), **the address of the ECB for the failing routine, and the ID (PID) of the current process**. AEGIS then executes a TRAP instruction, causing entry to the PROM mnemonic debugger with an `S` code." So the address in a crash record is an **ECB**, not a PC — which is what a previous investigation concluded the hard way. And the way in: "make sure the NORMAL/SERVICE switch is in the SERVICE position and type CTRL/<RETURN>" |
| 109–110 | §5 SYSTEM DUMPS | **a documented route to a full memory + MMU dump** | To floppy (first 1 Mbyte only), to cartridge tape, or to another node. `>DI C` then `>G x00C00` with `x = 1` for all nodes except DNx60/DN5xx-T; `>DU` for DN3000s. `DB` reads the format back. The tape "must not be write-protected (arrow in the upper left should point away from safe)", which is `WRP` from the operator's side |
| 110–116 | §5 /SYSTEST/SSR_UTIL | **five tools that answer questions this project has asked** | **`ALL_STCODE`** "displays a list of all the system status codes for this operating system revision" — chapter 4's table for SR10.4 rather than for Jan 87. **`FIXVOL`** examines and repairs "LV and PV labels, the VTOC, or disk block headers", the structures `ap_volume` and `ap_awd` model. **`RWVOL`** reads one physical disk address into a buffer, the in-machine form of this project's offline block dumps. **`JUMPER`** "graphically displays the locations and functions of jumpers on all system printed circuit boards". **`RINGLOG`** monitors network traffic in and out of a node. Also `CPTAPE`/`CPFLP`, `DISK_ERR`, `DMPF`, `FMPD`, `LSYSERR`, `MBD`, `NETLOG`, `OBJDMP`, `TRPT`, `XMT` (examines magtape), `ZEROTR` |

### Chapter 6 — peripheral I/O, READ END TO END (PDF 117–138, all 22 pages)

| PDF pages | Section | Yield | Notes |
| --- | --- | --- | --- |
| 117 | §6 **AT BUS DEVICES (SERIES 3000 MACHINES)**, disk parameters | **resolves a disagreement this core was carrying, and pins the ISA mapping** | Three rows: Ethernet Contr. `IRQ10 (IRQ9)`, `DRQ6 (DRQ3)`, `058000-058800`, `300-310`; SPE Controller `IRQ4 (IRQ9)`, `05FC00-05FF80`, `3F8-3FF`; PC Coprocessor `IRQ11 (IRQ15)`, `DRQ5`, `C00000-CFFFFF`. **The first row contradicts this handbook's own chapter 12** — p. 12-26 and p. 12-28 both say "IRQ9 Ethernet board 1" — and agrees with `008778-03` §14.1, Figure 14-3 and Table 2-3. Four sources against two summary tables in the chapter that also transposes the disk mask register: `AP_BOARD_ETHERNET_IRQ` is right and the disagreement is closed. **And the physical ranges pin `physical = 0x040000 + (ISA << 7)` from two cards** at opposite ends of the I/O space, where it had one card and an oracle tap. Then DISK PARAMETERS: `DTYPE` classes 000 (14" ring/disk PRIAM), 100 (8" ANSI), 200 (8" ring/disk SMD) with cylinders, heads, blocks/track and total blocks per model |
| 118 | §6 DISK PARAMETERS continued, classes 300–600, floppy, SMD | **a third source on both modelled drives** | Class 600 (5 1/4" ESDI) is the pair this core shapes: `604 Maxtor 380 | 1224 | 15 | 18 | 330480` and `607 Microp. 170 | 1024 | 8 | 18 | 147456`. One cylinder more than `image/ap_awd.h` carries in each — see PDF 120, which resolves it. The **floppy** row confirms `ap_afd` exactly: `77 | 2 | 8 | 1232`. **The Class 500 block is an erratum**: three different geometries all print `73458` in TOTAL BLOCKS, and Class 600 prints each of the three correctly one block down |
| 119 | §6 SEEK TIMES | **confirms the floppy model from outside, and disagrees on the Winchester** | Per-drive `T-to-T / AVG / MAX`, rpm, latency, transfer rate and average read, with the footnote "Average read = Avg. Seek time + Avg. Latency + Sector Time". **The floppy row is arithmetic on this core's own constants**: `MAX 231` is 77 x 3 ms, `AVG 77` is one third of that, and `AVG READ 176.0` is that plus 83.3 ms latency plus a 1024-byte sector at .0625 Mbyte/s. So a second document publishes the *unsettled* seek and it is the step time times the format's cylinder count — `afd_suite` asserts all three. **The Winchester row disagrees with `008778-03` Table 6-5 by a uniform 10%**: `AVG 27` against 30 and `MAX 52` against 58, with track-to-track, rpm, latency and transfer rate identical. Not a typo either side — 27 + 8.33 is the printed 35.3, and the Micropolis's two rows check the same way. 30 ms is kept, on the rule that a part's own approval table beats a nineteen-drive summary in the chapter whose neighbouring table has a copy-paste column |
| 120 | §6 badspot and diagnostic cylinders, I/O MAP ALLOCATION | **resolves the cylinder disagreement outright** | `604 Max 380` reserves its badspot cylinder at **1222** and its diagnostic pair at **1220**-1221, using **1220**; `607 Mic 170` reserves 1022 and 1020, using 1020. The note: "For the 6XX dtypes **four cylinders are reserved** - the last for manufacturer encoded badspots, two diagnostic cylinders, and Apollo's badspot cylinder." 1220 + 4 is 1224 and 1020 + 4 is 1024, so the handbook counts cylinders and the oracle numbers them, and a badspot cylinder at 1222 only exists if 1223 does. **Both documents are right.** Then I/O MAP ALLOCATION: floppy at I/O pages 0-1, Winchester 40-41, ring transmit 42-45, ring receive 46-49, second receive channel 4A-4D, colour DMA 5F-7F, bit blit 80-9F, Multibus C0-FF |
| 121 | §6 I/O MAP diagram | `none` **(and it says which machines)** | The full address path — 16-bit controller address, 0-2 strapped bits, 18-bit virtual DMA address, 8-bit VPN through a 256-entry IOMAP at `FFF800`-`FFF9FE` to a 12-bit PPN and a 22-bit physical address. Headed **DN4xx/6xx/DSP160**, so the allocation table above is those machines' and not the Series 3000's, whose counterpart is the DMA page register `board/ap_dmapage.h` models |
| 122–123 | §6 880 keyboard map and physical chart | `none` **(a different keyboard)** | The 880's key map and its 16x16 physical code chart. This is the predecessor of the Low-Profile keyboards `ap_kbd` models; recorded so the charts that follow can be told apart |
| 124 | §6 880 keyboard chart, translated (user mode) | `none` | The 880's ASCII-mode chart, the counterpart of PDF 123's physical one |
| 125–127 | §6 Low-Profile **Model I** map, physical chart, translated chart | `none` **(the predecessor)** | Three pages: the key map, then both 16x16 charts. Model I's left pad has `L1`-`LF` with `L1A`/`L2A`/`L3A`, and its right pad `R1`-`R6` — a different naming from Model II's positional scheme, which is why the two sets of charts must not be mixed |
| 128 | §6 **Low-Profile Model II Keyboard — Map** | **the key numbering, positionally** | The layout drawing that names every key position: `LA0`-`LF2` down the left pad in six rows of three, `A0`-`A4` then a gap then `A5`-`A9` across the top, `B1`-`B15`, `C1`-`C14`, `D0`-`D14`, `E0`-`E13`, `F1` the space bar, and on the right `RA0`-`RA4`, `RC1`-`RC4`, `RD1`-`RD4`, `RE1`-`RE3`, `RF1`-`RF3`. **There is no `RB` row**, which a reader assuming a regular grid would invent |
| 129 | §6 **Low-Profile Model II Keyboard Chart — ASCII Mode** | **the inverse map a frontend needs**  | A 16x16 table indexed by the **ASCII code the keyboard emits**, each cell naming the physical key and its modifier: `+` shift, `^` control, `:` up transition. So it answers "which key, held how, produces byte `41`" — which is exactly what synthesising a keystroke needs and what this project's console work has had to guess at. The up transitions occupy the high half, so the `+ 0x80` rule `AP_KBD_RELEASE` already carries holds in this set too |
| 130 | §6 **Low-Profile Model II Keyboard Chart — Keystate Mode** | **the code-to-key map, and `AP_KBD_RELEASE` confirmed**  | The other set: indexed by scan code, naming the key. Columns `0`-`7` are the down transitions and `8`-`F` the same keys' up transitions, so **key-up is key-down plus `0x80`** — `AP_KBD_RELEASE 0x80u` and `AP_KBD_KEYS 0x80u` are both this table read as a rule. The key naming is positional: `A0`-`A9`, `B0`-`B15`, `C1`-`C14`, `D1`-`D14`, `E0`-`E13` across the main block, `LA0`-`LF2` for the left function pad, `RA0`-`RF3` for the right, plus a `LED ON` code. **Not every code in `00`-`7F` is a key** — the table has blanks, which this core does not model |
| 131–132 | §6 MAGTAPE CONTROLLER, system configuration pointer and blocks | `none` **(a Multibus device)** | Controller control page `FE8000`, interrupt vector `B3`, MULTIBUS interrupt level 3, IOMAP entries `FFF980`-`FFF9FE`. The System Configuration Pointer at `xxxFF6` chains to a System Configuration Block and then a Channel Control Block with `CCW`, `GATE` and a parameter-block pointer, and the driver's two-instruction start sequence: `MOVE.W #$11FF,GATE` then `MOVE.B #0,$FE80AA` to wake the controller |
| 133 | §6 magtape controller control and status words | `none` **(a device this core does not model)** | The Multibus 9-track magtape controller — chapter 4's module `22`. CONTROL WORD bits `W` bus width, `G` grab bus before tape movement, `S` streaming, `H` 100 ips, `R` reverse, `D` grab bus during DMA, `L` link, `I` interrupt when done, `M` mailbox interrupts, `UU` unit select. STATUS WORD `S` started, `C` completed ok, a 25-value error code, then `O` online, `L` load point, `E` end of tape, `R` ready, `B` formatter busy, `P` write protected. **A third device with the same condition set** as QIC-02 and the boot PROM's tape codes — blank tape, filemark encountered, write attempted on protected tape — in a fourth encoding. Documented and not modelled: no Multibus magtape in the Series 3000 |
| 134 | §6 MULTIBUS DEVICES | `none` **(none of them fitted)** | Nine cards by interrupt level, product code, part number, CSR page and memory-mapped range: Interlan Ethernet, FPS array processor, TITN X25, DOMAIN 3270/3770, two Ciprico tape controllers, Intel SMD, Xylogics FSD, IKON Versatec, Printronix parallel printer. A Series 3000 has no Multibus, so this is an inventory of what the older families carried |
| 135 | §6 REGISTER SET | **Apollo's software register convention** | The 68000 registers drawn out, and two of them are named: **`A5` is `DB`, "Start of Data Section"**, and **`A6` is `SB`, "Top of current Stack Frame"**. That is a software calling convention rather than hardware, and it is worth having: this project hand-disassembles boot PROMs and AEGIS code, where `A5`-relative and `A6`-relative addressing is otherwise unexplained. The SR field map repeats p. 4-25's |
| 136 | §6 TIMERS, **TOUCHPAD**, Xylogics controller | **the Mode 0 absolute packet, and three parameters no figure gives** | Timer and calendar register addresses in two address spaces (`8800`/`FFAC00`), which are the older families' rather than the Series 3000's. Then the **touchpad**: "approximately 30 data points per second **through the same SIO port (zero) as the keyboard**, at a speed of **1200 baud**", four bytes — escape `E8`, then X's low 8, then Y's low bits over X's high 4, then Y's high 8. **Those three coordinate bytes are `008778-03` Figure 13-7's `B2`/`B3`/`B4` unchanged**, so an absolute builder writes the same bytes in Mode 0 and Mode 3 and differs only in what precedes them — which is what `AP_KBD_MOUSE_ESCAPE_ABSOLUTE` was defined for and why nothing used it. And "the range of X and Y coordinates is approximately **30 to 1100**", not 0 to 4095. Transcribed into `ap_kbd.h`; the Mode 3 plan item no longer needs a page render for either mode |
| 137–138 | §6 Xylogics controller IOPB and CSR | `none` **(a Multibus card, not fitted)** | The 24-byte I/O parameter block field by field — command code with `AUD`/`RELO`/`CHEN`/`IEN` flags, ECC code, controller type, error/completion code, interleave factor and throttle, drive type and unit select, head/sector/cylinder addresses, sector count, data transfer address and relocation address, head offset, a next-IOPB pointer for chaining, and four ECC fields. Then the CSR (`GBSY ERR DERR IPND ADRM AREQ AACK DRDY`), the drive status byte (`ONCL DRDY WRPT DPB SKER DFLT`), the eighteen bit-number mnemonics and sixteen command codes (`0` NOP through `F` Buffer Dump, including `9` Rd Drive Stat, `B` Set Drive Size and `C` Self Test), and a twenty-five-line abbreviation key. **Chapter 6 ends here** |

### Chapter 7 — DN300/DN320/DN330, READ END TO END (PDF 139–177, all 39 pages)

Chapters 7–11 are other machine families and were deferred for that reason. The
expectation is confirmations rather than findings, and the first five pages meet
it — recorded anyway, because a walk that skips the pages it expects to be empty
cannot say they were.

| PDF pages | Section | Yield | Notes |
| --- | --- | --- | --- |
| 139–140 | §7 ADDRESS SPACE (DN300/320) and (DN330), CACR, CAAR | `none` **(and one confirmation)** | Two physical-to-virtual maps for machines with an I/O map rather than a DMA page register — `pft` at `4000`, `mmu` at `8000`, `sios` at `8400`, `timers` at `8800`, `ring` at `9800`, `disk, tape, cal` at `9C00`. Then the **68020's** CACR with `CC`, `CE`, `FC`, `EC` at bits 3-0 and CAAR, reached by `MOVEC` from `002` and `802`. `ap_m68030_cache.h` has `CI`, `CEI`, `FI`, `EI` at exactly those bits and `ap_m68030_control.h` the same two register numbers — the 68030 adds the data-cache half above them, and the instruction half is unchanged |
| 141 | §7 CONFIGURATION | `none` | The DN3xx block diagram: CPU and MMU on the SBUS with main memory, boot PROM, SIO, real-time clock and MMU control, then display memory and controller, DMA controller, ring controller and a combined disk/floppy controller. Three SIO channels, keyboard on the first |
| 142 | §7 DISK (FLOPPY/WINCHESTER) CONTROLLER | `none` **(a different controller)** | The DN3xx's combined controller at `9C00`, with ANSI command and parameter registers, sector/cylinder/head, interrupt control, floppy data and control, **and the calendar in the same block** at `+20`-`+24`. Its CONTROLLER STATUS gives sixteen bits each with what resets it. Nothing to do with the Series 3000's OMTI 8620, and worth the row for that reason: the two families' disk controllers share no register |
| 143 | §7 ANSI COMMANDS | `none` | The 8" ANSI drive interface's parameter-out commands — attention control, write control, cylinder address high and low, select head, and the optional set including sectors/track, bytes/sector, read and write permit cylinders, spin control and offset control. The Series 3000's drives are ESDI behind the OMTI, so none of this reaches it |
| 144–145 | §7 ANSI parameter-in commands, attention status, sense bytes, interrupt control, controller commands, floppy control | `none` | Twenty-two parameter-in commands with a `*` marking the time-dependent ones that set attention on completion; ATTENTION STATUS's eight bits each with what clears it; two sense bytes with each bit marked mandatory or optional. The controller's own nine commands run `00` No-op to `08` Select head, and "any command clears the controller status register" |
| 146–147 | §7 DISPLAY CONTROL AND STATUS REGISTER, BLT registers | `none` **(a different display)** | The DN3xx display at `9400`: control and status, `DEB`, window-source and destination X/Y, and the BLT destination counters — `CCCCC = -1 - ABS(WDSY - WDEY)`, a two's-complement line count, with the X counter in 16-bit words and an end-bit register holding `WDEX mod 16`. Nothing in common with the Series 3000's display controller |
| 148–151 | §7 DMA CONTROLLER | `none` **(a different part entirely)** | "DMA controller is a **Motorola M68450**", four channels at `9000`: ring receive header, ring receive data, ring transmit, winchester/floppy — with the channel priority register fixing exactly that order, receive header highest and disk lowest. Every register of the 68450 documented: CSR, CER's eighteen error codes, DCR, OCR, SCR, CCR, MTC, MAR, BTC, BAR, MFCR, CPR, and four marked "not used". The Series 3000 has two i8237s and a DMA page register instead, so this shares no register with `device/ap_i8237.h` — recorded because "the DMA controller" names two different parts across this handbook |
| 152 | §7 FAULT FRAME | `none` **(the 68010's, not the 68030's)** | The DN300/320 bus/address error stack frame: status register, program counter, frame format/VOR, special status word, fault address, then internal registers, data output buffer, data input buffer and instruction register out to `+3A`. That is the 68010's 29-word long frame; `ap_m68030`'s is the 68030's, and the two are laid out differently |
| 153–155 | §7 DN330 bus/address error frames, frame format word, special status words, fault types | **the CPU confirmations, and they are exact** | The DN330's frame-format list is `ap_m68030_frame_format_t` name for name and **word count for word count**: `0000` four-word, `0001` throwaway, `0010` instruction (6 words), `1001` coprocessor mid-instruction (10 words), `1010` medium bus error (16 words), `1011` long bus error (46 words). Format `1000`, the 29-word 68010 frame, is correctly absent from a 68030 model. The DN330's SPECIAL STATUS WORD is `ap_m68030_ssw.h` bit for bit -- FC 15, FB 14, RC 13, RB 12, DF 8, RM 7, RW 6, SIZ 5-4, FCN 2-0 -- including the size encoding `00` longword, `01` byte, `10` word, `11` three-byte. The DN300/320's SSW beside it is the 68010's and is laid out differently, which is why the two pages must not be merged |
| 156 | §7 FAULT VECTORS | **the six-word frame's list, confirmed** | The whole vector table with each vector's frame length and format. `05` Zero Divide, `06` CHK, `07` TRAPV and `09` Trace all take format `2` at length `C` — which is `ap_m68030_exception.c`'s six-word list exactly, plus the 68030-only MMU configuration vector the 68020 has no entry for. Bus and address error take `20,5C` in formats `8,A,B`. The FP vectors run `30` FPBSUN at `0C0` through `36` FPSNAN at `0D8`, which is `AP_M68030_VECTOR_FPCP_BSUN = 48` through `SNAN = 54` in the same order. The DN3xx's own interrupt assignments — SIO 1, keyboard 2, ring 3, display 4, disk/floppy 5, timers 6, parity 7 — are that family's, not the Series 3000's |
| 157–158 | §7 floating-point format and FPCR | `none` | IEEE single and double laid out, and the DN330's Floating Point Control Register: eight exception-enable bits `bsun` through `inex1` in 15-8, rounding precision in 7-6, rounding mode in 5-4, bits 3-0 unused. Chapter 12's delegation already confirmed the 68881's FPCR against this core; this is the same register a second time |
| 159 | §7 Floating Point Status Register (DN330), FPU (DN320) | `none` | The FPSR's four bytes: condition code (negative, zero, infinity, NAN/unordered), quotient byte with sign and seven least significant bits, exception status byte (`bsun` through `inex1`) and accrued exception byte (`iop`, `ovfl`, `unfl`, `dz`, `inex`). Then one sentence on the DN320's FPU: "essentially the same as PEB without the cache (and different microcode)" |
| 160 | §7 MEMORY CONTROL/STATUS REGISTERS | **narrows a `PROVISIONAL` this core has carried** | The DN300/320's MSR at `8006`: `FAILING PPN` in 15-4, then **DMA** at 3, **LEFT** byte parity error at 2, **RIGHT** at 1, traps enabled at 0 — with the control register at `8005` mirroring the lane bits and "writing MSR clears parity error condition". `ap_parity.h`'s open item is which of its four lane bits is which byte, and this is the first document in the project to lay a parity register out. On that 16-bit bus **the more significant byte takes the higher bit number**, which is the opposite of what this core assumes. Different register, different machine, so the value is unchanged — but two of the four candidate orderings are now one with a document behind it. The DMA-cycle bit has no counterpart here and is named rather than assumed away |
| 161 | §7 DN330 Memory Control/Status, LEDs/hardware revision, MMU PID/PRIV | **a four-lane parity register, laid out** | `FAILING A(21:2)` in 31-12, then **"Byte Parity Error Flags"** in 11-8 labelled `3 2 1 0` — so flag `n` is at bit `8 + n` — annotated **"(0 => error)"**, which is the active-low sense this core already carries for its own lane bits. Above them Write Bad Parity, Parity Interrupt Enable, DMA Access and B Port Access. **Two sibling registers now index upward from the least significant byte and this core indexes downward from the most**; see `ap_parity.h`. Then a LEDs/hardware-revision register at `8008`, and the DN300/320 MMU's PID/PRIV register with ASID, domain, enable-PTT-access and enable-MMU bits |
| 162–165 | §7 MMU status, DN330 MMU, and the remaining DN3xx memory registers | `none` **(a custom MMU)** | The MMU Status Register's eight bits — access violation, page fault, bus timeout, normal mode, interrupt pending, PTT access enabled, MMU enabled — and the DN330's PID/PRIV register with ASID, domain, and an **FP Trap** bit "cleared by write to FPU Owner Reg". That last is the same *idea* as this core's `AP_BOARDREG_STATUS_FP_TRAP`, which `019411-A00` gives a dedicated clear location for: an FP trap is a condition with its own acknowledge, not something a status write clears. The MMU itself is Apollo's own, not the 68030's on-chip unit, so none of it reaches `ap_m68030_mmu` |
| 166 | §7 **PROM ENTRY POINTS**, ring registers | **the shape of a PROM jump table** | Eight entries at `100`-`11C`: `getc` returns a char in D1, `putc` prints D1, `init_dsk`, `read_dsk`, `reload_font`, `pollc` "returns char in D1, else -1 in d1.w", `quiet_ret`. This is the DN3xx's table and not the DN3500's, but it is the first time the project has seen one laid out — worth having for the boot-PROM disassembly work. Then the ring page at `9800`: transmit and receive command/status, `TMASK`, diagnostic command/status, ring ID and four ID registers |
| 167 | §7 ring transmit and receive commands, **transmit status** | **a fifth independent source on the acknowledge bits** | Sixteen transmit-status bits — interrupt pending, busy, disconnected, bi-phase error, elastic store buffer error, "no return (a complete pkt frame never arrived)", crc error, ack parity error, external error, "protocol error (the pkt hdr with FROM ID never came back)", `icopy`, `ack byte errbit`, `copy`, `wack`, underrun — and **two worked values**: "a successful transmit will have a transmit status of `0014`" and "a WACK will have `0012`". The first agrees with this core exactly; the second raises a precise question about which acknowledge field the controller's `icopy` mirrors, recorded in `ap_ring_frame.h`. Commands: `6000` starts a normal transmit or receive, `7000` forces a transmit, and "writing anything to this register clears the transmit interrupt" |
| 168 | §7 ring **receive status** and **diagnostic status** | **names the acknowledge field, and closes an escape** | The receive status's four acknowledge bits are glossed "somebody **before me** Intended to COPY / set the error detected bit / did COPY the pkt / WACKed the ptk" — a byte seen going past, and only the *late* field has copied and wait-ack in it. That is what turns the previous page's `0012` from a question into a disagreement; see `ap_ring_frame.h` and the plan item. The diagnostic status adds sticky bi-phase and elastic-store bits, a `connected to the network` bit, and a **9-bit bad-packet counter** that "sticks at 511", interrupts on 255→256, and counts "the number of times this node found an error in a packet going by (regardless of packet target node ID), found the error bit in the ackbyte clear, and so was the first to set the error bit to a one" |
| 169 | §7 ring **diagnostic command** and **TMASK** | **confirms the type field bit for bit, and names bit 0** | Diagnostic command: `dma test` (loop transmit DMA to receive DMA), enable interrupt, **connect** and **disconnect** to the network, delay on and off, and **`0200 snoop` — "accept all pkts but only set ackbyte for packets actually addressed to me"**, which is promiscuous receive with the acknowledge suppressed. Then `TMASK`: `80 broadcast, 40 hardware diagnostic, 20 thank you, 10 please, 08 paging, 04 user, 02 software diagnostic` — `ap_ring_frame.h`'s seven named type bits in exactly those positions — plus **`01 xtype3`**, which `[MAC]` Figure 2-6 calls reserved. And the sentence that explains the field: "**Except for BROADCAST, these bits are software defined.**" |
| 170 | §7 **SERIAL I/O INTERFACE** | **the ACR constraint, stated from the driver's side** | The DN3xx's SIO is "a Signetics SC2681 DUART" — the same part this core models — with the display keyboard on an MC6850 instead. Its register summary is `ap_mc68681`'s map. The finding is the sentence above it: "When both SIO lines are being used, it is possible to have incompatible baud rates due to limitations of the SC2681 chip. One SIO line **can't** have a baud rate from Group A while the other SIO line is set from Group B", with Group A `50, 7200` and Group B `75, 150, 2000, 19.2K`. That is `ACR[7]` described by its consequence, and this core has it structurally right — `acr` is one field of the part and both channels look their rate up against it. The two groups are subsets of the five codes where the tables differ, so the page confirms them without being a second copy. `sio_suite` now asserts that one `ACR` write moves both channels and neither part moves the other |
| 171 | §7 MODE REGISTER A, first and subsequent accesses | `none` **(all four channel modes confirmed)** | `MR1`'s bits/char, parity type and mode, error mode, RxRDY/FFULL select and "drop RTS (OP0) when input FIFO is full"; `MR2`'s stop-bit codes `0111` = 1, `1000` = 1.5, `1111` = 2 — `AP_MC68681_MR2_STOP_ONE` and `_STOP_TWO` — the CTS gate, the RTS drop, and **all four channel modes**: normal, auto echo, local loop, remote loop. This core implements each of the four as behaviour rather than as a stored value, and `ap_mc68681.c` distinguishes local from remote loopback in what they deliver |
| 172–173 | §7 STATUS REGISTER A, **CLOCK SELECT REGISTER A** | **the whole baud table, and a rounding this core carried** | The status register's eight bits with what resets each, then **both baud sets in full, sixteen codes each**. Fifteen agree with `ap_mc68681.c` exactly. **Code `2` is `134.5` baud in both sets**, where this file carried `135` — 0.37% fast, and a number that reads as the datasheet's. The table is in tenths now, `ap_mc68681_baud` rounds it for reporting so nothing a run prints changed, and the clock-pin derivation and a new exact character-time form divide by the tenths. The page also flags one of its own entries — set two's code `A` prints **`1800(??)`**, the document's own query — which this core carries as 1800 with the uncertainty attributed to the source |
| 174 | §7 AUXILIARY CONTROL, INTERRUPT STATUS and MASK registers | `none` | `ACR[3:0]`'s four input-change enables, `ACR[6:4]` counter/timer, and **`ACR[7]` "baud rate generator set select"** — the mechanism p. 7-32 gave the consequence of. The ISR's eight bits are `ap_mc68681.h`'s, with "counter ready **(not used)**" on this board |
| 175–177 | §7 command register, output port, **display keyboard MC6850** | `none` **(a different keyboard part)** | The 68681's command register and output-port config, then the DN3xx's display keyboard on an **MC6850** at `8420`-`8422`: a command register with clock divide, seven word-format codes, RTS-and-transmitter-interrupt control and a receiver-interrupt enable, and a data register. This core's keyboard is on the 2681, so none of it applies — recorded because "the keyboard interface" names two different parts across this handbook. **Chapter 7 ends here** |

### Chapter 8 — DN400/DN420/DN600, READ END TO END (PDF 178–227, all 50 pages)

| PDF pages | Section | Yield | Notes |
| --- | --- | --- | --- |
| 178 | §8 ADDRESS SPACE | `none` | The DN4xx/600 physical-to-virtual map: `pft` at `4000`, `mmu` `8000`, `sios` `8400`, `timers` `8800`, `floppy` `8C00`, `ring 1` `B800`, `ring 2` `BC00`, `disp 1` `F000`, `disp 2` `F400`, `Memory Ctl` `FC00`, two cache windows, `FPU Cmd`/`FPU CS`, `PEB Ctl`, three colour windows and a `multibus` window. A third family with a third layout |
| 179–182 | §8 CONFIGURATION, display and control board jumpers | `none` **(another family's straps)** | The DN4xx block diagram — PHOEBUS bus, block multiplexor, node peripheral adapter, Multibus with line printers, two mag tapes and an SMD controller — then jumper tables for three CRTs and two display boards, and the control board's nine jumpers by revision and line count. Board straps for hardware this core does not model |
| 183–187 | §8 BLT registers, display control and status, DN6xx control and status | `none` **(a different display)** | The DN4xx BLT's source and destination control, start and end registers with their OS virtual addresses, and the mapping used when a BLT names main memory. The `DCSR` bits — `GO`, `NIL` non-interlaced, `FROM MM`, `TO MM`, `IEOF`, `IDONE`, `NON CONF`, `DECR`, `FILL`, `ON`. Then the DN6xx colour display's own control and status: a micro-machine with a control store window, `CLKRUN`/`CLKSTEP` for single-stepping microcode, and a four-bit halt status whose codes include "BLT to main memory out of image memory region" |
| 188–189 | §8 FAULT FRAME, FAULT TYPES, FAULT VECTORS | **the OS's own name for a bus fault record** | The 68000's 14-byte bus/address error frame, and it names the AEGIS type: **`fault_$bus_info_t` in `fault.ins.pas`**, with `.access_address`, `.inst_reg`, `.function_code`, `.write_op` and `.not_inst`. Useful to the crash work — chapter 5 gave `DB`'s `ff` command for printing a fault frame and this gives the record's declared type. The vector table is the DN4xx's own assignment, with `B3` tape controller, `B4` storage module, `B6` line printer, `FB` timers and `FE` "CPU B-to-A" |
| 190 | §8 FLOATING-POINT FORMAT, **FLOPPY CONTROLLER** | **two `ST0` bits this core never named** | The floating-point section defers to chapter 7. The floppy is the µPD765 register set this core's `ap_omti` models, and p. 8-13 lays it out bit by bit. Six of `ST0`'s eight fields match exactly — the four interrupt codes in order, seek-end, equipment check with its "no track 0 after 77 step pulses" gloss, and the unit field. **`ST0[3]` "Set if FDD Not Ready" and `ST0[2]` "Head Address" had no constant and were never set.** The head bit is the one that misled: a read from side 1 returned an `ST0` saying side 0, while the `H` result byte was right all along. See `docs/PROJECT_STATUS.md` |
| 191 | §8 floppy STATUS REGISTERS 1, 2 and 3 | **sharpens the ST3 disagreement without deciding it** | `ST1` and `ST2` match `ap_omti.h` bit for bit, including `ST1[6]` and `ST1[3]` printed as hard zeros. **`ST3` is the open question, printed a third time** — `FT`, `WP`, `RDY`, `TR0`, `TS`, `HD`, `UN1`, `UN0`, all eight live, against `[OMTI]` §6.4.4's three live bits and five constants. What matters is *where*: this is chapter 8, and the DN400/420/600's address space has "Floppy Status", "Floppy I/O" and "Floppy DMA" at `8C00` with **no OMTI board in it**. A bare 765 wired to the drive is a machine where all eight bits genuinely come from the drive. So the handbook is printing the *part's* register — right for the family that has it bare, and again in chapter 12 for the family that has it behind a controller — which makes the two documents complementary rather than contradictory, and is exactly the shape `[OMTI]` describes. An explanation, not a measurement: the item stays open on the same discriminator, a driver that branches on bit 5 |
| 192–195 | §8 I/O MAP, memory control/status registers, memory board jumpers | `none` **(an ECC memory system this core has none of)** | The I/O map holds physical page numbers, "type `ppn_t` in `base.ins.pas`", and "supplies physical addresses for non-CPU memory references (ring/disk, PNA, display, floppy)". Four memory boards each with their own `MCSR`, whose **status register carries an eight-bit ECC syndrome** decoded to the failing data or check bit, plus uncorrectable-error and read-modify-write flags. The Series 3000's memory is parity-checked, not ECC, so none of it applies -- recorded because "memory status register" names two quite different things across this handbook. Then the boards' jumper tables by size and address range |
| 196–198 | §8 MEMORY MANAGEMENT UNIT, PID/PRIV, CPU A control, MMU status, bus status | `none` **(Apollo's own MMU)** | The page frame table and page translation table drawn as a diagram, then the registers: a four-value **domain** field ("00 user domain 0 (least protected)" to "11 supervisor domain 1 (most protected)"), an MMU status register with `M` PFT miss, `A` access fault, the function code, `R`, `V` and "miss occurred while CPU B running", and a bus status register naming the **power-off and service switches** and a "Big PFT (2K entries if not set, 4K if set)" bit. A dual-CPU machine with a custom MMU; the 68030's on-chip unit shares none of it |
| 199–200 | §8 MONITOR TIMING (DN6xx), PFTE and PTTE | `none` | A full video timing table for 800- and 1024-line monitors: `1H` 31.2 µs at 32.05 kHz, horizontal front porch 0.7, sync 3.0, back porch 3.5, blanking 7.2 and display 24.0 µs; vertically 13.2 ms at 75.7 Hz or 16.7 ms at 60.0 Hz. The page-table entry formats defer to chapter 7 |
| 201 | §8 **PEB**, the Performance Enhancement Board | `none` **(chapter 4's module 24, and not this machine's FPU)** | A microcoded floating-point board with a writable control store: `peb_ctrl` and `peb_status` at `FF7000`, FPU control store at `FF7800`/`FF7C00`, 1K and 4K WCS sizes, default microwords, a WCS error interrupt vector and an FPU exception vector, then a table of entry addresses per operation — SP add, subtract, reverse subtract, multiply, divide, reverse divide, then the DP forms. This is what chapter 4's module `24` reports on (`fpu is hung`, `wcs verify failed`); a DN3500 has a 68881 and none of it |
| 202–205 | §8 PEB operation addresses, control register bits | `none` | The rest of the PEB's entry-address table — DP reverse subtract through reverse divide, the internal registers `UR0`-`URF`, the exception register, the integer accumulator, min/max, polynomial evaluation, multiply-accumulate — and a block "only available on the 4K control store 'FPU' used on the DN3XX-DN5XX": truncate, log, exp, sqrt, power, sin, cos, tan, atan and atan2, two of which the page marks as having bugs "not used by the library". Then the PEB control register's bits and a table of useful combinations for enabling the caches |
| 206 | §8 PEB status bits and **FPP commands** | `none` | `peb_fpu_cspe` control-store parity error, `peb_cache_pe`, `peb_fpu_xip` exception pending, `peb_fpu_upc` program counter, `peb_fpu_busy`. Then the FPP command set "from `fpp.ins.pas`" — load, store, add, subtract, inverse subtract, multiply, divide, inverse divide, compare and float/fix, each in value, address and constant forms. A software interface to a coprocessor board, not a register map |
| 207–208 | §8 FPP double-precision and complex commands | `none` | The rest of `fpp.ins.pas`'s interface — double precision load, store, arithmetic and compare, the float and fix forms, transcendentals, min and max, then single and double **complex** FAC operations including conjugate and real/imaginary exchange. A software calling interface, not a register map |
| 209 | §8 PEB complex operations, **RING/DISK controller** | `none` **(one board, both functions)** | The DP complex FAC operations, then the DN4xx's combined ring-and-disk controller at `BC00` — "today's Controllers are #2's". One register block carries both: cylinder MSB/LSB, disk command and status, head, sector, controller command, then **network type mask, receive command, transmit command** with receive and transmit status on the read side, three **node ID** reads, and separate interrupt acknowledges for disk, network transmit and network receive. The same *functions* as the DN3xx's ring page at `9800`, in a different layout and sharing a board with the disk |
| 210–211 | §8 ring/disk **DMA registers**, cylinder address | **settles a slip recorded ten chapters earlier** | Two 8237s on the board, one for disk and transmit and one for receive, and **each prints the whole write map**: command, request, single mask, mode, **clear byte pointer**, **master clear**, all masks — at that block's stride of two, offsets `8` through `F` with `E` unused. `ap_i8237_reg_t` position for position. **This settles p. 12-24's "Clear Byte Pointer `[900D]`"**, which the chapter 12 walk recorded as a slip because `D` is the part's master clear: the handbook prints the pair correctly *twice on consecutive pages here*, so it is one page of the handbook against two others rather than the part's manual against the handbook |
| 212 | §8 **PRIAM command and status registers** | `none` | The 14" PRIAM drive's own interface: commands `Sequence Up`, `Sequence Down`, `Restore`, `Seek`, `Fault Reset`, and a status register with `READY` ("the drive is up to speed, servo is locked on track"), `SEEKOK`, `SEEKFT`, `CYL 0`, `BUSY`, `DRVFLT`, `WRITPT` and `CMDRJT`. The Series 3000's drives are ESDI behind the OMTI, so this is the interface one generation back |
| 213–214 | §8 head/drive and sector select, disk controller command and status, **Packet Type** | **the type mask's matching rule, which no page had given** | The drive and head selects in one-hot-ish codes, sector select, and a disk command register of `READ`/`WRITE`/`FORMAT` plus an interrupt enable. The status register decodes to a table of seven outcomes — controller busy, PRIAM not ready, time out-or-sector error, DMA bus error, DMA over run, CRC error, disk operation ok — with `TIMOUT` glossed as "the last disk operation didn't finish before 3 revolutions of the disk". Then the ring's **Packet Type** register: `BRDCST` at bit 15 and `T6-T0` below it, with the rule p. 7-31's `TMASK` did not state — "**accept message if any bits in type field of message are set in corresponding bit of type mask**". That is an intersection test, not equality, and **this core has no type filter at all**: `ap_ring_station` accepts on broadcast-or-address alone. Named as a plan item rather than implemented, because both pages describe other controller generations |
| 215–216 | §8 ring **receive command** and **receive status** | **a published duration for a timeout nobody sets** | The receive command's `REC`, `STOP` ("abort a previously posted REC ... successful if the controller has not already seen a message begin"), `CON`, `DIS` ("this also happens on reset") and `INTE`. Then the receive status, and its `TIMOUT` is glossed with a number: "the last message received started but didn't finish in **2\*\*12 byte times**" — 4096, which is exactly `AP_RING_DATA_MAX_BYTES`, so the controller waits the longest a legal packet could take and then gives up. **This core defines three timeout bits and sets none of them**: `AP_RING_CTL_STATUS_TMO`, `AP_RING_CTL_XMIT_TMO` and `AP_RING_CTL_RCV_PE` appear in the header and in no `.c` at all. Named as a plan item; the figure is another generation's but it is the only published one |
| 217 | §8 receive status error causes, status decode table | `none` | The transmitter-side and receiver-side error lists that `PKTERR` summarises — elastic store, bi-phase, DMA overrun and bus error on both sides, plus `ACKPAR`, `SFTABT` software abort and **`NCOPY`** on the transmitter's, and `EORERR`/`CRCERR` on the receiver's. Then a decode table giving the bit pattern for each of eleven outcomes down to "Received OK" |
| 218 | §8 ring **transmit command** | **the token's length, from a second source** | `TMT`, `STOP`, `FTRANS` force-transmit "even though no token has been seen", `CH1DIS` to make a zero-length data field, `INTE`, and **`DELAY` — "Enable an additional 7 bit delay into the length of the network. This may be required to support the recirculation of the token, which is 9 bits."** `ap_ring_medium.h` already models a short ring needing cable to hold its own nine-bit token; this confirms the nine bits independently and shows the hardware treated the case as real enough to give the controller a switch for it |
| 219–220 | §8 ring **transmit status**, its error causes, and the **Node ID register** | **reopens an escape the previous page seemed to close** | Sixteen bits including `WACK` at 2, `COPY` at 3 and a separate `NCOPY` at 4 — and p. 8-43 defines all three: `MSGCPY` "the receiver successfully copied the message", `WACK` "**a receiver observed his node id, but wasn't enabled to copy**", `NCOPY` "no receiver observed his node address and/or was enabled to copy". So the DN4xx splits them by *address*, not by wanting-versus-able, `WACK` is Figure 2-8's wording exactly, and the register has **no `icopy` bit**, so it bears on the open question neither way. It also shows a status register gathering from both acknowledge fields — `PKTERR`'s causes come from the transmitter's side *and* the receiver's — which reopens the possibility that the DN3xx's `icopy` is the *early* field's. The two readings are level again. Then the **Node ID register**, 24 bits across three words at `BC0A`/`BC0C`/`BC0E` |
| 221–223 | §8 **DMA control/status registers** | **confirms `ap_i8237` register for register** | The command register bit for bit — `DACK Sense Active High`, `DREQ Sense Active Low`, `Extended Write`, `Rotating Priority`, `Compressed Timing`, `Controller Disable`, `Channel 0 Address Hold`, `Memory to Memory` — which is `AP_I8237_CMD_*` in exactly those positions. Then mode (`00` demand through `11` cascade, address decrement, autoinitialise, the four transfer types with `11` undefined, channel select), request, mask, all-mask, and a status register of "channel *n* Request" in 7-4 over "channel *n* has Reached TC" in 3-0 — which is `ap_i8237.c`'s live-request-over-latched-TC composition exactly. Two board details belong to the DN4xx and not the part: "notice that the address is shifted right by 1" and "C(15-00) is the desired count in **words minus 1**", which is how a 16-bit channel is wired. The whole block is at even offsets, so that board's DMA is stride 2 as this one's second controller is |
| 224 | §8 **SERIAL I/O INTERFACE**, display speaker | **134.5 baud, a third time** | Four serial lines as Data/Control-Status pairs with a separate bit-rate generator each — an ACIA-style part, not the 2681 — and a sixteen-entry rate table whose code `0011` is **`134.5`**. That is the third document in this walk to print the rate as 134.5, after p. 7-34 and the 68681's own table; `ap_mc68681` carried 135 until this session. Then a fact with no counterpart here: "**SIO line 0 is wired to the Display Speaker** - asserting RTS will emit a constant sound - toggling DTR with RTS set emits different tones". The DN3000's beeper is on the keyboard instead, with the command sequence chapter 12 gave |
| 225–227 | §8 **SIO write and read control/status registers** | `none` **(a Zilog SIO, not the 2681)** | Write registers 0 through 7 and read registers 0 through 2 of a Z80-SIO-family part: WR0's register-select and command codes (`Send Abort`, `Channel Reset`, `Reset Tx Underrun/EOM Latch`, `Return from Interrupt` on channel A only), WR1's interrupt modes including "Int. on all Rx Chars (Parity affects vector)", WR2 as the interrupt vector on **channel B only**, WR3's receive width and hunt phase, WR4's clock multiplier and SDLC sync modes, WR5's transmit width with DTR and RTS, WR6/WR7's sync characters, and RR0/RR1's status with the SDLC residue table. A synchronous-capable part with CRC generators and SDLC framing; the 2681 this core models has none of it. **Chapter 8 ends here** |

**Chapter 8's pattern**: fifty pages, almost all of it hardware this family has and
ours does not — a PEB, a BLT display, an ECC memory system, a custom MMU, a Zilog
SIO, PRIAM drives. Its value was concentrated in the pages describing parts the
two families *share*: the floppy controller's `ST0` (two bits nobody had named),
the 8237 (confirmed register for register, and settling a byte-pointer address
chapter 12 misprints), and the ring (a type-mask matching rule, a published
timeout, and a correction to an inference of mine about the acknowledge bits).

### Chapter 9 — DN460/DN660/DSP160, READ END TO END (PDF 228–241, all 14 pages)

| PDF pages | Section | Yield | Notes |
| --- | --- | --- | --- |
| 228–229 | §9 ADDRESS SPACE, BLT registers, CACHE, **CPU CONTROL REGISTERS** | `none` **(a microcoded machine, not a 68k)** | An address space with a `sequencer`, a `wcs`, a `decoder i/o` and four `i_cache` windows. "MD commands (`DC`, `IC`) are used to turn data and instruction caches on and off. Machine must be in physical mode to toggle the instruction cache." Then the CPU control registers, and the machine is not a 68030 at all: "DNx60 is a **micro-coded machine** with a forward-mapped address translation mechanism (using in-memory page tables). There is no PTT or PFT." Its control registers are reached by `MOVEC` — current region registers `120`-`13F`, current ASID `11F`, floating-point ASID `11E`, a purge-translation-buffer register `11D` with four purge scopes, and `119` "Address Translation and Data Cache" with enable bits and a domain bit |
| 230–231 | §9 CONFIGURATION, **control panel (CPIO) registers** | `none` **(and the Normal/Service switch a third time)** | A block diagram with two CPUs and a **CPIO** control processor on a PHOEBUS bus. The CPIO's control register enables the power-down switch, halts the CPIO and hands bus mastership; its status register reports "`C` - 0 => This is CPIO running, 1 => This is CPU running", bus timeout, interrupt pending, and **"`N` - Normal mode, 0 => Service Mode"** — the same switch and the same polarity as `AP_BOARDREG_STATUS_NORMAL_MODE`, now seen on a third machine. A separate **LED register** at `8004` holds the diagnostic display, where the DN3000's is the control register's upper byte |
| 232 | §9 **micro machine control registers** | `none` **(none of them have addresses)** | "None of these registers have mapped addresses" — a WCS control register, decode RAMs, a microsequencer and a CPIO/microexec port reached some other way. Its control and status byte has a read-only half (micro machine frozen, in a trap routine, traps enabled, fetching, running-or-frozen, data available in each direction) and a writable half that can macro-step, micro-step and **clock-step** the machine. A microcoded processor with a debugger in hardware |
| 233–234 | §9 display jumpers and DCSR (delegated), **FAULT FRAME**, FAULT TYPES | `none` | Two sections defer to chapter 8. The long fault frame is the DNx60's own — status register, PC, a vector offset with a `0110` format nibble, the faulting address and an `'0BAD' SIGNAL` word — and the page says which parts are borrowed: "CPIO fault frames are the same as DN300 (68010). CPU short fault frames are also the same as DN300." The fault-type groups repeat chapter 7's |
| 235–236 | §9 **FAULT VECTORS**, floating-point registers | `none` **(confirms the FP vector order once more)** | The DNx60's table, with its own `80`-`84` region, segment and page faults, access violation and a **floating-point ASID trap**, and `B3`-`B7` for the tape controller, storage module, line printer and parallel input. Its floating-point vectors run `31` inexact, `32` divide by zero, `33` underflow, `34` operand error, `35` overflow — the same order as p. 7-18's and as `AP_M68030_VECTOR_FPCP_*`, one slot short because this machine has no signalling-NaN vector. Then a Floating-Point Control Register with only the six exception enables and no rounding fields, which is not the 68881's |
| 237–239 | §9 floating-point status, floppy (delegated), **MCSR**, memory board jumpers | `none` | The FPSR's condition-code and exception-status bytes, then a memory system with "**64 possible memory control register addresses**" of which "only one board may respond" per `$100` range, "at intervals of `$20`", for old, new, interleaved and non-interleaved boards — up to four. Then twenty-eight jumper positions across four board configurations, and the board's own layout drawing |
| 240 | §9 **MEMORY MANAGEMENT UNIT**, region registers and segment maps | `none` **(and AEGIS's own type names)** | `RARS: ARRAY[0..26,0..31] OF RAR_T` and `SMAPS: ARRAY[0..26,0..7680] OF SMAPE_T`, with the declared types named — "`rar_t` in `vm.ins.pas`", "`smape_t` in `vm.ins.pas`" — and the structures: a region register holding a valid bit, a global bit and the physical address of a segment map, each covering 8 Mb of virtual address; a segment map entry holding valid, access rights and the physical address of a page map. Thirty-two hardware region registers. Useful beside chapter 5's `DB` commands for the same structures; the hardware is not this machine's |
| 241 | §9 monitor timing and ring/disk (delegated), **SERIAL I/O INTERFACE** | **confirms this core's DUART arrangement from a third machine** | "DNx60 uses **two SC2681 chips providing four SIO lines**" — the same part and the same count this core models — and the Group A/Group B constraint restated word for word from p. 7-32, with the same two rate lists. Then the pairing: "**Lines 0 (the keyboard, which runs at 1200 baud) and 1 are paired on one chip, and lines 2 and 3 are on the other chip**", which is `AP_SIO_KEYBOARD_PORT`/`_CHANNEL` and the 1200 baud `sio_suite` asserts. Three families, one arrangement |

### Chapter 10 — DN5xx, started (PDF 242–268)

| PDF pages | Section | Yield | Notes |
| --- | --- | --- | --- |
| 242 | §10 opening pointer, ADDRESS SPACE (DN550) | **an order number for a document class this project needs** | "FOR INFORMATION SPECIFIC TO THE DN5xx-T NODES, REFER TO THE **DN570-T/DN580-T/DSP500-T HARDWARE ARCHITECTURE HANDBOOK (009490)**." So Apollo published a hardware architecture handbook **per family**, each with its own order number — which makes `019411-A00`'s missing base volume a member of a numbered series rather than a one-off, and turns `ap_atbus.h`'s "search for the handbook" into "search for an order number". Checked: bitsavers holds `009492-00` and `009496-00` and **not** `009490`, so the sibling is missing too. Then the DN550's address space, a fourth layout |
| 243 | §10 ADDRESS SPACE (DN560/570/580) delegated, **CONFIGURATION** | `none` **(a VME machine)** | The DN560/570/580 address space defers to chapter 7's DN330. The block diagram is the first in this handbook built around a **VME bus** — a CPU with an SBUS and a B-PORT, expansion memory, a VME interface, and behind it a VME-to-ring board, a VME disk/tape controller and a VME-to-Multibus bridge, with "slots for Apollo or user supplied devices". Two SIO lines, video and keyboard hang off the display bus instead |
| 244–247 | §10 CPU, display, array and control board jumpers | `none` **(board straps and part numbers)** | Jumper placement drawings and settings for the DN550 CPU (APNs 4141, 4145), the DN560/570/580 CPU (APN 005373), the DN550/560 array and colour-controller boards, the DN570 display controller (APN 005710) and the DN580's pair (APNs 005209, 005213). One setting is legible enough to be worth a line: the array board's `W1` is a **timing delay** — "Right = Normal, Left = 3 nSec delay" — and `W2` selects board 1 or 2 |
| 248 | §10 DN550/560 and **DN570 display registers** | `none` **(a draw processor, not a frame buffer)** | The DN550/560's six display and colour windows, with the note "**display controller is a 600 with microcode changes**". Then the DN570's user control register, which is not a display register at all in this core's sense: `DR` reset, `ST` stop, `DC` continue "from last PC count", `DS` start — a **draw processor** with a program counter — plus a trap enable, a memory reset, a zoom-by-2 factor and an error handshake. The DN3000's display has a BLT and a LUT and no processor |
| 249–250 | §10 DN570 system control, DN580 control and status, **disk/tape/calendar controller** | **Apollo's own word for `RETENSION`** | The DN570's system control register repeats the draw-processor controls beside a context ID; the DN580's adds a FIFO-ready bit, horizontal and vertical blank, and a context-switch handshake. Then the DN5xx's combined controller, whose tape commands are a host-level set over QIC-02: four `Select tape drive` opcodes, read status, rewind, erase, **`17 Initialize (retension) tape`**, write and read blocks, `Write <count> filemarks`, space forward and reverse by blocks and by filemarks — with the two *reverse* forms marked "**(not implemented)**", which is Apollo declaring which optional QIC-02 commands its controller declined. The parenthesis in command `17` is a third source equating `INITIALIZATION` with `RETENSION`, after `QIC-02` §2's definition and `[SC499]`'s naming |
| 251 | §10 controller commands, **controller status**, disk status | **decodes chapter 4's boot-error table structurally** | The controller status register's bits are, one for one, p. 4-17's Disk/Tape status codes `11`-`1F`: `0002` controller diagnostic failed is code `11`, `0004` controller timeout is `12`, `0008` illegal controller command is `13`, `0020` memory parity during DMA is `15`, `0040` DMA overrun/underrun is `16`, `0080` DMA not at end of range is `17`. **So the boot PROM's `1n` is the bit number of this register**, and its `2n` is the disk-status byte below — whose values `01` seek did not complete through `0D` recovered overrun are p. 4-17's `21` through `2D` less `0x20`. Chapter 4 gave that table as a flat list of codes; this is its structure |
| 252 | §10 the controller's IOPB | `none` | Block and drive counts, header and data word counts with their ending counts "for last two blocks transferred", per-drive geometry (cylinders, heads, sectors, precompensation cylinder), a controller ID, ECC and underrun retry counters and an ECC offset and mask. A parameter block, not a register map |
| 253 | §10 **tape status from drive**, and the QIC-02 summary a fourth time | **confirms this session's `NDT` rows, and shows a misprint is not a slip** | `TAPE ST 0`/`TAPE ST 1` laid out bit by bit — `CNI`, `USL`, `WRP`, `EOM`, `UDA`, `BNL`, `FIL` over `ILL`, `NDT`, `MBD`, `BOM`, the two reserved bits and `POR` — which is `ap_qic.h` name for name. Then a fifteen-row **STATUS SUMMARY** that agrees with `QIC-02 Rev D` §5.3 cell for cell, **including the three rows this session's `NDT` fix rests on**: "Read error, no data" `100X0110`/`10100000`, "and EOM" `100X1110`/`10100000`, "and BOM" `100X0110`/`101X1XX0`. Four documents, one table. **And it reproduces p. 12-5's misprint** — "Marginal block detected" again prints Status 1 as `00010000` with the summary bit clear — so that is an error in the house table both chapters were set from rather than a dropped digit on one page. Recorded in `qic_suite.c`: a third copy would be expected to carry it too, and finding it there is not corroboration |
| 254–255 | §10 diagnostic error fields, the 32-entry transfer list, calendar and **VME address modifiers**, fault frame and types (delegated) | `none` | Three diagnostic words — "location of diagnostic failure", "what data should have been", "what data was" — then a 32-entry array of cylinder/head/sector, header address and data PPN with per-entry continue-on-error, interrupt and write flags. The VME address modifier register names the six function codes and three addressing widths, with `LW = 1 => use 32-bit transfers`. Fault frame and fault types defer to chapter 7 |
| 256 | §10 **FAULT VECTORS** | **the PROM service interface, and the FP vectors a third time** | The interrupt levels are the DN3xx's assignment again — SIO 1, display keyboard 2, PEB 3, VME 4, VME bus error 5, timers 6, parity error 7 — and the floating-point vectors are the full seven, `30` FPBSUN through `36` FPSNAN, matching p. 7-18 and `AP_M68030_VECTOR_FPCP_*` exactly for the third time. **The interesting part is vectors `41`-`4B`**: `getc`, `putc`, `init_dsk`, `read_dsk`, `reload_font`, `pollc`, `quiet_ret`, then **`write_dsk`, `log_error`, `crash`, `led_update`**. p. 7-28 gave the DN3xx the first seven of those as an inline jump table at `100`; this family has eleven, as vectors. So Apollo's PROM service interface is a stable set that grew, and `crash` and `led_update` are named entries — worth having when disassembling a boot PROM, which is a route this project has used three times |
| 257–258 | §10 VME pseudo-vectors, five delegated sections, DN550 memory jumpers | `none` | Six VME pseudo-vectors — ring, disktape, display, pbu — then floating-point format, floating-point registers, FPU, MCSR and MMU all deferring to chapter 7. Three memory jumpers whose settings name Apollo part numbers for a fully and a half populated board |
| 259–260 | §10 **MONITOR TIMING** (DN550/560, and **DN570**) | **confirms this core's colour raster, figure for figure** | Two tables in chapter 8's format. The DN550/560's is a 19-inch Hitachi or Ikegami at 33.855 kHz and 78.4 Hz. **The DN570's is headed "15-inch and 19-inch Panasonic" and is `ap_graphics.h`'s colour raster to the digit** — `1H` 19.79 µs at 50.519 kHz, H porches 0.942 and 1.88 either side of a 1.88 µs sync, 4.71 µs blanking, 15.08 µs display; `1V` 16.67 ms at 60.0 Hz, V porches 79.18 and 673.0, 831.0 µs blanking, 15.839 ms display. That derivation converts microseconds to pixels and needs two columns to close, so a second printing of all ten figures — for a different machine, under the monitor's own make — says they are the *display's* numbers rather than one chapter's, and that the two families drove the same tube |
| 261–263 | §10 MONITOR TIMING (DN580), the raster diagram, **MULTIBUS registers**, PFTE/PTTE (delegated) | `none` | A 19-inch Sony at 63.357 kHz and 59.99 Hz — a fourth monitor table — then the raster drawn as a figure, and the Multibus control/status register with a watchdog timer, a time-out bit "when MULTIBUS master talking to another MULTIBUS device", loopback and Xchange diagnostic enables, upper and lower Multibus enables, a multiport memory lock and `MULTIBUS INIT (asserted when 0)` |
| 264 | §10 **PROM ENTRY POINTS** | **the map three PROM disassemblies here did without** | Eleven entries at `104`-`12C` — `getc`, `putc`, `init_dsk`, `read_dsk`, `reload_font`, `pollc`, `quiet_ret`, `write_disk`, `log_error`, **`crash`**, **`led_update`** — under `100: dc.w 5,0` annotated "**5 => stingray**", where p. 7-28's DN3xx table reads `dc.w 2,0`, "2 => swallow". So the word at `100` is a **machine-type code** and what follows is a service table. **Checked against `roms/firmware/`: every boot PROM here has it**, thirteen entries and a zero terminator, every address inside its own image, and `quiet_ret` at `0005D8` identical across three different PROMs. Type codes: DN3000 `8`, DN3500 `7`, DS4500 `7` (aux `15` against the DN3500's `7`), DN2500 `9`. The DN3500's whole table is in `docs/PROJECT_STATUS.md` |
| 265 | §10 **RING REGISTERS** | **one footnote the DN3xx's identical table lacks** | The same page layout as p. 7-28 — transmit and receive command over status, `TMASK`, diagnostic command over status, ring ID, and four ID reads — with one addition: the `RING ID` read is annotated "**(\*) gate_array only. must be written after a reset**". So on a gate-array controller the node identity is a register software reloads rather than a PROM the board reads, and this core's controller *is* a gate-array one. Recorded in `ap_ring_ctl.c` and not acted on: this machine's node ID comes from battery RAM and the `011200` ROM window rather than from the ring board, and the difference is unobservable here — a driver that must rewrite the ID rewrites it and finds the same value |
| 266–268 | §10 ring command and status registers, **VME registers** | `none` | The ring transmit, receive and diagnostic registers repeat chapter 7's bit for bit. Then the VME side: an address modifier register with separate transmit and receive fields naming the six function codes and three addressing widths, a **memory size register** encoding 0.5 to 3.0 Mb in three bits with a power-off enable, and diagnostic control registers with a loopback bit, an arbitration-level field and per-line bus-request and interrupt-request masks |

**Chapter 6's pattern, worth stating**: it is a *cross-machine* peripheral
reference, so most of its pages describe hardware the Series 3000 does not have
— Multibus cards, the I/O map, the magtape and Xylogics controllers, two
earlier keyboards. Its value came from the four pages that do overlap, and in
every case from **comparison against a second document**: the AT bus table
against `008778-03`'s Ethernet chapter, the disk tables against the drive
specifications, the touchpad against Figure 13-7. Two of the three apparent
disagreements it raised dissolved into different conventions; one is real and
was kept on the part manual's side.


| 78 | §4 module `1B` tail, `1C` vfmt, `1D` circular buffer, `1E` **pbu manager** | `none` **+ one line that names a machine state this project has hit** | Startup/shutdown ends with two codes that are instructions rather than diagnoses — `no os paging file -- please run invol option 8` and **`no calendar on system -- please boot over network`**. The second is the state this project's calendar work is about from the other side: a machine whose RTC cannot be read is told to boot over the network rather than from its disk, which is what a dead battery looks like to a user. Then `vfmt`'s format-string errors and the **pbu manager**'s sixty-odd — the GPIO layer, and the codes are a device driver's whole vocabulary: `csr page is in use`, `interrupt routine not in library`, `reference to csr page caused bus timeout` |
| 79 | §4 module `1E` (cont.), `1F` line printer, `20` OS info, `21` **badspot manager** | `none` **(and the badspot list from the software side)** | The pbu manager's remaining codes, including `trap 6 executed outside of interrupt routine` — which is chapter 1 p. 1-23's `TRAP #6`, "SVC from GPIO interrupt routines", refusing to be used anywhere else. Then the **badspot manager**'s seven: bad checksum, bad count and **"missing minus-one"** in a physical badspot block, list too small, "no physical badspot blocks read or written", "partially read or written", duplicate entry. These are the failures of the structures chapter 2 laid out — `.phys_badspot_daddr` in the physical label at p. 2-20 and the bad spot list in the logical one at p. 2-19 — so the two chapters again describe one thing from its layout and from its failures |

| 76 | §4 modules `13` display driver, `14` **volume manager**, `15` calendar | `none` **+ the warning convention shown working** | Forty display-driver codes, then the volume manager's six — and the first of them is `(0014**FFFF**) Warning: disk is write protected`, which is p. 4-1's "negative = warning" in practice rather than in prose. The rest bear on this project's volume work: "unable to dismount the boot volume", "logical volume is not mounted", and **"physical volume replaced since mount"**. The calendar manager's five are all date parsing, including **"timezone specified is unknown"** — the field p. 2-19 puts in the logical volume label as a UTC delta and a timezone name |
| 77 | §4 modules `16`–`1B`, ending at **startup/shutdown** | **names the code behind a recorded finding** | The cross-process debug manager (which is what `DB` talks to), deferred execution, the level-2 eventcount and process managers, import/export — and then module `1B`, startup/shutdown, whose first code is **`(001B0001) node ID mismatch`**. That is almost certainly the code behind this project's recorded result that "a volume recording node `22222` ran on a machine calling itself `12345` and Domain/OS declined to run as a node its disk contradicts": the refusal has a name and a module. Recorded rather than claimed — nothing here shows that *this* code was the one emitted, only that it exists and says exactly that |

| 74 | §4 module `11` the **network**, module `12` the **fault handler** | **identifies `CRASH_STATUS`'s module** + `none` | Thirty-two network codes — "remote node failed to respond to request", "too many transmit retries", "receive bus error", "memory parity error during transmit", "conflict with another node listing", and **"no nodeid prom on this system"**, which is the condition `board/ap_nodeid` models the presence of. Then module `12`, "**OS / fault handler**" — and that is `CRASH_STATUS 00120020`'s module. Its first codes are the processor's own: odd address error, illegal instruction, integer divide by zero, CHK, arithmetic overflow, privileged instruction violation, undefined TRAP, unimplemented instruction, bus time-out, invalid user stack pointer, access violation |
| 75 | §4 module `12` (cont.) — **and the code this project has carried** | **KILLS A LEAD, and settles the decode** | `(00120020) supervisor fault while resource lock(s) set` — printed, word for word as this project has recorded it. **And now decomposed**: module `0x12` is the fault handler, error `0x20` is its thirty-second code. **So the chapter-1 lead is dead.** p. 1-20's resource lock table gives `flp_$lock` `100000`, `ast_$lock` `20000` and `file_$lock_lock` `20`, which sum to `120020` exactly — and that is a **coincidence**, made seductive by the message's own text being about locks. `CRASH_STATUS` is an ordinary AEGIS status word and its low half is an error number, not a lock bitmask. Carried as "suggestive, not settled" for exactly this reason; settling it required the module table rather than more arithmetic. Also here: the 68881's exceptions as OS faults at `22`-`26`, `2E` and `2F`, `ac power failure`, and `unexpected bus error during system initialization` |

| 73 | §4 module `0E`, the **name server**, and `0F` the file server | **CONFIRMS `E0007` FROM THE DOCUMENT** | `(000E0007) name not found` — printed. This project established that reading by measurement: reproducing the crash and reading the naming server's own code, recorded in memory as `e0007-name-not-found`. Here it is in Apollo's table, and the module is named by its company — `not a link`, `invalid link operation`, `bad directory`, `directory not found in pathname`, `name server helpers clocks are skewed`, `cant find name server helper`, `cache entry is stale`. So module `0E` is the **name server**, and `E0007` is its seventh error. A measured conclusion and a documentary one meeting, which is the strongest position an entry in that memory can be in |

### Chapter 3 — programming information, read end to end (PDF 59–68, 10 pages)

| Pages | Section | Status | What it yielded |
| --- | --- | --- | --- |
| 61 | §3 **Virtual memory** map | `none` **(and it hands our machine on again)** | Two columns, reverse-mapped machines and the DNx60: trap page, PROM, user process private data, a **guard segment** either side of the user stack, PM static data, global libraries, per-process supervisor space, unwired stacks, AEGIS, the display bitmaps and the I/O control pages — with the **GLOBAL A** and **GLOBAL B** boundaries marked and the private user-mode and supervisor-mode regions named. Ends "refer to the appropriate node chapter ... for forward mapping machines", so like p. 3-2 it excludes the modelled machine; chapter 12's `[physical \| virtual]` pairs are the DN3000's answer and are read. The guard segments are the detail worth keeping: a stack with an unmapped page at each end is why a runaway stack faults instead of quietly overwriting its neighbour |
| 62 | §3 **CALLING SEQUENCE — PIC** | `none` **(and the most useful page in the chapter for reading Domain code)** | The Domain calling convention, both sides. The caller `PEA`s each argument's **address** last-to-first, loads the entry point from an ECB into `A0`, `JSR (A0)`, then pops `4*n`. The callee `LINK SB,#autosize`, `MOVEM.L reg_save_mask,-(SP)`, `MOVE.L A0,DB` — so `A0` arrives holding the data base and `SB` is the stack base — and reaches its first argument at `8(SB)`. Return unwinds in the same order. And the rule: "**Registers other than D0, D1, A0, A1 are preserved**". Nothing here is hardware, but this session disassembled three firmware routines by hand and this is the shape they are written in; it is recorded for the next person who has to read Domain code out of a ROM or a dump |

| 63–64 | §3 **CONDITION CODES**, **CONDITIONAL TESTS** | `none` **(reprints, and they say so)** | Both footnoted "Reprinted from MC68000, page A-4" — the per-instruction condition-code effects with their special definitions, and the sixteen `cc` encodings with their boolean tests. Motorola tables this shelf already holds, recorded as reprints rather than counted as independent corroboration |
| 65–66 | §3 **FILENAME SUFFIXES**, **PATHNAME SYNTAX** | `none` **(reference for reading a volume)** | The suffix conventions by tool, and — more useful — the **pathname symbols**: `//` network root, `/` node entry directory, `~` naming directory, `\` parent, `.` or none the working directory, and `` `node_data `` for `/sys/node_data[.nn]`. Legal name characters are `A-Z a-z 0-9 $ _ .`, and "these conventions are not requirements; the operating system does not check a file's contents against its name" |
| 67 | §3 **STACK FRAME** | `none` **(and it completes p. 3-4)** | The frame p. 3-4's `LINK` builds: `SB+00` the caller's SB, then optional **FCW pointers**, the return PC, and the arguments from `+08`. The trick worth keeping — "non-nil FCWs are distinguished from return PCs by the fact that they are **odd addresses**". An FCW is a type and a register mask over an offset from SB to the save area, and its type field names the FPU: `0` unknown, **`1` 68881 floating point registers**, `2` DNx60. So a frame walker learns from the frame itself which floating-point file was saved, and this machine's is type 1 |
| 68 | §3 **STATUS WORD**, `status_t` | **decodes a code this project has chased** | Four fields in one longword: bit 31 `F`, "module couldn't handle error"; bits 30-24 the **subsystem**; bit 23 `A`, "asynchronous fault; only set during delivery of fault"; bits 22-16 the **module**; bits 15-0 a **module-specific error code**. **That decomposes `E0007`**, which this project recorded as "name not found" from behaviour rather than from structure: `0x000E0007` is subsystem 0, module `0x0E`, error `0x0007` — so the code names a module and its seventh error, and the page points at "Chapter 4, Error Codes and Messages" to name them. Chapter 4 is the next chapter in this reading order, and this is the question to take into it |

| 59 | §3 **ADDRESSING MODES** | `confirms` **(a reprint, and it says so)** | The twelve effective-address modes against the four addressing categories — Data, Memory, Control, Alterable — with mode and register fields and assembler syntax. Footnoted "**Reprinted from MC68000, page B-1**", so it is a copy of a Motorola table this shelf already holds rather than an Apollo statement. Recorded because this core enforces mode legality "by category rather than approximated" and this is that table; nothing here is a second source in the sense the rest of the walk means it |
| 60 | §3 **ADDRESS SPACE — Physical** | `none` **(and it hands our machine back to chapter 12)** | Two columns, for non-DNx60 machines and for the DNx60: a trap page at 0, the PROM at `400`, I/O objects, two display bitmaps, an optional half-megabyte, a **PROM data area** and then main memory. The last line is the one that matters here: "**Refer to Chapter 12, DN3000 for information about address space for the DN3000**" — so this page excludes the modelled machine outright and points at a chapter already read end to end. No gap. One cross-reference worth keeping: main memory begins after the PROM data area at `100400`, which is where chapter 12 puts the DN3000's exception page — the same structure, named from two directions |

### Chapter 2 — the file system, read end to end (PDF 35–58, 24 pages)

The chapter that paid for itself: two defects in `image/ap_volume.h`, the 32
bytes in every sector explained, and the complete offline lookup chain.

| Pages | Section | Status | What it yielded |
| --- | --- | --- | --- |
| 38 | §2 **DIRECTORY STRUCTURE** overview, **Directory Info Block** | `none` **(reference for reading a volume offline)** | A directory is a header, a linear list of sequentially used entries, an info block, hash threads, and entry blocks holding hashed entries and link text. The info block carries a version, its own length, the header length (8), and **two default ACL UIDs** — one applied to directories catalogued here and one to files. Domain/OS's structure; this core serves the blocks it lives in |
| 39 | §2 **Directory Entry**, **Directory Entry Block** | `none` **(and the shape this project's volume comparison walks)** | An entry is a **32-byte name**, then a name length and an **entry type** — `0` not in use, `1` name/UID pair, `3` name/link-data pair — then four words that are either the UID or a link-text description. An entry **block** is **150 bytes**: a forward and backward block number making a doubly linked list, a use count, a **block type** — `0` not in use, `-1` a hash block with three directory entries, `-3` a link-text holding block — and then either **three entries** or up to 144 characters of link text. This is the block `compare-volumes-by-root-directory-block` dumps and compares by hand; here is its layout |
| 40 | §2 **Directory Header**, and the **name hash** | `none` **(the most directly usable page in the chapter so far)** | The header's figures, with the defaults printed: version 1, the hash-thread count, **linear list 18** entries, **pool 429** entry blocks, **3 entries per block**, the high block number, the free block thread, the entry count and a **maximum of 1300** entries per directory. **And the hash itself, complete**: `sum := 0; for i := 1 to lnth do sum := ord(name[i]) + 2*sum; HASH_VAL := sum mod HASH_VALUE` over a 32-character name. That is enough to *find* an entry in an offline volume image rather than scan for it, which is what this project's directory-block comparisons have been doing by hand |

| 41 | §2 **Notes on directories** (cont.) — the add and search rules | `none` **(completes p. 2-6's hash)** | The rest of the algorithm the previous page began: adding walks the free block list or bumps the high block number, initialises the block, threads it onto the hash chain and puts the entry first; **searching** looks in the linear list, hashes the name, follows the hash thread to the first entry block with that synonym, searches **all three** of its entries, then follows the next block number until it is zero and returns NOT FOUND. With p. 2-6's hash this is a complete directory lookup, which is what an offline volume reader needs |
| 42 | §2 **DISK BLOCK HEADER**, **DISK $ERROR INFO** | **CLOSES A NAMED UNKNOWN IN `ap_awd.h`** | `blk_hdr_t` is **exactly 32 bytes**: the 64-bit UID of the object the block belongs to, the page number within that file, the time written, `BLKTYP`/`SYSTYP`, a checksum and the disk address. `BLKTYP` is 0 for data and 1-3 for level-1/2/3 index blocks in the file map; `SYSTYP` is 0 file, 1 directory, 2 system directory. **That is the thirty-two bytes `AP_AWD_SECTOR_BYTES` carries beyond 1024**, which this core recorded as "not established here" from the day the constant was written. So 1056 is 1024 of file data plus a per-block file-system header, not drive formatting overhead — and a raw-image reader now knows what the first 32 bytes of every sector are. Nothing changes: `.awd` images are whole sectors moved verbatim, header and all, as the controller does. Transcribed into `image/ap_awd.h`. Also on the page, `DISK $ERROR INFO` — what a driver records on a disk error: the time, the disk address, bad-track and track-per-cylinder figures, the **requested** header against the **read** header, the volume index and physical page, and a status word |

| 43 | §2 **DISK/VOLUME FORMAT** | **the on-disk layout, and an arithmetic coincidence worth chasing** | The whole shape of a volume: **block 00 of the physical volume is the physical volume label** (1 block); then **block 00 of the logical volume is the logical volume label** (1 block); **block 01 of the LV is the boot file, 10 blocks**; then free blocks, the VTOC, the BAT, more free blocks, and — at the **last block of the logical volume** — a **second copy of the logical volume label**, followed by the next logical volume. "**All disk addresses (DADDRs) in a logical volume are relative to the start of a logical volume.**" And the caveats: bad spots may leave the VTOC non-contiguous and not adjacent to the BAT, there may be dead space between logical volumes, and "bad spots may not be added to the VTOC once it has been INVOLed". **The boot file agrees with chapter 1 exactly**: p. 1-21 has the PROM asking for `/sysboot`, "records 2-b on track 0", and `2`-`b` is ten records — the same ten blocks this page gives the boot file, one block further on because the PV label precedes the LV label. Two chapters, one layout. **The coincidence to chase, recorded and not concluded**: `image/ap_volume.h` has `AP_VOLUME_LABEL_BASE 0x440`, measured, and 0x440 is byte 1088 — which is block **1** of a 1056-byte-sectored image (0x420) plus exactly the **32-byte block header** p. 2-8 defines. If that is what it is, the constant is the *logical* volume label's payload where the header calls it the physical one. The arithmetic fits to the byte and nothing here confirms it; it needs the label's own fields checked against p. 2-10's layout before anything is renamed |

| 44 | §2 **FILE MAP** | `none` **(and it bounds a file)** | Thirty-two direct pointers at `+00`-`+7C`, then a level-1 indirect at `+80`, a level-2 at `+84` and a level-3 at `+88`, each indirect block holding **256 pointers**. The classic direct/indirect scheme, and the page does the arithmetic: maximum file size is `(32 + 256 + 256² + 256³) × 1024` = **17,247,300,000 bytes**. "File map resides in VTOCE and AST entries", which ties it to the two tables chapter 1 defined. Nothing for this core; a raw-image reader needs it to follow a file past its thirty-third block |
| 45 | §2 **REGISTRY FORMAT** — header and PPO record | `none` | `ppo_$header_t`: a transaction UID for salvaging, committed and local flags, the entry count, the oldest software that can read and the oldest that can write, a minimum password length and the record length. Then `ppo_$record_t`, a 32-byte PPO name with its length and a UID. This is the registry chapter 1's boot files list reaches for — "if no registries are available, you can login only as USER.NONE.NONE" — and nothing in it is hardware |

| 46 | §2 **Account Header**, **Account Record** | `none` | `acct_$header` mirrors the registry header — a salvage transaction UID, committed and local flags, entry count, oldest reader and writer, minimum password length, record length, and an expiry period. `acct_$record_t` is the account itself: person, project and organization UIDs, a password, an expiry date, a last-login time, flags, a node type and a home directory of up to 256 bytes. The last three are marked "local registries only" |
| 47 | §2 **Registry Record** | `none` | `rgy_$registry_t`: flags, a count of path names, then a length-prefixed list of them. One sentence worth keeping — "**First pname is path of original registry; it is used as lock**" |
| 48 | §2 **STREAM FILE HEADER** | `none` **(and a second 32-byte header not to confuse with the first)** | "Stream file header is the **first 32 bytes of a file** to be accessed by the stream interface" — header length and version, a CRC defined as `-(integer sum of longwords 1 through n)`, the record length, the file length including the header, and a flag word: record type (`00` variable with counts, `01` fixed, `10` no record structure), ASCII or binary, explicit type, sparse — "file may have holes" — carriage control, and a concurrency field of N-readers-or-1-writer, N-and-1, or N-and-N. **Not the same 32 bytes as p. 2-8's disk block header**: that one prefixes every *block* on the volume, this one prefixes a *file*'s data. A reader of a raw image meets both and they are easy to confuse |

| 49 | §2 **UNIQUE IDENTIFIER (UID)**, and the **UID hash** | `confirms` **(the clock unit, from chapter 1)** + **the lookup chain's last link** | `uid_t` is two longwords: the top 32 bits of the clock in **4 ms units**, then a four-bit counter "if more than one UID is generated in one four-millisecond interval" and the **Node ID** in the low bits. The 4 ms unit is chapter 1 p. 1-6's clock — "low-order bit represents approx. four milliseconds" — from a second chapter. **And the hash**: "X = the four words of the UID XORed together; INDEX = X mod TABLE_SIZE ... e.g. `vtoc_hdr.vtoc_size`". With p. 2-6's name hash that completes an offline lookup: pathname → directory entry → UID → VTOC entry, all three steps documented |
| 50 | §2 **UIDS — System**, the canned UIDs | **SETTLES THE `0x440` QUESTION** | `/os/nuc/uid_list.asm`'s canned UIDs, and the four that matter are the **disk structure** series: `pv_label_$uid` `00000200,0`, `lv_label_$uid` `00000201,0`, `vtoc_$uid` `00000202,0`, `bat_$uid` `00000203,0`. With p. 2-8's block header — whose first field is the UID of the object the block belongs to — **the label a block holds is readable from the block itself**, and `media/dn3500-sr10.4-installed.awd` answers directly: block 0 at `0x0000` carries `00 00 02 00 …` and block 1 at `0x0420` carries `00 00 02 01 …`. So `AP_VOLUME_LABEL_BASE 0x440` is `0x420 + 0x20` — block **1**, past its header — and the mount history this core reads is the **logical** volume label's, where `image/ap_volume.h` called it the physical one. Corrected there; the constant keeps its name because every offset built on it is right. Block 2's header UID is a real object's and its low bytes are `01 23 45`, the node ID, which is p. 2-15's UID layout confirming itself. Also here: `sysboot_$uid` `00000315,0`, `directory_$uid` `00000312,0` and `name_$canned_root_uid` `00000308,0` |

| 51 | §2 canned **person, project, organization and ACL** UIDs | `none` | The `005xx` series — `acl_$sys_user_uid`, `acl_$sys_proj_uid`, `acl_$login_uid`, `acl_$locksmith_uid`, `acl_$sys_org_uid`, `acl_$nil_subs_uid` — then the ACL type UIDs `acl_$file_acl` and `acl_$dir_acl`, and the canned file and directory ACLs themselves in the `0001xxxx` and `0002xxxx` series. The registry's side of the protection model; no hardware |
| 52 | §2 **VOLUME LABEL — LOGICAL**, `lv_label_t` | **CONFIRMS `ap_volume.h` OFFSET FOR OFFSET** | The page the last three rows were converging on, and it agrees with this core completely. Label-relative: `+00` version, `+04` the logical volume name, `+24` the volume's UID, `+2C` the **BAT header** — which is p. 2-3's, whose own note said its offsets were "from start of label", closing that loop — `+4C` the VTOC header, then `+B0` `.label_write_time`, `+B4` `.last_mounted_node`, `+B8` `.node_boot_time`, `+BC` `.mounted_time`, `+C0` `.dismounted_time`, `+C4` `.salvage_node`, `+C8` `.salvage_time`. **All seven of `image/ap_volume.h`'s offsets are on that list at those names**, which turns `0x440` from a number that worked into one that is understood — and the heading confirms from the diagram what the block UIDs proved from the image: it is the **logical** label. **Two fields this core does not read**, now named: `+CC` is `.salvage_mode` over `.sys_shut_state`, the **shutdown state** — the other half of the fourteen-day boot refusal, and the thing to reach for if the timestamps ever prove insufficient — and `+D0`-`+D8` are the dump times and the UID of the item being dumped |

| 53 | §2 logical volume label (cont.) | `confirms` **+ names the shutdown states** | `+E0` the UTC delta in minutes and the timezone name, `+E4` the last valid time, `+EC` a bad-spot barrier and `+F0` a bad-spot list running to `+3FC` — so the label is `0x400`, exactly one block's data past its 32-byte header. **`sys_shut_state`'s three values**: `lv_dismounted = 0`, `lv_mounted = 1`, `lv_salvaged = 2`, which is the field named on the previous page. `salvage_mode` is "currently unused; always = 1". The barrier's two sentinels: `FFFFFFFF` no overflow, `FFFFFFFE` means `+F0` holds the overflow block's address. And p. 2-9's second copy, restated — "the alternate LV label is a copy of the LV label and lives at or near the end of the logical volume" |
| 54 | §2 **VOLUME LABEL — PHYSICAL**, `pv_label_t` | **A DEFECT FOUND AND FIXED** | "The PV label is the first block (cylinder 0, track 0, block 0) of a physical volume", so its payload begins at image `0x20`. Label-relative: `+00` version, **`+02` a six-character `APOLLO` signature**, `+08` the volume name, `+28` the volume's UID, `+30` disk type, `+34` total blocks, `+38` blocks per track and tracks per cylinder, `+3C`-`+60` the ten logical volume addresses, `+64`-`+88` their ten alternate labels, then the badspot and diagnostic cylinders, sector start and size, and the pre-compensation cylinder. **`image/ap_volume.h` had the name at `0x22`, which is the signature**: it reported `"APOLLODN3500"` where the volume is called `DN3500`, six characters of constant prefix on every name it had ever read. `0x28` is `label + 08` and the field is 32 bytes. Fixed, with the signature kept as its own constant — a block whose label does not begin `APOLLO` is not a Domain physical volume, which is a check this file can now make instead of a prefix it used to report. The fixtures were rebuilt too: they wrote `"APOLLODN3500"` *at* the signature's offset, so they could not have told a corrected parser from the old one |

| 55 | §2 **VTOC BLOCK**, `vtoc_blk_t` | `none` **(and the arithmetic closes)** | A VTOC block is a next-block pointer for the hash bucket and then **five VTOC entries** at `+04`, `+D0`, `+19C`, `+268` and `+334` — a stride of `0xCC`, 204 bytes — ending at `+400`. So `4 + 5 × 204 = 1024`, exactly one block's data past its 32-byte header, which is the same arithmetic the logical volume label closed on p. 2-19. **Or** the block is 256 file maps of 128 bytes each, "when the VTOC block contains a file map, the block is pointed to by `vtoce.fm2[1-3]`" |
| 56 | §2 **VTOC ENTRY**, `vtoce_hdr_t` | `none` **(the per-object record)** | Offsets in **decimal** on this page: version, system type and a flag byte; the object's UID at `+04`; the UIDs of its type definition and its ACL; `+28` current length in bytes; `+32` blocks used; `+36` date-time last used and `+40` last modified; `+44` the UID of the directory it is catalogued in; `+52` extra DTM bits and a reference count; `+56` a file lock key — 100 bytes of header inside the 204-byte entry p. 2-21 spaces out. The flags: in-use, a two-bit concurrency control (none, shared, exclusive), permanent, immutable, **`F` "file needs salvaging" (`.trouble`)**, and local-access-only |
| 57 | §2 **VTOC HEADER**, **VTOC MAP ENTRY** | `confirms` **(p. 2-18's `+4C`)** | "The VTOC header lives in the logical volume label. **Offsets given are from the start of the label**" — and they run `+4C` to `+B0`, which is exactly the span p. 2-18 labels `.vtoc_hdr`, between the BAT header and `.label_write_time`. The pair of "offsets are label-relative" notes on pp. 2-3 and 2-23 now both land where the label diagram says. Contents: version and blocks-for-hash, VTOC blocks used, and four VTOC indexes — **the network root, this volume's root directory, the AEGIS paging file and the boot file** — then an eight-entry VTOC map. A map entry is a count of consecutive blocks and the address of the extent's first, and "VTOC extents are preallocated by INVOL to be near the middle of the logical volume and to avoid badspots" |
| 58 | §2 **VTOC INDEX**, `vtocx_t` | `none` **(three forms, and the last page of the chapter)** | One longword with three readings: a **local** object is the disk address of its VTOC block with a 3-bit index selecting one of the five entries; a **remote** object has bit 31 set, a touch-ahead count, and a **Network ID** — so an index says outright whether the object lives on this node; and a third form carries only a logical volume number for "local, but DADDR is unknown". `INDX` is "index of VTOC entry in VTOC block (0-4) **or File Map index (0-7)**", which is the two readings of a VTOC block from p. 2-21 reflected in the index that reaches it |

| 35 | §2 **ACLS STRUCTURE** — ACL header and record | `none` | The access-control list's header — version, the type of object it can be an ACL for, a default node list for new entries, the entry and node-list counts — and the record that is a header followed by an array of entries. AEGIS's protection layer; nothing under it is hardware |
| 36 | §2 **ACL Entry**, **BLOCK AVAILABILITY TABLE** | `none` **(and the BAT's shape, which is worth having)** | An ACL entry is four 64-bit UIDs — person, project, organization, subsystem — then a node ID, an expiry date and the rights word. Then the **BAT**: `array[0..255] of bat_lword_t`, so **one 1024-byte block of 256 longwords**, "resides in contiguous records", and the rule that matters — "**BAT bit = 1 if block is available**", with bit 0 of word 0 being the block at `bat_hdr.base_add`. A free-space bitmap Domain/OS owns; this core serves the blocks under it and has no reason to read it, but a tool that wanted to say how full a volume is would start here |
| 37 | §2 **BLOCK AVAILABILITY TABLE HEADER**, `bat_hdr_t` | `none` **(new structure, and the offsets are label-relative)** | "**BAT header lives in logical volume label. Offsets given are from start of label.**" `+2C` number of blocks represented, `+30` number of free blocks, `+34` the disk address of the first BAT block, `+38` the block number the first BAT bit stands for, `+3C` three flags, `+40` the BAT step. The flags are `V` volume trouble — "set by OS if volume needs salvaging, cleared by SALVOL" — `C` volume CHUVOLed and `B` volume being CHUVOLed. **This is the *logical* volume label**, where `image/ap_volume` reads the *physical* one at base `0x440`, so the two do not collide; it is new structure rather than a correction. `V` is the bit a salvage-needed volume carries, which is worth knowing beside this project's mount-history work |

### Chapter 1 — AEGIS, read end to end (PDF 12–34, 23 pages)

Almost all of it is AEGIS's own structures, above the hardware this core models,
and those rows say `none` — which is the point of recording them. Four pages
carry something: the clock, the ring packet format, the boot files and the trap
codes.

| Pages | Section | Status | What it yielded |
| --- | --- | --- | --- |
| 12 | §1 **AEGIS SYSTEM RELATIONSHIPS** | `none` (recorded) | The one-page diagram the chapter opens on: user, name server, pathname to UID, the **96-bit address** as a 64-bit UID and a 32-bit offset, then disk structure, paging, physical memory, the MMU and virtual memory. Operating-system architecture rather than hardware, so nothing for this core — but the 64/32 split is the shape `image/ap_volume` and the naming-server investigations work in |
| 13 | §1 **ACTIVE OBJECT TABLE / ACTIVE SEGMENT TABLE**, `aote_t` | `none` | "A wired system-wide table that describes the current state of active objects", dynamically allocated at startup from physical memory size, with the AOTE acting as a cache of the VTOC entry. The entry's layout — cached VTOC header, VTOC pointer, front and back links, AST link, event number and the `V I U G XXXX D EEEEEEE` flag word. AEGIS's own structure, in `vm.ins.pas`; this core models the machine under it |
| 16 | §1 **AST PAGE MAPS**, reverse and forward mapped | `none` **(and worth knowing which machine is which)** | Three layouts for one field: reverse-mapped, forward-mapped for the DNx60 and DN5xx-T, and "for machines with **PMMU (Series 3000)**" — `T N R H H H H H H | PHYS PAGE NUM | W W - - - M U P I V` resident, `T N R | FULL DISK ADDRESS | - - - M U P I V` not. These are AEGIS's software page maps rather than the MMU's tables, so nothing here changes; recorded because a reader tracing a page fault through a crash dump needs to know which of the three a Series 3000 uses |
| 14 | §1 **ACTIVE SEGMENT TABLE**, `aste_t` | `none` | The AST entry: back link to the AOT, file segment number, front and back links, pointer to the file map, event number, and the `I U F R V V V V | P C C C C C C C` / `Q…Q | S S N N N N N N` flag words — `in_trans`, `used`, `fm_mod`, `remote`, the volume index, the hold count and the number of pages resident. Forward-mapped machines add the MSTE back-thread chain and the pmap's physical address. AEGIS's own table, sized from physical memory at startup |
| 15 | §1 **ACTIVE OBJECT TABLE HEADER** | `none` | The header in front of the AOT: a hash table of start pointers — `[0..60]`, or `[0..250]` on a forward-mapped machine — then the AOTE and ASTE free pointers and free lists, processes per volume, the AOT and AST sequence numbers, the **number of dismounts**, the dismounter's event count and the grace-replace pointer. The dismount count is the one field here this project has touched from the other side: `volume-label-mount-history` records reading mount and dismount times out of a volume label, and this is where the running system keeps the counter |
| 17 | §1 **CLOCK** | `confirms` **— a third source for the prescaler, and it names the three clocks' jobs** | The software clock is 48 bits, "low-order bit represents **four microseconds**", split `.high16`/`.low32`, and "**hardware clock is only 16 bits**. The upper 32 bits are maintained by clock interrupt routine" — which is the 6840's counter width and the software above it. Then the three: "**Clock 1: 4 microseconds (real-time clock), Clock 2: 8 microseconds (process timer), Clock 3: 128 microseconds (real-time intervals)**". The first two are `AP_TIMER1_HZ` and `AP_TIMER2_HZ` exactly. **The third looks like a factor-of-8 disagreement and is not**: `AP_TIMER3_HZ` is 62,500 Hz, a 16 µs period, and `ap_mc6840.h` already records the reconciliation — `CR3`'s bit 0 selects "CLK ÷1" or "CLK ÷8", and 16 µs prescaled by eight is 128 µs. So this page is a **third** document on that divisor, and it adds what the other two do not: which clock does which job, and that AEGIS runs timer 3 *prescaled* |

| 18 | §1 **DISK CONTROLLER TABLE ENTRY**, `dcte_t` | `none` **(reference for a driver trace)** | One entry per controller: type and number, status, lock number, block header pointer and page, **I/O register page pointer**, the page-zero interrupt vector, the actual interrupt entry and the device-dependent routine, then the four driver entry points — `disk_dinit`, `disk_do_io`, `disk_error_que` — the unit number and the physical DVTE indexes. Four DISK FLAGS: `N` no headers on device, `C` do checksumming for this controller, `D` driver supports multi-read requests, `L` liberty (SMD) type winchester. AEGIS's own driver table; nothing to model, and it is the structure a boot trace's disk calls run through |
| 19 | §1 **DISK VOLUME TABLE ENTRY** `dvte_t`, **EVENT COUNT** | `none` **(and the running system's side of the volume work)** | State, blocks per volume, owner process, **base disk address**, the UID of the physical or logical volume, a pointer to the controller table, the DVTE index, unit number, **disk type, blocks per track, tracks per cylinder**, the BAT step and flags. `state` is 0 free, 1 being mounted, 2 assigned, 3 mounted; `flags` are `U` use_caller_blkhdr, `N` no_crc_retry, `W` write_protect. This is the in-memory counterpart of the on-disk labels `image/ap_volume` reads, and the geometry fields are the ones a mounted volume answers from — worth having beside `compare-volumes-by-root-directory-block` |
| 20 | §1 **FAULT DIAGNOSTIC RECORD**, `fault_$diag_t` | `confirms` **(the function codes)** | A record with the magic pattern **`$DFDF`** at its head, then the status word, the register file to `+46`, a `bus_info` byte, the access address, the instruction register and flags. `bus_info` decomposes as `R` read operation, `N` not-an-instruction-reference, and `FFF` the **function code** — `001` user data, `010` user program, `101` supervisor data, `110` supervisor program, `111` interrupt acknowledge. Those are the 68000 family's FC2-FC0 and this core's `AP_M68030_FC_*` exactly. Nothing to change; the `$DFDF` pattern is what identifies one of these in a dump |
| 21 | §1 **MAPPED SEGMENT TABLE (MST)** | `none` | "A wired array indexed by asid and va. Each mste describes one segment of virtual memory", sized `asid_tn * mst_pages_tn` with `mstes_per_page = page_size div sizeof mste_t`. AEGIS's segment table, above the MMU this core models |

| 22 | §1 **MAPPED SEGMENT TABLE ENTRY**, `mste_t` | `none` **(and a per-family field worth noting)** | The UID of the file, the file segment number with its flag byte, and the VTOC-entry index. The **access field** is AEGIS's protection encoding, five bits: `00000` nil, `00010` read, `00011` read-execute, `00110` write-read, `00111` write-read-execute, then the same four again with the supervisor bit — `10010` through `10111`. The flag byte's middle differs by machine and the page spells it out: `--PPP` for the DNx60, `PPPPP` for the DN5xx-T, **`----P` for the DN3000**, `AAAAA` for others. Software protection above the MMU this core models, so nothing changes; recorded because the per-family shape is the kind of thing a reader assumes is uniform |
| 23 | §1 **MEMORY MAP (MMAP)**, `mmape_t` reverse mapped | `none` | "For **all machine types** an MMAPE is **12 (decimal) bytes** in size" — one of the few sizes in this chapter that does not vary by family. The entry: PTT index with an in-use flag, previous and current list, the next and previous physical page numbers, the low disk address, and the ASTE index with a data/code bit. Flags `I` in use, `A` wired or in transit, `R` modified but the DTM should not be updated, `O` on a memory page list, `D` holding data rather than code |
| 24 | §1 the **memory map's eight lists**, `ws_hdr_t` | `none` **(reference for a paging trace)** | The lists by name and index — `mmap_$xfree` 0, `xhi_pure` 1, `xpure` 2, `ximpure_l` 3, `ximpure_r` 4, `xos_shared` 5, `xnet_pager` 6, `xinit_proc` 7 — with what each holds, down to "pages being 'cached' by one of the network paging servers ... replaces the remote paging pool". Then the list header: current and maximum size, the first page's PPN anchoring a doubly threaded list, the owning process's last run time, the last scan time, an interrupt count and a protected size "list size won't be forced below this". This is the structure a page-fault or purifier trace walks |
| 25 | §1 **OS MAPPING** | `none` **(one number worth carrying)** | The diagram joining the two spaces: a single-node process virtual address space of **2²⁴**, divided into per-process user, per-process supervisor and global, mapped by **PID** into a network-global object space of **2³²** addressed by **UID**. Architecture rather than hardware — but the 24-bit process space is the figure a reader needs when a virtual address in a crash dump looks too small for the 32-bit MMU this core models |

| 28 | §1 **PROCESSES**, the PID/ASID table | `none` **(reference for a boot trace)** | Sixty-four processes by PID and address space: 1 the initial user process and DM, 2 null, 3 wired DXM, 4 and 5 the page purifiers local and remote, 6 unwired DXM, **7 network-receive server, 8 network-paging server, 9 network-request server**, 10 terminal helper, 11 the first user process, then user processes to 64. "Processes with an ASID of 0 run entirely in global space." Nothing to implement; this is the table that names what a PC sample or a process switch in a boot trace belongs to |
| 26 | §1 **PAGING SYSTEM** | `none` | The diagram joining the pieces the preceding pages define: UID into the VTOC's object page map, the Active Segment Table loaded on reference, the memory map table, physical memory, and the Mapped Segment Table indexed `UID:FSN` by PID through the OS map primitive. Architecture, no registers |
| 27 | §1 **PROCESS CONTROL BLOCK**, `procl_t` | `none` **+ it strengthens the chapter-5 lead** | The saved register file `d2`-`d7`, `a2`-`a7` and the USP, the ready-list links, the clock value at the start of `ec_$wait`, PID and ASID, the virtual timer and the **floating-point processor type**, CPU time, priority with its minimum and maximum, an inhibit count and a bus status. **`+40` is the RESOURCE LOCK WORD** (`.rlock`), which is what p. 1-20's lock table decodes — and the state byte's `T` bit is "time slice end with **rlock <> []**", the same concept the `CRASH_STATUS` message names. Three pages of this chapter point at the same word — and the crash status turned out **not** to be it: p. 4-7 gives `00120020` as the fault handler's error `20`. The `.rlock` word is real and the crash message is about locks; the *encoding* is not a lock set |
| 29 | §1 **RING PACKET FORMAT**, message header and type field | `confirms` **(the header, field for field)** + **two things to carry** | `+00` TO ADDRESS, `+04` TYPE FIELD, `+06` zeros and EARLYACK, `+08` FROM ADDRESS, `+0C` header data — which is `AP_RING_HDR_DESTINATION`, `_TYPE`, `_ZEROS`, `_EARLY_ACK`, `_SOURCE` and `_DATA` at 0, 4, 6, 7, 8 and 12 exactly. **Bit 15 of the type field is BST, broadcast**: "set in a packet intended to be received by all receivers. If it is set, the To Address field is ignored" — which is `ap_ring_header_addresses`'s second case, from a second document. **Two things this core does not have.** The type field's other fifteen bits are matched against a **controller Type register** — "if any bit selected in the Type register is a 1, the message is received" — and no such register appears in p. 12-29's DN3000 register map, so whether this controller has one is open; the page says "the APOLLO I only implements bits 14 through 8". And it gives the header data as **0–1024 bytes** ending at `+40C`, where `010005-00` gives the whole header as 12 to 1024 bytes and this core follows the protocol spec at 1012 data bytes. The ring's own standard wins; the disagreement is recorded |
| 30 | §1 **Early Acknowledge Field** | `confirms` **(every bit, and the reason for three of them)** | Bit 3 `ICP` intend-to-copy, bit 1 `PAR` parity, bits 7, 4 and 0 fixed at zero, bits 6, 5 and 2 don't-care. That is `AP_RING_EARLY_INTEND_TO_COPY`, `AP_RING_EARLY_PARITY` and `AP_RING_EARLY_MUST_BE_ZERO 0x91` — bits 7, 4 and 0 — to the bit. **And it gives the reason**, which this core had as a constant without one: the three zeros are "inserted to prevent the remaining bits in the late-acknowledge byte from being modified by the bit-stuffing protocol". `ICP` is "set by an addressed receiver if it was set up to copy a message **and the type field matched**. A NAK ... is indicated when no receiver sets this bit", and the parity is odd across the byte. Also stated: "for the purposes of the CRC calculation the EARLY ACK field is treated like a byte of zeros ... allows receivers to modify the ACK field without having to recompute the CRC" |
| 32 | §1 **SYSTEM BOOT FILES** | `none` **(reference for every boot this project runs)** | What each agent asks for, in order: the PROM wants `/sysboot`, "**records 2-b on track 0**"; `sysboot` wants `/saun/aegis` and `/saun/salvol` "only if salvage required"; `aegis` wants the OS paging file, `//`, `/`, `/com`, `/sys/node_data` and `` `node_data/boot_shell ``; the shell wants `/sys/apollo_logo` and `` `node_data/startup_shell ``; `env` chooses between `/sys/dm/dm`, `/sys/boot` and `/sys/spm/spm` on "go", "sh" or "spm"; `dm` wants `` `node_data/startup[.191,.color,.1280color,.1280bw] `` — the per-display startup files a `--screen` choice selects between. Two notes worth having: a tape boot "is first searched for BSCOM/RBAK_SHELL", and with no registries "you can login only as **USER.NONE.NONE**" |
| 33 | §1 **SYSTEM DIRECTORIES** | `none` **(and it explains this project's `sau7`)** | The `/sys` tree by purpose, and the **SAU numbering by machine**: `/sau1` DN4xx/600, `/sau2` DN300/320/330, `/sau3` DSP80/80A/90, `/sau4` DNx60 and DSP160, `/sau5` DN550/560/570/580, `/sau6` DN5xx-T, **`/sau8` DN3000 and DSP3000**. **There is no `/sau7` in this table** — and `/sau7` is the directory this project's SR10.3 cartridge work looks for, because it is the Series 3500's and postdates a January 1987 handbook. The gap in the numbering is the answer rather than an omission |
| 34 | §1 **TRAP CODES** | `none` **(and one line that matters for MD)** | The sixteen `TRAP` vectors: `0`-`4` are SVCs with that many arguments, `5` an SVC with any number, `6` an SVC from GPIO interrupt routines, `7`-`D` undefined and reflected to user space, `E` a software-generated fault (`pfm_$error_trap`), and **`F` undefined — "traps to PROM debugger"**. That last is the entry to MD from a running system, which is the debugger this project drives through the serial console |

| 31 | §1 **Message Data**, **RESOURCE LOCK** | `none` **+ a lead worth carrying into chapter 5** | The ring packet's data field: "varies in length from **0 to 1024 bytes** but must always be an **even** number of bytes (on the Apollo I the controller always uses word DMA) ... transmitted verbatim" — which is the ring controller's word-DMA constraint stated from the software side, and agrees with `ap_ring_ctl`'s word-addressed buffer. **The lead is the lock table**: twenty-six named resource locks with their bit values, `network_$serv_lock` at `1` through `time_$lock` at `2000000`, including `file_$lock_lock` `20`, `ast_$lock` `20000` and `flp_$lock` `100000`. This project's `CRASH_STATUS 00120020` — "supervisor fault while resource lock(s) set" — decodes under that table as exactly those three, and `00120020` is `100000 + 20000 + 20`. **Not claimed** — and rightly: p. 4-7 later prints `(00120020) supervisor fault while resource lock(s) set` as module `12` error `20`, so the sum is a **coincidence** and the lead is dead. See the chapter 4 row for PDF 75 |

*(chapter 12 is read end to end, its delegations included; chapter 1 is a
five-page sample with the rest owed; rows are added as the remaining chapters
are)*
