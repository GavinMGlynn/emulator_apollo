# `002398-04` walked whole — coverage record

*Domain Engineering Handbook*, Rev 4, January 1987. **330 PDF pages.**
Marked `APOLLO CONFIDENTIAL. INTERNAL USE ONLY.` on every page.

## Why this file exists

`CLAUDE.md`: *a document that turns out to contain one unimplemented thing is a
document that must be read WHOLE — page by page, line by line.* This one has
triggered it repeatedly and was never derived in full. It has been *consulted*
at least five times and yielded something every time:

- p. 12-2 gave the keyboard beeper its command sequence and its 300 ms auto-off,
  which `ap_kbd.h` records as having "stopped being a decline when the handbook
  turned up".
- p. 12-23 and p. 12-25 gave the DMA channel usage and the DMA page register's
  offset-to-channel map — "the handbook that prints the table `008778-03`
  Table 2-6 omits" (`ap_dmapage.c`).
- p. 12-28 gave the DN3000's interrupt request line assignments, which settled
  the ring's `IRQ2` against a three-way disagreement (`ap_board.h`).
- its `CRASH_STATUS` list gave *supervisor fault while resource lock(s) set*
  for `00120020`.

Five consultations, five facts. **A document consulted about one question was
never consulted about the rest** — the same sentence that opened the
`008778-03` walk, which is now finished at 209 of 209 pages and whose record is
`008778-03_WALK.md`.

## How to read this record

Every page range gets a row, including the ones that yield nothing: a section
recorded as *no emulator content* is a finished section, and that is what makes
a **walked** document distinguishable from a **sampled** one later. Pages are
read as **images**, never as a text layer.

Status values: `derived` (everything in it is in code or in a named gap),
`confirms` (agrees with code already, nothing to change), `none` (no emulator
content), `GAP` (something is missing from code and is named).

**Debts are recorded in the Resume block, not left implicit.** The `008778-03`
walk incurred two — both from reading a later page out of order to establish a
section's extent — and both were paid, *because they were written down*.

## Resume here

**Next unread page: 46** — chapter 1 is **read end to end** (PDF 12–34);
chapter 2 is started, PDF 35–45 read.

**CHAPTER 12 IS READ END TO END: PDF 279–313, all 35 pages.** Front matter read
(5–11). Chapters 1–11 and the appendices are unread and are *not* a debt — see
the reading order below; they are scheduled, not skipped.

**35 content pages of 330.**

**Owed: nothing. Chapter 12's three delegated sections are read**, out of order
and recorded as such. They are chapter 12's content held in other chapters, and
all three **confirm** this core:

| Delegated from | To | Read at | Result |
| --- | --- | --- | --- |
| p. 12-25 | chapter 7 | PDF 153–155 (7-15 – 7-17) | `confirms` the 68030 frames |
| p. 12-26 | chapter 7 | PDF 157–158 (7-19 – 7-20) | `confirms` the 68881 FPCR |
| p. 12-35 | chapter 9 → chapter 7 | PDF 241, 170 (9-14, 7-32) | `confirms` the DUART; the `dtr_b` bit is **defined nowhere** |

The rest of chapters 7 and 9 stays scheduled in its normal place; only the three
delegated sections were read early, and the rows below say which pages. The
timers are delegated too — p. 12-35's "same as all previous systems" — with no
chapter named, so there is nothing to follow.

**Chapter 1 owes nothing.** All twenty-three pages, PDF 12–34.

## The reading order, and why it is not front-to-back

`008778-03` was walked in page order because every chapter described the machine
this core models. This handbook does not: **chapters 7 through 11 are other
machine families** — DN300/320/330, DN400/420/600, DN460/660/DSP160, DN5xx,
DSP80/80A/90 — and only **chapter 12 is the DN3000**, which is the family
`src/core/model/` is built around.

So this walk takes **chapter 12 first**, then chapters 1–6 (which are
machine-independent: AEGIS structures, the file system, error codes, debugging,
peripheral I/O), then 7–11 and the appendices. Every page is still read; only
the order changes, and it changes so that the pages most likely to hold a
missing fact are reached first.

That decision is recorded here rather than left to look like an omission, and
the Resume block above is the authority on what has actually been covered.

## Structure

| Chapter | Subject | Handbook pages | PDF pages |
| --- | --- | --- | --- |
| front | title, conventions, contents | i–xii | 1–11 **(read)** |
| 1 | AEGIS — OS structures, ring packet format, trap codes | 1-1 – 1-23 | ? |
| 2 | File system — ACLs, BAT, directories, **volume label**, VTOC | 2-1 – 2-24 | ? |
| 3 | Programming information — address space, stack frame, status word | 3-1 – 3-10 | ? |
| 4 | Error codes — **boot PROM diagnostics per family**, MD, sysboot | 4-1 – 4-25 | ? |
| 5 | System debugging — DB, MD, **crash analysis**, system dumps | 5-1 – 5-17 | ? |
| 6 | Peripheral I/O — device addresses, **keyboard charts**, magtape | 6-1 – 6-20 | ? |
| 7 | DN300, DN320, DN330 | 7-1 – 7-32 | ? |
| 8 | DN400, DN420, DN600 | 8-1 – 8-50 | ? |
| 9 | DN460, DN660, DSP160 | 9-1 – 9-14 | ? |
| 10 | DN5xx | 10-1 – 10-26 | ? |
| 11 | DSP80, DSP80A, DSP90 | 11-1 – 11-9 | ? |
| **12** | **DN3000** — the modelled family | 12-1 – 12-35 | **279–313** |
| A | ASCII character set | A-1 | ? |
| B | Powers of two | B-1 | ? |
| — | Index | I-1 | ? |

**Both navigation aids are to be checked against the document**, which is the
last lesson `008778-03` taught: its front contents omitted chapter 15 and its
back index omitted chapter 16, each missing a chapter the other had. The table
above is from the contents (PDF 6–11); the index has not yet been read.

## What chapter 12 is expected to carry

Recorded before reading, so that "we already had that" cannot be decided after
the fact. Its contents list names, for the **DN3000**: address space by physical
address, beeper, calendar, **cartridge tape** with six named registers (data,
command, control, status, DMA go, DMA reset), configuration, **CPU control/
status register**, **disk (floppy/Winchester) controller**, display colour and
monochrome, fault frames and fault types, fault vectors, floating-point
registers, **parity error register**, **programmable interrupt controller**,
**ring**, **SIO**, and **timers**.

Every one of those has a module in `src/core`. This is a register-level chapter
for exactly the machine this core models, and it has never been read whole.

## Coverage

| Pages | Section | Status | What it yielded |
| --- | --- | --- | --- |
| 5 | Conventions | `none` (recorded) | The symbol table the rest of the document is written in: `+00` is "offset, in hexadecimal", `-` an unused bit, `<pa \| va>` a physical or virtual address, `set of 0..31` is 32 bits, `1..32` is "32 values; 5 bits". Worth having before any register table is read, since a misread convention is a misread table |
| 6–11 | Table of contents | `none` | Transcribed into the structure table above |
| 262, 278 | probes for chapter 12's start | `none` (recorded) | Read to map handbook pages to PDF pages, which the contents does not give. 262 is 10-21 and 278 is 11-9, so chapter 12 begins at **PDF 279**. Recorded rather than left implicit, per the rule that a page read out of order is either recorded or owed. 278 also carries the DSP80's **PROM ENTRY POINTS** table -- `getc`, `putc`, `init_dsk`, `read_dsk`, `reload_font`, `pollc`, `quiet_ret`, `write_disk`, `log_error`, `crash`, `led_update` at `$100`-`$12C` -- and chapter 12 has its own, which is where to compare against the DN3500 boot PROM |
| 279 | §12 **ADDRESS SPACE BY PHYSICAL ADDRESS** | **SETTLES A QUESTION `008778-03` LEFT OPEN THREE TIMES** + `confirms` **(the whole core board)** | Three columns -- physical, virtual, **I/O Bus** -- which is what `008778-03` Table 2-7 has and gets wrong in one row. **`50000 tape ... 200`.** The `008778-03` walk hit that arithmetic three times: Table 2-7 names ISA `218` beside physical `050000`, Table 8-1's prose repeats "Device Address 218", and Figure 15-5 shows the *SPE board's alternate* jumpering claiming `218`-`21F`. `218` maps to `050C00` and `200` maps to `050000`, so the physical column and the ISA column could not both be right. **The handbook says `200`**, agreeing with the physical column, with our measured `AP_TAPE_ADDR`, and with the oracle tap. Recorded in `board/ap_tape.h`; the constant does not change, but it no longer rests on preferring one column of a self-contradicting table. **Confirms, in one table**: `4D000 win`/`1A0`, `58000 ethernet brd1`/`300`, `5D800 Mono`/`3B0`, `5F800 flp`/`3F0`, and the core-board block `8000 mmu/cpu`, **`8400 sios` — one row, the DS3000's single 2681** — `8800 timers`, `8900 calendar`, `9000`/`9100` DMA, `9200` DMA page register, `9300` parity, `9400`/`9500` the two PICs, plus `20000 PMMU` and `22000 68881 FP` in CPU space and the DMMU's RP/TB/TB-flush/status at `0`/`4000000`/`8000000`/`C000000`. **Two things to follow up**: the table lists **eight ring pages** (`51000`-`51C00` at ISA `220`-`238` and `59000`-`59C00` at `320`-`338`), which is a layout `RING.md` should be checked against; and it puts **ethernet board 2 at ISA `308`** where `008778-03` Figure 14-4 jumpers the alternate card to `310`. Neither is resolved here |
| 280 | §12 address space (cont.), **BEEPER** | `confirms` **+ names the mapping's granularity** | The tail of the table -- `5FC00 sio2`, `A0000 dsp_mem color`, `FA0000 dsp_mem mono`, `100000-8FFFFF main mem`, `900000->FFFFFF` AT-compatible memory space -- and then the formula: "**Every 8 bytes** of the bus i/o space (0-3FF) is mapped to **one page** of DN3000 space. Formula ... `(AT_addr/8) * $400 + $40000`". That is arithmetically identical to `ETHERNET.md`'s measured `physical = 0x040000 + (ISA << 7)`, since `(a/8) * 0x400` is `a << 7` -- so the measurement and the handbook are the same rule. What the handbook adds is the **granularity**: the unit is an 8-byte ISA block occupying a 1-KB page, which is the shape `ap_tape.h`'s measured "four registers at stride 1 ... aliased through the 256-byte range" is a sample of. **BEEPER** is the passage `ap_kbd.h` already quotes -- `$FF $21 $81 $00` on, automatic off after 300 ms, `$FF $21 $82 $00` off -- re-read in place |

| 281 | §12 **CALENDAR** `[8900 | 3FFAD00]` | `confirms` **(the whole page, offset for offset)** | The MC146818's register file and the 50 bytes of battery-backed RAM behind it: `00`–`09` the time and alarm registers with their ranges, `0A` command, `0B` status, `0C`/`0D` unused, then the configuration table — `0E-11` CHECKSUM, `12-15` VALID PATTERN, `16-1D` MEM BOARD ARRAY, `1E-21` NODEID, `22-25` DEV BIT ARRAY, `26` RING TYPE, `27` DISP TYPE, `28` DISK TYPE, `29-3F` unused — and the DEV BIT ARRAY's eight bits: `0 = flp, 1 = ctape, 2 = win, 3 = fpu, 4 = ring, 5 = user device, 6 = ethernet, 7 = serial/parallel board`. **Every one of those is already in `board/ap_calendar.h`**, which quotes this same layout in its header and defines each offset and each device bit. Nothing to change. Recorded as a `confirms` rather than skipped, because that is the difference between *establishing* that a module is complete against its source and *assuming* it: this page is what the calendar's configuration table was derived from, and the walk is what proves the derivation was total rather than partial |

| 282 | §12 calendar **Command**/**Status** registers, **CARTRIDGE TAPE** `[50000 | 3FF9C00]`, **Tape Status Byte 0** | `confirms` **(both parts)** + **one naming trap** | The calendar's two control registers, bit 7 each: `0A` "Command Register" carries **UIP**, "1 => update in progress"; `0B` "Status Register" carries **SET**, "1 => abort current update, allow initialization". Both are in `device/ap_mc146818.h` as `AP_MC146818_A_UIP` and `AP_MC146818_B_SET` at `0x80`. **The trap is the naming**: the handbook calls `0A` *Command* and `0B` *Status*, where the part's own datasheet calls them Register A and Register B — and it is A that holds UIP and B that holds SET, so the bit positions agree and only the labels differ. A reader moving between the two documents should not read "Status Register" as the part's Register B. The page also cites `/os/ker/os_cal_asm.asm` as where these are defined, which is an AEGIS source path and not something this core can reach. Then **CARTRIDGE TAPE at `50000`** — the address settled two rows above — and its **Data Register (read)**: "Drive status information is returned as a series of **6 bytes** through the data register after a read status command is sent", which is `AP_QIC_STATUS_BYTES 6`. **Tape Status Byte 0** in full: bit 7 zero to mark the byte, then `noc` cartridge not in place, `usd` unselected drive, `wp` write protected, `eom` end of media, `ude` unrecoverable data error, `bnl` bad block not located, `fm` filemark detected. All eight are in `device/ap_qic.h` at the same positions -- `AP_QIC_EXS_BYTE_0` through `AP_QIC_EXS_FILE_MARK` -- and byte 1's bits are there too, so this page confirms a layout the module already carries whole |

| 283 | §12 **Tape Status Byte 1**, the **STATUS SUMMARY**, bytes 2–5, **Command Register (write)** | `confirms` **(and independently recovers two opcodes)** + **one table to check against** | Status byte 1's eight bits — `ill` illegal command, `nod` no data detected, `mgn` marginal block, `bom` beginning of media, `bpe` reserved for bus parity error, `erm` reserved for end of recorded media, `por` power on/reset — all in `device/ap_qic.h` at the same positions. Bytes **2–5** are named: high and low bytes of the **data error counter** and of the **underrun counter**, which is what `ap_qic`'s `data_errors` and `underruns` are, now confirmed as 16-bit counters split across two bytes each. **The Command Register decomposes the opcodes**: bits 7–5 are a command *type* and bits 4–0 the data — `000` Select, `001` Position, `010` Write Data, `011` Write File Mark, `100` Read Data, `101` Read File Mark, `110` Read Status, `111` reserved. Every opcode in `ap_qic.h` falls out of that: `40`/`60`/`80`/`A0`/`C0` are the five types with zero data, `01` is Select with `MMMMM = 00001`, and `21`/`22`/`24`/`26`/`27` are Position with `00MMM` = 1, 2, 4, 6, 7. **That independently recovers the two opcodes `[SC499]`'s summary table had struck through with a previous owner's pen** — `22` ERASE and `26` SELECT Q11 — which `ap_qic.h` had restored from §1.13.1's numbered descriptions and defended by the series they sit in. A second document, in a different form, generates the same series. **One table recorded and not yet acted on**: a fifteen-row **STATUS SUMMARY** giving which *pairs* of status bytes mean which condition — "no cartridge", "no drive", "read abort", "read error, no data and EOM", and so on. This core sets status bits individually, and the summary is the check on whether it can ever emit a pair the controller would not. Its "Status 0" column also prints patterns with bit 7 set, where byte 0's marker bit is zero, so the column's convention needs establishing before it is used |

| 284 | §12 tape **Control**, **Status**, **DMA Go** and **DMA Reset** registers | `confirms` **— and it is the polarity confirmation, from Apollo's side** | Four registers, and this page independently states the fact `device/ap_sc499.h` calls the thing "this suite used to get wrong". **Control (write only) `[50001]`**: bit 7 `rst` "Reset controller microprocessor", 6 `req` "Request to LSI chip", 5 `ien` "Enables interrupts", 4 `dni` "Enables DONE int, dni = 0 masks DONE int", 3–0 unused — `AP_SC499_CTL_RESET`/`REQUEST`/`IEN`/`DNIEN` at `80`/`40`/`20`/`10` exactly. **Status (read only) `[50001]`**: bit 7 `irq` **"0 =>"**, 6 `rdy` **"0 =>"**, 5 `exc` **"0 =>"**, 4 `don` **"1 =>"**, 3 `dir` **"1 =>"**, 2–0 unused — which is `AP_SC499_ST_*` at `80`/`40`/`20`/`10`/`08` with `ST_UNUSED 0x07`, and **the three active-low bits are exactly the three this core marks active low**. `[SC499]`'s own page prints that polarity in a column its text layer drops, and reading `40` as "ready asserted" was the misreading this module was corrected for; here is a second document, from the machine's vendor rather than the controller's, printing the same polarity as an explicit `0 =>`. **And the interrupt flag is stated as a disjunction with its gating**: "Interrupt request flag, 'or' of rdy and exc, and done **if dni is set**" — which is `ap_sc499_irq`'s list *and* its DONE-gated-on-DNIEN term, in one sentence. **DMA Go `[50002]`** and **DMA Reset `[50003]`**: "Any write to this register will cause DMAGO / RSTDMA to be active", which is why both are modelled as functions that ignore what is written. Cites `/os/ker/ct.pas` |

| 285 | §12 **CONFIGURATION**, the DN3000 block diagram | `confirms` **(structure, no registers)** | An ASCII block diagram of "the major components of the DN3000 cpu and memory system, AT-compatible bus, and peripherals". **CPU**, **MMU** and **FP** on one side; **SIO**, **PROM**, **TIMER**, **CALENDAR**, **DMA** and **RAM** hanging off the **DN3000 bus**; then a **Bus Interface** block to the **AT-compatible Bus**, carrying **Winch**, **Floppy**, **Graphics**, **Network** and **Ct Tape**. That is the dual-bus arrangement `008778-03` §1.1 and §1.2 describe and `board/ap_board.c` models — the DN3000 bus for the core-board devices, the AT bus behind an interface for the cards — drawn rather than stated. No register appears on it, so nothing to derive; recorded because a `confirms` row is what distinguishes a page that was read and agreed from one nobody opened |

| 286 | §12 **CPU (MMU) CONTROL/STATUS REGISTER** `[8000 | 03FFB400]` / `[8100 | 03FFB500]` | **THREE GAPS NAMED** + `confirms` **(the parity field and the acknowledge)** | The register `board/ap_boardreg.h` models, given as two bit maps. **Status (read)**: `mto` mem space timeout, `uto` i/o bus, `dto` i/o bus DMA timeout, `pdm` parity during DMA, `pio` IO parity error on an i/o bus reference, `cto` on-board CPU timeout "(ref to non-existent mem)", **`pe3`–`pe0` "1 => parity error (failing byte)"**, `ip` int pending, `fp` fp owner trap, `iot` i/o bus i/o cycle timeout, `nm` normal mode. The four `pe` bits are `AP_BOARDREG_STATUS_PARITY_MASK 0x00F0` exactly, and **"To clear bus timeouts or parity conditions from status register, write to the status register. This register is readonly"** is `ap_boardreg`'s write-acknowledges rule, stated. **Control (write)**: `ld7`–`ld0` in the upper byte, `pe3`–`pe0` "force parity err (bytes 3-0)" at 7–4 -- `AP_BOARDREG_CONTROL_PARITY_LANE_MASK` -- then `dg` enable diag mode, `fp` enable fp owner trap, `rsa` reset on-board devices, `nme` non-maskable interrupt enable at bits 3, 2, 1, 0. Bits 3 and 2 are `FORCE_BAD_PARITY` and `FPU_TRAP` already. **The three findings**: (1) the upper byte is documented **"1 => led off"** — **since settled**: the reference boot opens `FF EF DF FE EE DE ...` and reaches `00` once, which is a machine starting all-dark and lighting lamps as it goes, so the polarity is real and `ap_boardreg_post_code` is right to store the byte uninverted; the polarity is now recorded beside it so a **clear** bit reads as a **lit** lamp; (2) **bit 1 `rsa`, "reset on-board devices", has no constant at all**, and the page adds the exclusion "**Neither RSA nor the reset instruction reset the SIO lines**"; (3) bit 0 is `AP_BOARDREG_CONTROL_INTERRUPT_ENABLE` here but is **`nme`, non-maskable interrupt enable**, and the page states a gate this core does not implement -- "**Non-maskable interrupts must be enabled to receive parity errors**", which would make `ap_parity`'s level-7 path conditional on this bit. Also stated: the register "is write-only (**the BSET instruction may not be used** to turn bits on and off)" and both registers "respond to byte mode access". All three are named in `docs/COMPLETION_PLAN.md`; none is implemented here, because each needs the firmware checked against it rather than a constant added |

| 287 | §12 **DISK (FLOPPY/WINCHESTER) CONTROLLER** `[4D000 | 3FFA800]` — status, jumper and interrupt mask registers | `confirms` **(the status register bit for bit)** + **one transposition** | The Winchester half of `ap_omti`, at the `4D000`/ISA `1A0` page 279's table gives it. **Status register**: `int` interrupt request, `dma` dma request, `slc` controller selected, `ph` "1 => command phase, 0 => data phase", `dir` "1 => transfer to host", `avl` available for transfer — which is `[OMTI]` Table 4-2's `IREQ`/`DREQ`/`BSY`/`C/D`/`I/O`/`REQ` at `20`/`10`/`08`/`04`/`02`/`01`, all six in the same positions as `AP_OMTI_ST_*`. "Write anything to this register to RESET controller" and, for the jumper register, "write anything ... to generate controller select" are Table 4-1's two write-side functions, stated. **Jumper Setting Register `[4D002]`** names the configuration byte's low four bits `j1`-`j4`, "jumperN in place" — `[OMTI]`'s `W20`-`W23`, so this core's measured `FC` is jumpers 1 and 2 fitted. Recorded in `ap_omti.h`. **The transposition**: the **Interrupt Mask Register `[4D003]`** is drawn with `dma` at bit 1 and `int` at bit 0, where `[OMTI]` §4.2 gives "Bit 1 INTERRUPT ENABLE ... Bit 0 DMA ENABLE" and `omti8621.cpp` has `INTE 0x02`/`DMAE 0x01`. The part's own manual is followed; the disagreement is named in `ap_omti.h`. **Two drawing conventions to know before reading any register on these pages**: unused bits are printed `0` where `[OMTI]` says "Not Used (Set to 1)" — so `AP_OMTI_ST_FIXED` is unaffected — and the *section header's* address is repeated on the first register, which is why the status register is printed at `4D000` when p. 12-10 gives that address to the data port and Table 4-1 puts status at offset 1 |

| 288 | §12 **Command/Data/Status Register** `[4D000]` — the CDB, the control byte, the status phase byte | `confirms` **(a second source for the whole descriptor block)** + **one gap named** | "All commands are given by moving **6 bytes** ... The copy command is the only exception which requires **10**", and the three phases — which is `AP_OMTI_CDB_SHORT`/`_LONG` and `ap_omti_phase_t`. The byte layout is §5.1.1's: `MSB CYL` at byte 1 bit 7, head in 4-0, `Cyl.High` in byte 2's top two with the sector below, cylinder low in byte 3, interleave-or-block-count in 4, control in 5. It draws `LUN` across bits 6-5 where §5.1.1 has bit 6 "not used" and bit 5 the LUN; ours is bit 5. **The control byte's step table matches `[OMTI]` §5.2 value for value in seven of eight rows**, and the eighth is Apollo's own: `001` — "10 microseconds, buffered step" — is marked **N/A**, a rate none of this machine's drives takes, and `000` is named "unknown drive". Recorded in `ap_omti_cdb.h`. **Status phase byte**: bit 1 error and bit 5 drive number are §5.3's, and the page adds a legend for **bit 6**, "1 => winchester status, 0 => tape status", which §5.3 gives as one of four bits set to zero — not modelled, since the tape is an SC-499 at its own address and does not answer here. **The gap this page opened is §5.3's own**: bits 3-2 are an **Error Recovery Status** field this file had never named. Always `00` here because nothing in the model retries, which is now stated with the four values rather than left implicit — `COMPLETION_ERROR_RECOVERY_*` |

| 289 | §12 **Error and Operation Codes** | **GAP FOUND AND FIXED** + `confirms` **(twenty op codes and nineteen sense codes)** | A sense-code list and an op-code list, both agreeing with `[OMTI]` Appendix A and §5.1.2 almost everywhere. **The finding is `23 Volume Overflow`** — printed here beside the four codes this core emits, and there was no path in `ap_omti.c` that could produce it. Appendix A-5 defines it: "after the **commencement of a multiblock command**, the end of volume was reached", one line below `21 Illegal Disk Address`, "a command with a Sector Address beyond the capacity of the drive". **The two divide the same failure by when it happens, and every multiblock path here reported `21` for both** — telling a driver its descriptor block was wrong when the block had been accepted and sectors had already crossed the bus. Eight sites fixed: `feed`, the streaming write, READ VERIFY, `1E`, `1F`, COPY, READ LONG and WRITE LONG's placement; the address-refusal helper now takes the code, and two arms that ended with a bare sense byte now report **where** the transfer stopped. Two tests. Detail in `PROJECT_STATUS.md`. **Two op codes differ** and both are recorded rather than adopted: Assign Alternate Track `$10` (§5.1.2: `11`; `10` is CHECK TRACK FORMAT) and Read to Sector Buffer `$30` (§5.1.2: `1E`) — and the second is settled by measurement, since Domain/OS issues `1E` in this core's own boot. Apollo also lists two diagnostic codes the appendix stops short of, `32` and `33`, which is where a vendor firmware ROM would show. And the page gives **MD's disk error line** — `DISK operation ERROR: xx recordnum unit type`, "returned by `sense_status`" — now in `MD.md` |

| 290 | §12 **Floppy** `[5F800]` — Digital Output and Main Status registers | `confirms` **+ three careless annotations** | The floppy half at ISA `3F0`, with `3F2` and `3F4` where `[OMTI]` Table 4-3 puts them. **Digital Output**: `BEN`/`AEN` motor enables at 5 and 4, `ENB` "enable interrupts and DMA" at 3, `RST` at 2, `SLC` drive select at 0, bits 7-6 reserved — `AP_OMTI_DOR_*` exactly. **Main Status**: `RQM` "data register ready to send/receive", `DIO` direction, `EXM` "execution mode for non-DMA transfers", `CB` busy — ours, and the prose "Recalibrate, Specify, and Seek return no status information" confirms `ap_omti_fdc_result_bytes` answering zero for those three. **What not to take from it**: the reset bit is annotated "1 => Reset" where both OMTI manuals say the floppy is reset when the bit is **0** — `AP_OMTI_DOR_NOT_RESET` is right — and the annotation immediately above it reads "Drive motor B enable" for the bit named `AEN`, printing the same legend twice. The status register is drawn with **four** seek-mode bits `D3B`-`D0B`, the generic 765's, where `[OMTI]` Table 4-3 and the 8640's §5.1 both give bits 3-2 as reserved and two drives; ours is two. Three loose statements on one page, which is the calibration for p. 12-14 |

| 291 | §12 **Status Registers 0, 1 and 2** `[5F805]` | `confirms` **(ST1 and ST2 entirely; ST0 but for two bits)** | ST1's six live bits — `ECY` end of cylinder, `DE` data error, `OR` overrun, `ND` no data, `NW` write protected, `MA` missing address mark — and ST2's seven — `CM` deleted data-address mark, `DD`, `WC` wrong cylinder, `SH` scan equal hit, `SN` scan not satisfied, `BC` bad cylinder, `MD` — are `AP_OMTI_ST1_*` and `AP_OMTI_ST2_*` position for position, every one. ST0's interrupt code, seek end, equipment check and unit field likewise. **It differs in two bits**: `NR` not ready at 3 and `HD` head address at 2, which `[OMTI]` §6.4.1 gives as "Not Used - Always zero". Same shape as p. 12-14's ST3 and resolved the same way — the part's own manual, with the handbook's names recorded in `ap_omti.h` |

| 292 | §12 **Status Register 3**, **Additional Control Register** `[5F806]`, **Digital Input Register** `[5F807]` | **TWO GAPS FIXED** + **one open question named** | **The Additional Control Register is a documentary gain**: `[OMTI]` Table 4-3 lists it by name and never decomposes it, and this page is the only source on the shelf for its fields — `WP2`-`WP0` write precompensation at bits 2-0, `PN2` "interface pin 2 (density & speed control)" at 3, `PN4` and `PN6` above. Named in `ap_omti.h` and exposed; none is acted on, because precompensation and recording density are properties of a magnetic surface and an `.afd` image is decoded sector data. **Reading it against the code found a defect it would not otherwise have shown**: `3F6` and `3F7` were writing into **one byte**, so selecting a data rate cleared whatever precompensation had been programmed. Table 4-3 has them as two registers; they are two fields now, `3F7`'s rates transcribed from the 8640's §5.1 — `00` 500 Kbit/s, which is the rate `008778-03` §7.2 requires of this drive and so the value a reset already selects. Hashed, tested. **ST3 is the open question.** `[OMTI]` §6.4.4 and the 8640's §5.6.4 give three live bits (`WP`, `T0`, `HD`) and five constants; this page gives the generic 765's eight — `FT`, `WP`, `RDY`, `TR0`, `TS`, `HD`, `UN1`, `UN0`, each "from drive" — and `008778-03` §5.4.1.2 does name the part an "FDC765". **Our code was following neither**: it built the byte as `ALWAYS | unit`, the part's constant and the handbook's unit field at once, answering `03` for drive B — a value no document describes. Resolved to the part's own manual, with the handbook's table, the reason for the choice and the discriminating experiment recorded in `ap_omti.h` and `PROJECT_STATUS.md`. The page also names all seven bits `[OMTI]` calls reserved in the **Digital Input Register** — `WG`, `HD3`-`HD0`, `UN1`, `UN0` — recorded and not modelled, since three of them describe a surface this image format does not have |

| 293 | §12 **Diskette Control Register** `[5F807]` | `confirms` **— of a fix made one page too early** | Bits 7-2 unused, bits 1-0 `TRN RTE` transfer rate: `00` 500 KBPS, `01` 300, `10` 250, `11` N/A. Cited to `/os/ker/flp.pas`. That is the table `ap_omti.h` had just taken from the **8640** manual's §5.1 because `[OMTI]` does not decompose the register — and here it is for this machine, at this address, agreeing row for row. It also confirms, from Apollo's side, the thing p. 12-14 had made visible: `3F7` is a register in its own right and not part of `3F6`. Nothing to change; recorded because a fix that a later page independently confirms is worth more than one that merely still passes its test |

| 294 | §12 **DISPLAY (COLOR)** `[5E800]`, display memory, status and display-type registers | `confirms` **(the geometry and the ID)** + **one disagreement with the oracle** | "4 planes, each **1024x1024** (1024x800 visible)" and display memory at `A0000`, which is `ap_graphics`'s 4-plane geometry and its buffer width — taken from `008778-03` §1.5.3's "512 KB ... in four 128-KB planes" and now stated directly. The **Display-Type Register at `5E801` reads `08`**, which is `AP_SCREEN_COLOUR_4_PLANE = 8`: the screen-kind encoding came from the oracle and is now Apollo's. The status register's eight bits are the eight this core drives — but **two of the colour board's names are exchanged relative to the oracle's aliases**: bit 5 is `syn` "composite sync" where MAME calls it `DONE`, and bit 2 is `ad` "a-d conversion done" where MAME calls it `SYNC`. Bit 0 is `lok`, "okay to load LUT", where MAME calls it `V_FLAG` on a 4-plane board. `PROVISIONAL`: what this core *drives* is the oracle's arrangement, the boot PROM's display-present probe tests bit 2, and the handbook describes the 4-plane board where the 8-plane one is a later design. Named in `ap_graphics.h` and `PROJECT_STATUS.md` with the experiment that would settle it |

| 295 | §12 **Raster Op Register** `[5E802]`, **BLT Control Register** `[5EC00]` | `confirms` **(all sixteen operations and the mode field)** + **one field explained** | The raster op register is **16 bits on the DN3000**, four 4-bit fields `rp3`-`rp0`, one per plane — which is the baseline `008778-03` §10.3's "ROP Register specifiers increased to **32 bits**" is measured from, so the two documents describe the same register on two boards and this core models the wider one. All **sixteen** operations match `ap_graphics_rop_t` name for name, `$0` Zero through `$F` One. The BLT Control Register is `CR0`: bits 7-5 the mode, bits 4-0 the shift. **Six of the eight modes are listed and `101` and `110` are not** — the same two the oracle marks `???`, so two independent sources have the same hole, which is a stronger statement than "MAME does not know". **And the shift count is "Signed ... (positive is right-shift)"**, five bits, -16 to +15. That is what the shifter's rotate-when-≥16 branch has always implemented — copied from the oracle and described here as a mechanical rule — so the field now has a meaning rather than a recipe. `ap_graphics_cr0_shift_signed` added, tested |

| 296 | §12 **Misc Control Register** `[5EC02]`, **Plane Select Register** `[5EC04]` | `confirms` **(both registers, bit for bit)** | `CR1`'s eight bits, in Apollo's names: `adb` additive blt or normal memory access, `dvc` disable vert mode clock, `dhc` disable horiz clock, `ren` raster op enable, `rst` reset sync generator, `dpc` diagnostic pixel clock, `syg` enable sync generator, `ven` video enable — `AP_GRAPHICS_CR1_*` exactly, including the colour board's top two. It also **explains** what this core had observed: the three clock bits are "watched by hardware only when **syg = 0**", so with the sync generator off they are the clocks, which is why writing one steps a counter. `CR2` likewise: bits 7-6 the BLT source control with all four values printed — "all ones (used for vectors)", "replicate 4 lsb of data bus", "replicate lsb of shifter", "use source data unchanged (normal use)" — bits 5-4 the source plane, bits 3-0 the destination mask with "**0 => select plane N**" and "any combination". The active-low destination select and the four access modes were the oracle's; they are Apollo's now |

| 297 | §12 **Chip Init Register** `[5EC06]`, **Write-enable Register** `[5E800]`, the **LUT registers** | **A GAP CLOSED** + **a part named** + `confirms` | **`CR3A` is an 8255A's control register** — "Initializes the 8255A chip. Set just once per system reset", `$80` "set all ports to basic output mode". This core implements the part's bit set/reset command on it and ignores anything with bit 7 set, which was a mask that happened to work; it is now the part's own mode-set word, and port C is wired to `CR1`. **The initialization sequence is printed in full** — `Chip_reg $80`, `Misc_cntl $8B` "enable display, normal memory access", `Plane_sel $CE` "select plane 0", `Blt_cntl $E0` "normal memory access, no shift" — four registers with Apollo's own reading of each value, which `graphics_suite` now asserts as one test. Write-enable: sixteen bits, "**0 => writing enabled, 1 => writing disabled**", confirming the protect-and-OR model. **And the gap**: the 4-plane board's palette is three registers, Red `5EC01`, Green `5EC03`, Blue `5EC05`, each an entry in the top nibble and a four-bit level in the bottom, write-only, sixteen entries. `ap_scanout_palette` answered an even grey ramp flagged "not real" and named these registers as the thing it could not model. **Implemented**; detail in `PROJECT_STATUS.md` |

| 298 | §12 **Diagnostic Register** `[5EC07]` | **CLOSES A "no document exists" NOTE** | The A/D converter's own port on the colour board. **Write**: bits 7-3 zero, bit 2 one, bits 1-0 the channel — 00 Red, 01 Green, 10 Blue **Video Output**. **Read**: `00` 0.00 V, `01` 0.01, `80` 1.28, `FF` 2.55. `ap_graphics.h` carried "the search for a document is exhausted ... the only mention of it anywhere in `docs/references/` is `002398-04` p. 4-23, and it is an *error code* rather than a specification", and described the converter as reading "a monitor's identification and a brightness pot". Both wrong, and the page that says so was four pages from the ones being quoted, in the chapter the registers live in. The channel encoding is **the one `ap_graphics_adc` already implements** — arrived at from the oracle, now confirmed from Apollo — and the read format adds the scale nothing here had: the byte is hundredths of a volt, so the boot PROM's `[52, 70)` range check is 0.52 V to 0.70 V. The register was gated "8-plane only" and answered `FF`, so the one board with a port of its own was the one that could not be asked. **Implemented and tested** |

| 299 | §12 **DISPLAY (MONOCHROME)** `[5D800]`, status, display-type, raster op, BLT control | `confirms` **(all eight status bits and the ID)** | "Display memory `[FA0000]` **2048x1024** (1280x1024 visible)", which is `ap_graphics`'s monochrome geometry and the buffer width `008778-03` §1.5.3's 256 KB divides out to. The **Display-Type Register at `5D801` reads `09`** = `AP_SCREEN_MONO_19_INCH`. The status register's eight — `blk`, `vb`, `hs`, `rmc`, `alt`, `vs`, `hck`, `vd` — are this core's monochrome names in this core's positions, every one, which is what makes p. 12-16's two exchanged colour aliases a disagreement about the *colour* board specifically rather than about the register. The raster op register has one 4-bit field, "definitions for rp values same as color controller", and the BLT control register is "same as color". **Both are printed at the colour block's addresses** (`5E802`, `5EC00`) although this section's own block is `5D800`: the monochrome pages were written by copying the colour ones and only the first two addresses were changed |

| 300 | §12 monochrome **Misc Control** and **Plane Select**, chip init, write-enable | `confirms` **+ one digit corrected** | The monochrome `CR1`: bit 7 `inv` "Inverse Video Control (0 => 1 is black ...)" and bit 6 `b17` — "**BLT Mode 4 destination address bit 17** (syg = 1) / when syg=0, disable vert mode clock" — against this core's `AP_GRAPHICS_CR1_MONO_DADDR_16`. **Bit 17, not 16**, and the bit has two functions chosen by whether the sync generator runs; the oracle's constant name is kept and the digit recorded, since neither function is modelled. The monochrome plane select keeps only the BLT source control in bits 7-6 and marks 5-0 unused, which is what one plane leaves. Chip init and write-enable are "same as color". Cites `color4_$disp_regs in /us/ins/color4.ins.pas`. Again at the colour block's addresses |

| 301 | §12 **DMA CONTROL** `[9000]`/`[9100]`, channel usage, command and status registers | `confirms` **(all eight command bits)** + **one table that disagrees with `008778-03`** | "DMA control uses two Intel **8237** DMA Controller Chips. The first supports only 8 bit transfers (channels 0-3) and the second 16 bit transfers (channels 5-7). Channel 4 is used to cascade channels 0-3 to the cpu and so is not available for dma." The **Command Register**'s eight bits — `ack` DACK sense active high, `drq` DREQ sense active low, `slt` extended write, `pri` rotating priority, `tim` compressed timing, `ctl` controller disable, `hld` hold enable, `mmc` memory to memory — are `AP_I8237_CMD_*` position for position, the two polarity bits included. The **Status Register** is requests in 7-4 and terminal counts in 3-0, which is what `ap_i8237`'s `status` field holds. **The channel usage table is a kernel allocation table and disagrees with the hardware one**: `CH1 SDLC option` where `008778-03` Table 2-4 has DRQ1 **Tape** and Table 8-1 straps the SC-499 to "DMA Channel 1", and `CH7 avail` where Table 2-4 reserves DRQ7 for the Winchester. Reading that fits both: "avail" means unclaimed in the base configuration — and it follows that a machine cannot carry both the tape and the SDLC option, which nothing states outright. Recorded in `ap_dmapage.h`; nothing changes |

| 302 | §12 8237 **mask**, **mode** and **clear byte pointer** registers | `confirms` **+ one address slip** | Single Channel Mask at `900A` (channel in 1-0, `msk` at 2), All Channels Mask at `900F` (`c3m`-`c0m`, 1 disables), Mode at `900B` — mode in 7-6 with `00` demand, `01` single, `10` block, `11` cascade; `ads` address decrement at 5; `aut` auto-initialize at 4; transfer type in 3-2 with `11` illegal; channel in 1-0. Every one is `AP_I8237_MODE_*` and `AP_I8237_REG_*` unchanged. **The slip**: "Clear Byte Pointer `[900D]`. This register must be written to (any value) before loading address and count registers" — the 8237A's byte-pointer flip-flop is offset **`C`**, and `D` is **master clear**, so a driver following that line would reset the controller before every transfer. The other five addresses on the page are right, which is what makes it a slip and not a board difference |

| 303 | §12 **DMA Page Registers** `[9200]`, and a forward reference | `confirms` **(a page already derived)** + **schedules chapter 7** | The seven page registers in their non-order, the "high 8 physical address bits", and the 1024-byte note — all already quoted verbatim in `ap_dmapage.h`, which cites this page. Re-read in place rather than skipped, which is the difference between a page consulted and a page walked. **And the finding is the last two lines**: "FAULT FRAME AND FAULT TYPES — Refer to the FAULT FRAME section of **CHAPTER 7**, DN300,320". So chapter 12 delegates the DN3000's fault frames to a chapter the reading order treats as another machine family. Recorded in the Resume block: chapter 12 is not finished until chapter 7's fault-frame section is read |

| 304 | §12 **FAULT VECTORS** | `confirms` **(the vector table and eleven IRQ assignments)** + **one disagreement about the modelled card** | The whole 68000 exception table with the DN3000's own annotations — `1F 07C` "**Parity Error non-maskable level 7**", which is `ap_parity`'s autovectored level 7 and its vector; `18 060` spurious; `19-1E` "Auto vectors 1-6 (unused)"; the FPU's seven at `0C0`-`0D8`; and `40-9F 100` "**User Int Vectors - used for prom entry pts**", which is where this family's boot PROM keeps the entry-point table PDF 278 showed for the DSP80. Then vectors `A0`-`AF` at `280`-`2BC` against the sixteen IRQs: timers, sio "(2 ports)", **ring**, "PIC 2 to PIC 1 (unseen by software)", tape, floppy, calendar, the two ethernet boards, PC-board primary and alternate, "Diagnostic Interrupt", winchester. `IRQ2` for the ring and `IRQ3` for the cascade are `AP_BOARD_RING_IRQ` and the measured cascade, stated a second time; the ring's vector `A2` also confirms the `162` side of the tension `ap_board.h` records against finding 53d's `163`. **The disagreement**: "IRQ9 - Ethernet board 1, IRQ10 - Ethernet board 2", where `008778-03` §14.1 says the *first* card uses IRQ10 and the second IRQ9, and Figure 14-3 draws one card jumpered "Interrupt Level 10" and "Hex Address 300" **together**. `AP_BOARD_ETHERNET_IRQ` stays 10 — a figure of one card's straps outranks two summary tables, and the handbook's neighbours transpose a mask register and misaddress a DMA register — and the disagreement is named in `ap_board.h` rather than resolved away |

| 305 | §12 **PARITY ERROR REGISTER** `[9300]`, **PROGRAMMABLE INTERRUPT CONTROLLER** `[9400/9500]` | **GAP FOUND AND FIXED** + `confirms` | `008778-03` Table 2-8 gives the register at `9300` only a name — "Latch Page on Parity" — and this core modelled it as sixteen bits of storage on that basis. This page gives its contents: "**PARITY ERROR REGISTER (read only)**", bits 13-0 the **FAILING PPN**, "the upper two bits must be masked off", cited to `mmu_$parity_ppn`. `ap_board.c`'s `parity_error` already latched `address >> 10`, which the fourteen-bit field confirms as a page number on a 1024-byte page — but a **bus write stored here**, so a program could overwrite the one thing a parity handler exists to read. **Fixed**: read-only, with a test, and the width recorded as the DN3000's 24-bit space rather than enforced, since `019411-A00` gives the Series 4000's page number as bits `<25:10>`. The PIC section confirms the rest: two 8259As, master `9400` taking lines 0-7, slave `9500` taking 8-15 "cascaded through request line **3** of the master", `0A`-then-read for the request register, `0B` for in-service, `20` non-specific EOI, and the mask register with "1 => channel is masked" |

| 306 | §12 **PIC Initialization Sequence**, **Interrupt Request Line Assignments** | `confirms` **(every initialization word)** | "Interrupt vectors are programmed to start at **$280**", then the words themselves: master `ICW1 19` "level triggered, need ICW4", `ICW2 A0`, `ICW3 08` "have slave at IRQ3", `ICW4 01` "normal EOI, 8086 mode", `OCW1 FF`; slave `ICW2 A8`, `ICW3 03` "am slave with ID 3", the rest alike. `ap_board.h` already cited the boot PROM's own `ICW2 = A0`; here is the whole sequence with Apollo's reading of each word, and every one is a value `ap_i8259` decodes. The assignment table repeats p. 12-26's, and adds the consequence: "On slave interrupt, EOI slave first, then master. The priority of **IRQ8-15 is higher than IRQ4-7** since they use the second PIC which is slaved to the master at IRQ3" — which this core gets from modelling the cascade rather than from a priority table, and is the kind of statement a model has to *earn* rather than encode. One difference between the chapter's two tables: p. 12-26 names `IRQ13` "Diagnostic Interrupt" and this one leaves it blank |

| 307 | §12 **RING** `[51000-51C00 59000-59C00]` — the buffer, and the whole register map | `confirms` **(the read/write map and the pipelined port)** + **two version differences recorded** | The eight ring pages page 279 flagged, tabulated: ISA address, physical, AEGIS virtual, and **what each offset means read and written**, which are different registers — `220`-`226` read `Node_ID3`-`Node_ID0` and write `SOFT_RCV_REQ`, `XMIT_ACK`, `RCV_ACK`, `TIMO_ACK`; `228`-`22E` write `ERR_BITS_CLR`, `GPS_CLR`, `SOFT_XMIT_REQ`, `LERR_CLR`; `23E` reads `Node_ID_CHECKSUM`; `320` reads `BOARD_TYPE` and writes `BOARD_RESET`; then `MISC`/`XMIT`/`RCV` status and command, `RAM_ADDR`/`RAM_DATA`, and the six packet counters. **Every write-side name is `AP_RING_CTL_W1_*` already**, and the whole second-window layout is what `ap_ring_ctl` decodes — this page is where several of those names came from, re-read in place. **And it states the pipelining outright**: "After setting ram_addr, the **first word read should be discarded**", which this core derived from the firmware ("both readers discard a `move.w` before their loop") and which is now documented rather than inferred. **Two version differences, both consistent with the single-board/two-board split `008778-03` §2.11 shows**: the DN3000's first window reads the **node ID** where this core's reads a **PROM image**, and its buffer is "an **8K WORD** buffer ... room for 7 rcv msg buffers (each 1k bytes of header and 1k bytes of data) and 1 xmit msg buffer" — 16 KB, internally consistent, against the 64 KB this core's ring firmware walks in its own memory test. Recorded, not reconciled: two boards, and the modelled one is the board the firmware tests |

| 308 | §12 **MISC_STS**, **RCV_STS** `[59400/59404]` | `confirms` **(every bit of both)** | `nct`, `tmo`, `xby`, `rby`, `iov`, `rlk`, `esb`, `bpe`, `gps`, `xi`, `ri`, `tmi` — the twelve live bits of MISC_STAT, at the twelve positions `AP_RING_CTL_STATUS_*` gives them, with the active-low ones marked `0 =>` as this core has them. RCV_STAT's sixteen likewise, `pe` down to the three `rc2`-`rc0` counter-output bits whose meanings this file already quotes from this page. Cited by `ap_ring_ctl.h` before the walk reached it; read whole now, and nothing in it is new |

| 309 | §12 **XMIT_STS** `[59402]`, both layouts | `confirms` **(including the layout switch)** | One register with two meanings for bits 14-8, chosen by bit 15 `pe`: with it clear, `cpd`/`wak`/`icp`/`pke`/`de`/`abt`/`ife`; with it set, `tmo`/`syn`/`ern`/`frm`/`akp`/`abt`/`ife`. That is `AP_RING_CTL_XMIT_PE` "selects bits 14-8" and the two sets of constants beneath it. The transmit tags are confirmed as a *state* encoding rather than two flags — "`00` msg complete, `01` data being transmitted, `11` hdr being transmitted" — and `ife` is documented as meaningful "IFF abt = 1", which is the conditional this core already applies |

| 310 | §12 **MISC_CMD**, **RCV_CMD**, **XMIT_CMD**, the address registers | `confirms` **(and the firmware settles a bit position the page leaves ambiguous)** | `bpm`, `nct`, `td1`, `td2`, `lpb` in MISC_CMD; `rcv` alone in RCV_CMD; `fen`, `ten`, `ine` in XMIT_CMD — every one `AP_RING_CTL_*_CMD_*`. Three statements this core models and one it records: "Force Transmit flag is a **modifier** to Transmit Enable, not a separate command"; "Set `ten` to 0 to **abort** an enabled transmit"; "Set `rcv` to 0 to abort an enabled receive"; and "**Bad packet marking enable is only operational on the single board**", which is a fourth instance of the two-version split. The bit *positions* are read from the firmware rather than counted off the ASCII box — `RING_PROC` writes `$800` and `$900` to MISC_CMD, which is `nct` alone and `nct` with `lpb`, fixing the field at bits 11 and 8. The address registers' byte-swapped layout is the one `ap_ring_ctl.c` already cites from this page |

| 311 | §12 **COUNTER REGISTERS**, hardware packet types | `confirms` **(the counters are 8254s, and the control words prove it)** | "The packet counters ... are implemented with **Intel 8254** Chips", which is what `ap_ring_ctl` models at the second window's `+800` and `+C00` banks. The page prints the control words a driver writes, and every one decodes as an ordinary 8254 word: `$3000`, `$7000`, `$B000` to load counters 0, 1 and 2; `$C200`, `$C400`, `$C800` to latch one counter's count **and status**, or `$CE00` "to prepare to read all three" — which is the read-back command for all three counters. So the documented sequences exercise `ap_i8254`'s read-back path, and the "status in high byte of word, lsb, msb" ordering is that command's own reply format. **Also here, and for `RING.md` rather than for code**: the hardware packet types — `80` broadcast, `40` hw_diag, `20` thank you, `10` please, `8` paging, `4` user, `2` sw_diag |

| 312 | §12 **RING SOFTWARE STATUS INFO** | `none` **(AEGIS, not the controller)** + `confirms` **(the XMIT_STS bit names, from a third angle)** | `RING_$SEND_STAT_T`'s sixteen bits, what each is counted in, what `netstat` calls it, and — the useful half — **how each is derived from the DN3000's XMIT_STS**: "copied" is "NO errors AND `cpd` AND `icp`", "nacked" is "NOT `pe` AND NOT `icp`", "overrun" is "`abt` AND `ife`", "not connected" is `nct`, and so on. That is AEGIS's mapping and not the controller's, so there is nothing here to implement; what it does is name every XMIT_STS bit a third time, in a document written for whoever reads a running system's statistics. Four bits are marked "**Status not defined for DN3000 transmits**" — bus error, biphase error, esb error and one more — which is worth knowing before a DN3000's `netstat` output is trusted |

| 313 | §12 **SIO** `[8400]`, **TIMERS** `[8800]` — the chapter's last page | `confirms` **+ delegates two more sections** | "**Channel A is the keyboard, Channel B is the single sio line**", which is `ap_sio.h`'s "§3.9: `SIO_O` is used for the keyboard. Channel A of the first part" from a second document, and confirms the single user line one 2681 gives this machine. "The chip is also used to provide **timing for memory refresh**" — a use no register exposes and nothing here models, recorded because it explains why the part is programmed on a machine that is not using its second channel. Then the delegations: SIO is "same as first chip of **DN460 (CHAPTER 9)**, except for the definition of the output port register in which the **`dtr_b` bit has moved**" — so chapter 9 holds this machine's SIO register definitions and one named difference — and TIMERS is "same as all previous systems", with no chapter named at all |

### Chapter 12's delegated sections, read out of order

Recorded here rather than under chapters 7 and 9, because they are chapter 12's
content and the reason they were read early is that chapter 12 sends the reader
to them. The rest of both chapters is unread and stays scheduled.

| Pages | Section | Status | What it yielded |
| --- | --- | --- | --- |
| 153–155 | §7 **BUS/ADDRESS ERROR STACK FRAME FORMAT (DN330)**, **FRAME FORMAT/VECTOR OFFSET WORD**, **SPECIAL STATUS WORD**, **FAULT TYPES** — p. 12-25's delegation | `confirms` **(the frame set and both bus-fault layouts)** | The medium frame at `+00` SR, PC, `1010 0000 0000 1000`, internal register, special status word, instruction pipe stages C and B, data fault address, internal registers, data output buffer — sixteen words — and the long frame's continuation through stage B address, data input buffer and internal registers to `+5C`, forty-six words. The format table gives `0000` four-word, `0001` throwaway, `0010` six-word, `1001` coprocessor mid-instruction (10 words), `1010` medium bus error (16), `1011` long bus error (46), which is `ap_m68030_frame_format_t` code for code and word count for word count. The one entry this core does **not** have is `1000`, "29-word 68010 bus error", which the 68030 does not produce — correctly absent rather than missed. Also here: "Real fault address: `DF` => data fault address; `FB` => if medium then PC + 4, if long then stage b address", and the special status word's FC/size/read-write/RMW fields. `FAULT TYPES` groups the exceptions by whether the instruction is aborted, taken before the next instruction, or processed normally — the 68000 grouping, unchanged |
| 157–158 | §7 **FLOATING-POINT FORMAT**, **FLOATING-POINT REGISTERS (DN330)** — p. 12-26's delegation | `confirms` **(the whole FPCR)** | Single and double precision with the implied normalisation bit, exponent plus 127 and plus 1023 — IEEE, and what `ap_m68882_format` implements. The **Floating Point Control Register** is the 68881's: zero in 31-16, then `bsun`, `snan`, `operr`, `ovfl`, `unfl`, `dz`, `inex2`, `inex1` in 15-8, `prec` in 7-6 (00 extended, 01 single, 10 double, 11 "undefined, reserved"), `rnd` in 5-4 (00 nearest, 01 zero, 10 minus infinity, 11 plus infinity) and unused in 3-0. `AP_M68882_FPCR_IMPLEMENTED` is `0x0000FFF0` — exactly bits 15-4 — and `AP_M68882_ROUND_*` and `AP_M68882_PRECISION_*` carry the same four and three values in the same order. Every field of the register, confirmed against a document written for this machine family rather than against the part's manual alone |
| 241, 170 | §9 **SERIAL I/O INTERFACE** (9-14) and §7 **SERIAL I/O INTERFACE** (7-32) — p. 12-35's delegation, which delegates again | `confirms` **(the part, the register map and both baud tables)** + **one fact defined nowhere** | Chapter 9 sends the reader to chapter 7, so the chain is 12 → 9 → 7 and both were read. The part is a **Signetics SC2681**, which is `ap_mc68681`'s family, and 7-32 prints the whole register map — MR/SR-CSR/CR/RHR-THR for both channels, IPCR-ACR, ISR-IMR, IPR-OPCR, and the two Set/Reset Output Port Bits registers — at the offsets this core decodes, with the counter registers and the reserved one omitted from the summary rather than absent. **Both pages state the baud constraint**: "one SIO line can't have a baud rate from Group A while the other SIO line is set from Group B", Group A being 50 and 7200 and Group B 75, 150, 2000 and 19.2K. That is `ACR[7]` selecting one of two rate tables **for the whole chip**, which `ap_mc68681` already models — and the six rates Apollo names as group-exclusive are exactly entries where this core's two tables differ, at codes 0, 3, 7, 10 and 12. A precise confirmation of both tables from an Apollo document. **And the fact chapter 12 flagged as *different* is in none of the three chapters**: p. 12-35 says the DN3000's SIO is the DN460's "except for the definition of the output port register in which the `dtr_b` bit has moved", and no page in the chain decomposes the output port register. `008778-03` gets one bit of it — "In the DS3000, [IRQ13] is connected to **Output Port Bit 7** of the 2681 SIO chip", which `ap_sio` already implements — and says nothing about DTR beyond a connector pin. Recorded as a documentary dead end rather than an open question, because the chain is exhausted. Chapter 7's machines also carry a **separate MC6850** for the display keyboard at `8420`/`8422`, which the DN3000 does not — its keyboard is the DUART's channel A |

### Chapter 2 — the file system, started (PDF 35–45 of 35–58)

| Pages | Section | Status | What it yielded |
| --- | --- | --- | --- |
| 38 | §2 **DIRECTORY STRUCTURE** overview, **Directory Info Block** | `none` **(reference for reading a volume offline)** | A directory is a header, a linear list of sequentially used entries, an info block, hash threads, and entry blocks holding hashed entries and link text. The info block carries a version, its own length, the header length (8), and **two default ACL UIDs** — one applied to directories catalogued here and one to files. Domain/OS's structure; this core serves the blocks it lives in |
| 39 | §2 **Directory Entry**, **Directory Entry Block** | `none` **(and the shape this project's volume comparison walks)** | An entry is a **32-byte name**, then a name length and an **entry type** — `0` not in use, `1` name/UID pair, `3` name/link-data pair — then four words that are either the UID or a link-text description. An entry **block** is **150 bytes**: a forward and backward block number making a doubly linked list, a use count, a **block type** — `0` not in use, `-1` a hash block with three directory entries, `-3` a link-text holding block — and then either **three entries** or up to 144 characters of link text. This is the block `compare-volumes-by-root-directory-block` dumps and compares by hand; here is its layout |
| 40 | §2 **Directory Header**, and the **name hash** | `none` **(the most directly usable page in the chapter so far)** | The header's figures, with the defaults printed: version 1, the hash-thread count, **linear list 18** entries, **pool 429** entry blocks, **3 entries per block**, the high block number, the free block thread, the entry count and a **maximum of 1300** entries per directory. **And the hash itself, complete**: `sum := 0; for i := 1 to lnth do sum := ord(name[i]) + 2*sum; HASH_VAL := sum mod HASH_VALUE` over a 32-character name. That is enough to *find* an entry in an offline volume image rather than scan for it, which is what this project's directory-block comparisons have been doing by hand |

| 41 | §2 **Notes on directories** (cont.) — the add and search rules | `none` **(completes p. 2-6's hash)** | The rest of the algorithm the previous page began: adding walks the free block list or bumps the high block number, initialises the block, threads it onto the hash chain and puts the entry first; **searching** looks in the linear list, hashes the name, follows the hash thread to the first entry block with that synonym, searches **all three** of its entries, then follows the next block number until it is zero and returns NOT FOUND. With p. 2-6's hash this is a complete directory lookup, which is what an offline volume reader needs |
| 42 | §2 **DISK BLOCK HEADER**, **DISK $ERROR INFO** | **CLOSES A NAMED UNKNOWN IN `ap_awd.h`** | `blk_hdr_t` is **exactly 32 bytes**: the 64-bit UID of the object the block belongs to, the page number within that file, the time written, `BLKTYP`/`SYSTYP`, a checksum and the disk address. `BLKTYP` is 0 for data and 1-3 for level-1/2/3 index blocks in the file map; `SYSTYP` is 0 file, 1 directory, 2 system directory. **That is the thirty-two bytes `AP_AWD_SECTOR_BYTES` carries beyond 1024**, which this core recorded as "not established here" from the day the constant was written. So 1056 is 1024 of file data plus a per-block file-system header, not drive formatting overhead — and a raw-image reader now knows what the first 32 bytes of every sector are. Nothing changes: `.awd` images are whole sectors moved verbatim, header and all, as the controller does. Transcribed into `image/ap_awd.h`. Also on the page, `DISK $ERROR INFO` — what a driver records on a disk error: the time, the disk address, bad-track and track-per-cylinder figures, the **requested** header against the **read** header, the volume index and physical page, and a status word |

| 43 | §2 **DISK/VOLUME FORMAT** | **the on-disk layout, and an arithmetic coincidence worth chasing** | The whole shape of a volume: **block 00 of the physical volume is the physical volume label** (1 block); then **block 00 of the logical volume is the logical volume label** (1 block); **block 01 of the LV is the boot file, 10 blocks**; then free blocks, the VTOC, the BAT, more free blocks, and — at the **last block of the logical volume** — a **second copy of the logical volume label**, followed by the next logical volume. "**All disk addresses (DADDRs) in a logical volume are relative to the start of a logical volume.**" And the caveats: bad spots may leave the VTOC non-contiguous and not adjacent to the BAT, there may be dead space between logical volumes, and "bad spots may not be added to the VTOC once it has been INVOLed". **The boot file agrees with chapter 1 exactly**: p. 1-21 has the PROM asking for `/sysboot`, "records 2-b on track 0", and `2`-`b` is ten records — the same ten blocks this page gives the boot file, one block further on because the PV label precedes the LV label. Two chapters, one layout. **The coincidence to chase, recorded and not concluded**: `image/ap_volume.h` has `AP_VOLUME_LABEL_BASE 0x440`, measured, and 0x440 is byte 1088 — which is block **1** of a 1056-byte-sectored image (0x420) plus exactly the **32-byte block header** p. 2-8 defines. If that is what it is, the constant is the *logical* volume label's payload where the header calls it the physical one. The arithmetic fits to the byte and nothing here confirms it; it needs the label's own fields checked against p. 2-10's layout before anything is renamed |

| 44 | §2 **FILE MAP** | `none` **(and it bounds a file)** | Thirty-two direct pointers at `+00`-`+7C`, then a level-1 indirect at `+80`, a level-2 at `+84` and a level-3 at `+88`, each indirect block holding **256 pointers**. The classic direct/indirect scheme, and the page does the arithmetic: maximum file size is `(32 + 256 + 256² + 256³) × 1024` = **17,247,300,000 bytes**. "File map resides in VTOCE and AST entries", which ties it to the two tables chapter 1 defined. Nothing for this core; a raw-image reader needs it to follow a file past its thirty-third block |
| 45 | §2 **REGISTRY FORMAT** — header and PPO record | `none` | `ppo_$header_t`: a transaction UID for salvaging, committed and local flags, the entry count, the oldest software that can read and the oldest that can write, a minimum password length and the record length. Then `ppo_$record_t`, a 32-byte PPO name with its length and a UID. This is the registry chapter 1's boot files list reaches for — "if no registries are available, you can login only as USER.NONE.NONE" — and nothing in it is hardware |

| 35 | §2 **ACLS STRUCTURE** — ACL header and record | `none` | The access-control list's header — version, the type of object it can be an ACL for, a default node list for new entries, the entry and node-list counts — and the record that is a header followed by an array of entries. AEGIS's protection layer; nothing under it is hardware |
| 36 | §2 **ACL Entry**, **BLOCK AVAILABILITY TABLE** | `none` **(and the BAT's shape, which is worth having)** | An ACL entry is four 64-bit UIDs — person, project, organization, subsystem — then a node ID, an expiry date and the rights word. Then the **BAT**: `array[0..255] of bat_lword_t`, so **one 1024-byte block of 256 longwords**, "resides in contiguous records", and the rule that matters — "**BAT bit = 1 if block is available**", with bit 0 of word 0 being the block at `bat_hdr.base_add`. A free-space bitmap Domain/OS owns; this core serves the blocks under it and has no reason to read it, but a tool that wanted to say how full a volume is would start here |
| 37 | §2 **BLOCK AVAILABILITY TABLE HEADER**, `bat_hdr_t` | `none` **(new structure, and the offsets are label-relative)** | "**BAT header lives in logical volume label. Offsets given are from start of label.**" `+2C` number of blocks represented, `+30` number of free blocks, `+34` the disk address of the first BAT block, `+38` the block number the first BAT bit stands for, `+3C` three flags, `+40` the BAT step. The flags are `V` volume trouble — "set by OS if volume needs salvaging, cleared by SALVOL" — `C` volume CHUVOLed and `B` volume being CHUVOLed. **This is the *logical* volume label**, where `image/ap_volume` reads the *physical* one at base `0x440`, so the two do not collide; it is new structure rather than a correction. `V` is the bit a salvage-needed volume carries, which is worth knowing beside this project's mount-history work |

### Chapter 1 — AEGIS, read end to end (PDF 12–34, 23 pages)

Almost all of it is AEGIS's own structures, above the hardware this core models,
and those rows say `none` — which is the point of recording them. Four pages
carry something: the clock, the ring packet format, the boot files and the trap
codes.

| Pages | Section | Status | What it yielded |
| --- | --- | --- | --- |
| 12 | §1 **AEGIS SYSTEM RELATIONSHIPS** | `none` (recorded) | The one-page diagram the chapter opens on: user, name server, pathname to UID, the **96-bit address** as a 64-bit UID and a 32-bit offset, then disk structure, paging, physical memory, the MMU and virtual memory. Operating-system architecture rather than hardware, so nothing for this core — but the 64/32 split is the shape `image/ap_volume` and the naming-server investigations work in |
| 13 | §1 **ACTIVE OBJECT TABLE / ACTIVE SEGMENT TABLE**, `aote_t` | `none` | "A wired system-wide table that describes the current state of active objects", dynamically allocated at startup from physical memory size, with the AOTE acting as a cache of the VTOC entry. The entry's layout — cached VTOC header, VTOC pointer, front and back links, AST link, event number and the `V I U G XXXX D EEEEEEE` flag word. AEGIS's own structure, in `vm.ins.pas`; this core models the machine under it |
| 16 | §1 **AST PAGE MAPS**, reverse and forward mapped | `none` **(and worth knowing which machine is which)** | Three layouts for one field: reverse-mapped, forward-mapped for the DNx60 and DN5xx-T, and "for machines with **PMMU (Series 3000)**" — `T N R H H H H H H | PHYS PAGE NUM | W W - - - M U P I V` resident, `T N R | FULL DISK ADDRESS | - - - M U P I V` not. These are AEGIS's software page maps rather than the MMU's tables, so nothing here changes; recorded because a reader tracing a page fault through a crash dump needs to know which of the three a Series 3000 uses |
| 14 | §1 **ACTIVE SEGMENT TABLE**, `aste_t` | `none` | The AST entry: back link to the AOT, file segment number, front and back links, pointer to the file map, event number, and the `I U F R V V V V | P C C C C C C C` / `Q…Q | S S N N N N N N` flag words — `in_trans`, `used`, `fm_mod`, `remote`, the volume index, the hold count and the number of pages resident. Forward-mapped machines add the MSTE back-thread chain and the pmap's physical address. AEGIS's own table, sized from physical memory at startup |
| 15 | §1 **ACTIVE OBJECT TABLE HEADER** | `none` | The header in front of the AOT: a hash table of start pointers — `[0..60]`, or `[0..250]` on a forward-mapped machine — then the AOTE and ASTE free pointers and free lists, processes per volume, the AOT and AST sequence numbers, the **number of dismounts**, the dismounter's event count and the grace-replace pointer. The dismount count is the one field here this project has touched from the other side: `volume-label-mount-history` records reading mount and dismount times out of a volume label, and this is where the running system keeps the counter |
| 17 | §1 **CLOCK** | `confirms` **— a third source for the prescaler, and it names the three clocks' jobs** | The software clock is 48 bits, "low-order bit represents **four microseconds**", split `.high16`/`.low32`, and "**hardware clock is only 16 bits**. The upper 32 bits are maintained by clock interrupt routine" — which is the 6840's counter width and the software above it. Then the three: "**Clock 1: 4 microseconds (real-time clock), Clock 2: 8 microseconds (process timer), Clock 3: 128 microseconds (real-time intervals)**". The first two are `AP_TIMER1_HZ` and `AP_TIMER2_HZ` exactly. **The third looks like a factor-of-8 disagreement and is not**: `AP_TIMER3_HZ` is 62,500 Hz, a 16 µs period, and `ap_mc6840.h` already records the reconciliation — `CR3`'s bit 0 selects "CLK ÷1" or "CLK ÷8", and 16 µs prescaled by eight is 128 µs. So this page is a **third** document on that divisor, and it adds what the other two do not: which clock does which job, and that AEGIS runs timer 3 *prescaled* |

| 18 | §1 **DISK CONTROLLER TABLE ENTRY**, `dcte_t` | `none` **(reference for a driver trace)** | One entry per controller: type and number, status, lock number, block header pointer and page, **I/O register page pointer**, the page-zero interrupt vector, the actual interrupt entry and the device-dependent routine, then the four driver entry points — `disk_dinit`, `disk_do_io`, `disk_error_que` — the unit number and the physical DVTE indexes. Four DISK FLAGS: `N` no headers on device, `C` do checksumming for this controller, `D` driver supports multi-read requests, `L` liberty (SMD) type winchester. AEGIS's own driver table; nothing to model, and it is the structure a boot trace's disk calls run through |
| 19 | §1 **DISK VOLUME TABLE ENTRY** `dvte_t`, **EVENT COUNT** | `none` **(and the running system's side of the volume work)** | State, blocks per volume, owner process, **base disk address**, the UID of the physical or logical volume, a pointer to the controller table, the DVTE index, unit number, **disk type, blocks per track, tracks per cylinder**, the BAT step and flags. `state` is 0 free, 1 being mounted, 2 assigned, 3 mounted; `flags` are `U` use_caller_blkhdr, `N` no_crc_retry, `W` write_protect. This is the in-memory counterpart of the on-disk labels `image/ap_volume` reads, and the geometry fields are the ones a mounted volume answers from — worth having beside `compare-volumes-by-root-directory-block` |
| 20 | §1 **FAULT DIAGNOSTIC RECORD**, `fault_$diag_t` | `confirms` **(the function codes)** | A record with the magic pattern **`$DFDF`** at its head, then the status word, the register file to `+46`, a `bus_info` byte, the access address, the instruction register and flags. `bus_info` decomposes as `R` read operation, `N` not-an-instruction-reference, and `FFF` the **function code** — `001` user data, `010` user program, `101` supervisor data, `110` supervisor program, `111` interrupt acknowledge. Those are the 68000 family's FC2-FC0 and this core's `AP_M68030_FC_*` exactly. Nothing to change; the `$DFDF` pattern is what identifies one of these in a dump |
| 21 | §1 **MAPPED SEGMENT TABLE (MST)** | `none` | "A wired array indexed by asid and va. Each mste describes one segment of virtual memory", sized `asid_tn * mst_pages_tn` with `mstes_per_page = page_size div sizeof mste_t`. AEGIS's segment table, above the MMU this core models |

| 22 | §1 **MAPPED SEGMENT TABLE ENTRY**, `mste_t` | `none` **(and a per-family field worth noting)** | The UID of the file, the file segment number with its flag byte, and the VTOC-entry index. The **access field** is AEGIS's protection encoding, five bits: `00000` nil, `00010` read, `00011` read-execute, `00110` write-read, `00111` write-read-execute, then the same four again with the supervisor bit — `10010` through `10111`. The flag byte's middle differs by machine and the page spells it out: `--PPP` for the DNx60, `PPPPP` for the DN5xx-T, **`----P` for the DN3000**, `AAAAA` for others. Software protection above the MMU this core models, so nothing changes; recorded because the per-family shape is the kind of thing a reader assumes is uniform |
| 23 | §1 **MEMORY MAP (MMAP)**, `mmape_t` reverse mapped | `none` | "For **all machine types** an MMAPE is **12 (decimal) bytes** in size" — one of the few sizes in this chapter that does not vary by family. The entry: PTT index with an in-use flag, previous and current list, the next and previous physical page numbers, the low disk address, and the ASTE index with a data/code bit. Flags `I` in use, `A` wired or in transit, `R` modified but the DTM should not be updated, `O` on a memory page list, `D` holding data rather than code |
| 24 | §1 the **memory map's eight lists**, `ws_hdr_t` | `none` **(reference for a paging trace)** | The lists by name and index — `mmap_$xfree` 0, `xhi_pure` 1, `xpure` 2, `ximpure_l` 3, `ximpure_r` 4, `xos_shared` 5, `xnet_pager` 6, `xinit_proc` 7 — with what each holds, down to "pages being 'cached' by one of the network paging servers ... replaces the remote paging pool". Then the list header: current and maximum size, the first page's PPN anchoring a doubly threaded list, the owning process's last run time, the last scan time, an interrupt count and a protected size "list size won't be forced below this". This is the structure a page-fault or purifier trace walks |
| 25 | §1 **OS MAPPING** | `none` **(one number worth carrying)** | The diagram joining the two spaces: a single-node process virtual address space of **2²⁴**, divided into per-process user, per-process supervisor and global, mapped by **PID** into a network-global object space of **2³²** addressed by **UID**. Architecture rather than hardware — but the 24-bit process space is the figure a reader needs when a virtual address in a crash dump looks too small for the 32-bit MMU this core models |

| 28 | §1 **PROCESSES**, the PID/ASID table | `none` **(reference for a boot trace)** | Sixty-four processes by PID and address space: 1 the initial user process and DM, 2 null, 3 wired DXM, 4 and 5 the page purifiers local and remote, 6 unwired DXM, **7 network-receive server, 8 network-paging server, 9 network-request server**, 10 terminal helper, 11 the first user process, then user processes to 64. "Processes with an ASID of 0 run entirely in global space." Nothing to implement; this is the table that names what a PC sample or a process switch in a boot trace belongs to |
| 26 | §1 **PAGING SYSTEM** | `none` | The diagram joining the pieces the preceding pages define: UID into the VTOC's object page map, the Active Segment Table loaded on reference, the memory map table, physical memory, and the Mapped Segment Table indexed `UID:FSN` by PID through the OS map primitive. Architecture, no registers |
| 27 | §1 **PROCESS CONTROL BLOCK**, `procl_t` | `none` **+ it strengthens the chapter-5 lead** | The saved register file `d2`-`d7`, `a2`-`a7` and the USP, the ready-list links, the clock value at the start of `ec_$wait`, PID and ASID, the virtual timer and the **floating-point processor type**, CPU time, priority with its minimum and maximum, an inhibit count and a bus status. **`+40` is the RESOURCE LOCK WORD** (`.rlock`), which is what p. 1-20's lock table decodes — and the state byte's `T` bit is "time slice end with **rlock <> []**", the same concept the `CRASH_STATUS` message names. Three pages of this chapter now point at the same word; whether the crash status *is* that word is still chapter 5's to answer |
| 29 | §1 **RING PACKET FORMAT**, message header and type field | `confirms` **(the header, field for field)** + **two things to carry** | `+00` TO ADDRESS, `+04` TYPE FIELD, `+06` zeros and EARLYACK, `+08` FROM ADDRESS, `+0C` header data — which is `AP_RING_HDR_DESTINATION`, `_TYPE`, `_ZEROS`, `_EARLY_ACK`, `_SOURCE` and `_DATA` at 0, 4, 6, 7, 8 and 12 exactly. **Bit 15 of the type field is BST, broadcast**: "set in a packet intended to be received by all receivers. If it is set, the To Address field is ignored" — which is `ap_ring_header_addresses`'s second case, from a second document. **Two things this core does not have.** The type field's other fifteen bits are matched against a **controller Type register** — "if any bit selected in the Type register is a 1, the message is received" — and no such register appears in p. 12-29's DN3000 register map, so whether this controller has one is open; the page says "the APOLLO I only implements bits 14 through 8". And it gives the header data as **0–1024 bytes** ending at `+40C`, where `010005-00` gives the whole header as 12 to 1024 bytes and this core follows the protocol spec at 1012 data bytes. The ring's own standard wins; the disagreement is recorded |
| 30 | §1 **Early Acknowledge Field** | `confirms` **(every bit, and the reason for three of them)** | Bit 3 `ICP` intend-to-copy, bit 1 `PAR` parity, bits 7, 4 and 0 fixed at zero, bits 6, 5 and 2 don't-care. That is `AP_RING_EARLY_INTEND_TO_COPY`, `AP_RING_EARLY_PARITY` and `AP_RING_EARLY_MUST_BE_ZERO 0x91` — bits 7, 4 and 0 — to the bit. **And it gives the reason**, which this core had as a constant without one: the three zeros are "inserted to prevent the remaining bits in the late-acknowledge byte from being modified by the bit-stuffing protocol". `ICP` is "set by an addressed receiver if it was set up to copy a message **and the type field matched**. A NAK ... is indicated when no receiver sets this bit", and the parity is odd across the byte. Also stated: "for the purposes of the CRC calculation the EARLY ACK field is treated like a byte of zeros ... allows receivers to modify the ACK field without having to recompute the CRC" |
| 32 | §1 **SYSTEM BOOT FILES** | `none` **(reference for every boot this project runs)** | What each agent asks for, in order: the PROM wants `/sysboot`, "**records 2-b on track 0**"; `sysboot` wants `/saun/aegis` and `/saun/salvol` "only if salvage required"; `aegis` wants the OS paging file, `//`, `/`, `/com`, `/sys/node_data` and `` `node_data/boot_shell ``; the shell wants `/sys/apollo_logo` and `` `node_data/startup_shell ``; `env` chooses between `/sys/dm/dm`, `/sys/boot` and `/sys/spm/spm` on "go", "sh" or "spm"; `dm` wants `` `node_data/startup[.191,.color,.1280color,.1280bw] `` — the per-display startup files a `--screen` choice selects between. Two notes worth having: a tape boot "is first searched for BSCOM/RBAK_SHELL", and with no registries "you can login only as **USER.NONE.NONE**" |
| 33 | §1 **SYSTEM DIRECTORIES** | `none` **(and it explains this project's `sau7`)** | The `/sys` tree by purpose, and the **SAU numbering by machine**: `/sau1` DN4xx/600, `/sau2` DN300/320/330, `/sau3` DSP80/80A/90, `/sau4` DNx60 and DSP160, `/sau5` DN550/560/570/580, `/sau6` DN5xx-T, **`/sau8` DN3000 and DSP3000**. **There is no `/sau7` in this table** — and `/sau7` is the directory this project's SR10.3 cartridge work looks for, because it is the Series 3500's and postdates a January 1987 handbook. The gap in the numbering is the answer rather than an omission |
| 34 | §1 **TRAP CODES** | `none` **(and one line that matters for MD)** | The sixteen `TRAP` vectors: `0`-`4` are SVCs with that many arguments, `5` an SVC with any number, `6` an SVC from GPIO interrupt routines, `7`-`D` undefined and reflected to user space, `E` a software-generated fault (`pfm_$error_trap`), and **`F` undefined — "traps to PROM debugger"**. That last is the entry to MD from a running system, which is the debugger this project drives through the serial console |

| 31 | §1 **Message Data**, **RESOURCE LOCK** | `none` **+ a lead worth carrying into chapter 5** | The ring packet's data field: "varies in length from **0 to 1024 bytes** but must always be an **even** number of bytes (on the Apollo I the controller always uses word DMA) ... transmitted verbatim" — which is the ring controller's word-DMA constraint stated from the software side, and agrees with `ap_ring_ctl`'s word-addressed buffer. **The lead is the lock table**: twenty-six named resource locks with their bit values, `network_$serv_lock` at `1` through `time_$lock` at `2000000`, including `file_$lock_lock` `20`, `ast_$lock` `20000` and `flp_$lock` `100000`. This project's `CRASH_STATUS 00120020` — "supervisor fault while resource lock(s) set" — decodes under that table as exactly those three, and `00120020` is `100000 + 20000 + 20`. **Not claimed**: whether the crash word's low half *is* the lock set is chapter 5's question, and the arithmetic working is suggestive rather than settled. Carried there |

*(chapter 12 is read end to end, its delegations included; chapter 1 is a
five-page sample with the rest owed; rows are added as the remaining chapters
are)*
