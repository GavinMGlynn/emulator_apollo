# `000792-A01` *Domain Pascal Language Reference* — walk coverage record

| Tag | File | Pages | Text layer | State |
| --- | --- | --- | --- | --- |
| `[PAS]` | `bitsavers/000792-A01_Domain_Pascal_Language_Reference_Dec90.pdf` | 611 | born-digital, clean | **Appendix F read whole; the language reference triaged** |

**A language reference that is a hardware source, and title triage would have
missed it.**

`CONTENT_TRIAGE.md` put it second on the shelf by hardware-term count and I
assumed false friends — `register` in the Pascal sense. **Sampling the matches
is what caught it**: 47 of the hits are `register`, but **33 are `68040` and 22
`MC68040`**, and they cluster in an appendix titled *Optimizing Floating-Point
Performance on MC68040-Based Domain Workstations*.

*The lesson is the method, not the document.* A term-count triage ranks; only
looking at what the terms **are** decides. Three of the four documents sampled
alongside this one were exactly the false friends expected — the Text Processing
Guide's 154 hits are all `register` in the `troff` sense, the C reference's 90
are the storage class, the Aegis Command Reference's 27 `csr` are a *command
named* `csr`. The fourth was this.

## Appendix F is Apollo's account of the 68040's floating-point trap

> "The 68040 has an on-chip floating-point unit that **directly supports only a
> subset of the 68881/68882 architecture**. Floating-point functionality that is
> not directly supported in hardware is provided through **system traps**; these
> system traps invoke a **kernel routine** that emulates the missing
> functionality."

`cpu/m68040/ap_m68040_fpu.h` already carries the Motorola side: Table 9-10's
twenty-six instructions, `FLOG2` missing from it, and two further manuals
proving the omission — twenty-seven in all. **What this adds is which of them
this machine's own software issues.**

**Table F-1, Emulated Intrinsic Functions**, is eight rows: `SIN`, `COS`, `TAN`,
`ATAN`, `EXP`, `LOG`, `LOG10`, `INT`, in FORTRAN, C and Pascal spellings. So the
instructions Apollo's compilers emit and the hardware refuses are **`FSIN`,
`FCOS`, `FTAN`, `FATAN`, `FETOX`, `FLOGN`, `FLOG10` and `FINT`/`FINTRZ`** —
eight of twenty-seven.

And the other nineteen may never appear:

> "Intrinsic functions other than those listed ... (such as ASIN, ACOS, and
> hyperbolic functions) are **always performed by run-time libraries that use
> only hardware-executed floating-point instructions**. For example, if your
> FORTRAN program calls the SINH intrinsic, the compiler **never generates the
> FSINH instruction**; instead, it generates a call to the `ftn_$dsinh`
> routine."

Only `-cpu 3000` (later `-cpu mathchip`) code contains any of them at all;
`-cpu any` contains none. **That is a workload fact and no Motorola manual can
carry it** — it says which eight to implement first if these are ever needed,
and that the rest may be unreachable on this machine.

*It also lands on today's finding.* A DS5500 takes **`1 x vector 11`** in a boot,
and this session established that one was `PTEST` at ROM `$58D2`. Vector 11 is
the line-F vector, and Appendix F says it is also the road every emulated
floating-point instruction takes. **The same handler at `7A42EDBC` serves both.**

## And Apollo confirms a Motorola warning from the user's side

`ap_m68040_fp_exception.h` carries `[040]` §3.7.3: "the FPSP transcendental
calculation results are **not the same** as for the 68881/68882", which is why a
68040 model must not reuse `m68882/ap_m68882_transcendental.c`. Appendix F.5 is
the same fact told to customers:

> "the results may differ slightly from those on 68020-based and 68030-based
> platforms. The differences are caused by **the algorithms used to approximate
> trigonometric and transcendental math functions**. Having two different sets of
> results does not mean that one is correct and the other incorrect ... both
> results can be acceptably precise approximations."

**Motorola says the numbers differ; Apollo says what that looked like to the
people who hit it.** Nothing changes — this core already refuses these
instructions, which *is* the hardware's behaviour.

## Two smaller things from §6.4.9

`-cpu` takes arguments this project has not seen named: **`any`**, **`3000`**
(renamed **`mathchip`**), **`mathlib`** and **`mathlib_sr10`**, with Table 6-3
giving what each causes the compiler to do and Table 6-4 "the relative
performance of the MC680x0 code generated with diff[erent arguments]".
`-cpu any` "imposes a severe performance penalty, usually a greater penalty than
that caused by instruction emulation".

## What is owed

The Pascal language itself — 560 pages of syntax and semantics — is **triaged as
not a hardware source**, on the same measurement that found Appendix F: outside
Appendix F and §6.4.9, its hardware terms are `register` in the language's own
sense. Appendix E, *Optimizing Floating-Point Performance*'s sibling on
pre-68040 systems, is owed if the 68882 model ever needs a workload fact.
