# MC68030 User's Manual — walk coverage record

The DN3500's processor, and the whole of Phase 2.

| Tag | File | Pages | Text layer | State |
| --- | --- | --- | --- | --- |
| `[030]` | `motorola/MC68030_Users_Manual_3ed_1990.pdf` | 608 | yes (1.06 M chars) | audit done 2026-08-25; **§1-§11 walked**, 498 pages |

Third Edition, 1990. Fourteen sections plus Appendix A (*M68000 Family
Summary*) and an index — the structure read off the contents pages rather
than assumed.

## The audit, and what it is for

Opened per the plan item's own instruction: **audit before rendering**. The
check is one grep and it has reversed a document's premise three times in this
project — `[OMTI]` §5 was recorded as "owed" while the code cited 37
subsections; `[6840]`'s item said "no record" while the model derived sixteen
sections; `[8259]`'s record claimed the "whole programming model" at 11 of 24
pages. The point is to tell **`derived`** (taken from the document, field by
field, with the text quoted) from **`walked`** (a page-by-page pass) from
**genuinely unread** — because only the third needs rendering, and the second
is where this project's defects have actually been.

## Census

Every `[030]` citation in `src/core`, by the section it names — 90 in all:

| § | Title | Citations | State |
| --- | --- | --- | --- |
| 1 | Introduction | §1.3, §1.3.2 | sampled |
| **2** | **Data Organization and Addressing Capabilities** | **none** | **unread — see below** |
| **3** | **Instruction Set Summary** | **none** | **unread — see below** |
| 4 | Processing States | Table 4-1 only | sampled |
| **5** | **Signal Description** | **none** | **unread — see below** |
| 6 | On-Chip Cache Memories | §6, §6.1, §6.1.3, §6.3.1 | partly derived |
| 7 | Bus Operation | ch. 7, §7.3.1, §7.3.5-7.3.7, §7.4.2, §7.4.3, §7.5.1, §7.7, Table 7-2 — **21** | **derived** |
| 8 | Exception Processing | §8, §8.1, §8.1.1, §8.1.5, §8.1.7, §8.1.9, §8.2.1, §8.2.2, Tables 8-1/8-5/8-6, Figure 8-9 — **25** | **derived** |
| 9 | Memory Management Unit | §9.2-§9.7.6, Table 9-3, Figures 9-10/9-37/9-38 — **28** | **derived** |
| 10 | Coprocessor Interface Description | §10.2, §10.4.9 | **walked 76/76, 2026-09-06** |
| 11 | Instruction Execution Timing | §11, §11.2, §11.2.2, §11.3, §11.6, §11.6.1, §11.6.3 — **14** | **walked 62/62, 2026-09-06** |
| **12** | **Applications Information** | **none** | **unread** |
| **13** | **Electrical Characteristics** | **none** | **unread** |
| **14** | **Ordering Information and Mechanical Data** | **none** | **unread** |
| **A** | **M68000 Family Summary** | **none** | **unread** |

## The three gaps that are not merely unread

**§2, Data Organization and Addressing Capabilities — and the addressing modes
came from somewhere else.** `ap_m68030_ea.h` derives the whole effective-address
decode from **`[PRM]` §2** — Figure 2-2 and Tables 2-1 through 2-4, named
explicitly — and cites nothing from `[030]`'s own §2, which covers the same
subject for this exact processor. That is not wrong: the plan records that the
PRM's figures "are all intact, so nothing here is derived", which was a
deliberate choice about scan quality. But it means **the processor's own chapter
on its own addressing modes has never been opened**, and `CLAUDE.md`'s
resolution order puts the part's own manual first precisely because a sibling
"often carries the note that resolves an ambiguity in ours".

**§3, Instruction Set Summary — same shape.** The instruction work cites
`[PRM]` Table 8-2 and the per-instruction pages throughout. `[030]` §3 is the
68030's own summary of the same set, including whatever it says about the
instructions the 68030 adds or changes. Never consulted.

**§5, Signal Description — and this one is structural.** `ap_m68030_bus.c` is
built from §7, *Bus Operation*, with 21 citations. §5 is the chapter that
**defines the signals §7 operates on**. A bus model derived from the timing
chapter without the signal chapter is exactly the arrangement that let the write
strobes be wrong until `[030]` §7.3.2 was transcribed — recorded in
`COMPLETION_PLAN.md` as "the tail was recorded as one difference (DS) and turned
out to be three". §5 is where `DSACK`, `STERM`, `CIIN`, `CBREQ`, `AVEC` and the
rest are specified.

*Sections 12, 13 and 14 and Appendix A are expected to be applications,
electrical and mechanical* — but that is a statement about the **contents page**,
not about the pages. `[8259]` hid `TJLJH` in a range dismissed as electrical and
`[8237]` hid two rule pages in one dismissed as mechanical, both in this same
session. They get read.

## Verdict, and the shape of the job

**§7, §8, §9 and §11 are `derived`** — 88 of the 90 citations — and a
field-by-field pass over them is a *verification*, not a discovery. That is
worth doing (it is what found `ST3` in `[OMTI]` §6.4 and Register B in
`[146818]`), but it is second in priority.

**§2, §3 and §5 are genuinely unread, and §5 is load-bearing for a module this
project has already had a defect in.** They come first.

*Estimated pages*: §2 ~30, §3 ~30, §5 ~40 — about 100 pages for the unread
core, against 608 for the whole. The audit has therefore turned "render 608
pages" into "render 100, then verify 400" — which is the whole reason it is the
first step.

## §5, SIGNAL DESCRIPTION — WALKED, 12/12, 2026-08-25

PDF 127-138, document pages 5-1 to 5-12. Read as images. The first section of
this manual anyone has walked, and it was chosen because `ap_m68030_bus.c` is
built from §7's *timing* without the chapter that defines the signals §7 uses.

**Table 5-1's signal index and Table 5-2's signal summary are the whole of it** —
every signal with its mnemonic, direction, active state and whether it
three-states. Captured in full: `FC0`-`FC2`, `A0`-`A31`, `D0`-`D31`,
`SIZ0`/`SIZ1`, `OCS`, `ECS`, `R/W`, `RMC`, `AS`, `DS`, `DBEN`,
`DSACK0`/`DSACK1`, `STERM`, `CIIN`, `CIOUT`, `CBREQ`, `CBACK`, `IPL0`-`IPL2`,
`IPEND`, `AVEC`, `BR`, `BG`, `BGACK`, `RESET`, `HALT`, `BERR`, `CDIS`,
`MMUDIS`, `REFILL`, `STATUS`, `CLK`.

### Four rules this core already obeys, three of them uncited

- **§5.7.1: "`CIIN` is ignored during all write cycles."** `ap_m68030_access.c`
  consults `board_inhibits` at three places, **all in the read path**; the write
  path never reads it. Correct — and correct *by construction* rather than by
  citation, which is the kind of thing that survives until someone changes the
  write path for another reason.
- **§5.11.1: "`CDIS` does not flush the data and instruction caches; entries
  remain unaltered and become available again when `CDIS` is negated."**
  `cache_disable` gates `ap_m68030_cache_enabled` and flushes nothing. Correct,
  uncited.
- **§5.6.6**: "During two-clock synchronous write cycles, the MC68030 **does
  not assert `DS`**." The bus model has `AP_M68030_TERM_STERM` and the
  synchronous path; whether it suppresses `DS` on that path specifically is a
  §7 question and is listed below rather than guessed at here.
- **§5.10.1**, the `RESET` instruction: "resets external devices only; **the
  internal state of the processor is not altered**." Modelled, and the manual's
  own wording is now cited beside it.

### One stale comment, corrected

`ap_m68030_step.c`'s `RESET` arm said counting the reset "is the whole of the
observable effect **until there are devices to reset**". There are:
`ap_machine.c` watches `external_resets` and calls `ap_board_reset_devices` when
it moves. The instruction has reached the board's reset line since that was
wired, and the CPU-side comment was never updated — the code-comment form of the
staleness `check_docs.py` catches in the living documents and cannot catch here.

### One signal not modelled at all

**`MMUDIS`** — Table 5-1 lists it as an input and §5.11.2 defines it: it
"dynamically disables the translation mechanism of the MMU", and "the assertion
of `MMUDIS` does **not** flush the address translation cache; ATC entries become
available again when `MMUDIS` is negated". Zero occurrences in `src/`.

*Almost certainly unreachable, and the same shape as the SCN2681's `0x0C`*: it
is an emulator-support pin, and nothing in `src/core/board` or
`src/core/machine` drives either it or `CDIS`, so both are tied inactive on this
machine. Named in the plan rather than modelled, with the note that if `CDIS`
ever becomes drivable then `MMUDIS` should arrive with it — they are the same
paragraph of the manual and the same kind of pin.

## §2, DATA ORGANIZATION AND ADDRESSING CAPABILITIES — WALKED, 40/40, 2026-08-25

PDF 47-86, document pages 2-1 to 2-40. Read as images.

**The premise of walking it was that `ap_m68030_ea.h` derives the entire
effective-address decode from `[PRM]` §2 and had never opened the processor's
own chapter on the same subject. The result is that the two agree** — and that
is worth more than a defect would have been, because the decision to use
`[PRM]`'s figures was made on scan quality and has now been checked rather than
trusted.

### §2.5 is the 68030's own version of the tables we took from `[PRM]`

- **Figure 2-4**, *Effective Address Specification Formats* — the brief and full
  extension words with `D/A`, `REGISTER`, `W/L`, `SCALE`, `BS`, `IS`, `BD SIZE`
  and `I/IS`. **`BD SIZE` `00` = Reserved**, `01` Null, `10` Word, `11` Long,
  which is exactly what `AP_M68030_BD_RESERVED = 0` records and what the plan
  says was "Reserved, not null".
- **Table 2-1**, *IS-I/IS Memory Indirection Encodings* — all sixteen rows.
  `IS`=0 with `I/IS`=`001` is *Indirect Preindexed*; `IS`=1 with the same `001`
  is *Memory Indirect*. That is precisely the trap the plan records from
  `[PRM]` Table 2-2 — "a decoder that reads I/IS alone silently produces the
  wrong addressing mode" — stated here as its own table. Both reserved regions
  (`IS`=0/`I/IS`=`100`, and `IS`=1/`I/IS`=`100`-`111`) are present, and
  `AP_M68030_INDIRECT_RESERVED` covers them.
- **Table 2-2**, *Effective Addressing Mode Categories* — data, memory, control,
  alterable, per mode. All four are `ap_m68030_category.h`'s.

### Three rules checked against the code, all holding

- **§2.4.4/§2.4.5, and §2.8.1 a third time**: "If the address register is the
  stack pointer and the operand size is byte, the address is incremented
  [decremented] by **two rather than one**." `ap_m68030_address_step` tests
  `reg == 7u && operand_size == 1u`, with the sentence quoted and the note that
  A7 is the stack pointer whichever of the three it names — which is what
  §2.8.1's "the *active* stack pointer is automatically decremented or
  incremented by two" confirms.
- **§2.3's bit-field operands**: offsets "may have values in the range of
  **−2³¹ to 2³¹−1**", widths 1 to 32, and the memory field does *not* wrap where
  a register one does. `bitfield_spec_t.offset` is `int32_t` and the two wrap
  behaviours are modelled separately, with the register case cited to `[PRM]`
  §1.7.1 and commented "modelling both the same way is wrong in one of them".
- **§2.2.3's control-register access rules**: "for all CCR operations, the upper
  byte is read as all zeros and is ignored when written" — `ap_m68030_write_ccr`
  and `read_ccr` both mask to `AP_M68030_CCR_MASK`; and SFC/DFC have "only bits
  2:0 implemented ... the upper 29 bits are read as zeros and are ignored when
  written" — the write path masks `& 0x7u`.

### One bound stated here and not modelled

§2.5: "**The longest instruction for the MC68030 contains 10 extension words.**
It is a MOVE instruction with full format extension words for both the source
and destination effective addresses and with 32-bit base displacements and
32-bit outer displacements for both addresses." Nothing in `src/` enforces a
maximum. Not a defect — the decoder consumes what each mode specifies and a
longer instruction cannot be encoded — but it is a stated invariant that no
assertion checks, and it is the kind of bound a fuzzer would want.

*Also captured*: §2.1's operand types (bit, bit field 1-32, byte, word, long,
quad); §2.2's register organisation including quad words in any two data
registers "without restrictions on order or pairing"; §2.3's big-endian memory
layout and "instruction words must be aligned on word boundaries" against data
that need not be; §2.6's programmer's view; §2.7's family compatibility note
that `SCALE` = 0 is the one encoding common to both extension-word formats; and
§2.8's stacks and queues.

## §3, INSTRUCTION SET SUMMARY — WALKED, 32/32, 2026-08-25

PDF 87-118, document pages 3-1 to 3-32. The third and last of the sections the
audit found genuinely unread.

**Same result as §2: it confirms.** The instruction work cites `[PRM]`
throughout and never `[030]` §3; the two agree.

- **Table 3-13, Conditional Tests** — the sixteen conditions with their
  encodings and boolean formulae, `T` = `0000` and `F` = `0001` footnoted "*Not
  available for the Bcc instruction*". That is exactly what the branch item
  records from `[PRM]` Table 3-19: conditions 0 and 1 decode as `BRA` and `BSR`
  because `Bcc` cannot take them.
- **Table 3-12, Condition Code Computations**, two sheets — every instruction
  against `X`,`N`,`Z`,`V`,`C` with the special definitions spelled out as
  boolean expressions, including the shift/rotate `r`=0 rows. This is the
  definitive cross-check for `ap_m68030_alu.c` and is now on the shelf as such.
- **Tables 3-1 to 3-11 and 3-14** — data movement, integer arithmetic, logical,
  shift/rotate, bit manipulation, bit field, BCD, program control, system
  control, MMU and multiprocessor operations, then the whole alphabetised set
  across five sheets.

### Three rules checked against the code, all holding

- **§3.2.4**: a register shift count is taken "**modulo 64**".
  `ap_m68030_step.c` computes `d[count] % 64u` with the sentence quoted.
- **Table 3-6's note**: "All bit field instructions set the N and Z bits as
  shown for BFTST **before** performing the specified operation." The bit-field
  dispatch computes the flags from the field "as *found*, before any
  modification" and then switches on the operation — the ordering the note
  requires.
- **§3.5.3** restates §2.3's bit-field offset range, −2³¹ through 2³¹−1, a third
  time. Modelled as `int32_t`.

### One documented effect that is vacuous here, now said so

**§3.5.4**: `NOP` "forces synchronization of the integer unit pipeline by
waiting for all pending bus cycles to complete." This core executes strictly in
order and accounts each bus cycle to the instruction that issued it, so nothing
can be in flight when `NOP` runs and there is no state for a synchronisation to
wait on — correct by construction, the same shape as `CIIN` being ignored on
writes. `NOP` was a bare `return true` with no comment; it now carries the
citation and the condition under which it stops being vacuous (a store buffer,
or any out-of-order completion).

*Also noted, so it is not read as a contradiction*: §3.1 says an instruction may
be "as many as **11 words**" where §2.5 says "**10 extension words**". They
agree — one counts the operation word and the other does not.

## §1, §4 and §6 — WALKED, 2026-08-25

PDF 29-46 (§1, 18 pages), 119-126 (§4, 8) and 139-162 (§6, 24). All three were
`sampled` — one or two citations each — and are now read through.

**§1 corrects a claim in the plan.** Figure 1-4, the Status Register, **renders
perfectly legibly at 150 dpi**. A Phase 2 item says `ap_m68030_regs.c` used
`[PRM]` §1.3.2's Figure 1-8 "whose Figure 1-8 survives the scan where the 68030
manual's does not" — that is wrong, and was evidently decided without rendering
the page. The layout matches `AP_M68030_SR_*` bit for bit (T1 15, T0 14, S 13,
M 12, I 10-8, X 4, N 3, Z 2, V 1, C 0), and `AP_M68030_SR_DEFINED = 0xF71F` is
exactly §2.2.3's "only 12 bits of the status register are defined". Following
`[PRM]` was harmless; the reason given for it was not true.

*Also from §1, both already modelled with their sentences quoted*: §1.7's
"**22-Entry Fully Associative ATC**" (`AP_M68030_ATC_ENTRIES 22`), and §1.9's
caches of "256 bytes ... organized as 16 entries, each containing a block of
four long words" (`AP_M68030_CACHE_LINES 16`). Table 1-1's eighteen addressing
modes and Table 1-2's instruction list are §2 and §3's tables in summary form.

**§4** gives Table 4-1's address-space encodings (the one citation this section
had), the privilege levels and how they change, §4.3's exception vectors and
Figure 4-1's general stack frame. Notable and already right: "when the exception
being processed is an interrupt and the M bit is set, the M bit is cleared,
putting the processor into the interrupt mode"; and `STOP` leaves the processor
"stopped, not halted" — a normal processing state without bus cycles.

**§6** is the fullest of the three and confirms throughout. §6.1's five
conditions for an access to be cachable; `CDIS` disabling regardless of `CACR`;
`CIIN` per-bus-cycle and `CIOUT` per-page; §6.1.1/§6.1.2's tag and valid-bit
structure; **§6.1.2.1's write allocation**, whose two `WA` cases
`ap_m68030_cache.h` quotes verbatim; §6.1.2.2's rule that "the read portion of a
read-modify-write cycle is always forced to miss in the data cache", which
`ap_m68030_cache.c` gates on; and §6.1.3's single-entry and burst fill modes with
the six conditions under which `CBREQ` is not asserted.

*One rule worth having that this record had not captured*: "Read accesses from
the CPU address space and address translation table search accesses are **not
stored in the data cache**."

## §7, BUS OPERATION — WALKED, 106/106, 2026-08-26

PDF 163-268. The largest section, holding 21 of the 90 citations, and the first
of the four `derived` sections to be verified rather than discovered. **It
confirms throughout, and three of the confirmations are of fixes this project
made the hard way.**

### The write-cycle strobes, verified state by state

`COMPLETION_PLAN.md` records the write-timing item as "the tail was recorded as
one difference (DS) and turned out to be three". §7.3.2 gives the states, and
all three are there:

- **S1**: "the processor asserts `AS` ... The processor also asserts **`DBEN`**
  during S1" — where the read cycle asserts `DBEN` in S2.
- **S3**: "The processor asserts **`DS`** during S3, indicating that the data is
  stable on the data bus" — where a read asserts it in S1.
- **S5**: "negates `AS` and `DS` ... R/W, SIZ0-SIZ1, FC0-FC2, and **`DBEN` also
  remain valid throughout S5**" — where a read negates `DBEN` in S5.

§7.1.5 and §7.1.6 state the same two rules as prose, so the section says it
twice. The read cycle's states confirm the other half: S1 `AS`+`DS`, S2 `DBEN`,
**S4 "samples `CIIN` at the beginning ... at the end of S4 the processor latches
the incoming data"** — which is `ap_m68030_bus.h`'s comment verbatim — and S5
negating all three.

### The pipe's deferred bus error, verified

§7.5.1: "If a bus error occurs on an instruction fetch, the processor **does not
take the exception until it attempts to use that instruction word**. Should an
intervening instruction cause a branch or should a task switch occur, the bus
error exception does not occur." That is exactly what `pipe_suite` pins — "the
status bit travelling with its word, so a bus error faults where the word is
*used*, not where it was fetched".

### Cycle lengths, confirmed

§7.2.9's three-clock minimum for `DSACKx` termination and §7.2.10's two-clock
minimum for `STERM` are `AP_M68030_TERM_DSACK` and `AP_M68030_TERM_STERM`. The
`NOTE` that "`STERM` and `DSACKx` should **never** be asserted during the same
bus cycle" is satisfied by construction here, the termination being one enum.

### Four rules worth having that the earlier sections did not give

- **§7.2.9 extends §5.7.1's `CIIN` rule**: it is ignored "when the appropriate
  cache is not enabled or when cache inhibit out (`CIOUT`) is asserted. It is
  also ignored during write cycles **or translation table searches**." The
  table-search case is new.
- **§7.6 gives `NOP` a concrete purpose** that §3.5.4 only names — an external
  write whose `BERR` controls program flow — and then says it "is an irregular
  situation ... not required by most systems", and unnecessary even with
  error-detection circuitry "since the MMU always checks the validity of write
  cycles before they proceed to the data cache". Added to the `NOP` comment.
- **§7.4.1.2**: an autovector's number is "the sum of the interrupt level plus
  24 ($18)", and §7.4.1.3 gives the spurious-interrupt vector as **24**.
- **§7.5.4**: a double bus fault leaves the processor asserting `STATUS`
  continuously, and "**only an external reset operation can restart a halted
  processor**" — though bus arbitration still occurs.

*Also captured*: Tables 7-1 to 7-9 in full — `DSACK` codes, `SIZ` encodings,
byte-enable generation, memory-alignment bus-cycle counts, and the six
`DSACK`/`BERR`/`HALT` and `STERM`/`BERR`/`HALT` termination cases; §7.3.3's
twelve-state read-modify-write; §7.3.7's burst states with the three abort
conditions; §7.4's CPU-space encodings; and §7.7's bus-arbitration state
machine.

## §8, EXCEPTION PROCESSING — WALKED, 34/34, 2026-08-26

PDF 269-302. The second `derived` section verified, 25 citations. **It found a
real defect** — the only one in `[030]` so far.

### The defect: a traced `STOP` stopped

§8.1.7: "**The STOP instruction does not perform its function when it is
traced.** A STOP instruction that begins execution with T1 = 1 and T0 = 0 forces
a trace exception after it loads the status register. Upon return from the trace
handler routine, execution continues with the instruction following the STOP,
and **the processor never enters the stopped condition**."

`ap_m68030_step.c`'s `STOP` arm set `cpu->stopped = true` unconditionally.
Tracing *is* modelled here — `AP_M68030_VECTOR_TRACE` is raised after any
instruction when `T1` is set — so a debugger single-stepping over a `STOP` would
have wedged the machine: the trace exception is taken and the processor is
already stopped.

Fixed, and the detail that matters is *when* the trace mode is read: **before**
the `write_sr`, because "begins execution with T1 = 1" is the entry state and a
`STOP` whose immediate clears the trace bits would otherwise look untraced. The
status-register load still happens — the manual has the trace forced *after* it.
`step_suite` 297 → 299, with the untraced case pinned as the control.

*Measured, not assumed*: the identity boot is **unchanged** at
`42B14372F3677EE8` with the same clocks and final PC, so Domain/OS never traces
a `STOP` and the fix is inert on the reference workload.

### What it confirms

- **Table 8-1's vector assignments**, all 64 defined plus the 192 user vectors,
  with the `STATUS` column. Vector 12 is "(Unassigned, Reserved)" on the 68030
  where the 68020 had a coprocessor pre-instruction; 57 and 58 are "Defined for
  MC68851 not used by MC68030".
- **§8.1.1's ten reset operations**, and this core implements all ten *with the
  distinction the closing paragraph draws*: step 6 invalidates both caches, and
  "the reset exception does **not** flush the address translation cache (ATC)".
  `ap_m68030_reset_state` clears the caches and leaves the ATC, quoting that
  sentence.
- **§8.1.2/§8.1.3**: a bus error on an instruction prefetch is deferred "until
  it attempts to use the prefetched information" — §7.5.1 again — and an address
  error is "internally initiated. A bus cycle is not executed".
- **Table 8-5's exception priority groups**, 0 through 4, with the rule stated
  backwards that "the lower the priority of an exception, the sooner the handler
  routine for that exception executes" — which `ap_m68030_step.c` already quotes
  at the trace site.
- **Table 8-6's six stack frames** with their format codes and what the stacked
  PC points to for each exception, and **§8.2.1's Special Status Word** bit by
  bit (`FC`, `FB`, `RC`, `RB`, `DF`, `RM`, `RW`, `SIZE`, `FC2-FC0`).
- **§8.1.9's interrupt recognition**, including level 7's transition
  sensitivity — a second level 7 is taken on a lower-to-7 transition even with
  the mask at 7 — and the throwaway format-1 frame built on the interrupt stack
  when `M` is set.

*Also captured*: §8.1.5's A-line (vector 10) and F-line (vector 11) rules,
including that an F-line with bits [11:9] = 0 and undefined subsequent words is
unimplemented in supervisor mode but a **privilege violation** in user mode;
§8.1.6's seventeen privileged instructions; §8.1.11's breakpoint acknowledge and
its `BERR` → illegal-instruction path; and §8.2.2/§8.2.3's two ways to complete
a faulted bus cycle.

## §9, MEMORY MANAGEMENT UNIT — WALKED, 86/86, 2026-08-26

PDF 303-388. The largest citation block — 28 of the 90 — and the third
`derived` section verified. **It confirms throughout; no defect.**

### What it verifies

- **Table 9-3, MMUSR bit definitions** for both `PTEST` level 0 and levels 1-7,
  which is what `ap_m68030_mmusr.c` derives from, including which bits are
  *undefined* when `I` is set.
- **§9.7.5.3's three MMU configuration exception rules**, and this core has all
  three: the `TIx` sum plus `PS` plus `IS` must be 32; a reserved `PS` value
  (`$0`-`$7`) is illegal; and a `PMOVE` loading `CRP`/`SRP` with `DT` = 0 faults
  — **"the register is loaded with the new value before the exception is
  taken"**, which `ap_m68030_step.c` does in that order.
- **The `TIx` sum's own subtlety**: "the `TIx` fields are added together **until
  a zero field is reached**". `ap_m68030_tc_is_consistent` breaks on the first
  zero and quotes that sentence — summing all four would accept configurations
  the hardware rejects.
- **§9.2.2 restates that a reset does not flush the ATC** — the third place this
  manual says so, after §8.1.1's step list and its closing paragraph.
- **§9.6's MC68030/MC68851 differences**: six 68851 features the 68030 lacks
  (access levels, breakpoint registers, root pointer table, task aliases,
  lockable ATC entries, globally shared entries), the 22-entry ATC, and the
  instructions that must be avoided — `PVALID`, `PFLUSHR`, `PFLUSHS`, `PBcc`,
  `PDBcc`, `PScc`, `PTRAPcc`, `PSAVE`, `PRESTORE`. `ap_m68030_step.c` already
  cites **p. 9-51** for `PFLUSHR`/`PFLUSHS`, so this page was consulted before.
- **§9.4's ATC entry format** — `V`/`FC`/logical and `B`/`CI`/`WP`/`M`/physical
  — and §9.5.1.1's descriptor fields, which `ap_m68030_atc.h` and
  `ap_m68030_desc.c` carry field for field.

### One rule that is emergent rather than coded, like the 6840's

§9.5.2: "The limit field is **effectively disabled** if `L/U` is set and the
limit field contains zero or if `L/U` is clear and the limit field contains
`$7FFF`." `ap_m68030_desc_index_within_limit` is
`lower ? (index >= bound) : (index <= bound)` — a lower bound of zero always
passes, and an upper bound of `$7FFF` always passes a 15-bit index. Both
"disabled" cases fall out of the comparison, and the manual's word
*effectively* is the tell that they are consequences rather than rules.

### What §9 does not contain

§9.9 and §9.10 are **operating-system design guidance** — how to lay out page
tables, how many levels to use, and a worked example OS with `Vallocate` and a
bus-error handler in pseudocode. Thirty pages of it, and none is part behaviour.
Read and recorded as such rather than skipped, but it is why a 86-page chapter
yielded no correction.

## §10, COPROCESSOR INTERFACE DESCRIPTION — WALKED, 76/76, 2026-09-06

PDF 389-464, document pages 10-1 to 10-76 (10-76 is blank). Read as images.
Section boundaries established from the page footers rather than the contents
page: §10 runs PDF 389-464, §11 465-526, §12 527-572, §13 573-574, §14 575-579,
Appendix A 581-584.

**The audit called this section `sampled`, at two citations — §10.2 and
§10.4.9 — against a chapter that specifies the entire protocol the 68882 talks
over. It was the largest gap left in the manual, and it found a real defect.**

### The defect: an addressing-mode refusal is an F-line, not a protocol violation

`ap_m68030_step.c` had one refusal value, `FP_SOURCE_PROTOCOL_VIOLATION`, for
every way a coprocessor instruction could name an effective address it may not.
It reported all of them as **vector 13**, the coprocessor protocol violation.

**Table 10-6, *Exceptions Related to Primitive Processing*, has two separate
columns**, and four of its rows are stated again in prose, each naming the
F-line trap outright:

| Where | Rule | Refusal |
| --- | --- | --- |
| §10.2.3.3.1 | cpSAVE: control alterable or predecrement only | "Other addressing modes cause the MC68030 to initiate **F-line emulator exception** processing" |
| §10.2.3.4.1 | cpRESTORE: predecrement barred | "Invalid effective address encodings cause the MC68030 to initiate **F-line emulator exception** processing" |
| §10.4.8 | evaluate and transfer EA: control alterable only | aborts "by writing a `$0001` to the control CIR and initiates **F-line emulation exception** processing" |
| §10.4.9 | evaluate EA and transfer data: EA must be in the primitive's declared class | "not a member of the class specified by bits [8-10] ... initiating **F-line emulation exception** processing" |
| §10.4.16 | transfer multiple coprocessor registers: control/postincrement in, control alterable/predecrement out | "Invalid addressing modes cause the MC68030 to abort the instruction ... and to initiate **F-line emulator exception** processing" |

while the *length*, *direction*, *conditional-misuse* and *write-to-nonalterable*
refusals stay in the Protocol column, and §10.4.9 names those there one at a
time. Table 10-6's own F-Line cell for the §10.4.9 row reads "Valid EA Field
Does Not Match EA in Op-Word", against a Protocol cell listing "Length is Not 1,
2, or 4 and EA = Register Direct" and "Attempt to Write to Nonalterable Address
**Even if Address Declared Legal in Primitive**". The split is exactly: *which
class the mode is in* is an F-line, *what is done with a legal one* is a
protocol violation.

**It is not only a vector number.** §10.5.2.1 puts a protocol violation on the
ten-word mid-instruction frame (Figure 10-43), which resumes by re-reading the
response CIR; §10.5.2.2 puts an F-line on the four-word pre-instruction frame
(Figure 10-41), which **restarts the instruction**. So the old behaviour handed
a handler a frame of the wrong shape, pointing it back into a dialogue that was
never opened — the main processor checks the operation word's mode before it
touches a CIR at all.

Fixed at the four sites the manual settles outright: `fetch_fp_source`'s data
class check (§10.4.9), `execute_fmovem`'s two class checks (§10.4.16), and
`execute_fp_state`'s cpSAVE and cpRESTORE checks (§10.2.3.3.1, §10.2.3.4.1).
*Verification: `step_suite` 299 → 302 — the two existing tests changed vector,
and cpSAVE/cpRESTORE had no addressing-mode test at all, which is why the wrong
vector was never encoded in one. The new privilege test is the control: a
user-mode `FSAVE (A0)+` is a privilege violation and not an F-line, because
§10.2.3.3.2 puts the supervisor check "prior to any communication with the
coprocessor".*

### Three refusals left as protocol violations, and why they are open

The manual settles the class question only for primitives whose *declared* class
it prints. Three of our sites turn on what the **68882** declares, which is an
`[882]` question and is recorded here to settle in that walk rather than guessed
at now:

- `store_fp_destination`'s `!ea_is_data_alterable`. If the FPCP declares
  Table 10-4 code `001` (Data Alterable), a `(d16,PC)` destination is a class
  mismatch and an F-line; if it declares `101` (Data) and relies on the main
  processor, it is §10.4.9's "write to a nonalterable address even if declared
  legal", a protocol violation — which is what this core reports and what its
  comment cites.
- `transfer_fp_control`'s memory-class check, for the `FMOVE` control-register
  forms.
- `execute_fp_conditional`'s `!ea_is_data_alterable` for `FScc`. §10.2.2.2.2 has
  the main processor evaluate that EA itself and never names the exception.

### One reading promoted to a transcription

`ap_m68030_step.c`'s arm for coprocessor types `110` and `111` took the F-line
trap and said so as **a reading**: "the manual defines what the four categories
do and is silent on what a fitted coprocessor does with a fifth". **§10.5.2.2 is
not silent**: "Any F-line operation word with bits [8:6] = 110 or 111 causes the
MC68030 to initiate exception processing **without initiating any communication
with the coprocessor for that instruction**." The vector was right, the claim of
silence was not, and the sentence goes further than the inference did — no CIR
is touched and no abort mask written, which is what the arm does by returning
before the part is consulted. The same paragraph settles the other half: a word
with bits [8:6] = `000`-`101` that maps to no valid coprocessor instruction is
an F-line too. Comment corrected in place with the citation.

### Two rules this core already obeys without citing them

- **§10.5.3, coprocessor reset**: "the coprocessor should be affected by
  external reset signals only and **not by RESET instructions**, because the
  coprocessor is an extension to the main processor programming model". The
  `RESET` instruction reaches `ap_board_reset_devices`, and the 68882 is
  `machine->fpu` rather than a board device, so it is not in that list. Correct
  by construction, and now cited.
- **§10.1.4.3 / Figure 10-3**: the MC68030 "never requests a burst operation
  during a coprocessor (CPU space) bus cycle, nor does it internally cache data
  read or written during coprocessor (CPU space) bus cycles". Nothing in this
  core issues a CPU-space cycle for the FPU at all — the primitive exchange is
  modelled at the level of the work it asks for, not the bus traffic — so there
  is no cycle to cache or burst. Stated so that a later per-cycle coprocessor
  interface inherits the rule rather than rediscovering it.

### Two documentary errors

- **Bit [14] is printed as bit [4].** §10.4.2's prose says "Bit [4], the PC bit,
  specifies the pass program counter operation", where Figure 10-22 and every
  one of the thirteen primitive figures put `PC` at bit 14 and bit 4 inside the
  `PARAMETER` field. A `1` lost in the scan or in the typesetting; the figures
  are unanimous.
- **Table 10-6 transposes the two increment modes, and names the wrong part.**
  Its *Transfer Multiple Coprocessor Registers* F-Line rows read "EA Not Control
  Alterable or `(An)+` for CP to Memory Transfer" and "... or `-(An)` for Memory
  to CP Transfer". §10.4.16's own prose has it the other way round and the right
  way round — "For transfers from the effective address to the coprocessor
  (DR = 0), the control addressing modes and the **postincrement** addressing
  mode are valid. For transfers from the coprocessor to the effective address
  (DR = 1), the control alterable and **predecrement** addressing modes are
  valid" — which is also the direction every M68000 instruction uses. This core
  follows the prose. Separately, sheet 2's *Transfer Status and/or ScanPC* row
  says "Trace Made Pending if **MC68020** in Trace on Change of Flow Mode", a
  leftover from the 68020 manual this table was lifted from; §10.4.17's prose
  says MC68030.

### The generic interface is wider than this part's instruction

§10.2.3.4.1 allows cpRESTORE "all memory addressing modes except the
predecrement addressing mode", which admits `#<data>`. The 68882's own
`FRESTORE` is defined over control modes and postincrement, and that is the
coprocessor fitted here, so the narrower rule is kept. Recorded to check against
`[882]` and `[PRM]`.

### Captured in full, and checked against the code

- **Figure 10-1**, the F-line operation word: `1111`, `CpID` in bits 11-9,
  `TYPE` in 8-6, type-dependent in 5-0. `ap_m68030_coproc_decode` reads exactly
  those fields.
- **§10.1.3's CpID rules**: `000` is not a coprocessor instruction — it is the
  on-chip MMU when the type field is also zero, and unimplemented otherwise;
  `001`-`101` are reserved to Motorola with `001` defined as the 68881/68882;
  `110`-`111` are user-defined. "The MC68030 never generates coprocessor
  interface bus cycles with the CpID equal to zero (except via the MOVES
  instruction)."
- **Figure 10-3 and Figure 10-4**, the CPU space map: `FC2:FC0` = `111`,
  `A19:A16` = `0010`, `A15:A13` = CpID, `A4:A0` = the CIR, and `A31:A20` and
  `A12:A5` always zero — so CIR set *n* is at `$2n000`, `$20000` for the MMU
  through `$2E000` for CpID 7, with `$xx000`-`$xx01F` the registers and the rest
  reserved.
- **Figure 10-5 and §10.3, the CIR set**, every offset: `$00` response (16),
  `$02` control (2 bits, `XA` at 1 and `AB` at 0), `$04` save (16), `$06`
  restore (16), `$08` operation word (16), `$0A` command (16), `$0C` reserved,
  `$0E` condition (16, selector in 5-0), `$10` operand (32), `$14` register
  select (16), `$18` instruction address (32), `$1C` operand address (32). The
  exception acknowledge mask is `10`₂ and the abort mask `01`₂, both with
  fourteen undefined high bits.
- **The four instruction categories and their formats**: cpGEN (Figure 10-6,
  type `000`), cpBcc.W/`.L` (Figures 10-9/10-10, types `010`/`011`), cpScc
  (Figure 10-11, type `001`, 0-5 EA extension words), cpDBcc (Figure 10-12,
  `001001` in bits 8-3), cpTRAPcc (Figure 10-13, `001111` in bits 8-3), cpSAVE
  (Figure 10-15, type `100`) and cpRESTORE (Figure 10-17, type `101`). This
  core's `ap_m68030_coproc_type_t` matches, and its comment "cpScc, cpDBcc,
  cpTRAPcc" for type `001` is right — the three are told apart by bits 5-0.
- **Table 10-1, cpTRAPcc opmode**: `010` one operand word, `011` two, `100`
  zero; every other encoding invalid.
- **Table 10-2, format word encodings**: `$00` empty/reset, `$01` not ready come
  again, `$02` invalid format, `$03`-`$0F` reserved *and interpreted as invalid*,
  `$10`-`$FF` valid with the low byte a length that must be a multiple of four.
- **Table 10-3, the null primitive's six encodings** across `CA`/`PC`/`IA`/`PF`
  /`TF`, for general and conditional categories.
- **Table 10-4, the eight valid-EA class codes**: `000` control alterable, `001`
  data alterable, `010` memory alterable, `011` alterable, `100` control, `101`
  data, `110` memory, `111` any.
- **Table 10-5, the main processor control register selectors**: `x000` SFC,
  `x001` DFC, `x002` CACR, `x800` USP, `x801` VBR, `x802` CAAR, `x803` MSP,
  `x804` ISP; every other code a protocol violation.
- **All thirteen response primitives** (§10.4.3-§10.4.20) with their bit
  patterns, reprinted together in §10.6 — busy, null, supervisor check, transfer
  operation word, transfer from instruction stream, evaluate and transfer EA,
  evaluate EA and transfer data, write to previously evaluated EA, take address
  and transfer data, transfer to/from top of stack, transfer single/multiple
  main processor registers, transfer main processor control register, transfer
  multiple coprocessor registers, transfer status register and scanPC, and the
  three take-exception primitives. **Response primitives with bits [13:8] =
  `$00`, `$0B`, `$18`-`$1B`, `$1F`, `$28`-`$2B`, `$38`-`$3B` or `$3F` are
  protocol violations** — undefined and reserved.
- **Figure 10-36, the register select mask**: `A7`-`A0` in bits 15-8 and
  `D7`-`D0` in 7-0, transferred `D0`-`D7` then `A0`-`A7` regardless of direction.
- **The three stack frames** §10 uses, and all three match §8's Table 8-6:
  pre-instruction is format `$0` and four words (Figure 10-41), post-instruction
  format `$2` and six (Figure 10-45), mid-instruction format `$9` and ten
  (Figure 10-43) carrying scanPC, internal register, operation word and
  effective address.
- **§10.4.1, the scanPC**, and where it points when the first primitive is read:
  for cpGEN the word after the command word; for cpBcc the word after the
  operation word; for cpScc, cpTRAPcc and cpDBcc the word after the condition
  specifier. `ap_m68030_step.c` fetches in exactly that order.
- **§10.3.8 and Figure 10-21, operand CIR alignment**: everything aligned to the
  most significant byte, long-word transfers wherever possible, the remainder
  aligned high. This is the sentence `read_operand_bytes` already cites.

## §11, INSTRUCTION EXECUTION TIMING — WALKED, 62/62, 2026-09-06

PDF 465-526, document pages 11-1 to 11-62. Read as images.

**This was the `derived` section the audit said needed verification rather than
discovery, and the verification passes: every transcribed number reproduces its
page.** The defect it found is not in the transcription at all — it is a rule in
the *prose*, which a table-driven reading would never have reached.

### The defect: an RMC read must miss the data cache, and did not

§11.4's note, under *Effect of Data Cache*:

> **RMC cycles (e.g., TAS and CAS) are forced to miss on data cache reads.
> Therefore, a data cache hit has no effect on these instructions.**

The sibling statement is §6.1.2.2, read from the page image, and it is the
fuller one:

> The read portion of a read-modify-write cycle is **always forced to miss in
> the data cache**. However, if the system allows internal caching of
> read-modify-write cycle operands (`CIOUT` and `CIIN` both negated), the
> processor either uses the data read from memory to update a matching entry in
> the data cache or creates a new entry with the read data in the case of no
> matching entry.

`ap_m68030_access_read` looked the address up in the data cache before anything
else and never consulted `access->rmc`, and it passed a **literal `false`** for
the flag into `ap_m68030_cache_read`. So a `TAS` or `CAS` whose operand was
already cached answered from the line at zero bus cost.

**It is not only a timing error.** A semaphore read answered from a local line
cannot see a write another bus master made since — nothing invalidates the entry
on someone else's write. Forcing the miss is what makes the read see memory,
which is the entire reason the operation is indivisible.

**The one constant hid three things**, all of them on the read cycle of an
indivisible pair:

1. the early lookup in `ap_m68030_access_read` could hit;
2. `ap_m68030_cache_burst_request` never saw the RMC that §7.3.6 says suppresses
   `CBREQ` — the check was written and correct, and the argument that drove it
   was always false;
3. `ap_m68030_cache_read`'s own `bus->rmc = read_modify_write` **cleared** the
   signal on exactly the cycle that must assert it.

Fixed at both sites, with `access->rmc` passed through. *Verification:
`access_suite` 16 → 18 — one test that the forced miss happens and that the fill
still runs (the rest of §6.1.2.2), and one control proving the same sequence hits
with the flag clear. The first was checked against the unfixed code and fails
there, so it discriminates. Identity boot **unchanged** at `42B14372F3677EE8`.*

**§6 was marked `partly derived` and this is a §6 rule nobody had implemented.**
That is the shape `CLAUDE.md` names: the chapter was consulted about the
questions someone happened to ask — write allocation, burst filling, the CACR
bits — and §6.1.2.2 was not one of them. §11 found it by asking a timing
question.

### Two rules obeyed uncited, now cited

- **§11.2.2, the cache holding register**: "provides instruction words to the
  pipe, **regardless of whether the instruction cache is enabled or disabled**",
  and prefetch requests go "simultaneously" to the holding register, the cache
  and the bus controller, so "even if the instruction cache is disabled, an
  instruction prefetch may hit in the cache holding register and cause an
  external bus cycle to be aborted". `ap_m68030_fetch_word` consults the holding
  register first and never reads the enable bit. Cited in `ap_m68030_fetch.h`
  because the shape invites the opposite: the thing is *called* the cache
  holding register and sits beside the cache in Figure 11-1.
- **§6.1.2.2's last sentence**: "Table search accesses, however, are completely
  ignored by the data cache; it is never updated for a table search access."
  True by construction — `ap_m68030_walk` reads descriptors through its own
  callback and never through `ap_m68030_cache_read`. Cited in
  `ap_m68030_walk.h`.

### The transcription verified, row by row

All **59 rows** of `ap_m68030_timing_table.c` reproduce their page images
exactly — §11.6.6 MOVE, §11.6.8 arithmetic/logical, §11.6.9 immediate, §11.6.11
single operand, §11.6.12 shift/rotate, §11.6.15 conditional branch and §11.6.16
control — head, tail, `CC` and `NCC` with its `(r/p/w)` triple. So do
`ap_m68030_ea_timing.c`'s §11.6.1 fetch and §11.6.3 calculate rows.

**And the trap that module records is confirmed.** Its comment says five `p`
counts "were transcribed as zero until the page image was read", because the
text layer rendered `4(1/1/0)` as `4(1/010)`. Page 11-26 read as an image gives
`(d16,An)`, `(xxx).W`, `(xxx).L`, the brief-format indexed row and both
immediates as `(1/1/0)` or `(0/1/0)` in the no-cache column. The extraction
still mangles them. This is `CLAUDE.md`'s "read the page image" rule with the
receipt attached.

**§11.3.4's worked example is mislabelled, and `M68030_TIMING.md` was right to
say so.** The example's second instruction is written `SUBA.L D1,A2` and carries
head 4 and `CC` 4; §11.6.8 gives `SUBA.L Rn,An` as 2/0/2 and `SUBA.W Rn,An` as
4/0/4. The example used `SUBA.W`'s numbers under `SUBA.L`'s name. Checked
against the page rather than trusted.

### Two documentary errors, new

- **§11.6.3's `([B],I,d32)` row** (p. 11-31) gives its instruction-cache case as
  `12(2/0/0)` where `([B],d32)` immediately above is `12(1/0/0)` and every other
  row in the block is one read. The *calculate* table includes fetch time "only
  for the first level of indirection", so two reads is not a thing this row can
  mean. A `1` set as a `2`.
- **§11.6.5's jump-EA table** (p. 11-36) lists **`([B],d32)` twice**, with
  identical figures, where the cea and fea tables at the same point run
  `([B],d32)` then `([B],I,d32)`. A duplicated row rather than a wrong number,
  so nothing is lost — but the block has one row more than the space it
  enumerates.

### Captured, and checked against the model

- **§11.2's eight independently scheduled resources** and Figure 11-1's block
  diagram: microsequencer, instruction pipe (stages B, C, D plus the 32-bit
  cache holding register), instruction cache, data cache, and the bus
  controller's three — micro bus controller, instruction fetch pending buffer,
  write pending buffer — plus the MMU. §11.2.5.1 and §11.2.5.2 give each pending
  buffer as **single** and interlocked.
- **§11.2.5.3's odd-word special case**: a prefetch of a word at an odd-word
  boundary that misses both the holding register and the instruction cache reads
  *both* words of the long word in one cycle from 32-bit memory, and even before
  odd from an 8- or 16-bit port. `ap_m68030_pipe` fills the whole long word,
  which is the same thing.
- **§11.3.1-§11.3.4**: the definitions of `CC`, head, tail and average `NCC`,
  Figure 11-3's derivation of overlap, and Equations (11-1) and (11-2) — which
  `ap_m68030_overlap.c` implements and `overlap_suite` checks against §11.3.4's
  arithmetic. §11.3.3's italicised caution is worth having in one place:
  *"Because the no-cache-case times assume no overlap, the head and tail values
  listed in these tables do not apply to the no-cache-case values."*
- **§11.4's data-cache rules** 1a/1b/1c and rule 2, and the note that a data
  cache hit is "ignored for computational purposes" in operations whose `CCop`
  already includes an operand fetch (`BFFFO`, `CHK2`). This core does not adjust
  a table figure at all — it measures the bus, so a data cache hit moves the
  answer by not costing anything, which is the same effect arrived at from the
  other side.
- **§11.5's wait-state rules** 1a-1c, 2a-2b, 3a-3b, and the `MOVEM` equations
  footnoted in §11.6.7 — `I-Cache Case = (8+4n)` for `w ≤ 2` and
  `(8+4n)+(w-2)n` beyond, with `Tail = 0` for all wait states on `EA,RL`; and
  `(4+2n)+(n-1)w` / `(4+2n)+(n-1)w+(w-2)` with `Tail = (n-1)w` or
  `(n)w+(n)(w-2)` on `RL,EA`.
- **§11.6's six stated assumptions**, which are what the transcribed numbers are
  numbers *under*: two-clock bus cycles, long-word-aligned operands including
  the stack, a 32-bit bus, **the data cache not enabled**, no exceptions, and
  every translation resident in the ATC.
- **§11.6.7's special-purpose MOVE table** including `MOVEC Rn,Cr-A` at 6 clocks
  against `Rn,Cr-B` at 12, and which control registers fall in each group — A is
  USP, VBR, CAAR, MSP, ISP; B is SFC, DFC, CACR.
- **§11.6.17's exception table**: `RESET` instruction **518** clocks, interrupt
  23 on the interrupt stack and 33 on the master stack, and the four traps
  (illegal, A-line, F-line, privilege violation) all at 18(1/0/4).
- **§11.6.18's save and restore**: short bus fault 36, long 62, and the six
  `RTE` forms, `RTE (Coprocessor)` among them at 26(7/0/0) — seven reads, which
  is the ten-word frame.
- **§11.7's table-search timing**, published as a shell script rather than a
  table: `cough` 7 clocks from the missed bus cycle to the first microinstruction
  ("6 to 9 depending on i- and d-state at miss"), `startup` 8 and `termination`
  8, less 2 when there is a function-code lookup; `6+bus+max(bus-4,0)` per long
  descriptor and `3+bus` per short; `4+max(bus-3,0)` per pointer U-bit set and
  `2+max(bus-3,0)` for the page U/M; a long page one "box" less than a long
  pointer; and an early-termination penalty of 3. Plus the worked table for
  nineteen tree shapes.
- **§11.8's interrupt latency**: ~200 clocks for the 68030 alone, the worst case
  being `MOVEM.L ([d32,An],Xn,d32),D0-D7/A0-A7` with the last fetch aborted by a
  bus error — and up to **ten** table searches for one
  `MOVE.L (od,[bd,An,Rm]),(od,[bd,An,Rm])`.
- **§11.9's bus arbitration latency**, which verifies `ap_m68030_arb.h` rather
  than correcting it: the bus is not relinquished during a read-modify-write,
  and "**the address translation search is an extended read-modify-write
  operation**" — the rule `ap_m68030_walk.h` already cites from §9, arrived at
  from the arbitration end.

### One stale comment corrected

`ap_m68030_access.h`'s `bool rmc;` carried a trailing `/* TC's E bit */`,
contradicting the four-line block above it that correctly describes it as RMC
held across an indivisible pair. A leftover from the `translating` field that
was deleted when `ap_m68030_translating` began reading the register.

## Owed

§12, §13, §14 and Appendix A, all four unread — 57 pages. §12 is *Applications
Information* at 46, §13 *Electrical Characteristics* at 2, §14 *Ordering
Information and Mechanical Data* at 5, and Appendix A *M68000 Family Summary* at
4. They get read whole regardless of those titles: `[8259]` hid `TJLJH` in a
range dismissed as electrical and `[8237]` hid two rule pages in one dismissed
as mechanical.

*Corrected 2026-09-06.* This section previously read, in three contradictory
sentences left from successive edits: "§10-§14 and Appendix A ... §2, §3 and
§12-§14 plus Appendix A are unread; §1, §4, §6 and §10 are sampled". §2 and §3
were walked on 2026-08-25 and the line was never updated — the staleness
`CLAUDE.md` warns of in walk records, in the record's own summary rather than in
a row. Original kept here because it is why the census table above and this
section disagreed.
